# CLAUDE.md — пам'ять проєкту svv-webhook-bot

> Цей файл Claude Code читає АВТОМАТИЧНО на старті кожної сесії.
> Він — єдиний міст контексту між різними чатами (сесії НЕ діляться пам'яттю).
> Тримай його актуальним: коротко, фактологічно, без води.

## Що це за проєкт

Крипто-трейдинг бот (Bybit/ф'ючерси) зі Smart-Money-логікою. Працює як
веб-застосунок Flask + кілька фонових daemon-потоків. Відповіді користувачу —
**українською**.

## ПРОФЕСІЙНО — ЗАВЖДИ (правило назавжди, весь проєкт)

Проєкт **професійний, а не аматорський** — і в UI, і в логіці/даних.

**UI.** Будь-яка сторінка/блок/таблиця — тільки **професійний, охайний вигляд**:
єдина система відступів і типографіки, стримані кольори з акцентами (не строкато),
1px-бордери, вирівнювання в стовпчик, картки однакової висоти, без «стрибання»
елементів, uppercase-підписи для метрик, monospace для чисел. Стат-картки —
в один ряд (горизонтальний скрол на вузькому). НЕ аматорський підхід.

**Логіка/дані.** Жодних «на око достатньо» зрізань кутів. Та сама метрика,
показана у двох місцях, ЗАВЖДИ рахується з ОДНОГО джерела правди — інакше
з'являються протиріччя (див. кейс PD-зони нижче). Кожен показник рахується на
задекларованому TF/вікні; коротке ролінг-вікно НЕ видавати за структурний
діапазон. Фікси — з тестом, що фіксує поведінку.

**Приклад-урок (PD Premium/Discount).** Банер рішення показував «PD Premium 89%»,
тоді як бейдж графіка — «Discount 17% (1H)» для тієї ж монети. Причина:
`position_evaluator._score_pd_zone` рахував Premium/Discount з ролінг-вікна
20 барів 15m, а бейдж/фільтр — з дилінг-діапазону `pd_zone_timeframe` (1H).
Фікс: `evaluate_entry`/`_score_pd_zone` приймають `pd_pct`(+пороги) і, коли задано,
класифікують САМЕ його (єдине джерело — `scanner.get_pd_pct()` +
`get_pd_thresholds()`); ролінг-вікно лишилось лише як fallback для Health-шляху.
Тест: `test_pd_zone_source.py`.

## VOB-алерт: ВАЛІДНІСТЬ = СВІЖІСТЬ (не зламати!)

Volumized-OB-сигнал (`vob_alert_enabled`) фаєриться в `smc_scanner._scan` на
НОВОМУ OB (edge за `formation_time`). **Природа свінг-OB:** OB стає валідним лише
через ~`volumized_swing_length` барів після своєї свічки (свінг мусить
підтвердитись) — тому бокс на графіку стоїть «назад у часі», хоча сигнал свіжий.
Це НЕ баг.

- **⏱ ГЕЙТ СВІЖОСТІ = «СИГНАЛ ЛИШЕ В МОМЕНТ ПОЯВИ» (вимога користувача, не
  зламати!).** `vob_alert_max_age_bars` **дефолт 0 = АВТО `swing_length + 2`**
  (при swing=5 → 7 барів ≈ 35 хв на 5m). Правило користувача дослівно:
  «зʼявився на графіку VOB — ми його опрацювали і все, більше до нього не
  повертаємось; блок, що утворився пів дня тому, ми лише БАЧИМО на графіку, а
  сигналом він був тоді, а не тепер».
  Чому саме `swing+2`: свінг-OB стає видимим лише через ~`swing_length` барів
  після своєї свічки, тож на ПЕРШОМУ показі його вік уже ≈ `swing_length`; +2
  бари — запас на скан-цикл.
  **Старий блок → `stale`:** позначаємо ОПРАЦЬОВАНИМ (щоб більше не
  повертатись), лічильник НЕ рухаємо, сигналу немає — чекаємо НАСТУПНИЙ блок.
  Гейт діє в ОБОХ гілках: і в `vob_one_per_ob` (раніше його там НЕ БУЛО — саме
  тому SUIUSDT-блок від 03:55 вистрілив о 14:18, вік 125 барів ≈ 10.4 год), і в
  catch-all. Явне `max_age=None` вимикає вікно (лише за потреби).
  ⚠️ Була спроба зробити «0 = без ліміту віку» — користувач це СКАСУВАВ.
  Тест: `test_signal_only_at_moment_of_appearance`.
- **🔎 «БЛОК НА ГРАФІКУ = СИГНАЛ» + ЧИСТИЙ ЛОГ (вимога користувача).** Дві
  проблеми, які виправлено разом:
  1) **Поточний блок ковтався як «базова лінія».** `_vob_alert_seen` не
     персиститься → після рестарту ПЕРШИЙ показ кожної монети давав first_sight
     (тиха база, БЕЗ сигналу) — тобто блок, який user БАЧИТЬ на графіку ЗАРАЗ,
     не фаєрився, бот чекав НАСТУПНИЙ. **Фікс:** `_vob_edge_outcome` тепер на
     ПЕРШОМУ показі, якщо блок СВІЖИЙ (вік ≤ поріг) → повертає **'fresh'** (це
     сигнал, одразу у фільтри), а не 'first_sight'. Старий блок на першому показі
     → 'first_sight' (тиха база, бо старий блок не сигнал). У процесі роботи:
     duplicate (ft≤base) / fresh (новіший+свіжий) / stale (новіший+старий).
  2) **Лог засмічувався** (57 рядків «базова лінія» тощо). **Фікс:**
     `_vob_log_decision(...)` пише ЛИШЕ в `self._vob_diag[symbol]` (in-memory,
     для get_state/UI) і **НІКОЛИ в activity_log**. У 🧾 Лог роботи бота йдуть
     ЛИШЕ реальні події з основного шляху: **fired** (`signal`) та **filtered**
     (`signal`+`rejected`). Статуси stale/duplicate/first_sight/epoch/no_candidate
     — тільки в `vob_diag` (структуровано, для UI-колонки «стан VOB»), без флуду.
  `get_state()['vob_diag']` = {symbol→{outcome,label,age,max_age,ft,tf,detail,ts}}
  (+`vob_alert_enabled`). Рішення edge/свіжості — ЧИСТА `_vob_edge_outcome(prev,
  ft,age,max_age)` (юніт-тест). Тест: `test_vob_transparency.py`.
- **CATCH-ALL: обробляємо ОБИДВА напрямки за один цикл (жоден VOB не губиться).**
  РАНІШЕ брався лише ОДИН кандидат — `max(newest_bull.ft, newest_bear.ft)`, тож
  коли за цикл з'являлись і бичачий, і ведмежий OB, протилежний зникав «між
  перевірками». ТЕПЕР `_vob_alert_seen[symbol]` — **ПЕР-НАПРЯМКОВИЙ dict**
  `{'LONG':ft,'SHORT':ft}`, а `_vob_pick_candidates(newest_bull,newest_bear)`
  повертає ОБИДВА (без breaker), відсортовані за часом (найновіший — останній,
  тож його статус лишається у `vob_diag`). Кожен бік проходить свій
  `_vob_edge_outcome` зі СВОЄЮ базою → протилежний новий VOB більше не
  маскується. `vob_one_per_ob`-епоха лишається СПІЛЬНОЮ на 1H-OB (перший фреш її
  витрачає). **Каденція:** VOB перевіряється раз на скан-цикл = повний прохід
  watchlist + `interval_secs` (деф. 60с). Короткоживучі OB (утв.+мітигація в
  проміжку) все одно можна недобачити → лік: менший `interval_secs`. Тести
  `_vob_pick_candidates` + пер-напрямкова база: `test_vob_transparency.py`.
  **UI:** на панелі монети (`templates/smart_money.html`) поряд з бейджем «SHORT
  5M» стоїть бейдж `sm-vob-status-badge` (глобали `wlVobDiag`/`wlVobAlertEnabled`
  з get_state) → показує, ЧОМУ по монеті є/нема реакції: ✅fired/⛔filtered/
  ⌛stale N/поріг/↺duplicate/👁first_sight. Так «бокс висить, а сигналу нема»
  завжди пояснено прямо в UI (стале = блок не свіжий, це не баг).
- **АУДИТ 3 значків watchlist (★ 1H-OB · ▲/▼ 5m-Volumized · ● 15m-тренд) —
  ВСІ до живого джерела (не зламати!).** Скарга: «значки живуть своїм життям».
  Причини й фікси:
  • **●**: сідився з `localStorage` → на завантаженні показував МИНУЛУ сесію ~10с.
    **Фікс:** `wlTrends={}` на старті, БЕЗ `localStorage` (ні init, ні setItem).
  • **★**: бекенд `get_chart_data` для свіжої монети рахував OB **інлайн на
    чарт-TF (5m)**, а бейдж підписував «OB 1H» → мітка ≠ дійсність. **Фікс:**
    інлайн-фолбек ПРИБРАНО — `last_ob` лише з реального рядка `sob_smc_ob_state`
    на `ob_filter_timeframe`.
  • **Усі три для ВІДКРИТОЇ монети** = живі значення панелі (`wlObLive`/
    `wlTrendLive`/`wlVolTrendLive`): панель на кожен перерахунок пише їх і МИТТЄВО
    оновлює елементи рядка (`#obst-<sym>`/`#dot-<sym>`/`#vt-<sym>`); `renderWatchlist`
    для `currentSymbol` бере ЗВІДТИ; 10с-полл НЕ перебиває їх кешем. Спільні
    рендери `wlObStarInner()`/`wlVolTriInner()` → рядок і панель малюють ІДЕНТИЧНО.
  Лишок (не критично): `_build_trends(use_restart_fallback=True)` може дати
  персист-тренд для ще НЕ сканованої монети після рестарту — коригується першим
  сканом (~60с). Правило: та сама метрика у двох місцях — ЗАВЖДИ одне значення,
  оновлення на кожну зміну.
  • **КОРІНЬ ▲/▼ «активна зелена / неактивна червона» (не зламати!):** було ДВА
    різні входи в ОДИН детектор. **Головна знахідка:** `_get_tf_data` НЕ клав
    ключ `'klines'`, тож `vol_data.get('klines') or klines_closed` мовчки давало
    **ЗАКРИТІ 3000 барів**, а панель/бокс рахували з `fetch_klines(400)` **із
    живим баром** → різний «останній OB» (і VOB-сигнал спізнювався до 1 TF,
    попри задокументовану вимогу «детекція на ЖИВОМУ барі»). **Фікс:**
    (1) `_get_tf_data` тепер зберігає `'klines'` = СИРІ бари з живим;
    (2) `_latest_volumized_ob(symbol, klines=None)` — ЄДИНА функція для ▲/▼,
    бейджа «5M» і боксу; ліміт `VOB_KLINES_LIMIT=3000` (було 400 → на частині
    монет OB не потрапляв у вікно і трикутник ЗНИКАВ);
    (3) скан кличе її зі СВОЇМИ барами (`klines=vol_klines`) → БЕЗ зайвого
    мережевого запиту, результат пишеться в той самий 30с-кеш, що читає панель.
    Результат: watchlist == панель == бокс для ВСІХ монет + швидша реакція VOB.
    Сигнали VOB лишились на `vol_result.newest_bull/bear` (той самий live-набір).
- **▲/▼ Volumized-тренд у watchlist = ОДНЕ ДЖЕРЕЛО з бейджем «5M» на панелі
  (не зламати!).** Був розхлябаний баг: у рядку watchlist ▼(червоний/SHORT), а на
  панелі «🟢 LONG 5M» — бо трикутник брався зі стейт-знімка фонового скану (~60с),
  а бейдж рахувався наживо при кліку. **Фікс:** `wlVolTrendLive[sym]` — панель на
  КОЖЕН перерахунок пише живий тренд і МИТТЄВО оновлює `#vt-<sym>`; `renderWatchlist`
  для ВІДКРИТОЇ монети бере тренд ЗВІДСИ, спільний рендер `wlVolTriInner()` (панель
  і рядок малюють ІДЕНТИЧНО). Правило: та сама метрика у двох місцях UI ЗАВЖДИ з
  одного значення, оновлення — на кожну зміну, а не «коли задумається».
- **Швидкість:** детекція лишається на ЖИВОМУ барі (`vol_data['klines']`) — НЕ
  переходити на закриті бари (це додало б до 1 TF затримки). Сигнал іде в ту ж
  мить, щойно OB став валідним.
- **⏱ ПРОФІЛЬ ЦИКЛУ + КЕШ БАРІВ (не зламати!).** Кожен цикл пише
  `[SMC] Scan #N: X symbols in Ys (Zs/coin) — main · tf · ob · vob`. Заміряно на
  проді: 33 монети ≈ 235с (7.1с/монету), з них `tf` 76с + `main` 37с + `ob` 11с =
  **мережа ~53%**. Причина: на монету тягнеться 3000 барів 15m + 3000 5m + 3000 1h
  (PD) + 700 1h (OB), а біржа віддає max 1000/запит → ~10 HTTP на монету.
  **Фікс без втрати якості:** `_get_tf_data(tf, use_cache=True)` кешує РЕЗУЛЬТАТ
  (klines+structure) МІЖ циклами до моменту закриття поточного бару — бо ці
  споживачі (PD, opposite-exit OB) беруть ЛИШЕ закриті бари, отже дані фізично не
  змінюються всередині бару (1H-дані качались ~15 разів на годину даремно).
  **Volumized TF викликається з `use_cache=False`** — там потрібен ЖИВИЙ бар.
  ⚠️ НЕ «оптимізувати» пропуском PD-розрахунку: `get_pd_pct()` живить банер
  Decision → повернеться баг «банер vs бейдж». І НЕ зменшувати глибину барів для
  Volumized: емпірично при 400 барах трикутники зникали на частині монет.
- Далі — `_signal_allowed` (OB/PD/Forecast) → `on_signal` → черга/угода.
- Тест: `test_vob_freshness.py`.
- **«1 VOB на 1H OB» = НУМЕРАЦІЯ VOB** (`vob_one_per_ob`, дефолт OFF). Проста
  детермінована логіка користувача: 1H-OB — це ТАКТ, у межах якого VOB
  НУМЕРУЮТЬСЯ; **VOB #1 = СИГНАЛ, #2,3,… — лише номер (НЕ сигнали)**.
  • `_ob_bt = _current_ob_bartime(sym)` (bar_time OB на `ob_filter_timeframe`);
    None → `no_1h_ob` (без 1H-OB сигналу нема).
  • `_vob_epoch_reset_needed(prev,ob_bt)` (інший bar_time) → **обнуляємо
    `_vob_counter[sym]=0`** і базуємо НАЯВНІ 5m-VOB (`_vob_alert_seen[sym]={side:ft}`
    → вони #0). Тобто наявний блок при появі 1H-OB **не сигнал** — ЧЕКАЄМО НОВИЙ
    (рішення користувача, розвилка B).
  • Кожен НОВИЙ VOB (обидва боки, `prev None or ft>prev`, **БУДЬ-ЯКИЙ вік — без
    вікна свіжості**, жодного не пропускаємо) → `_vob_counter[sym]+=1`, номер `_n`.
  • **«1 РЕЗУЛЬТАТИВНИЙ сигнал на 1H-OB» (вимога користувача).** Такт витрачає
    ЛИШЕ той VOB, що **пройшов УСІ фільтри й пішов на відкриття**, а не «будь-який
    перший». `_vob_is_signal_candidate(fired_epoch, ob_bt)` = кандидатом є КОЖЕН
    новий VOB, доки `_vob_epoch_fired[sym] != ob_bt`. Відсів фільтром такт **НЕ**
    витрачає → наступний VOB (#2, #3…) знову йде у фільтри. Після fire →
    `_vob_epoch_fired[sym]=ob_bt`, решта VOB цього 1H-OB → outcome `numbered`.
    У логах номер видно: «Свіжий Volumized OB (5m) LONG · VOB #3 · <розклад>».
  • Наступний свіжий 1H-OB → лічильник знову 0 → перший новий VOB знову #1.
  • **ТАКТ = `bar_time` КОЖНОГО 1H-OB, а НЕ напрямок (рішення користувача —
    НЕ міняти без його слова!).** LONG→LONG: наступний БИЧАЧИЙ 1H-OB теж
    СКИДАЄ такт і бот знову чекає новий 5m-VOB у відповідному напрямку; так
    само SHORT→SHORT. Тобто «один результативний VOB на КОЖЕН 1H-OB», а не
    «на фазу напрямку». Була спроба перевести такт на напрямок (щоб новий OB
    того самого боку НЕ поновлював) — користувач це ВІДХИЛИВ, зміну
    відкочено. Тест-замок: `test_every_new_1h_ob_resets_even_same_direction`.
  • **⚠️ СКИДАННЯ НЕ КОВТАЄ БЛОК, ЩО ВИНИК ПІСЛЯ 1H-OB (кейс NEARUSDT).** 1H-OB
    детектується на ЗАКРИТИХ 1h-барах → сканер бачить його із запізненням до
    ГОДИНИ. Скидання базувало ВСІ наявні 5m-блоки — разом із тим, що утворився
    ВЖЕ ПІСЛЯ 1H-OB, тобто саме той, який мав стати #1. Наслідок: на графіку
    висів 🟢 LONG-блок + бичачий 1H-OB, а фаєрились ЛИШЕ зустрічні SHORT (їх
    різав OB-фільтр) — «100% сигнал» мовчки пропускався. **Фікс:** базуємо ЛИШЕ
    блоки з `_vob_before_ob(ft, ob_bt)` (`ft <= ob_bt`); блок, що виник ПІСЛЯ
    1H-OB, лишається неопрацьованим → стає VOB #1 і йде у фільтри.
    Тест: `test_reset_does_not_swallow_block_formed_after_1h_ob`.
  • **ПЕРСИСТ (не зламати!):** увесь стан такту зберігається в БД під
    `DB_KEY_VOB_STATE='smc_vob_state_v1'` (`_persist_vob_state`/`_load_vob_state`):
    `seen`(пер-напрямкова база) + `epoch` + `fired` + `counter`. Пишеться на
    НОВОМУ такті та після РЕЗУЛЬТАТИВНОГО fire, читається в `__init__`. Без цього
    кожен рестарт (а `botupdate` робиться часто) обнуляв такт → UI показував
    «VOB 0 · чекаємо #1», і бот міг відкрити ДРУГУ угоду на ТОМУ САМОМУ 1H-OB.
    Тест round-trip: `test_vob_transparency.py`.
  **OFF-гілка = catch-all** (`_vob_edge_outcome` по кожному боку, дефолт без вікна
  віку — кожен новий VOB фаєрить). Пуре-хелпери: `_vob_epoch_reset_needed`,
  `_vob_is_signal_number`, `_vob_pick_candidates`, `_vob_edge_outcome`. Старі
  `_vob_epoch_decision`/`_vob_epoch_already_fired`/`_vob_epoch_fired` лишились як
  тест-хелпери, у нумерації НЕ використовуються (їх замінив лічильник). **ВАЖЛИВО:** 1H-OB стабільний у межах бару (детекція на ЗАКРИТИХ
  1h-барах) — тому re-open у межах години НЕ може бути «новим OB»; якщо угода
  re-open після видалення в межах бару — шукати ІНШИЙ шлях (черга Черга-4/
  reconcile), а не епоху. Тест: `test_vob_one_per_ob.py`.
- **ГЕЙТ НА РІВНІ ВІДКРИТТЯ (переживає close/delete) — фікс «фантомного re-open»
  GRAMUSDT.** Кейс: угоду закрили → за ~54с вона ВІДКРИЛАСЬ ЗНОВУ (мітка
  `Volumized OB → Черга-4`), «хоча в черзі її не було». Причина: scanner-глушіння
  `vob_one_per_ob` придушує лише АЛЕРТ, а `_vob_ob_epoch` — in-memory у сканері;
  але шлях відкриття інший: НОВИЙ 5m VOB (інший `formation_time`, ТОЙ САМИЙ 1H-OB)
  → `on_signal` → `intercept` → `_pending4` (бо після закриття `_fuel_managed`
  звільнено reconcile-ом і `_already_open`=False) → `_engine_tick_queue4` знову
  відкрив. Черга не показувала монету, бо `_pending4` попається ПРИ відкритті.
  **Фікс (fuel_filter, гейт лише коли scanner.vob_one_per_ob=ON):**
  `_opened_ob_epoch[sym]=bar_time` — позначка «вже відкривались на цьому 1H-OB»,
  ПЕРСИСТЕНТНА (переживає рестарт), тримається ПОПРИ закриття/видалення,
  скидається ЛИШЕ на НОВОМУ 1H-OB. Джерело bar_time — ТЕ САМЕ, що у сканера
  (`_ob_epoch_now` = `get_smc_ob_state(sym, ob_filter_timeframe)`).
  Перевірка `_ob_epoch_already_opened` стоїть у ДВОХ місцях: (1) `intercept` — НЕ
  ставити монету в Чергу-4 (`... and not _ob_epoch_used`); (2)
  `_engine_tick_queue4` — перед відкриттям (і прибрати з черги). Позначка
  ставиться `_mark_ob_epoch_opened` після успішного Q4-відкриття. Тобто «1 угода
  на 1 1H-OB» тепер гарантується на ЄДИНОМУ вузлі відкриття, а не лише глушінням
  алерту. Дефолт OFF → стара поведінка без змін. Тест: `test_vob_open_epoch.py`.

## SMC-чарт (/smart-money): налаштування відображення

Гармошка **⚙️ Settings** керує чартом. Ключові фічі:

- **🏁 Swing High/Low (Display, `show_swing_hl_enabled`/`swing_hl_timeframe`,
  дефолт OFF/1H).** Дві горизонтальні лінії — ТОЧНО як у TradingView/LuxAlgo.
  Рівні = trailing swing-екстремуми ДИЛІНГ-ДІАПАЗОНУ (`SMCScanner.
  _swing_trailing_range` — ЄДИНЕ ДЖЕРЕЛО з PD-зоною, тому лінії й бейдж
  Premium/Discount ніколи не розходяться). Назви за swing-трендом
  (`_swing_hl_labels`): бичачий → «Weak High»(зверху)+«Strong Low»(знизу);
  ведмежий → «Strong High»+«Weak Low». НЕ брати «останній HH/LH та HL/LL півот»
  — це давало проміжні хибні рівні (кейс ATOM: 1.3937/1.3252 замість 1.566/1.33).
  Стан: `swing_levels`={high:{price,label},low:{price,label}}|None, `show_swing_hl`,
  `swing_hl_tf`. Фронт малює з `swing_levels` (динамічні підписи).
  Тест: `test_swing_hl_levels.py`.
- **🔮 Forecast min-strength (`forecast_min_strength`: off/moderate/strong).**
  Поріг СИЛИ (впевненості) прогнозу ДО увімкнених 1H/4H-фільтрів напрямку: збіг
  напрямку зі слабшою за поріг впевненістю → 'weak' (не-збіг, ріже сигнал).
  strong ≥66%, moderate ≥40%. У `_forecast_filter_allows` (smc_scanner).
- **📦 Volumized OB TF** тепер включає **5m** (ALLOWED_VOL_TFS).
- **Два OB-бокси на графіку одночасно.** `volObPrim` (Volumized OB, зелений/
  червоний суцільний) і `obMatchPrim` (Require OB Match, teal/orange пунктир) —
  ДВА незалежні `VolOBPrimitive`, малюються обидва, коли увімкнені відповідні
  тумблери. **Критично:** старт боксу прив'язувати через `_snapSecToChart(sec,
  ohlc)` — `timeToCoordinate()` повертає null для часу МІЖ барами, тож OB на TF,
  дрібнішому за TF графіка (Volumized 5m на графіку 15m), інакше НЕ малюється.

## ФІЛЬТРИ ВХОДУ — СПІЛЬНІ ВОРОТА для ВСІХ сигналів (не зламати!)

`SMCScanner._signal_allowed(symbol, side)` — ЄДИНІ ворота УСІХ фільтрів, кожен
НЕЗАЛЕЖНИЙ зі своїм тумблером. Викликаються з ОБОХ шляхів входу:
1. **CHoCH/BOS** → `_send_alert` (як і було);
2. **Volumized OB alert** (`vob_alert_enabled`) → перед `_tm.on_signal(...,
   opened_by='vob_alert')` у скан-тіку.

Ланцюг незалежних фільтрів (кожен за своїм тумблером):
- **OB** (`ob_filter_enabled`) · **PD** (`use_pd_zone_filter`) ·
- **Forecast НАПРЯМОК** (1H/4H match + AND/OR) — `_forecast_filter_allows`, ЛИШЕ
  напрямок (силу НЕ враховує — `min_conf=0`);
- **Forecast МІН.СИЛА** — ОКРЕМИЙ `forecast_strength_filter_enabled` +
  `forecast_min_strength` (`_forecast_strength_allows`: пропускає лише якщо є
  прогноз у бік сигналу з впевненістю ≥ поріг на 1H або 4H);
- **Decision-вердикт (осн. напрямок)** — `decision_filter_enabled`
  (`_decision_gate`/`_decision_filter_allows`): банер Decision Center має
  рекомендувати ТОЙ САМИЙ напрямок (LONG/SHORT), що й сигнал (достатньо бути в
  осн. напрямку); NEUTRAL/протилежний → блок. 10с-кеш; at_intake — блок лише на
  протилежний, строго при відкритті (`_open` re-gate). ЦЕ — про банер «LONG 80%
  СИЛЬНИЙ», НЕ про Forecast-бейджі 1H/4H (їх гейтить Мін.сила).
- **POC «краще LONG/SHORT»** — `poc_filter_enabled` (`_poc_filter_allows`:
  напрямковий — ціна нижче POC → LONG, вище → SHORT, на POC → нейтрально). Рахує
  через ТОЙ САМИЙ `detection.volume_profile.compute_poc`/`price_vs_poc`, що й бейдж
  чарту, з ТИМИ САМИМИ параметрами: `poc_filter_market`/`poc_filter_tf`/
  `poc_filter_window_days` (дефолти FUTURES/1H/3д) → вердикт фільтра 1:1 з чартом.
  `compute_poc` має власний TTL-кеш (не б'є біржу на кожен сигнал).
Тести: `test_independent_filters.py`.

**Прозорість + «чекати вердикт»:**
- `_signal_allowed` повертає **(allowed, reason, detail)**; `detail` — РОЗКЛАД
  ЗНАЧЕНЬ кожного увімкненого фільтра (✓/✗ + числа: прогноз 1H/4H сторона%,
  Мін.сила поріг, PD %, OB tf, POC). Пишеться у 🧾 Лог РАЗОМ із сигналом
  (`Свіжий сигнал … · <detail>`) — щоб було видно, що КОЖЕН фільтр відпрацював
  (`_forecast_pair` дає сирі значення прогнозу).
- **Forecast «чекати вердикт», а не викидати:** `_signal_allowed(at_intake=True)`
  на ІНТЕЙКУ по прогнозу блокує ЛИШЕ явну протилежність; nodata/neutral →
  пускаємо в чергу (сигнал ЧЕКАЄ). Строга перевірка (match + Мін.сила) — при
  ВІДКРИТТІ: `fuel_filter._open` re-gate кличе `_forecast_filter_allows`/
  `_forecast_strength_allows` (строго) для scanner-опенів (не funding) → якщо
  вердикту ще нема / проти → `return False` (монета лишається в черзі, ретрай).
- Telegram open/close повідомлення БІЛЬШЕ не несуть рядок `🏷 opened_by` (прибрано
  на прохання) — мітка лишається в таблицях/лозі/модалці, але не в TG.

### 📐 «1H OB ЛИШЕ З CHoCH» (`ob_filter_choch_only`, дефолт УВІМК)

Order Block створюють **ОБИДВІ** події структури — `ob_detector` фаєрить
`storeOrdeBlock` і на BOS, і на CHoCH (точна Pine-семантика), а тег осідає в
`created_by_tag` (`sob_smc_ob_state`). Змістова різниця:
- **CHoCH-блок** — точка РОЗВОРОТУ, перший блок нової структури;
- **BOS-блок** — ПРОДОВЖЕННЯ, тренд уже йде і пробиває черговий свінг.

Раніше `_ob_filter_allows` дивився ЛИШЕ напрямок (умову «тільки CHoCH» колись
прибрали, але застарілий абзац лишався в docstring і суперечив коду — виправлено).
Тепер це **окремий тумблер**: УВІМК → ворота приймають лише CHoCH-створений блок.
- ⚠️ Перевіряється **ПОТОЧНИЙ (останній немітигований)** блок — детектор зберігає
  саме його. Тобто «лише з CHoCH» = «поточний блок мусить бути CHoCH-створений»;
  якщо після нього встиг сформуватись BOS-блок, вхід блокується (рух пішов у
  продовження). Це і є задуманий тісніший сетап, а не обмеження реалізації.
- ⚠️ Це **УТОЧНЕННЯ воріт `ob_filter_enabled`**, а не окремий фільтр: при
  вимкненому OB-фільтрі тумблер не робить нічого.
- ⚠️ На **малюнок ★-блоку**, **Manual SL** (`queue4_sl_source='1h'`) і **такт
  `vob_one_per_ob`** (`_current_ob_bartime`) НЕ впливає — вони й далі беруть той
  самий рядок БД. Тумблер — про ДОЗВІЛ НА ВХІД, а не про те, який блок існує.
- ⚠️ Рядок без тега (стара БД-строка) при УВІМК **блокує** — «будь-яка
  невизначеність = блок» — але скан перераховує рядок щотіку, тож це один цикл.
- **Прозорість:** `_ob_state_label(symbol)` дає `BEARISH/BOS`, і розклад у 🧾 Лозі
  тепер `OB(1h лише CHoCH BEARISH/BOS):✗`. Причини РОЗРІЗНЕНІ: «нема блоку» /
  «проти напрямку» / «створено BOS — продовження, а не CHoCH».
- UI: чекбокс `sm-ob-choch-only` у ряду типів сигналів (поряд із 🔒 1 VOB на 1H OB);
  у шапці налаштувань — `OB 1H (лише CHoCH)`.
- Дефолт — НОВИЙ ключ, тож `merged.update(stored)` віддає саме його; міграція
  (як `pilot_autofill_migrated_v1`) тут НЕ потрібна.
Тест: `test_ob_choch_only.py` (11).

### 💧 ФІЛЬТР ЛІКВІДНОСТІ ЗА НАПРЯМКОМ (`liq_filter_enabled`, дефолт OFF)

Ще один НЕЗАЛЕЖНИЙ фільтр у тому самому ланцюгу. Пропускає сигнал, лише коли
ліквідність **У БІК УГОДИ** ≥ порогу.
- **«За напрямком»** = частка маси, що ТЯГНЕ ціну туди, куди входимо:
  **LONG → `above_pct`**, **SHORT → `below_pct`** (та сама конвенція, що фарбує
  драбину: вище ціни = вгору = зелений). Переплутати боки = фільтр пускатиме
  рівно протилежне; тест-замок `test_direction_uses_the_side_that_pulls_our_way`.
- Налаштування (дефолти — дослівно за вимогою користувача): `liq_filter_exchange`
  **binance** · `liq_filter_bars` **336** (14 діб) · `liq_filter_min_pct` **75**.
  Сам тумблер — **OFF**, як і решта фільтрів блоку: новий фільтр не має мовчки
  звузити потік на робочій установці.
- **Джерело — ТА САМА `liq_scan.scan_one`**, що живить 📡 Tickr, тож «68% вниз»
  у фільтрі й на сторінці — одне й те саме число (урок PD-зони).

⚠️ **FAIL-OPEN, коли даних немає** — свідоме рішення, НЕ недогляд. Джерело
ЗОВНІШНЄ, і Binance регулярно блокує IP дата-центрів (418/451 — це вже
задокументовано для банера настрою). Якби недоступність блокувала, одна відмова
біржі тихо зупинила б ТОРГІВЛЮ. Це навмисно ІНАКШЕ, ніж в OB-фільтрі: там дані
наші власні (БД) і зʼявляться наступним тіком скану.

⚠️ **КЕШ ОБОВʼЯЗКОВИЙ** (`liq_filter_ttl_sec`, 300с). Ворота кличуться і на
сигнал, і на КОЖЕН тік Q4-recheck по кожній монеті черги — без кешу це сотні
HTTP за хвилину. Ключ = `(symbol, exchange, bars)`; **невдача кешується теж** —
інакше найбільший тиск на біржу припадав би саме на момент, коли вона лежить.
Зміна будь-якого параметра ЧИСТИТЬ кеш (інакше фільтр ще 5 хв відповідав би за
старою біржею).

- **🚦 Індикація доступності:** `GET /api/smc/liq-exchange-check` (проба =
  поштучний OI по BTC, тобто РІВНО той виклик, що робить фільтр; `?cached=1` —
  віддати останній стан без запиту, ним користується автополл). Бейдж
  `#sm-liq-ex-badge` показується **лише коли фільтр увімкнено**: 🟢 доступна ·
  🔴 недоступна (у тултипі — причина І попередження, що фільтр зараз НЕ блокує) ·
  ⏳ перевіряю. Клік = перевірити зараз. Стан оновлює і сама РОБОТА фільтра, не
  лише ручна проба.
⚠️ **СТОЇТЬ ОСТАННІМ У ЛАНЦЮГУ І РАХУЄТЬСЯ ЛИШЕ ЯКЩО РЕШТА ПРОПУСТИЛА**
(вимога користувача: «щоб не навантажувати біржу лишніми зверненнями»). Це
ЄДИНИЙ фільтр воріт, що ходить у ЗОВНІШНЮ біржу; решта читає власну БД/кеші,
тому вони рахуються ЗАВЖДИ — розклад мусить показувати всі увімкнені фільтри.
⚠️ Мало було ПЕРЕСУНУТИ блок у кінець: раніше кожен фільтр рахувався незалежно
від `allowed`, тож економію дає саме `if not allowed:` перед викликом. Порядок
лише робить її можливою. **Додаючи новий фільтр із мережевим запитом — ставити
його ТУДИ Ж.** Пропущений фільтр пише в розклад чесне
`Ліквідність[BINANCE≥75.0%]:⏭ не перевірялась (сигнал уже зарізано раніше)`,
а не мовчить (інакше виглядало б, ніби він пропустив).

### 🔁 ФОЛБЕК НА BYBIT, коли монети немає на обраній біржі (кейс MNTUSDT)

**Що показав скрін (04.09):** `Ліквідність[BINANCE≥70.0%](немає даних: MNTUSDT
не знайдено на binance (перевір назву)):✓` — монета просто **НЕ лістована на
Binance Futures**, тож фільтр проходив «порожняком» (fail-open), а 🧲 магніт
для TP-2 не рахувався взагалі.

**Фікс — у `_liq_snapshot`, тобто в ОДНОМУ місці.** Будь-яка невдача обраної
біржі (немає монети / немає OI / біржа лягла) → пробуємо
`LIQ_FALLBACK_EXCHANGE = 'bybit'`. Чому саме Bybit: **це біржа, на якій бот
ТОРГУЄ**, отже кожна монета watchlist там є ЗА ВИЗНАЧЕННЯМ — фолбек не «вгадує»,
а бере єдине надійне джерело.

⚠️ **Одна правка покриває ВСІ три шляхи** (вимога користувача: «при утворенні
сигналу і у відкритті угоди як автоматичному так і ручному») саме тому, що
джерело ОДНЕ: `_liq_filter_allows` (сигнал) · `get_liq_magnet` → TP-2 (відкриття,
і авто, і ручне — `_pilot_tick` працює для КОЖНОЇ відкритої позиції незалежно
від `opened_by`).

⚠️ **Фолбек НЕ мовчазний.** У зрізі `exchange` = хто РЕАЛЬНО дав числа,
`requested_exchange` = що обрано в налаштуваннях, `fallback` = прапорець.
У розкладі це видно: `Ліквідність[BINANCE≥70.0%](82.0% вгору · через BYBIT
(фолбек)):✓`. Інакше поріг «BINANCE≥70%» стояв би поруч із числом Bybit і
звірити його з біржею було б неможливо.
⚠️ Обидві мовчать → fail-open (як і було), але причина називає ОБИДВІ спроби.
⚠️ Обрано Bybit → фолбеку на себе немає (марна робота).
⚠️ Невдача коштує ДВІ спроби, тому кеш тут ще важливіший: 8 перевірок = 2 запити.
⚠️ **Сторінка 📡 Tickr біржу НЕ підміняє** — там користувач обирає її свідомо,
і мовчазна заміна була б брехнею. Фолбек живе саме у воротах/магніті.
🚦 Бейдж: 🟡 `BINANCE мовчить → BYBIT` (окремий стан `.warn`) — «недоступна» і
«працюємо через фолбек» це РІЗНІ речі для торгівлі.

- **Прозорість:** розклад у 🧾 Лозі — `Ліквідність[BINANCE≥75.0%](24.0% вгору):✗`,
  причина несе число і поріг.
- UI: секція «💧 Liquidity Filter» одразу після OB Filter; у шапці налаштувань —
  `· 💧 75% BINANCE`.
Тест: `test_liq_filter.py` (29).

**НІКОЛИ не відкривати сигнал в обхід `_signal_allowed`.** Був дефект: VOB-alert
кликав `on_signal` напряму й ОБХОДИВ усі фільтри — бот відкрив ASTERUSDT SHORT,
попри `Require Forecast 1H+4H match · AND · Сильний` з прогнозом LONG (фільтри
живуть лише у `_send_alert`, а VOB туди не заходив). Урок: КОЖЕН новий шлях
відкриття мусить проходити ці спільні ворота, інакше налаштування користувача —
фікція. Заблокований сигнал → `log_activity('rejected', reason)`.
Тест: `test_signal_gate_unified.py`.

## 🏷️ Мітки угод «Сигнал → Двигун» (не зламати!)

Кожна угода несе мітку ПОХОДЖЕННЯ у форматі **"<signal> → <engine>"**, щоб було
видно, ВІД ЯКОГО СИГНАЛУ вона пішла (раніше двигун черги штампував свою мітку, а
оригінальний сигнал губився — напр. ASTER SHORT від Volumized-OB показувався як
«Fuel Auto-Filter»).

- **Єдине джерело правди:** `detection/signal_labels.py` — `compose(signal,
  engine)`, `pretty_opened_by(raw)`, `signal_code_of(raw)`, мапи
  `SIGNAL_BADGES`/`ENGINE_BADGES`. **JS-дзеркало** ОБОВʼЯЗКОВЕ і має бути
  синхронним: у `templates/smart_money.html` та `infosite/app.js`
  (`prettyOpenedBy`, `signalIconHtml`, `SIGNAL_ICON_JS`).
- **Зберігаємо МАШИННІ коди** у `position['opened_by']` (напр. `vob_alert → Q4`),
  а бейджі (🟪 Volumized OB → 🎯 Черга-4) робимо лише ПРИ ПОКАЗІ. Так підрядки
  `funding`/`POC-сетап`/`external`/`choch` лишаються для логіки (substring-
  перевірки, `signal_code_of` для порівнянь).
- **Кожен двигун FF** тепер відкриває з `opened_by=_ob_compose(info.get('kind'),
  '<ENGINE>')` (Q1='EXH', Q2, Q3, Q4, Q3-VOB(funding)); `kind` = оригінальний
  сигнал із черги. Прямі опени (FF off) лишають сирий код сигналу (без двигуна).
- **Показ СКРІЗЬ** (вимога користувача — «скрізь означає скрізь»): іконка-
  «картинка» сигналу СТОЇТЬ ПОРЯД ІЗ НАЗВОЮ МОНЕТИ у КОЖНІЙ таблиці:
  • угоди — `histSymOpen`/`histSymCell`/архів/інфосайт;
  • черги Черга-1/2/3 — `ffQueueRowHTML` (з `t.kind`); Черга-4 — власний рендер
    (backend timers4 несе `kind`);
  • 🎯 POC-сетап — константний бейдж 🎯 (`signalIconHtml('POC-сетап')`);
  • 💰 Funding — МММ — уже має 💰 біля символу (джерело — funding-сканер).
  Плюс повна мітка у 🧾 Лозі (відкриття+закриття), у Telegram (рядок `🏷`), у
  модалці історії («🏷 Джерело»). DB-колонка `opened_by` розширена 40→80.
  ⚠️ Черги несуть СИРИЙ `kind` (сигнал), а не `opened_by` — двигуна ще нема
  (угоду ще не відкрито); `signalIconHtml` працює і на сирому коді.
- Коди сигналів: `choch`/`choch_bos` (smc_scanner on_signal), `vob_alert`
  (Volumized-OB alert), `vob` (funding-VOB), `poc` (POC-сетап → черга FF),
  `opp` (реверс), `external`, `manual`.
- **ФАНТОМНИЙ «CHoCH» — виправлено.** `poc_setup._route_to_ff` жорстко ставив
  `intercept(kind='choch')` → запис POC-сетапу в черзі показувався як «CHoCH»,
  хоча CHoCH-алерти вимкнені (`choch_alerts_enabled=False`; `_process_alerts`
  чесно виходить рано — реальний CHoCH НЕ фаєриться). Тепер `kind='poc'` →
  правильний бейдж 🎯 POC-сетап скрізь. `intercept._kind_lbl` теж знає 'poc'/'opp'.
  Урок: НЕ використовувати чужий код сигналу як дефолт при маршрутизації в чергу.
- Тести: `test_signal_labels.py`.

## Як запускати

- Точка входу: `main_bot.py` (для gunicorn: `gunicorn main_bot:app`).
- Flask-фабрика: `web/flask_app.py` → `create_app()`; об'єкт `app`.
- Локально dev: `python web/flask_app.py` → `0.0.0.0:5000`.
- Прод-порт береться з env `PORT` (дефолт 10000), хост через `RENDER_EXTERNAL_URL`.
- Перевірка синтаксису після правок: `python -m py_compile <файл>`.

## Архітектура (головне)

- **`web/flask_app.py`** — усі HTTP-маршрути та JSON API. Великий файл (~5600 рядків).
- **`detection/fuel_filter.py`** — ядро стратегії «FF» (liquidation-fuel + черга на
  вхід + двигун відкриття). Daemon-клас `FuelFilterDaemon`. Синглтон:
  `get_fuel_filter()`, ініт `init_fuel_filter(...)`. Стан читається через
  `ff.get_state()`.
- **`detection/trade_manager.py`** — відкриття/закриття позицій, перехоплення
  сигналів у чергу FF, формування Reason.
- **`detection/funding_monitor.py`** — сканер funding-ставок (окрема таблиця 💰).
- **`detection/smc_scanner.py` / `smc_analyzer.py`** — детекція CHoCH / CHoCH+BOS
  (TF 15m). ТІЛЬКИ свіжий CHoCH/CHoCH+BOS за напрямком увімкненої кнопки потрапляє
  в чергу FF.
- **`storage/db_operations.py`** — шар БД. `get_db()`, методи
  `db.get_setting(key, default)` / `db.set_setting(key, value)` (значення —
  JSON-серіалізовні).
- **`templates/smart_money.html`** — головна UI-сторінка стратегії FF.

## Ключові конвенції стратегії FF (не зламати!)

- **СЕАНСИ ММ (головна логіка черги).** Сеанс = зафіксований напрямок ММ BTC
  (LONG/SHORT), тримається в `_btc_verdict_dir`/`_btc_verdict_since`
  (`_update_btc_verdict`). Правила:
  - **WAIT (ML збалансований, |dir| ≤ 0.1) = ПАУЗА:** напрямок сеансу,
    таймер і черга ЗБЕРІГАЮТЬСЯ; `_btc_paused=True`; таймер продовжує лічити;
    банер показує «напрямок · ⏸ ПАУЗА»; двигун під час паузи НЕ відкриває.
  - **Той самий напрямок знову** (LONG→WAIT→LONG) = той самий сеанс.
  - **Протилежний фліп** (LONG↔SHORT) = НОВИЙ сеанс: скид таймера + **чистка
    всієї черги** (`_pending={}`) + скид `engine_attempts`. Це ЄДИНИЙ випадок,
    коли ММ чистить чергу (замість старої per-flip OP-4 чистки протилежних).
- **Черга `_pending` ПЕРСИСТЕНТНА, відновлюється ЗА СЕАНСОМ.** Зберігається в
  БД і відновлюється на старті. Стале не тягнеться, бо на першому тіку після
  старту, якщо live ML протилежний відновленому сеансу → чергу чистить фліп
  сеансу; якщо той самий напрямок/WAIT → монети валідні й чекають далі.
- Монета входить у чергу ЛИШЕ зі свіжого CHoCH/CHoCH+BOS за напрямком увімкненої
  кнопки (`intercept`). Показ у таблиці НЕ фільтрується кнопками (кнопки керують
  лише відкриттям) — інакше при WAIT черга «зникала».
- Паливо (ММ-напрямок) рахується для монет у черзі; на BTC START (сеанс тримає
  напрямок ≥ поріг, не пауза) двигун відкриває угоду, якщо паливо монети
  співпадає з напрямком сигналу.
- 9 операцій видалення з черги пронумеровані й керуються `_QUEUE_OPS_ALLOWED`
  (всі зараз `True`). OP-4 = чистка черги на фліпі сеансу.
- Банер ₿ BTCUSDT = напрямок СЕАНСУ, а джерело live-напрямку — **МММ-БАБЛО**
  (`_fuel_dir_smoothed('BTCUSDT')['status']`), ТОЧНО як комірка «МММ» монет:
  LONG / SHORT / None(`|dir| ≤ 0.1` = ⚖ рівновага «напрямку немає» → WAIT/пауза).
  Контраріанська liqmap-модель (за запитом). Сила банера = |fuel_dir|×100.
  Сеанс-механіка (пауза на WAIT, фліп на протилежний, START після
  `start_signal_minutes`) — без змін; `_btc_trend_dir()` більше не використовується.

## Черга-3 «🎯 Готовність» (стратегія на grade_setup)

Третя, ОКРЕМА черга/двигун (поряд із Чергою-1 і Чергою-2, усі незалежні тумблери).
Живиться тими самими CHoCH/CHoCH+BOS через `intercept()` (гілка `q3` → `_pending3`).

- **Тригер входу:** двигун `_engine_tick_readiness` відкриває монету у напрямку
  кнопки, коли її SMC-«готовність» `grade_setup(...)` **HOT АБО `score ≥
  queue3_open_min_score`** (дефолт **43**; 0 = лише HOT). Строгий HOT
  (score ≥ 70 + усі блоки, без вето) на реальному потоці майже не спрацьовує
  (спостережений max ~66), тож поріг дає стратегії реальні угоди. КАЛІБРУВАННЯ
  53→43: розворотні CHoCH+BOS системно занижуються блоком таймінгу CTR (свіжий
  розворот б'є проти CTR), і хороші угоди падали в «СЕРЕДНІЙ» (38–52) під 53. **БЕЗ ₿ START /
  сеансів ММ** — весь контекст ₿/CTR/зона/ліквідність уже ВСЕРЕДИНІ `grade_setup`
  (див. `detection/setup_grader.py`). Санітарні ворота: кнопки LONG/SHORT, дедуп,
  вже-в-угодах, ціна.
- **Запобіжники Черги-3 (калібровано по activity_log).** Дані показали, що з 116
  «_open відхилив» більшість — жорсткі вето виснаженості (>80%) і слабкого МММ на
  сетапах, що вже пішли в наш бік (напр. NEARUSDT SHORT SCORE 53 ХОРОШИЙ, рух +1%,
  вичерп 81-89% → різалось). Тому:
  - `queue3_ignore_exhaustion` (дефолт **True**) — Q3/Q3-VOB пропускають ЖОРСТКЕ
    вето виснаженості у `_open` (`skip_exhaustion=`); виснаженість ЛИШАЄТЬСЯ м'яко
    в `grade_setup`/SCORE. Знімає і `max_exhaustion_pct`-гейт, і safeguard-вето.
  - `safeguard_mm_price_override` (дефолт **True**, tf `safeguard_mm_price_tf`=15m,
    ГЛОБАЛЬНО) — слабкий МММ (<`safeguard_mm_min`) НЕ ріже вхід, якщо `_candle_
    momentum(tf)` = напрямок угоди (ціна чітко йде в бік). Контраріанський МММ
    часто «мовчить» на чистому тренді.
  - `queue3_require_ob_match` (дефолт **True**) — `_ob_match_ok(sym,side,s)`: при
    увімкненому OB-фільтрі сканера НЕ відкривати ПРОТИ OB на `ob_filter_timeframe`
    (ріже лише ЯВНИЙ контр-OB; немає OB → дозволяємо, щоб не вбити Q3-VOB). Гейт у
    Q3 (`_engine_tick_readiness`, OB міг фліпнути за час очікування) і в Q3-VOB
    (`_vob_open_or_trail` — funding-опен взагалі не проходить сканерний `_send_alert`).
  - Лог Q3 «_open відхилив» тепер несе КОНКРЕТНУ причину з `_engine_skip`.
- **CTR більше НЕ ріже розвороти (2 ворота прибрано).** Було: (а) вето «CTR проти
  входу» в `grade_setup` капало бал ≤43; (б) `safeguard_ctr` у `_open` давав хард-
  відмову «CTR проти». Свіжий CHoCH+BOS за визначенням проти CTR → ці ворота
  різали КОЖЕН розворот. Тепер: `setup_ctr_mode` (дефолт **soft**) — CTR у грейді
  партіал (0.2) без вето; `queue3_ignore_ctr` (дефолт **True**) — Q3 пропускає
  CTR-перевірку safeguard (МММ/виснаженість лишаються). `_soft_safeguard(...,
  skip_ctr=)`, `_open(..., skip_ctr_safeguard=)`. Режими CTR: soft/normal/off.
- Читає готовий кеш `self._setup_cache[sym]` (той самий, що й колонка
  «Готовність»); `_pending3` додано в `targets` для `_refresh_setup_cache`.
- **Повне логування** кожного рішення (opened/hold/skipped + розклад блоків) →
  таблиця `sob_readiness_log` + рядок у Лог роботи бота (`activity_log`, source
  `Q3`). Тумблер `readiness_log_enabled` (дефолт ON). Анти-флуд: незмінний
  hold/skip пишеться не частіше ніж раз на `READINESS_LOG_MIN_GAP` (300с).
- Налаштування: `queue3_enabled` (дефолт OFF), `queue3_open_min_score` (43),
  `queue3_ignore_ctr` (True), `queue3_ttl_hours` (6 — протермінування, щоб монети
  не висіли годинами), `setup_ctr_mode` (soft), `readiness_log_enabled`.
  Q3-лог у `activity_log` несе пласкі `su_*` блоки
  (structure/poi/zone/liq/mm/timing/context) — щоб CSV-експорт був аналізовним.
- API: `GET /api/fuel-filter/readiness-log?limit&symbol&outcome`;
  `POST /api/fuel-filter/queue3/{delete,clear}`. UI: тумблер + таблиця «Черга-3»
  на `/smart-money`. get_state → `timers3`/`pending3_visible`/`queue3_enabled`.
- Тести: `test_readiness_strategy.py`.

## ⚡ Скальперська «Готовність» для funding-монет

Окремий SMC-грейд ЛИШЕ для монет таблиці «💰 Funding — МММ» (`_anomalies`) на
швидкому TF. `_compute_setup` параметризовано за TF (`base_tf`/`htf_tf`/
`write_exit`); скальп-варіант зве його з `base_tf='5m'`, `htf_tf='15m'`,
`write_exit=False` (не чіпає 1H-«Готовність виходу»). Окремий кеш
`_setup_scalp_cache` + `_refresh_setup_scalp_cache` (власний TTL/cap, тільки
funding-монети). Той самий `grade_setup` — інші свічки; на швидких TF
структура/зона шумніші → трактувати як ОКРЕМИЙ показник, не 1:1 з 1H.
- Налаштування: `funding_setup_scalp_on` (OFF), `funding_setup_tf` (5m),
  `funding_setup_htf` (15m), `funding_setup_ttl` (45с), `funding_setup_max_per_cycle` (8).
- get_state → funding-рядки несуть `setup_scalp`; UI — колонка «⚡ Скальп» у
  funding-таблиці + блок налаштувань у акордеоні. Інфосайт дзеркалить колонку.
- Навантаження мінімальне: klines кешуються (TTL), тротл cap/цикл; funding-
  підмножина мала (одиниці–десятки монет).
- TG на «хороший сигнал» по колонці Скальп: `scalp_tg_on`/`scalp_tg_min_score`
  (HOT або score≥поріг) / `scalp_tg_cooldown_min` / `scalp_tg_dir`. Метод
  `_scalp_setup_alert` (edge-trigger+кулдаун) у `_run_alerts` → топік 💰 funding.
- Панель `/api/fuel-filter/panel/<sym>` віддає `setup_scalp`; TradingView-оверлей
  (`tools/tradingview_mm_overlay.user.js`, v1.7.3) показує ⚡ Скальп-бейдж.

## 🎯 Шаровий конфлюенс (1..5) для funding-монет + консолідований TG

`_funding_layers(sym, a)` рахує 5 шарів У БІК напрямку монети: 1) МММ ≥ легкий
(сила ≥10) + тренд сили АСИМЕТРИЧНО за напрямком: **SHORT — НЕ слабшає (↑ або →;
гасне ЛИШЕ на ↓)**, бо на встановленому спаді сила часто на плато (→) і строге ↑
гасило шар майже завжди; **LONG — СТРОГО РОСТЕ (↑)**, як було раніше · 2) SCORE ≥
СЕРЕДНІЙ (≥40) · 3) Готовність(1H) ≥ СЕРЕДНІЙ (≥38) · 4) Скальп ≥ СЕРЕДНІЙ (≥38) ·
**5) ЦІНА у бік — свіжий рух (~15 хв) з 💰 Funding Rate Scanner: LONG=РОСТЕ (up),
SHORT=СПАДАЄ (down)**. Кожен шар несе `dir` (напрямок монети, КОЛИ засвічений) →
у колонці кольори за напрямком: **усі зелені (LONG) або всі червоні (SHORT)**.
Повертає {count, `base`(=усі 5), base4, layers[]}. get_state → funding-рядки несуть
`layers`; UI — колонка «🎯 Шари» (`ffLayersCell` фарбує крапки за `l.dir`).
- **5-й шар «Ціна» — ПОСТІЙНИЙ (світиться в колонці)**, живиться
  `self._funding_price` (кеш з `_get_funding_price_dirs()` → `FundingMonitor.
  get_price_dirs()`; оновлюється в тіку поряд з `_funding_trends`). Джерело —
  вікно `PRICE_WINDOW`=15хв, мертва зона `PRICE_DEADZONE`=0.10% (funding_monitor).
- **VOB (1m) = ОКРЕМИЙ ОДНОРАЗОВИЙ ТРИГЕР сигналу (не колонка-шар).**
  `_funding_vob(sym,d)` ПОСТІЙНО моніторить 1m-OB для КОЖНОЇ funding-монети (кеш
  8с) → `detect_volumized_obs(swing=5, ob_end_method='Wick', max_atr_mult=3.5,
  zone_count='Low')` → найновіший НЕ-breaker OB у бік. На НОВОМУ OB
  (`formation_time` ≠ `_vob_seen`) перевіряємо всі 5 шарів САМЕ ЗАРАЗ: якщо
  `base ≥ layer_tg_min` (cap 5) → сигнал; інакше чекаємо наступний OB.
- На НОВОМУ OB (інший formation_time) + кулдаун → ОДИН TG «🎯 Рекомендація бота»
  (`_layer_signal_alert`/`_send_layer_alert`, топік 💰 funding). Це ЗАМІНЯЄ старе
  «рекомендована ботом» повідомлення.
- Налаштування: `layer_tg_on` (OFF), `layer_tg_min` (скільки з 5, дефолт 5),
  `layer_tg_cooldown_min` (30). VOB-параметри — константи `_VOB_*` (1m/5/Wick/3.5/Low).
- Тести: `test_readiness_strategy.py` (funding_layers 5-шарів + ЦІНА-напрямок +
  VOB-alert + all-layers-gate).
- **Сортування таблиці «💰 Funding — МММ» — ЗА ШАРАМИ** (get_state): більше
  засвічених шарів вище; тайбрейк — довше тримається (`held_sec`).

## 🎯 Черга-4: пер-шарові тумблери (економія сервера)

Кожен із 4 шарів Черги-4 має ВЛАСНИЙ тумблер: `queue4_new_mm_on`,
`queue4_old_mm_on`, `queue4_setup_on`, `queue4_runway_on` (усі дефолт True =
стара поведінка). **ВИМКНЕНИЙ шар:** (1) НЕ рахується — `_queue4_layers` не
смикає `_fuel_dir_smoothed`/`_fuel_dir_legacy`, не читає setup-кеш, а
`_refresh_setup_cache` НЕ додає pending4 (важкий grade_setup) → менше load;
(2) колонка ховається у таблиці (класи `q4c-new/old/setup/runway` на th+td,
`_ffApplyQ4Cols()`). `_queue4_layers` повертає `required` = к-сть УВІМКНЕНИХ;
двигун відкриває коли `base ≥ required` (усі увімкнені збіглись; required=0 →
не відкриває). `Новий МММ` і `Запас` беруть дані з того самого `fn`
(`_fuel_dir_smoothed`) — рахуємо `fn` лише якщо хоч один із них увімкнено.
**Поріг «Новий МММ» = «рівновага» (0):** шар зараховується, якщо МММ НЕ ПРОТИ
(нейтраль АБО в бік), блок лише коли явно ПРОТИ (бо при силі ≤10% напрямку немає,
і суворе `nd==side` при «рівновага» ніколи не збиралось). Інші пороги — суворо
(напрямок у бік + сила ≥ порогу). Лічильник у 🎯 = `n/required` (к-сть увімкнених).

## 🎯 Черга-3: авто-відкриття за «VOB + 5 шарів» (funding-монети)

Той самий моніторинг VOB у `_layer_signal_alert` живить ДВА незалежні виходи на
НОВОМУ Volumized OB (edge за formation_time): (1) TG-сигнал (`layer_tg_on`);
(2) авто-відкриття угоди `queue3_vob_open` (дефолт **ON**). Логіка —
`_vob_open_or_trail(sym,a,d,ob,lay,s,now)`:
- **Нова угода:** усі 5 шарів зійшлись + монети ще немає в угоді → `_open(...,
  opened_by='Q3-VOB(funding)', skip_ctr_safeguard=True)` за напрямком. **SL = межа
  блоку OB + буфер**: SHORT → `top*(1+buf)` (над верхом), LONG → `bottom*(1-buf)`
  (під низом); `buf = queue3_vob_sl_buffer_pct/100` (дефолт 0.10%). SL ставиться
  через `tm.update_manual_sl_tp(sym, manual_sl=, is_shadow=)`.
- **Повторний VOB** по вже відкритій ботом монеті → угоду НЕ відкриваємо, лише
  ПЕРЕСУВАЄМО SL на новий блок. Трекер `self._vob_trade` {sym→{side,sl,ftime,
  entry,mode}}; чиститься, коли монета зникла І позиції вже нема.
- **Ворота «проти загального тренду»** (`queue3_vob_block_against_trend`, дефолт
  **ON**): НОВУ угоду не відкриваємо, якщо напрямок проти ЗАГАЛЬНОГО тренду монети
  (`dir_overall` ≈2 год з `_funding_price`): LONG у загальному ↓ / SHORT у ↑ →
  skip (лог `Q3-VOB`). 'flat' — дозволяємо. Трейл SL — без цих воріт.
- **TG «Рекомендація бота»** тепер надсилає `_vob_open_or_trail` (не окремий
  alert-branch, щоб не дублювати), формат мінімальний (`_send_layer_alert(sym,a,
  d,sl,mode)`): `🎯 Рекомендація бота · #SYM 🔴 SHORT` + `🛑 SL: <ціна>` (mode
  'open') / `♻️ Угода вже відкрита — змінено лише SL: <ціна>` (mode 'trail').
  Якщо `queue3_vob_open` OFF, а `layer_tg_on` ON — падає на просту «Рекомендацію»
  (mode 'signal', без SL) за старим порогом `layer_tg_min`.
- Усе (open / sl_moved / skipped) пишеться в 🧾 Лог роботи бота (`activity_log`,
  source `Q3-VOB`) з міткою «монета з фандингу».
- Налаштування: `queue3_vob_open` (True), `queue3_vob_sl_buffer_pct` (0.10),
  `queue3_vob_block_against_trend` (True). UI — у гармошці **«⚙️ Налаштування
  (Черга-3)»** (`templates/smart_money.html`), куди перенесено ВСІ параметри
  Черги-3. Заголовок колонки шарів — лише «🎯».
- **💰-мітка у відкритих угодах** (`histSymOpen` у smart_money + `renderTrades` в
  infosite): монета з фандингу (є в `anomalies` АБО `opened_by` містить funding).
- Тести: `test_vob_open_*` (+ ворота тренду) у `test_readiness_strategy.py`.

## ✦ «Золотий funding» TG — формат + затримка-підтвердження

`_send_gold_alert(sym,a,step)` формат (топік 💰 funding): `✦ FUNDING 🟢LONG /
#SYM / 💰 Funding: -2.000%`. Показуємо СНЕПНУТЕ чисте значення (`step` зі знаком
ставки) → завжди точне (−0.500/−1.500/−2.000%), а не сире (−0.495/−1.503%).
Перед відправкою рівень має протриматись `funding_gold_confirm_sec` (дефолт 30с,
«чітке визначення»); далі повтор раз на `funding_gold_cooldown_min` (60хв), поки
той самий рівень тримається. Тест: `test_gold_funding_confirm_then_repeat`.

## 💰 Funding Rate Scanner — колонка «Price» (dashboard)

Таблиця «💰 Funding Rate Scanner» (`templates/dashboard.html`, дані з
`FundingMonitor.get_watchlist()`) у колонці **Price** показує ЧІТКИЙ свіжий
напрямок ЦІНИ: `price_dir` (up/down/flat) + `price_chg_recent` (% за вікно
`PRICE_WINDOW`≈15хв) — **▲ росте (зелений) / ▼ спадає (червоний) / ▬ рівно**.
Плюс ЗАГАЛЬНИЙ тренд монети (`price_dir_overall`/`price_chg_overall`,
`PRICE_WINDOW_LONG`≈2год, deadzone 0.30%) — рядок «загалом ▲/▼ X%», щоб не входити
проти загального тренду (той самий `dir_overall` живить ворота VOB-відкриття).
Цей самий блок (`price` у get_state anomaly-рядку = `_funding_price[sym]`)
показується ДРУГИМ РЯДКОМ у таблиці «💰 Funding — МММ» (bot + infosite):
«💹 Ціна: ▲ +X% 15хв · 📈 Загалом: ▲ +Y% ~2год», вирівняно в стовпчик.
Це окремо від `price_change` (сумарно від старту стеження, тепер у підказці).
`_recent_price_move(rates)` рахує рух по останніх ~15 семплах (мертва зона
`PRICE_DEADZONE`=0.10%). Ту саму метрику віддає `get_price_dirs()` для 5-го шару.

## Закриті угоди: постійний архів у БД + швидкість закриття

- **Кожна закрита угода (real+paper) БЕЗУМОВНО пишеться в постійну таблицю
  `sob_trade_archive`** (`_archive_closed` → `db.archive_trade`, гейт по
  прихованому `archive_trades` ПРИБРАНО). Ролінг-блоб `tm_closed_trades` (кеп
  `CLOSED_TRADES_LIMIT`=2000) лишається для живої стрічки. get_state віддає ВЕСЬ
  блоб (не 50) — таблиці рендерять усе з захистом-сигнатурою (не «стрибають» під
  час скролу).
- **Таблиці «📜 Recent Closed» / «Recent Paper Closes» — прокрутка + копіювання +
  «за весь час (БД)».** Клас `.tm-vhscroll` (max-height, sticky-заголовок);
  `copyTableToClipboard()` → TSV у буфер; `toggleArchiveView('closed'|'shadow')`
  тягне ВСЮ історію з `/api/trade-archive/export?is_paper=…` і показує read-only
  (жива стрічка тоді не перемальовує — `window._tmArchiveMode`).
- **Швидкість закриття:** цикл монітора виходів — налаштовний
  `monitor_interval_secs` (дефолт **4с**, було жорстко 10с; clamp [2,30]).
  Ціна береться з кешу сканера (без API-хіта), тож частіше = швидше закриття
  без навантаження. Реконсіляція з Bybit — на власному `reconcile_interval_secs`
  (n_ticks рахується від ефективного інтервалу монітора). Сам close-ордер
  reduce-only шлеться синхронно, важке (real-PnL/архів/notify) — у фоні
  (`_finalize_close_async`), тож не блокує монітор.

## Лог Черги-3: рух у черзі + виснаженість

`_log_readiness(..., move_pct=, exhaustion=)` — у `sob_readiness_log` і в UI-лозі
несе **рух ціни від входу в чергу** (`move_pct`, + = у наш бік; `_pending3[sym]
['added_price']`) і **виснаженість** — щоб було видно, що монета вже відпрацювала,
поки стояла в черзі. У рядку логу: «… · рух +2.3% · вичерп 72% · …».

## Telegram-бот (`web/tg_bot.py`) — БЕЗПЕКА розсилки

Відповідь адміна юзеру НІКОЛИ не повинна ставати масовою розсилкою:
- Свайп-Reply на шапку «Повідомлення від користувача» → лише тому юзеру. Звʼязок
  `_reply_map` **персиститься в БД** (`tg_reply_map`, `_load_reply_map` на старті)
  → переживає рестарти. Якщо звʼязок усе одно втрачено — `chat_id` парситься з
  тексту шапки (`_chat_id_from_text`).
- Reply без звʼязку → підказка `/reply <chat_id>`, **не розсилка**.
- Масова розсилка ЛИШЕ за явним `/announce <текст>` (`_admin_broadcast`). Звичайне
  повідомлення адміна → підказка, ніколи не broadcast.
- Тести: `test_tg_reply.py`.

## БД-ключі (storage)

- `fuel_filter_settings` — налаштування FF (вкл. `queue3_enabled`,
  `readiness_log_enabled`).
- `fuel_filter_state` — JSON-блоб стану: timers, fuel_managed, anomalies(=funding),
  engine_attempts, fuel_ema, fuel_hyst, btc_verdict_dir/since, pending, pending2,
  **pending3**.
- `fuel_filter_scan_list` — дозволені для сканування символи.
- Таблиця `sob_readiness_log` — пер-рішення лог Черги-3 «Готовність». Префікс
  `sob_` → автоматично в аналізі Database Administration; входить у
  `_SERVICE_TABLES_TIME` → її ЧИСТИТЬ і ручна «🗄️ Службові», і DB-autoclean.

## Основні JSON API (read-only — для інфо-сайту)

- `GET /api/fuel-filter/state` → весь стан FF (`ff.get_state()`): черга, таймери,
  funding-таблиця, банер BTC, статуси. **Головне джерело для інфо-сайту.**
- `GET /api/stats`, `GET /api/trades`, `GET /api/signals`, `GET /api/sleepers`,
  `GET /api/orderblocks`, `GET /api/health`.
- `GET /api/scheduler/status`, `GET /api/events`.
- Сторінки: `/` (головна), `/smart-money` (FF), `/tickr`, `/trades`, `/settings`.

> Маршрути, що ЗМІНЮЮТЬ стан (POST: toggle, scan, delete-timer, force-open,
> settings, trade/close…) — інфо-сайту НЕ потрібні й не повинні викликатись.

## Git / деплой (важливо для кожної сесії)

- Робоча гілка: `claude/analyze-project-01Gopju9D7AHv4pgvetccBeB`. Розробляти й
  комітити сюди.
- **PUSH ЗАБЛОКОВАНИЙ:** git-relay повертає 403 (egress-політика середовища). Це
  НЕ мережевий збій — retry не допомагає, обходити заборонено.
- **ФОРМАТ ВІДДАЧІ ЗМІН (завжди!):** віддавати користувачу САМІ ЗМІНЕНІ ФАЙЛИ
  (не патч) зі збереженням структури папок, запаковані в **ZIP**. Тобто
  `detection/trade_manager.py` лежить у zip за шляхом `detection/trade_manager.py`.
  Користувач розпаковує в корінь репозиторію й пушить локально.
- **У ZIP — ЛИШЕ файли, змінені в ЦІЙ конкретній правці** (вимога користувача),
  а НЕ весь накопичений набір. Тобто якщо цього разу змінено лише
  `templates/smart_money.html` — у zip лише цей файл (+ `CLAUDE.md`, якщо
  оновлював пам'ять). НЕ перекладати весь бандл щоразу.
- Не створювати PR без явного прохання.

## Окрема сесія для інфо-сайту

Інфо-сайт розробляється в ОКРЕМОМУ чаті. Бриф: див. `INFO_SITE.md`.
Принцип «в парі»: бот пише стан → інфо-сайт лише ЧИТАЄ його через JSON API.
Жодного спільного коду, жодних кнопок керування на інфо-сайті.

## Черга-4: «Старий МММ» — поріг «рівновага» = «не проти» (як у «Новий МММ»)

У `_queue4_layers` режим `require` для **Старого МММ** при `mmo_min <= 0`
(«рівновага») тепер зараховує шар, якщо МММ **НЕ ПРОТИ** (нейтраль АБО в бік);
блок лише коли явно ПРОТИ — ТА САМА логіка, що вже діяла для «Новий МММ».
Причина: при слабкій силі статусу напрямку немає, і суворе `od == side` при
«рівновага» не збиралося НІКОЛИ (кейс: «Старий МММ 7% рівновага» — шар не
зараховувався). Інші пороги (>0) — суворо: напрямок у бік + сила ≥ порогу.

## VOB: «новий блок» = НЕ опрацьований (пастка breaker — не зламати!)

РАНІШЕ новизна VOB визначалась «водяним знаком»: `ft > _seen[side]`. **Пастка:**
коли поточний OB стає **breaker**, він випадає зі списку, і `newest_bull/bear`
стає СТАРІШИЙ блок із МЕНШИМ `formation_time` → умова `ft > prev` НІКОЛИ не
виконується → блок вічно `duplicate`, а UI показує «VOB N · чекаємо #N+1»,
хоча на графіку блок давно змінився (скарга: «висить казна який VOB»).
**Фікс:** `_vob_alert_seen[sym][side]` тепер **СПИСОК опрацьованих ft**
(`VOB_SEEN_CAP=12`), а рішення дає `_vob_outcome(done_list, ft, age, max_age)`:
`duplicate` (ft уже в списку) / `first_sight` / `stale` / `fresh`. Новим є
БУДЬ-ЯКИЙ ще не опрацьований блок — навіть зі старішим часом. Хелпери
`_vob_seen_list` / `_vob_seen_add` роблять міграцію зі старого int-формату
(і в `_load_vob_state`). Тест: `test_vob_outcome_breaker_trap`.

## Watchlist: кнопка ⇅ A-Z

Поряд із лічильником «N / 100» — кнопка `sm-wl-sort-btn` (`toggleWlSort()`):
сортує СПИСОК ПОКАЗУ за алфавітом; монети, що починаються з ЦИФРИ
(1000PEPE, 1INCH…) — у САМИЙ КІНЕЦЬ. Серверний watchlist НЕ змінюється
(`wlSymbols` лишається у серверному порядку, сортується лише копія в
`renderWatchlist`). Стан кнопки — у `localStorage['wlSortAlpha']` (це UI-
налаштування, не дані — на відміну від `wlTrends`, який звідти прибрано).

## ⚠️ Застарілі місця, знайдені при аудиті тестів (не виправлено)

- `test_readiness_strategy.py::test_ob_match_gate` кличе `ff._ob_match_ok(...)`,
  якого в `detection/fuel_filter.py` **вже немає** (метод видалено/перейменовано).
  Тест падає на цьому місці; попередні 12 тестів файлу проходять.
- Виправлено в тому ж файлі: стаби `_funding_vob` відставали від сигнатури
  `(sym, d, vob_tf)`; легасі-тести TG-шарів тепер явно ставлять
  `funding_route_q4=False` (бо дефолт True → VOB іде прямо в Чергу-4).
- **Відновлено мертве налаштування:** у `_layer_signal_alert` змінна `need`
  (`layer_tg_min`) рахувалась, але НЕ застосовувалась → TG-алерт летів на КОЖЕН
  новий VOB. Тепер алерт лише коли `base ≥ layer_tg_min` (як у пам'яті проєкту).

## ⚡ ШВИДКІСТЬ ЦИКЛУ: паралельний префетч барів (не зламати!)

Вимога користувача — «жоден сигнал не втрачений через великі проміжки». Заміряно
профілем: 33 монети ≈ 235с (7.1с/монету), з них **мережа ≈53%** — на монету йде
~7 HTTP-запитів (3000 барів = 3 сторінки по 1000 на КОЖЕН TF; `fetch_klines`
НЕ кешує). Тому:

- **`_prefetch_klines(md, symbols, specs)`** тягне бари для (символ × spec)
  **ПАРАЛЕЛЬНО** (`ThreadPoolExecutor`, `PREFETCH_WORKERS=8`) у `self._prefetch`
  ПЕРЕД послідовним циклом. Обробка лишається ПОСЛІДОВНОЮ — спільний стан,
  сигнали, БД не чіпаємо (жодних гонок).
- **`_prefetch_specs()`** — рівно ті набори, що споживає скан: головний TF
  (`KLINES_LIMIT`), Volumized TF (3000), `ob_filter_tf` (700), `pd_zone_tf`
  (3000). Вимкнений Volumized НЕ вантажиться.
- **`_pf_klines(md, sym, tf, limit)`** — ЄДИНА точка отримання барів у скані:
  бере з префетчу (`pop`, одноразово) або робить звичайний запит. Використана в
  3 місцях: головні бари, `_get_tf_data`, `_update_smc_ob`.
- Пауза між монетами `0.2с → 0.01с` (мережі в циклі вже майже немає; 0.2×33 було
  6.6с чистих втрат). Профіль циклу тепер друкує ще й `prefetch Xs`.
- **`DEFAULT_INTERVAL_SECS` 60 → 10**, нижня межа валідації `30 → 5`. Плюс
  ОДНОРАЗОВА міграція `interval_migrated_v2`: старе збережене значення ≥60 один
  раз переводиться на новий дефолт (далі значення користувача поважається).

⚠️ НЕ паралелити САМУ обробку монет — там спільний стан (`_vob_*`, кеші, БД) і
відкриття угод. Паралельне лише ЗАВАНТАЖЕННЯ. Тест: `test_scan_prefetch.py`.

## 🎚 Пороги «якості» — ЄДИНЕ ДЖЕРЕЛО (setup_grader) · не дублювати числа!

**Кейс-урок (XRPUSDT, Черга-4).** Колонка показувала «38% **СЕРЕДНІЙ**», а поріг
стояв «Готовність ≥ **СЕРЕДНІЙ**» — і шар НЕ зараховувався. Причина: одне слово
означало РІЗНІ числа. Смуги грейда перекалібрували (`_GRADE`: ВІДМІННИЙ 72→**70**,
ХОРОШИЙ 55→**53**, СЕРЕДНІЙ **38**, СЛАБКИЙ **24**), але випадайка в HTML лишилась
зі старими `25/40/55/72`, а `fuel_filter` хардкодив `40`/`38` у шарах funding.

**Правило:** пороги беруться ЛИШЕ з `detection/setup_grader.py`:
- `grade_bands()` → `[{min,label,color}]` (без службового «ВИЧЕРПАНО»);
- `grade_min('СЕРЕДНІЙ')` → 38.

Хто вже переведений: `_funding_layers` (SCORE/Готовність/Скальп → `_grade_min
('СЕРЕДНІЙ')`), дефолт `queue4_setup_min` = **38**, `ff.get_state()['grade_bands']`
→ фронт будує випадайку «Готовність ≥» через `_ffSyncGradeOptions()` (HTML-опції
лишились лише як фолбек і вже вирівняні: 24/38/53/70).
⚠️ НЕ вписувати ці числа руками в нових місцях — брати з `grade_min()`.

## 🧾 ЛОГ VOB: лише РЕАЛЬНІ ПРОПУСКИ (чистка за обсягом)

Заміряно на проді: **561 запис за 8.5 год**, з них VOB — 290 (52%), а корисних
дій (TM/FF) — 1.4%. Розподіл VOB: `duplicate` 165 + `numbered` 93 +
`no_candidate` 29 = **51% усього логу** — це СТАБІЛЬНІ СТАНИ, а не події.

**Правило:** `_vob_log_decision` пише в `activity_log` (`source='VOB'`) ЛИШЕ
реальні ПРОПУСКИ сигналу — **`stale` / `epoch` / `no_1h_ob`** (+ анти-дубль за
ключем `(outcome, ft, side, num)`).
НЕ пишемо: `duplicate` / `numbered` / `no_candidate` / `first_sight` — вони
завжди видні в бейджі `vob_diag` на панелі в реальному часі; і `fired`/`filtered`
— їх пише основний шлях (`signal` + `rejected` з розкладом фільтрів).
Тест: `test_log_only_real_misses_not_stable_states`.
⚠️ Історія рішень: спершу було «лог чистий» → потім «логувати КОЖЕН VOB» →
тепер компроміс: у лозі події, у бейджі — стани.

## 🔁 Черга-4: повторна перевірка фільтрів на ВИХОДІ (`queue4_recheck_filters`)

Дефолт **УВІМКНЕНО**. Перед `_open` монета ще раз проходить ТОЙ САМИЙ ланцюг
сканера, що пускав її в чергу — `scanner._signal_allowed(sym, side,
at_intake=False)` (**СТРОГО**: на інтейку дозволялось «чекати вердикт», на
відкритті потрібен фактичний дозвіл). Причина: монета може стояти в черзі
годинами (TTL 15+ год), за цей час 1H-OB/прогноз/Decision можуть перевернутись.
Не пройшла → **НЕ відкриваємо**, лог `Черга-4 🔁 повторна перевірка фільтрів не
пройдена: <причина> · <розклад>`, монета ЛИШАЄТЬСЯ в черзі й спробує на
наступному тіку (доки не викине TTL/застій). Реалізація —
`_q4_recheck_filters(sym, side)`; помилка перевірки НЕ блокує (fail-open).
UI-тумблер `ff-queue4-recheck` у гармошці «⚙️ Налаштування (Черга-4)».
Тест: `test_q4_recheck.py`.

## 🖼 VOB = САМЕ ТОЙ БЛОК, ЩО НА ГРАФІКУ (обмежено графіком — не зламати!)

Вимога користувача: «що виводиться на графік, то і вважається VOB-сигналом».
Панель малює **РІВНО ОДИН** бокс — `volObPrim.setOB(d.volumized_ob)`, а це
`latest_ob` (найновіший серед ОБОХ боків, та сама функція `_latest_volumized_ob`
і ті самі бари). Тому кандидат для сигналу/нумерації/логу — **лише він**:
`_vob_chart_candidates(vol_result)` → `[]` або `[(side, ob, ft)]`.

**Що було не так:** бралися найновіші блоки ОБОХ боків
(`_vob_pick_candidates(newest_bull, newest_bear)`). Протилежний бік часто був
багатоденної давності — у проді траплялись блоки віком **1500-2800 барів 5m
(5-10 діб)**: на графіку їх НЕ видно, але вони крутили лічильник (ETH «VOB #8»,
LDO «#7»), засмічували лог і навіть фаєрили сигнали. Тепер такі блоки не
беруться взагалі.

`breaker` НЕ є сигналом: такий бокс малюється пунктиром із «✕» — зона вже
знецінена, вхід по ній помилковий.
`_vob_pick_candidates` лишився лише як тест-хелпер (у скані НЕ використовується).
Тест: `test_chart_candidate_is_the_drawn_block_only`.

## 🕒 Лог VOB: чесний підпис (вік + час формування, БЕЗ слова «Свіжий»)

Слово «Свіжий» у рядку сигналу БРЕХАЛО (реальний кейс: SUIUSDT «Свіжий
Volumized OB» для блоку віком **125 барів ≈ 10.4 год**). Прибрано.

⚠️ **ВИПРАВЛЕНО САМ ОПИС:** тут раніше стояло «вікно свіжості за замовчуванням
ВИМКНЕНЕ (`vob_alert_max_age_bars=0`)» — це НЕПРАВДА і суперечило розділу
«VOB-алерт: ВАЛІДНІСТЬ = СВІЖІСТЬ» вище. У коді `0 = АВТО swing_length + 2`
(`_max_age = _cfg_age if _cfg_age > 0 else (_sl + 2)`), тобто вікно ДІЄ.
Вимкнути його можна лише явним `max_age=None`. Чесний підпис потрібен і при
увімкненому вікні: у гілці `vob_one_per_ob` нумерація бере блоки БУДЬ-ЯКОГО
віку, тож багатогодинний блок у лозі трапляється й там.

`_vob_age_label(ft, age, tf)` — ЄДИНЕ джерело підпису: `вік N барів (~X год) ·
утв. DD.MM HH:MM UTC`. Використовується і в рядках сигналу, і в статусах
(`_vob_log_decision`). Голий `ft=1787536500000` більше НЕ пишемо — його
неможливо звірити з графіком очима.
Тест: `test_age_label_is_honest_and_human`.

## 🔇 Анти-флуд Q4-recheck + полегшення сторінки логу

- **Q4-recheck флудив:** гейт `queue4_recheck_filters` писав рядок на КОЖЕН тік
  двигуна, доки монета висить у черзі → один ZECUSDT дав **48 однакових рядків**
  за 8.5 год. Тепер `_q4_recheck_logged[sym] = ((side, reason), ts)`: пишемо лише
  коли ЗМІНИЛАСЬ причина або раз на `Q4_RECHECK_LOG_GAP` (900с). Пройшла
  перевірка → запис знімається.
- **Сторінка гальмувала** не через бота, а через UI: таблиця логу тягнула
  **600 рядків кожні 5 секунд** і щоразу повністю перебудовувала DOM. Тепер:
  ліміт **300**, інтервал **15с**, і головне — **сигнатура** (`tb.dataset.sig`):
  якщо нічого не змінилось, DOM НЕ чіпаємо. Повна історія — кнопками CSV/JSON.

## ✋ РУЧНИЙ СИГНАЛ (кнопка в банері Decision)

Кнопка **«✋ Сигнал»** поряд із ⬆LONG/⬇SHORT (`sm-decision-btn-signal`).
Напрямок = **вердикт банера** (`window._smDecisionDir`, ставиться при рендері).

**ВАЖЛИВО (рішення користувача):** ручний сигнал **НЕ проходить фільтри
сканера** — рішення ухвалює людина. Ендпоінт `POST /api/smc/manual-signal`
{symbol, side} одразу кличе `TradeManager.on_signal(opened_by='manual')`, тобто
йде на обробку **ПІСЛЯ фільтрів**: черга (intercept) → двигун → відкриття.
Гейти самої ЧЕРГИ (шари Черги-4, ₿-сеанс, recheck) лишаються — вони не є
фільтрами сканера. У 🧾 Лог пишеться `✋ РУЧНИЙ сигнал <side> — фільтри
пропущено · <розклад>`; якщо фільтри БУЛИ Б проти — це теж видно в розкладі.
Мітка угоди — `manual` → бейдж **✋ Ручний** (уже був у `signal_labels.py`,
`smart_money.html` і `infosite/app.js` — нову картинку не вигадували).

## 🪗 Гармошки та полегшення сторінки

- **🧾 Лог роботи бота** — гармошка, за замовчуванням **ЗГОРНУТО**
  (`toggleActivityLogPanel`, стан у `localStorage['actLogOpen']`). Поки згорнуто
  — таблиця **НЕ опитується і НЕ малюється** взагалі. Полл також пропускається
  при `document.hidden` (неактивна вкладка).
- **ГАРМОШКИ НА ВСІХ ВАЖКИХ СЕКЦІЯХ** — єдина функція `togglePanel(id)`
  (`toggleQueuePanel` лишився як аліас для заголовків Черг). Кожна секція має
  пару `#<id>-body` + `#<id>-caret`; стан памʼятається окремо в
  `localStorage['qPanel_<id>']`; список — `_PANEL_IDS`.
  Секції: **q1..q4** (Черги 1-4), **poc** (🎯 POC-сетап), **fund**
  (💰 Funding — МММ), **tm** (💼 Trade Manager), **testmode** (🧪 Test Mode).
  ⚠️ У гармошку Черги складено **ВЕСЬ** її вміст (налаштування, вкладки
  напрямку, engine-gate, таблиця) — назовні лишається ЛИШЕ шапка з тумблером і
  кнопкою «Очистити всі». У `tm`/`testmode` використано ГОТОВІ контейнери
  `#tm-body`/`#testmode-body`, які вже були в розмітці.
- **Колонки «МММ» і «Готовність виходу» ПРИБРАНО** з таблиць відкритих угод
  (real + paper). Алгоритм НЕ видалено:
  • `trade_manager.POSITIONS_EXIT_GRADE = False` — гейт розрахунку `exit_grade`
    (він рахувався на КОЖНУ позицію на КОЖЕН тік монітора, деф. 4с — найважче);
  • у `/api/tm/state` закоментовано збагачення `fuel_str/fuel_str_prev/fuel_dir`.
  Щоб повернути — `POSITIONS_EXIT_GRADE = True`, розкоментувати 3 рядки у
  flask_app і повернути `<th>`/`<td>` (colspan 12/11 → 14/13).

## ⚡ ЧЕРГА-4: ПАКЕТ ПРИСКОРЕННЯ (A1+A2+A3+B1+B2+C1+C2) — не зламати!

**Заміряна причина гальм.** На КОЖЕН рядок Черги-4 за один 30с-такт liq-map
збирався **~7 разів**: двигун (`_fuel_dir_smoothed` + шари у СВІЙ бік + шари у
ПРОТИЛЕЖНИЙ) + UI-полл (ще одні шари), а `_queue4_layers` усередині кликав
`_fuel_dir_smoothed` І `_fuel_dir_legacy`, кожен з яких будував `lm.get_state()`
САМ. `lm.get_state()` = `liqmap_get_events(limit=3000)` → до 3000 ORM-рядків +
`.to_dict()` + кластеризація, **без кешу**. Плюс ~20 `db.get_setting()` на монету
(`get_mm_settings` — 8 окремих сесій SQLAlchemy на КОЖЕН `compute_mm`).
При 100 монетах у черзі це ~700 збірок / 30с. І **все це — під глобальним
`self._lock` у `get_state()`**, тобто HTTP-запит сторінки блокував ТОРГОВИЙ daemon.

**A1+A2 — `_liq_state(symbol, force=False)` = ЄДИНА точка знімка liq-map.**
`_fuel_dir`, `_fuel_dir_legacy`, `_btc_liqfuel_raw` і панельний шлях беруть ЗВІДТИ.
TTL `LIQ_STATE_TTL=20с` (< `CYCLE_SECS=30` → кожен такт двигуна все одно читає
свіжий знімок; джерело — daemon liq-map — оновлюється раз на 60с, тож 20с ФІЗИЧНО
нічого не змінює). Стеля памʼяті `LIQ_STATE_CAP=400`. **`lst is None` («liq-map ще
не піднявся») НЕ кешується** — інакше після рестарту бот мовчав би цілий TTL.
Свіжість ЦІНИ не постраждала: обидві МММ-функції накладають `_live_price()` поверх
`mark_price` знімка. `_liq_decay_profile()` — той самий `get_setting` із кешем 60с.

**A3 — `mm_model.get_mm_settings` кешується 60с** (+ `invalidate_mm_settings()`).
Помилка БД НЕ кешується (щоб не «залипнути» дефолтами).

**B1 — розділено I/O і арифметику:**
- `_q4_snapshot(sym, s)` — ЄДИНЕ I/O (fn/fo/su + `on` = стан тумблерів);
- `_q4_layers_from(snap, side, s)` — **ЧИСТА функція, ЖОДНОГО I/O** → ДРУГИЙ
  напрямок (фліп-кандидат) коштує НУЛЬ;
- `_queue4_layers(sym, side, s)` лишився як сумісний фасад (знімок + арифметика)
  для поодиноких викликів (фільтр входу в `intercept`).
⚠️ Тумблери арифметика бере **зі знімка** (`snap['on']`), а НЕ з налаштувань ще
раз — інакше тумблер, перемкнутий між знімком і розрахунком, дав би «визначений»
шар без даних під ним.
⚠️ У двигуні `fuel = _snap['fn'] or self._fuel_dir_smoothed(sym)` — фолбек
ОБОВʼЯЗКОВИЙ: коли ОБИДВА шари «Новий МММ» і «Запас» вимкнені, знімок свідомо не
тягне `fn`, а `mark` двигуну потрібен завжди.

**B2 — шари рахує ТІЛЬКИ двигун; `get_state()` їх ЧИТАЄ.**
`_engine_tick_queue4` кладе обидва напрямки в `_q4_layers_cache[sym]=(ts,{side:lay})`
(`_q4_cache_put`), `get_state` бере через `_q4_layers_cached` (промах/протухле >
`CYCLE_SECS*3` → рахує на місці, це дешево завдяки `_liq_state`). Два виграші:
(1) UI більше НЕ рахує нічого під локом; (2) **таблиця показує РІВНО ті числа, за
якими двигун ухвалив рішення** — розбіжність «в таблиці одне, вирішив інше»
структурно неможлива. Кеш чиститься на кожному виході з черги (`_q4_cache_drop`
у всіх 6 `_pending4.pop`, `delete_timer4`, `clear_all_timers4`) + прибирання
сиріт у кінці такту.

**C1 — сигнатура таблиці Черги-4** (`_ffQ4Sig` → `tb.dataset.sig`): нічого не
змінилось → DOM не чіпаємо. `held_sec` СВІДОМО не входить у сигнатуру — секунди
малює `data-since` + глобальний 1с-тікер. `_ffApplyQ4Cols()` викликається ДО
перевірки сигнатури, тож перемикання колонок працює завжди.

**C2 — сервер рахує лише РОЗГОРНУТІ секції.** `GET /api/fuel-filter/state?sections=q4,fund`;
`get_state(sections=None)` = все (сумісність — **інфо-сайт параметр не шле**).
`FuelFilterDaemon.HEAVY_SECTIONS = ('q4','fund')`. Фронт: `_ffWantedSections()`
дивиться `#q4-body`/`#fund-body`, `togglePanel` на РОЗГОРТАННІ важкої секції
одразу смикає `loadFuelFilterStatus()`. Відповідь несе `served:{q4,fund}` — фронт
ОБОВʼЯЗКОВО його читає, інакше порожній `timers4` (секцію не просили) прочитався б
як «черга порожня» і стер би таблицю. Лічильник `pending4_visible` рахується
ЗАВЖДИ (шапка чесна навіть коли гармошка згорнута); для funding пропускається
ЛИШЕ `_funding_layers` — усі побічні ефекти циклу (rate_min/vol24h/епізоди) і
`ffFundingSyms` лишились.

**⏱ Профіль такту:** `[FF-Q4] tick: N coins in Xs (Ys/coin)` — щоб ефект був
у ЦИФРАХ, а не «здається швидше».

**Тести:** `test_q4_perf.py` (17). Головний замок —
`test_split_gives_identical_result`: знімок+чиста функція мусять дати БАЙТ-У-БАЙТ
те саме, що монолітний `_queue4_layers`. Плюс `test_layers_from_is_pure_no_io`
(будь-яке I/O в чистій функції = падіння тесту).

## 🎯 Черга-4: ♾ без терміну · джерело SL · ручне відкриття · сортування

**♾ «Без терміну» (`queue4_no_ttl`, дефолт OFF).** Запис живе, доки його не
ЗАМІНИТЬ ПРОТИЛЕЖНИЙ сигнал по тій самій монеті (механізм заміни вже був:
`_q4_opp_replace` в `intercept` + `gate_exempt`), або поки не відкриється /
не приберуть вручну. **Вимикає ОБИДВА виселення за часом** — і TTL, і «викид
при застої»: інакше монета зникала б через 30 хв і заміняти було б НІЧОГО.
Санітарні виходи (вже в угоді, 1H-OB уже відпрацьовано) лишаються.
Рішення винесені в ЧИСТІ функції `_q4_ttl_expired(added_at, now, ttl_h, no_ttl)`
та `_q4_stagnant(lit_at, now, nl_min, no_ttl)` (юніт-тести). UI: тумблер
`ff-queue4-nottl`; `_ffQueue4TtlSync()` ГАСИТЬ поля TTL і «застій», щоб було
видно, що вони не діють.

**🛑 Джерело Manual SL (`queue4_sl_source`, дефолт `'1h'`).**
- `'1h'` — межа **1H Order Block**: `db.get_smc_ob_state(sym, ob_filter_timeframe)`
  → `bar_high`/`bar_low`. Це ТОЙ САМИЙ ★-блок, що на графіку. ⚠️ НЕ рахувати OB
  інлайн — це вже давало «мітка каже 1H, а цифри з іншого TF» (кейс ★ watchlist).
- `'15m'` — межа **Volumized OB на 15m** (`_q4_ob_bounds_vob`), форма блоку за
  параметрами сканера (swing/end/atr/zone), щоб збігалось із боксом на графіку.
SL = межа + `queue3_vob_sl_buffer_pct`: SHORT → `top*(1+buf)`, LONG → `bottom*(1-buf)`.
Якщо обраного блоку немає — береться ДРУГЕ джерело (щоб угода не лишилась без
SL), і в 🧾 Лозі пишеться, з ЯКОГО саме джерела став рівень + позначка «фолбек».
Немає жодного — SL не вигадуємо, пишемо в лог.

**✋ Ручне відкриття з таблиці** — кнопка ✋ у колонці «Дія» →
`POST /api/fuel-filter/queue4/open` {symbol} → `ff.force_open_queue4(sym)`.
Напрямок береться З ЗАПИСУ В ЧЕРЗІ (відкривається рівно те, що видно в рядку).
Ворота Черги-4 пропущено (збіг шарів, 🔁 повторна перевірка, «1 угода на 1H-OB»)
— рішення ухвалює людина, як і в ✋ ручному сигналі. Запобіжники `_open`
(ціна/розмір) лишаються. Мітка — `_ob_compose('manual','Q4')` = **✋ Ручний →
🎯 Черга-4**. Після відкриття: чистка черги + кешу шарів, старт таймера,
`_mark_ob_epoch_opened` (щоб авто-двигун не відкрив ДРУГУ на тому самому 1H-OB)
і той самий `_q4_set_vob_sl`. `_open` відхилив → монета ЛИШАЄТЬСЯ в черзі.

**⇅ Сортування таблиці Черги-4** — клікабельні заголовки (`ffSortQueue4`,
`_ffQ4SortVal`, стрілки `_ffUpdateQueue4HeaderArrows`), **за замовчуванням
«🎯 Шари» ↓** (`_FF4_SORT_DEFAULT`), тайбрейк — довше в черзі. Сила МММ/Запасу
сортується ЗІ ЗНАКОМ за напрямком запису: «34% у бік» вище за «71% ПРОТИ»
(інакше сортування показувало б найгірші рядки зверху). Порядок входить у
сигнатуру `tb.dataset.sig`, тож зміна сортування перемальовує таблицю.

Тести: `test_q4_controls.py` (20).

## 🛑 Manual SL Черги-4: 4 виправлені дефекти (не зламати!)

Аудит за скріном (10 paper-позицій + NEOUSDT) виявив у `_q4_set_vob_sl`:

1. **ЛОГ БРЕХАВ.** Результат `tm.update_manual_sl_tp(...)` **ІГНОРУВАВСЯ**, а в
   🧾 Лог писалось «SL з … → X». TM має ВЛАСНУ валідацію
   (`update_manual_sl_tp`: SHORT → рівень мусить бути ВИЩЕ ПОТОЧНОЇ ціни, LONG →
   нижче) і на відмову НІЧОГО не змінює. Тобто угода лишалась **БЕЗ стопа**, а в
   лозі стояло, що стоп поставлено. Q3-VOB (`_vob_open_or_trail`) робив це
   ПРАВИЛЬНО — писав «не прийнято: <причина>». Тепер Q4 так само: `ok` → рядок зі
   стопом **і відстанню `% від входу`**; `not ok` → «ВІДХИЛЕНО Trade Manager:
   <причина> — угода БЕЗ стопа».
2. **1H-OB брався БЕЗ звірки bias із напрямком угоди.** Для SHORT брався
   `bar_high` БУДЬ-ЯКОГО блоку; бичачий 1H-OB лежить ПІД ціною → стоп нижче ціни
   → відмова TM (див. п.1, мовчки). Черга-4 приймає ОБИДВА боки і вміє фліпати за
   показниками, тож «напрямок ≠ bias 1H-OB» — звичайна ситуація, і з дефолтом
   `queue4_sl_source='1h'` вона стала б частою. Тепер `_q4_sl_candidates` ВІДКИДАЄ
   1H-джерело з протилежним bias → фолбек на Volumized (він за побудовою бере
   блок потрібного боку з `bullish_obs`/`bearish_obs`).
3. **Бік рівня не перевірявся ДО виклику.** Додано ЧИСТУ `_q4_sl_side_ok(sl, side,
   ref)` — ТЕ САМЕ правило, що в TM, з ТІЄЮ САМОЮ ціною (`tm._get_current_price`).
   Рівень не з того боку → джерело пропускається, беремо наступне; жодне не
   підійшло → SL не вигадуємо і пишемо в лог УСІ причини.
4. **`is_shadow` ВГАДУВАВСЯ:** `not _tm_has_position(sym, True)` — «немає
   реальної, отже тіньова». Якщо позиції не було ЗОВСІМ, писали в тіньову книгу і
   отримували мовчазне «No open shadow position». Тепер за ФАКТОМ (як у Q3-VOB):
   `_real`/`_shadow`; немає жодної → чесний рядок у лозі.

**Що НЕ виправлено (політика ризику — рішення користувача, не баг):**
- **Немає стелі/підлоги відстані SL.** Зі скріна: ALGO 1.40% … **WIF 15.56%** —
  при однаковому розмірі позиції ризик різниться в 11 разів.
- **Q4 не ставить TP** узагалі (R:R не заданий).
- **SL ставиться ОДИН раз** і не трейлиться (на відміну від Q3-VOB).
- ⚠️ `update_manual_sl_tp` пише власний рядок у лог із `source='user'` («Користувач:
  Manual SL → …») навіть коли рівень поставив ДВИГУН. Вводить в оману; лишено як є.

Тести: `test_q4_controls.py` (розділ «Джерело Manual SL»).

## 🛡 ГАРАНТІЯ СТОПА: авто-SL більше НЕ «чекає» (кейс MNTUSDT — не зламати!)

**Що сталось.** MNTUSDT paper SHORT @0.51360 відкрився з Черги-4, а поле Manual SL
лишилось ПОРОЖНЄ. У 🧾 Лозі: «Авто-SL з OB: OB на 15M протилежний (BULLISH) —
чекаю BEARISH». При цьому на графіку висів ★1H-OB **ВЕДМЕЖИЙ** (бейдж
«🔒 OB Short 1H ★», ~0.5250-0.5285) — ідеальний якір для стопа шорта.

**Корінь.** У проєкті ДВА незалежні авто-SL, і жоден не гарантував стоп:
1. `trade_manager._auto_ob_manual_sl` (тумблер `q2_auto_ob_sl`) — дивився РІВНО
   ОДНЕ джерело: OB на `q2_auto_ob_sl_tf` (деф. **15m**, це НЕ `ob_filter_timeframe`
   сканера!). Bias не збігся → `_diag(...)` і `return`. Угода висіла без стопа
   скільки завгодно, бо «SET ONCE» лише про успіх, а невдача ретраїлась вічно.
2. `fuel_filter._q4_set_vob_sl` — свій ланцюг (виправлений раніше).
Q4-шлях ставить `manual_sl` БЕЗ `_auto_ob_sl_val`, тож TM бачить його як «рівень
від користувача» і не втручається — саме тому в інших монет SL був, а в MNT ні.

**Фікс (`_auto_ob_manual_sl` тепер ЛАНЦЮГ, останній крок — гарантія):**
1. OB на `q2_auto_ob_sl_tf` (як було, пріоритет збережено);
2. **★ OB на `ob_filter_timeframe`** — ТОЙ САМИЙ блок, що намальовано на графіку
   (додається, лише якщо це ІНШИЙ TF);
3. **Volumized OB у бік угоди** — `bullish_obs`/`bearish_obs`, тобто за побудовою
   потрібного боку (bias перевіряти не треба); breaker пропускається;
4. **`autosl_fallback_pct` % від ВХОДУ** (`autosl_fallback_on`, деф. **ON, 2%**) —
   стоп буде ЗАВЖДИ. OFF повертає стару поведінку «краще без стопа».
Кожен кандидат перевіряється на БЕЗПЕЧНИЙ бік від поточної ціни (інакше наступний
тік монітора закрив би угоду). Причини пропуску ВСІХ джерел пишуться в той самий
рядок логу — видно, чому взято саме це джерело.

**`autosl_max_pct`** (деф. **0 = вимкнено**) — стеля відстані у % від входу:
надто далекий блок ПІДТЯГУЄТЬСЯ до стелі (сам вибір блоку не змінюється). Причина:
на проді спостережено розкид ризику **ALGO 1.40% … WIF 15.56%** при однаковому
розмірі позиції.

⚠️ Bias-правило: LONG → потрібен **BULLISH** блок (беремо `bar_low`), SHORT →
**BEARISH** (беремо `bar_high`). Протилежний блок лежить не з того боку ціни.
⚠️ «SET ONCE» і «не чіпати SL, введений руками» — збережено без змін.
UI: тумблер `ff-autosl-fallback` + `ff-autosl-fallback-pct` + `ff-autosl-max-pct`
у гармошці **🛡 Авто-SL та ворота відкриття** (див. розділ нижче).
Тест: `test_autosl_chain.py` (14).

## 🐞 UnboundLocalError `exh` у `_open` — КОЖНЕ відкриття Черги-4 падало

**Симптом (зі скріна):** ✋ ручне відкриття з Черги-4 → «Не відкрито: Помилка:
cannot access local variable 'exh' where it is not associated with a value».

**Корінь.** У `_open` змінна `exh` присвоювалась ЛИШЕ всередині
`if not skip_safeguard:`, а читалась у ПІДСУМКОВОМУ рядку беззастережно.
**Обидва** шляхи Черги-4 (авто-двигун і ручне відкриття) передають
`skip_safeguard=True` — тобто падало ЗАВЖДИ.

⚠️ **Найгірше — де саме падало.** На ОСТАННЬОМУ рядку `_open`, коли угода вже
відкрита і записана в `_fuel_managed`. Двигун ловив виняток у `except` (лише
`print` у stdout) і МОВЧКИ пропускав усе, що йде після відкриття:
- `self._timers[sym]` — таймер не стартував;
- `note_signal_consumed` — **сигнал не вигорав**, тобто захист «запис
  відпрацював» не вмикався;
- `_mark_ob_epoch_opened` — позначка «1 угода на 1H-OB» не ставилась;
- `_q4_set_vob_sl` — **стоп ставила інша, незалежна гілка** (`trade_manager.
  _auto_ob_manual_sl`). Саме тому в лозі стояло `autosl · TM`, а не
  `Черга-4: SL з …`, і налаштування `queue4_sl_source` фактично не діяло.

**Фікс:** `exh = None` ПЕРЕД умовою. Плюс `except` у двигуні тепер пише в
🧾 Лог (`Черга-4 ⚠️ збій після відкриття: … — перевірте позицію`), а не лише
в консоль: збій після відкриття не має бути невидимим.
Тести-замки (AST-перевірка порядку запис/читання): `test_q4_controls.py`.

## 📨 ЧАСТКОВЕ ЗАКРИТТЯ — у ГРУПОВИЙ ТОПІК і в ОДИН РЯДОК

`_partial_close` і `_partial_close_shadow` кликали `_notify` **без
`category`**, а без категорії повідомлення йде в ОСОБИСТИЙ бот адміна (він
зарезервований під службові). Тому TP-1 сипався в приват замість групи.
Тепер `category='trades'` в обох, і замість шести рядків — один:
`✂️ 🔴 #DOTUSDT −50% · $0.82470 · +1.30%` (paper — з міткою 🧪).
Рядки рівнів у `_tp_lines` втратили підписи **«(50%)»** і **«(повний)»**
(на прохання): лишились `🎯 TP-1: ціна (+x%)` / `🎯 TP-2: ціна (+y%)`.
Тести: `test_autosl_chain.py`.

## ✏️ РУЧНИЙ Manual SL — АВТОПІЛОТ НЕ ЧІПАЄ (кейс SOLUSDT, не зламати!)

**Скарга: «чому я не можу вручну виставити Manual SL?»** Лог 01.09 показав, що
рівень зберігався — і його щоразу затирали через ~20 секунд:

```
16:58:14 ✏️ Користувач: Manual SL → $96.82
16:58:38 🤖 Бот (Автопілот · структура): Manual SL → $99.9094
16:59:09 ✏️ Користувач: Manual SL → $96.82
16:59:31 🤖 Бот (Автопілот · структура): Manual SL → $99.9094
```

**Корінь:** `_pilot_tick` передає в `plan()` ручний рівень просто як
`prev_stop`. Ратчет `better_stop` бачить структурний стоп «кращим» (для LONG
вище = ближче до прибутку) → `act='trail'` → `update_manual_sl_tp` перезаписує.
Автопілот не знав, що рівень поставила ЛЮДИНА. Для Manual TP правило «оператора
не чіпаємо» вже діяло (`_pilot_apply_tp`), для SL — ні. Це була неузгодженість.

**Фікс:** перед застосуванням трейлу перевіряємо `manual_sl_src == SRC_USER` →
трейл НЕ застосовується, у лог іде ОДИН рядок (`_pilot_sl_user_lock`, інакше
монітор писав би його щотіку) з підказкою, як повернути керування.
⚠️ Замок НЕ вічний: **очистити поле SL** → `_pilot_sl_user_lock` знімається і
автопілот знову веде стоп сам. Нове ручне значення теж скидає позначку, щоб
пояснення зʼявилось у лозі й для нього.
Тести: `test_autosl_chain.py` (розділ «РУЧНИЙ SL»).

## 🏷 Походження Manual SL/TP: 🤖 бот ↔ ✏️ користувач (не зламати!)

Раніше два різні за природою рівні виглядали ОДНАКОВО, а `update_manual_sl_tp`
ЗАВЖДИ логував «Користувач: Manual SL → …» — навіть коли рівень поставив двигун.

- **`update_manual_sl_tp(..., origin='user', origin_label=None)`.** Дефолт `'user'`
  → усі наявні виклики (UI-ендпоінт `/api/tm/positions/manual-sltp`) поводяться як
  раніше. Бот-виклики передають `origin='auto'` + людський підпис:
  Черга-4 (`Черга-4 · <джерело>`), Q3-VOB (`відкриття`/`трейл`), POC-сетап.
- Пишеться на позицію: **`manual_sl_src`/`manual_tp_src`** (`'auto'`|`'user'`) +
  **`manual_sl_by`/`manual_tp_by`** (підпис). SL і TP — НЕЗАЛЕЖНО. Зняття рівня
  (`0`) прибирає й позначку. Відхилений рівень позначки НЕ лишає (нічого не мутує).
- `trade_manager._auto_ob_manual_sl` пише позначку НАПРЯМУ (`SRC_AUTO`,
  `Авто-SL · <джерело>`) — він мутує `pos` без `update_manual_sl_tp`.
- **🧾 Лог:** `🤖 Бот (<label>): Manual SL → X` (source `TM`) або
  `✏️ Користувач: …` (source `user`). В `extra` іде `origin`.
- **🎨 UI (`_sltpSrcStyle`)**: поле Manual SL/TP фарбується — **блакитний
  `#38bdf8` = виставив БОТ** (той самий колір, що бейдж «Авто-SL з OB»),
  **бурштиновий `#fbbf24` = змінено ВРУЧНУ**, сірий = порожньо. Рівень без
  позначки (стара угода) — нейтральний сірий, НЕ вигадуємо походження.
  Легенда — у `<th>` обох таблиць. Тултип несе підпис джерела.
- ⚠️ Інфосайт (`infosite/app.js`, `sltpCell`) кольори НЕ дзеркалить — окрема сесія.
Тест: `test_autosl_chain.py` (розділ «ПОХОДЖЕННЯ Manual SL/TP»).

## 🏦 Watchlist: сортування за капіталізацією

- **`GET /api/smc/mcap-ranks`** → `{ok, available, ranks:{SYMBOL:rank}, total, ranked}`.
  ОКРЕМИЙ ендпоінт, а НЕ поле в `/api/smc/state`: state опитується що 10с, а ранги
  міняються раз на добу. Джерело — `coingecko_client.get_rank()` (той самий, що
  живить `max_market_cap_rank` радара), TTL кешу — всередині клієнта.
- Фронт: кнопка `sm-wl-mcap-btn` («🏦 Cap») поряд із «⇅ A-Z». Режим ОДИН на список —
  `wlSortMode` = `'none'|'alpha'|'mcap'` у `localStorage['wlSortMode']` (є міграція
  зі старого `wlSortAlpha`); повторний клік по активній кнопці = скинути сортування.
  `wlSortAlpha` лишився як булеве дзеркало для сумісності.
- Ранги тягнуться ЛИШЕ коли режим увімкнено, не частіше ніж раз на 30 хв
  (`_wlEnsureMcapRanks`). Монета без рангу (немає в топ-250 CoinGecko) іде в
  КІНЕЦЬ (серед таких — за алфавітом), а не вигадує собі місце. У режимі `mcap`
  у рядку показується `#<ранг>` (або `#—`); в інших режимах — нічого (без шуму).
- Серверний `wlSymbols` НЕ змінюється — сортується лише копія в `renderWatchlist`.

## 🎯 Tickr: режим «Як у Watchlist» (той самий принцип відбору)

**Питання користувача:** за яким принципом підібрано watchlist і чи можна так
само підбирати монети з УСІЄЇ біржі. Аудит поточних 33 монет (ETH, XRP, BNB, SOL,
DOGE, TRX, LINK, AVAX, DOT, HYPE, LTC, SUI, NEAR, APT, ONDO, UNI, MNT, TAO, ENA,
ALGO, INJ, LDO, HBAR, OP, WIF, 1000PEPE, ORDI, NEO, STRK, ZEC, BTC…) показав: їх
об'єднує **НЕ сектор** (там і L1, і L2, і DeFi, і меми, і приватність), а три речі:
1. **USDT-ПЕРПЕТУАЛ** — спот бот не торгує, USDC-перп дублює ту саму монету;
2. **топ-150 за КАПІТАЛІЗАЦІЄЮ** (CoinGecko);
3. **реальна ЛІКВІДНІСТЬ** — обіг 24h та відкритий інтерес, щоб SMC-структура
   (OB/CHoCH) була чистою, а не намальованою шумом.
Плюс чого там НЕМАЄ: стейблкоїнів і обгорток (WBTC, stETH, PAXG) — власної
структури в них немає.

**Реалізація** — новий `sort_by='watchlist_like'` у `tickr_core.top_active`
(опція «🎯 Як у Watchlist (кап + ліквідність)» у випадайці на `/tickr`):
- пороги в `WL_LIKE` = {`mcap_universe`:150, `min_vol_usd`:50M, `min_oi_usd`:10M};
- виключення — СПИСКОМ `WL_LIKE_EXCLUDE` (стейбли/обгортки/золото), а НЕ
  евристикою «USD у назві»: інакше вилетіли б живі USUAL/USELESS;
- поле «VOL 24H ≥» ПЕРЕКРИВАЄ `min_vol_usd`; порожнє поле → дефолт режиму
  (інакше «як у Watchlist» пропускало б неліквід);
- сортування за РАНГОМ капіталізації (ключ `watchlist_like` = `-rank`, бо
  спільний `sort` спадний);
- у `warnings` іде РОЗКЛАД відсіву (не USDT-перп / поза топ-N / стейбл-обгортка /
  мілкий OI → скільки лишилось) — «чому мало монет» не має бути здогадкою;
- немає CoinGecko → явне попередження, а не мовчки порожня таблиця.
UI: `tkOnSortChange` ставить ТОП-N=100 (це ВІДБІР універсуму, а не «топ-20
найактивніших») і показує пояснювальний блок `#tk-sort-hint`.
Тест: `test_tickr_watchlist_like.py` (12).

## 🧬 «НОВА СИТУАЦІЯ ДЛЯ НОВОЇ УГОДИ» — кінець «угодам з повітря» (не зламати!)

**Кейс APTUSDT (26.08):** угоду закрито ВРУЧНУ о 03:03 → о 03:10:53 бот відкрив її
знову. У 🧾 Лозі — лише «Відкрито paper · Volumized OB → Черга-4», без жодного
контексту, звідки це взялось.

**Корінь — не окремий шлях, а ВІДСУТНІСТЬ вимоги нової підстави:**
1. закриття угоди не лишало ЖОДНОЇ позначки — для логіки входу монета одразу
   ставала «чистою», ніби угоди й не було;
2. запис, що вже лежав у черзі, відкривався наступним тіком двигуна незалежно
   від того, КОЛИ туди потрапив (а з ♾ `queue4_no_ttl` він живе ВІЧНО, тож ефект
   став постійним);
3. рядок «Відкрито» не ніс походження — зв'язок доводилось шукати очима по логу.

**Правило (`require_new_situation`, дефолт УВІМК).** Наступна угода по монеті
відкривається ЛИШЕ на сигналі, що виник ПІСЛЯ закриття попередньої.
- `note_trade_closed(sym)` — TM кличе на КОЖНЕ закриття (`_close_position` і
  `_close_shadow`); стамп у `_last_trade_end[sym]`, ПЕРСИСТЕНТНО.
- `_is_new_situation(sym, signal_at, s)` — ЧИСТА функція: `signal_at <= last_end`
  → відмова. Плюс окремий `open_max_signal_age_min` (0 = вимк).
- Гейт стоїть у **`_open`** — ЄДИНОМУ вузлі всіх автоматичних відкриттів. Кожен
  двигун передає `signal_at`: Q4/Q3/Q2/EXH → `info['added_at']` (час постановки
  в чергу = час сигналу).
- ⚠️ `by_hand=True` (✋ ручний сигнал, ✋ відкриття з Черги-4, `force_open_timer`)
  правило ОБХОДИТЬ — людина перекриває власне рішення.
- ⚠️ Немає `signal_at` або немає попереднього закриття → НЕ блокуємо (відмову не
  вигадуємо).

**✋ Ручне закриття** (`note_manual_close`) додатково **ЧИСТИТЬ УСІ ЧЕРГИ**
(`_pending`..`_pending4` + кеш шарів Q4). Це працює ЗАВЖДИ, незалежно від
налаштувань: запис, що лишився в черзі, відкрив би угоду наступним тіком і без
жодного сигналу. Опційний `manual_close_lock_min` (дефолт **0 = вимкнено**)
блокує авто-вхід на N хв; `clear_manual_close_block(sym)` знімає достроково.

**🧬 Походження в рядку відкриття** — `_origin_trace(sym, info, lay, now)`:
`сигнал: 🟪 Volumized OB · у черзі з 25.08 22:30 UTC (чекала 3г 12хв) · ціна на
вході в чергу X · шари 4/4 [розклад] · такт 1H-OB DD.MM HH:MM`. Тепер відповідь
на «звідки це взялось» стоїть У САМОМУ рядку, а не за кілька екранів угору.
`_fmt_wait` — читабельна тривалість (`45с` / `47хв` / `3г 12хв`).

UI: `ff-require-new-situation`, `ff-open-max-signal-age`, `ff-manual-close-lock`
у гармошці **🛡 Авто-SL та ворота відкриття** (див. розділ нижче).
Тест: `test_new_situation.py` (17).

## 🐞 Tickr «Як у Watchlist»: два виправлені дефекти

1. **BINANCE віддавав «залишилось 0».** `_activity_binance` для swap НЕ заповнює
   `oi_usd` (у Binance Futures немає bulk-ендпоінта OI — лише по-символьний, це
   ~600 HTTP). Гейт `min_oi_usd` викошував УСІХ, хто пройшов решту перевірок:
   «мілкий OI 37 → залишилось 0». **Фікс:** `_oi_available` — якщо в наборі немає
   ЖОДНОГО ненульового OI, гейт по OI ВИМИКАЄТЬСЯ, і в статусі прямо написано
   «OI — BINANCE не віддає його пачкою, фільтр по OI вимкнено». На Bybit OI є
   (bulk-тікери) → гейт працює повністю.
2. **`1000PEPE` вилітала як «поза топ-N».** Біржі лістять помножені контракти
   (`1000PEPE`, `1000BONK`, `1MBABYDOGE`) — у CoinGecko це `PEPE`/`BONK`/`BABYDOGE`.
   **Фікс:** `wl_base_variants(base)` — спершу шукаємо як є, потім без множника
   (`1000000/100000/10000/1000/1M/1K`). 1000PEPE РЕАЛЬНО є в робочому watchlist,
   тож без цього режим не відтворював би сам себе.

## 🚪 ПРАВИЛА ВИХОДУ: Opposite OB = ПОДІЯ · Forecast 1H/4H · Decision

### 🧱 Opposite OB Exit — лише НОВИЙ блок (виправлено!)

**Було:** `on_main_ob_update` перевіряв ЛИШЕ біас поточного рядка
`sob_smc_ob_state` на `opposite_ob_exit_timeframe`. Це СТАН, а не подія:
протилежний OB міг висіти на графіку ще ДО відкриття угоди — і найближчий скан
одразу її закривав. **Кейс MNTUSDT (25.08):** о 23:28:50 авто-SL писав «OB на 15M
протилежний (BULLISH)» (тобто блок УЖЕ був), а о 23:41 це ж правило закрило шорт
із підписом «Ціна вдарилась у протилежний Order Block» — при тому, що **код
ціну взагалі не перевіряв**. Підпис теж брехав.

**Стало:** закриваємо, коли протилежний OB **З'ЯВИВСЯ**:
- `_opp_ob_baseline(sym, pos, tf, row)` фіксує `bar_time` блоку, що стояв на
  момент ВІДКРИТТЯ угоди (`self._opp_ob_base[sym]`, ключ =
  `side|opened_at|exit_tf` → нова угода чи зміна TF перебазовують автоматично);
- перший прохід ЛИШЕ фіксує базу і НІЧОГО не закриває;
- далі закриття лише коли `bar_time` **НОВІШИЙ** за базу І біас протилежний;
- новий блок У НАШ бік **зсуває базу** (щоб наступний протилежний рахувався від
  нього), старіший блок (поточний став breaker) — не подія;
- у 🧾 Лог іде рядок із біасом, тегом і часом утворення блоку;
- підпис причини виправлено: «З'явився НОВИЙ протилежний Order Block
  (новіший за той, що був на вході)».
⚠️ База чиститься в `_close_position`/`_close_shadow` разом із `_signal_exit_at`.

### 🔮 Forecast 1H / 🔮 Forecast 4H / 🧠 Decision — САМОСТІЙНІ виходи

Три НЕЗАЛЕЖНІ правила, кожне зі своїм тумблером, **усі дефолт OFF**:
`use_forecast_1h_exit`, `use_forecast_4h_exit`, `use_decision_exit`.
Протилежний вердикт → закриваємо ОДРАЗУ, без підтвердження CHoCH.
⚠️ НЕ плутати з наявним `use_forecast_1h_close` — то «конфлюенс», йому потрібен
ще й розворот LTF (лишився без змін, дефолт ON).

- `_signal_exit_reason(sym, pos)` → `(reason_code, людський опис)` або None.
  Прогноз читається з `_get_forecast_both` (`f1_side`/`f4_side`, 1=LONG/−1=SHORT),
  Decision — з `compute_decision(...)['recommended']`.
- **Нейтраль / «немає даних» НЕ закривають** — інакше кожна пауза в прогнозі
  вибивала б з ринку. Закриває ЛИШЕ явний зустрічний бік.
- `_check_signal_exits(...)` кличеться з ОБОХ моніторів (`_monitor_position` і
  `_monitor_shadow_position`) ПЕРЕД перевіркою Manual SL/TP — якщо вердикт
  розвернувся, немає сенсу чекати, поки ціна дійде до стопа.
- **Тротл `SIGNAL_EXIT_TTL=20с` на монету** (`_signal_exit_at`): монітор тікає
  раз на 4с, а Decision важкий. Коли всі три тумблери OFF — не читається НІЧОГО.
- Причини: `forecast_1h_exit` / `forecast_4h_exit` / `decision_exit`; підписи
  додано і в `_close_detail`, і в бейдж-мапу TM, і в JS-мапу `smart_money.html`.

UI: тумблери `tm-use-forecast-1h-exit`, `tm-use-forecast-4h-exit`,
`tm-use-decision-exit` у блоці Exit Rules, одразу під Opposite OB Exit.
Тест: `test_exit_rules.py` (17).

## 🧟 Мертвий запис у черзі (кейс LDOUSDT) — гейт мусить ВИСЕЛЯТИ, не ретраїти

**Що показав лог (26.08).** Ланцюг LDOUSDT:
- `25.08 23:47:48` сигнал VOB #3 SHORT (усі фільтри ✓) → `23:47:49` у Чергу-4;
- `26.08 08:27:12` відкрито paper — **чекала 8г 39хв** у черзі;
- `26.08 10:29:34` закрито (`bot_tp_trail`, +1.15%);
- `26.08 11:21…11:31` — **30 однакових рядків** кожні ~22с:
  «🧬 сигнал від 25.08 23:47:48 UTC СТАРІШИЙ за закриття попередньої угоди».

**Корінь (моя недоробка попередньої правки).** Правило «нова ситуація» я поставив
у `_open`, і воно чесно ВІДМОВЛЯЛО — але **запис лишався в черзі**. Умова
`signal_at <= last_trade_end` **НЕЗМІННА** (обидві дати зафіксовані), тож запис не
міг стати валідним НІКОЛИ. З ♾ `queue4_no_ttl` його не виселяв ні TTL, ні застій
→ зомбі висів 11.5 год і флудив лог.

**Правило: гейт, який НІКОЛИ не пропустить, мусить ВИСЕЛЯТИ запис, а не ретраїти.**
1. **`note_trade_closed(sym)`** тепер не лише ставить риску часу, а й ОДРАЗУ
   прибирає з УСІХ черг записи з `added_at <= час закриття` (+ `_q4_cache_drop`,
   `_q4_lit_at`). Записи, новіші за закриття, лишаються. У лог — ОДИН рядок із
   переліком, що саме прибрано і скільки воно чекало.
   ⚠️ Під час відкритої угоди `intercept` у черги не додає, тож на практиці
   прибирається все, що лишилось від попереднього циклу.
2. **Запобіжник у самих двигунах** (Q4/Q3/Q2) — перед відкриттям
   `_is_new_situation(...)`; не пройшло → `pop` із черги + лог. Це рятує, коли
   `note_trade_closed` не спрацював (рестарт, стан із БД). ♾ «Без терміну» цей
   вихід НЕ вимикає — він не про час, а про відпрацьовану підставу.
3. **Анти-флуд у `_open`:** `_new_situation_logged = (symbol, reason)` — незмінна
   причина пишеться ОДИН раз, а не щотіку.

Тест: `test_new_situation.py` (22) — розділ «🧟 МЕРТВИЙ ЗАПИС У ЧЕРЗІ».

## 🔥 ВИГОРІЛИЙ СИГНАЛ + 🚨 ЗАСТРЯГ: запис із черги НЕ повертається в середину

**Правило користувача дослівно:** «Якщо запис вийшов із Черги-4 — монета пішла
далі по алгоритму. Потрапити сюди вона має ЛИШЕ відпрацювавши повністю НОВИЙ
алгоритм (новий сигнал + усі фільтри), а не зʼявитись невідомо звідки й
продовжити працювати ПОСЕРЕД алгоритму. З черги монета видаляється ПОВНІСТЮ.»
Плюс: «Якщо монета застрягає у Черзі-4 — її також потрібно повністю викинути.»

**🔥 `note_signal_consumed(sym, added_at)`** — кличеться з КОЖНОГО успішного
відкриття з черги (Q4 / Q3 / Q2 / EXH / ✋ ручне з Черги-4):
- `_consumed_signal_at[sym] = added_at` (ПЕРСИСТЕНТНО) — сигнал ВИГОРІВ;
- монета прибирається з **УСІХ** черг (`_pending`..`_pending4`) + `_q4_cache_drop`
  + `_q4_lit_at`, а не лише з тієї, що відкрила.
`_is_new_situation` тепер відхиляє БУДЬ-ЯКИЙ запис із `added_at <= _consumed_signal_at`
— **навіть поки угода ще ВІДКРИТА** (не чекаємо закриття). Повернення можливе
виключно через `intercept` з НОВИМ `added_at`, тобто новий сигнал сканера, що
пройшов усі фільтри.

**🧬 `_purge_spent_queue_records()` на СТАРТІ** (`start()` до першого тіку).
Стан приїжджає з БД, і серед відновлених записів міг бути відпрацьований — саме
так «зомбі» повертався в чергу й крутився в середині алгоритму. Прибираємо все,
де `added_at <= consumed_signal_at` або `<= last_trade_end`.

**🚨 `queue4_hard_max_hours` (деф. 24)** — абсолютна межа життя запису, яку
♾ `queue4_no_ttl` **НЕ вимикає**. ♾ означає «живе, доки не замінить протилежний
сигнал», а не «живе вічно за будь-яких умов». Чиста функція `_q4_stuck(added_at,
now, hard_h)`; при спрацюванні монета вилітає з **УСІХ** черг із рядком у лозі
(скільки провисіла, яка межа). 0 = вимкнути запобіжник (не рекомендовано).
UI: поле `ff-queue4-hardmax` поряд із ♾.

Тести: `test_new_situation.py` (28) — розділ «ВИГОРІЛИЙ СИГНАЛ + ЗАСТРЯГ».

### 🔗 Режим комбінування трьох виходів (`signal_exit_mode`)

Forecast 1H / Forecast 4H / Decision можна поєднати:
- **`'or'` (ДЕФОЛТ)** — «кожен окремо»: спрацювало БУДЬ-ЯКЕ увімкнене правило →
  вихід. Це поведінка, яка була до появи режиму (нічого не змінилось).
- **`'and'`** — «усі разом»: вихід ЛИШЕ коли ВСІ УВІМКНЕНІ правила одночасно
  проти позиції.

⚠️ Ключові домовленості:
- **Нейтраль / «немає даних» — це НЕ «проти»**, тож у режимі `and` вона ЛАМАЄ
  збіг і угода тримається (у `or` вона просто не спрацьовує).
- **Вимкнене правило у збігу НЕ бере участі** — інакше `and` не зібрався б ніколи.
- **Одне увімкнене правило** → `and` тотожний `or`, і причина закриття лишається
  ВЛАСНА (`forecast_1h_exit` тощо), а не збіднюється до загального коду.
- Збіг кількох дає окрему причину **`signal_exit_and`** («🔗 AND: усі увімкнені
  вердикти проти позиції»); у лозі перелічені ВСІ вердикти з відсотками:
  `AND: Forecast 1H LONG (70%) + Forecast 4H LONG (65%) + Decision LONG (80%) — усі проти SHORT`.
- Некоректне значення налаштування → дефолт `'or'` (не збій).

UI: випадайка `tm-signal-exit-mode` («кожен окремо (АБО)» / «усі разом (AND)»)
одразу під трьома тумблерами в блоці Exit Rules.

### 🎯 «ЧІТКО ВИЗНАЧЕНЕ» значення — інакше ЧЕКАЄМО (вимога користувача)

Правило дослівно: «Значення має бути чітко визначене LONG/SHORT, тобто чітке
розуміння; а якщо є нейтральне — чекаємо, поки не визначиться чітко.»

Кожен вердикт (Forecast 1H / 4H / Decision) проходить `_verdict(...)` і дає
**три** стани, а не два: `(проти?, ВИЗНАЧЕНО?, опис)`.
- **`визначено = False`** → правило НЕ спрацьовує **в жодному режимі**:
  • нейтраль (`side=0`, `recommended='NEUTRAL'`);
  • «немає даних» (порожній прогноз / Decision не порахувався);
  • бік є, але **впевненість < `signal_exit_min_conf`** → «ще не чітко».
- У режимі **`and`** невизначений вердикт ЛАМАЄ збіг (перевірка `all(det)` стоїть
  ПЕРЕД `all(hit)`), у **`or`** — просто пропускається. В обох випадках угода
  ТРИМАЄТЬСЯ і ми чекаємо, поки вердикт стане чітким.

**`signal_exit_min_conf`** (%, дефолт **0 = вимкнено**) робить «чіткість»
вимірюваною. При 0 будь-який явний LONG/SHORT вважається чітким (стара
поведінка); нейтраль і «немає даних» НЕ спрацьовують незалежно від порога.
⚠️ Поріг заданий, а відсотка у вердикті немає → підтвердити чіткість нічим →
вважаємо НЕвизначеним (не закриваємо).

UI: поле `tm-signal-exit-min-conf` («🎯 Чіткий вердикт ≥ N %») під випадайкою
режиму. Тести: `test_exit_rules.py` (30).

### 🧮 «СТАРИЙ МММ ⚖ РІВНОВАГА» → ВИХІД (`use_mm_flat_exit`, деф. OFF)

**Вимога користувача (15.09), дослівно:** «Додай вихід із угоди по показнику
"🧮 Старий МММ" — якщо нейтраль, закриваємо угоду.»

`_mm_flat_exit_reason(symbol, pos)` → `('mm_flat_exit', опис)` або None.

⚠️ **ДЖЕРЕЛО — `ff.mm_snapshot_for([sym])`**, ТОЙ САМИЙ знімок, що малює колонку
«🧮 Старий МММ» у таблиці відкритих угод і рядок 🧮 МММ-монітора. Власного
розрахунку тут НЕМАЄ і бути не може: інакше бот закривав би угоду за числом,
якого на екрані не видно (урок PD-зони). Тест-замок: у тілі функції є
`mm_snapshot_for` і немає `_fuel_dir_legacy` / `compute_mm` / `_liq_state`.

⚠️ **«НЕМАЄ ДАНИХ» — ЦЕ НЕ «НЕЙТРАЛЬ»** (головний запобіжник). Знімка по монеті
може не бути взагалі: бот щойно піднявся, liq-map ще не зібрала рівні, монету
щойно додали. Якби порожній рядок читався як рівновага, **КОЖЕН рестарт
закривав би ВСІ відкриті позиції**. Немає рядка → правило мовчить, таймер
підтвердження скидається. Тест: `test_missing_snapshot_is_not_flat`.

⚠️ **ПОЗА комбінуванням `signal_exit_mode`.** Ті три правила (Forecast 1H/4H,
Decision) — про ПРОТИЛЕЖНИЙ вердикт, і там нейтраль ЛАМАЄ збіг; тут нейтраль і
Є підставою вийти. В одному AND/OR це були б прямо протилежні правила, тому 🧮
перевіряється ОКРЕМО і ПЕРШИМ (читання готового знімка дешевше за прогноз/
Decision). Тест-замок: у `_signal_exit_reason` немає згадок про МММ.

⚠️ **Тумблер 🧮 МММ-монітора на це правило НЕ впливає** — знімок будується для
монет у відкритій угоді незалежно від нього (див. розділ монітора вище).

- `mm_flat_exit_mode`: **`'flat'`** (деф., дослівна вимога — виходимо лише на ⚖)
  / `'flat_or_against'` (ще й коли МММ розвернувся ПРОТИ угоди). Асиметрія
  дефолту свідома: просили саме про нейтраль, а розворот — окреме рішення про
  ризик. Некоректне значення → `'flat'`, а не збій.
- `mm_flat_exit_confirm_sec` (деф. **0** = закривати одразу). Поле є тому, що
  межа напрямку — це `|dir| ≤ 0.1`, тобто сила ≈10%: біля неї показник миготить
  між «рівновага» і «слабкий напрямок». `_mm_flat_since[SYMBOL]` тримає момент
  початку стану; повернення напрямку його ОБНУЛЯЄ (інакше короткі провали
  «накопичувались» би між епізодами).
  ⚠️ Витримка ⚖ мусить працювати і в режимі `'flat_or_against'` (тест-замок) —
  половина вимоги, яку легко загубити.
- **`mm_against_exit_confirm_sec` (деф. 60с) — ОКРЕМА витримка для РОЗВОРОТУ**
  (вимога 20.09, дослівно: «🧮 Старий МММ РОЗВЕРНУВСЯ ПРОТИ позиції — потрібно
  трішки витримки, бо секундна зміна і вибиває з угоди»).
  ⚠️ **ЦЕ СКАСОВУЄ правило 18.09** («коли "проти" то закривати відразу»).
  Обидва тест-замки ПЕРЕПИСАНО, а не «полагоджено»: вимога змінилась.
  ⚠️ **ЧОМУ ОКРЕМЕ ПОЛЕ, а не спільне з ⚖.** Це різні події: ⚖ — «тиску
  немає» (чекати дешево, витримка проти МИГОТІННЯ біля межі `|dir| ≤ 0.1`),
  розворот — «тиск ПРОТИ» (чекати дорого, витримка проти ОДНОГО такту
  зустрічного сплеску). Одне число на обидва означало б, що налаштувати можна
  лише одне з них.
  ⚠️ **ТАЙМЕРИ НЕ ПЕРЕТІКАЮТЬ:** `_mm_against_since[SYMBOL]` — свій; гілка ⚖
  знімає його, гілка розвороту знімає `_mm_flat_since`, а повернення напрямку
  (і «немає даних», і закриття останньої позиції) знімає ОБИДВА. Інакше
  перехід «рівновага → проти» подарував би новому стану відпрацьований чужий
  час, і закриття сталось би не тоді, коли його чекають.
  ⚠️ Дефолт **60с ≈ одне оновлення liq-map**: розворот мусить пережити хоча б
  ОДИН новий розрахунок джерела, а не тремтіння ціни всередині того самого.
  `0` = стара миттєва поведінка (тест-замок).
- Причини `mm_flat_exit` / `mm_against_exit` мають підпис у ВСІХ трьох місцях
  показу: розгорнутий `_close_detail`, бейдж TM і JS-мапа сторінки. У рядку
  розвороту тепер стоїть `· тримається Nс` (скільки він реально протримався).
- UI: `tm-use-mm-flat-exit` + `tm-mm-flat-exit-mode` + `tm-mm-flat-exit-confirm`
  + **`tm-mm-against-exit-confirm`** у блоці Exit Rules, **ПІСЛЯ рядка
  «🔗 Комбінувати»** і за роздільником — інакше читалося б як четверте правило
  того збігу (тест-замок на порядок). Поле розвороту стоїть ПІД полем ⚖, і
  кожне підписане своїм станом (тест-замок: інакше крутили б не те).

**🐞 ПОПУТНО ВИПРАВЛЕНО: тротл правил виходу був ПЕР-СИМВОЛЬНИЙ.**
`_signal_exit_at[symbol]` означав, що по монеті з ДВОМА позиціями (real +
paper) перевірку «з'їдала» та книга, чий монітор устиг першим, — друга
лишалась БЕЗ правил виходу на ціле вікно `SIGNAL_EXIT_TTL`. Той самий клас
помилки, що вже ловили на `_pilot_at` (кейс TRXUSDT). Тепер ключ дає
`_exit_key(symbol, is_shadow)` → `SYMBOL|R` / `SYMBOL|S`, і обидва `pop` при
закритті знімають ключ САМЕ своєї книги. ⚠️ Це стосується і трьох наявних
правил — вони теж перевірялись не для кожної книги. Таймер `_mm_flat_since`
навпаки СИМВОЛЬНИЙ (МММ — показник монети) і знімається лише коли по монеті не
лишилось жодної позиції (`_mm_flat_drop_if_idle`).

Тест: `test_exit_rules.py` (49) — розділ «🧮 СТАРИЙ МММ БЕЗ НАПРЯМКУ».
⚠️ Раннер файлу — РУЧНИЙ перелік викликів у `__main__`: новий тест, не доданий
туди, просто НЕ запуститься (на цю пастку вже наступали в `test_liq_scan.py`).

## 🎯 ДЖЕРЕЛО Manual SL = НАЛАШТУВАННЯ УГОДИ (не «як склалось»)

**Скарга:** у Черзі-4 стоїть «🛑 SL з 1H OB», а в 🧾 Лозі — «SL встановлено з
OB 15M». **Корінь:** у проєкті ДВА незалежні авто-SL, і кожен мав СВІЙ TF:
- `fuel_filter._q4_set_vob_sl` → `queue4_sl_source` ('1h' / '15m');
- `trade_manager._auto_ob_manual_sl` → власний `q2_auto_ob_sl_tf` (деф. **15m**).
TM нічого не знав про вибір користувача в Черзі-4. Коли SL ставив ВІН (бо Q4
свого не поставив), виходив 15m — попри налаштування.

**Фікс:** `_auto_ob_manual_sl` будує ланцюг ЗА ПОХОДЖЕННЯМ УГОДИ. Якщо
`opened_by` містить `Q4` — ПЕРШИМ іде саме те, що обрано в Черзі-4:
- `'1h'`  → ★OB на `ob_filter_timeframe` (мітка «Черга-4: 1H OB»);
- `'15m'` → `_from_volumized('15m')` — саме 15m, а НЕ сканерний
  `volumized_timeframe` (у користувача він 5m).
Далі — звичайний фолбек (`q2_auto_ob_sl_tf` → ★TF → Volumized → % від входу), і
в лозі видно, ЧОМУ обране джерело не спрацювало. Угоди НЕ з Черги-4 поведінку
не змінили. Тест: `test_autosl_chain.py` (розділ «ДЖЕРЕЛО SL…»).

## 🎯 АВТОПІЛОТ УГОДИ (`detection/trade_pilot.py`) — супровід по ГРАФІКУ

Вимога: «максимально вигідно тримати угоду і чітко вийти при максимальному
результаті». Не таймер і не фіксований %, а РЕАЛЬНІ обʼєкти графіка, які бот уже
рахує. Модуль — **ЧИСТІ функції без I/O**; контекст збирає TM.

**🎯 Ціль** (`collect_targets` → `pick_objective`) — усе ПОПЕРЕДУ руху:
пули ліквідації (`runway.next`/`runway.main` з liq-map) · екстремум дилінг-
діапазону (той самий `_swing_hl_for_tf`, що дає лінії Strong/Weak) · POC.
Відсів: ближче за `min_target_dist_pct` (шум) і далі за `max_target_dist_pct`.
Головна ціль = **НАЙДАЛЬШИЙ** змістовний обʼєкт (це і є «максимальний
результат»); найближчий лишається як `next_obstacle`.
⚠️ **ЦІЛЬ ФІКСУЄТЬСЯ ОДИН РАЗ** (`objective_lock`, зберігається в
`pos['pilot_objective']`). Без замка вона зникала б зі списку «попереду» рівно
тоді, коли ціна до неї дійшла, найдальшою ставала наступна — і фіксація не
наставала б НІКОЛИ (рухомі ворота). Тест: `test_objective_does_not_drift…`.

**🛡 Стоп** (`structure_stop`) — за ПРОТИЛЕЖНИМ екстремумом діапазону + буфер:
LONG під swing-low, SHORT над swing-high. Рівень з неправильного боку від ціни
(структура вже зламана) НЕ повертається. `better_stop` — **ратчет**: стоп
рухається ЛИШЕ в бік прибутку. Після проходження `be_at_r` × ризик стоп не
гірший за беззбиток (`be_lock_pct` лишає невеликий плюс).

**Рішення** (`plan`) → `hold` / `trail` / `take`, і `reasons` ЛЮДСЬКОЮ мовою.
`take` → закриття з причиною `pilot_target` («🎯 Автопілот: ціль на графіку
досягнута»). `trail` → `update_manual_sl_tp(origin='auto', 'Автопілот · структура')`.
`hold` у лог НЕ пишеться (це стан) — лежить у `_pilot_state`, віддається
`get_pilot_state(sym)`.

**Інтеграція:** `_pilot_tick` кличеться з ОБОХ моніторів після `_check_signal_exits`;
тротл `PILOT_TTL=20с` на монету; при `pilot_enabled=False` (ДЕФОЛТ) не читається
НІЧОГО. Контекст (`_pilot_context`) — ЄДИНЕ місце з I/O.
⚠️ `runway` береться, лише якщо його напрямок ЗБІГАЄТЬСЯ з боком угоди — інакше
«ціль» вказувала б у протилежний бік.
UI: тумблер `tm-pilot-enabled` + вікно цілей + буфер стопа/беззбиток у Exit Rules.
Тест: `test_trade_pilot.py` (17).

### 📊 Візуалізація автопілота в таблицях відкритих угод

Колонка **«🎯 Автопілот»** (real + paper, після Manual TP) показує, що
відбувається з позицією ПРОТЯГОМ усієї угоди, а не лише в момент дії:
- **смужка прогресу** — скільки шляху ВІД ВХОДУ до цілі пройдено
  (`trade_pilot.progress`: `pct` обрізаний у [0..100] щоб не «вилітав», але
  `done_pct` лишається СИРИЙ — видно й мінусовий рух);
- **ціль** зі значком типу: 💧 найближчий пул · 🌊 головний пул · 🏁 екстремум
  діапазону · ⚪ POC + скільки ще лишилось у %;
- **найближча перепона** окремим рядком (коли вона не збігається з ціллю);
- **остання дія**: ⏳ тримаємо · 🛡 стоп підтягнуто (×N разів за угоду) ·
  🎯 фіксація; у тултипі — повний розклад причин і таймфрейми.

⚠️ **Стан пишеться на КОЖНОМУ такті автопілота** (`_pilot_state`), не лише на
`hold` — інакше колонка «замерзала» б між діями. `/api/tm/state` лише ЧИТАЄ
готовий знімок; жодних розрахунків на цьому гарячому ендпоінті (той самий урок,
що з прибраною колонкою МММ). Коли `pilot_enabled=False` — поле `pilot` навіть
не додається в payload.

**Таймфрейми автопілота** (тепер керовані з UI):
- **структура (swing high/low)** — `pilot_swing_tf`; порожньо = «як у графіка»,
  тобто `swing_hl_timeframe` сканера (деф. **1H**). Це джерело і структурного
  стопа, і однієї з цілей — тож рівні збігаються з лініями Strong/Weak на екрані;
- **пули ліквідації** — liq-map, вікно **24 год** (не свічковий TF);
- **POC** — `pilot_poc_hours`, деф. **72 год**;
- частота рішень — монітор 4с із тротлом `PILOT_TTL=20с` на монету.

## 🧬 ПОХОДЖЕННЯ — В ОДНОМУ РЯДКУ З ВІДКРИТТЯМ (кейс OPUSDT)

**Що показав експорт логу (27.08).** Черга-4 писала ланцюг походження ОКРЕМИМ
записом (`log_activity(..., source='Q4')`) — і в експорті **не було ЖОДНОГО
рядка `opened,Q4`**: `awk '$8=="opened"' → 15×FF, 1×TM, 0×Q4`. Лишалось лише
«Відкрито paper · 🟪 Volumized OB → 🎯 Черга-4 · …» без контексту.

**Правило: один рядок = одна подія.** Ланцюг тепер їде РАЗОМ із самим фактом
відкриття, тож загубитись не може:
- `_open(..., origin_trace=...)` → `tm.set_origin_trace(sym, text)` ПЕРЕД
  делегуванням;
- TM кладе його на позицію (`pos['origin_trace']`) у `_open_shadow` і в
  реальному шляху **ДО** запису в лог;
- рядок «Відкрито …» друкує `· 🧬 <ланцюг>`.
Окремий Q4-запис прибрано (інакше дублювався б). Флip-нотатку («ФЛІП: сигнал
був X, показники — Y») перенесено в сам ланцюг.

**Друга прогалина: пройдена 🔁 повторна перевірка була МОВЧАЗНОЮ.** По OPUSDT у
лозі стояло п'ять відмов «прогноз 1H проти», а потім тихе відкриття — не було
видно, з ЯКИМИ значеннями фільтри нарешті склались. Тепер `_q4_recheck_filters`
зберігає розклад успіху в `_q4_recheck_pass_detail`, і при відкритті пишеться
`Черга-4 🔁 повторна перевірка ПРОЙДЕНА · <розклад> — відкриваємо`
(лише якщо ДО того були відмови — щоб не плодити рядки на рівному місці).

## 🎯 TP-1 / TP-2 від автопілота + компактна колонка

**Колонка «🎯 Автопілот» — В ОДИН РЯДОК** (`pilotCellHTML`): значок дії ·
мінісмужка 34px · ціль зі значком типу · залишок % · `🛡N` (скільки разів
підтягнуто стоп). Усе інше — у тултипі, щоб таблиця лишалась щільною.

**АНАЛІТИКА ПОДІЛУ** (`trade_pilot.plan_targets`, ЧИСТА функція):
- **TP-2 = ГОЛОВНА ціль** (найдальший змістовний обʼєкт попереду) → повний вихід.
  Береться ЗАФІКСОВАНА ціль угоди (`objective_lock`), а не перераховується.
- **TP-1 = НАЙБЛИЖЧА змістовна перепона МІЖ входом і TP-2** → частковий вихід.
  Саме там рух статистично найчастіше сповільнюється (пул ліквідності / POC /
  межа діапазону), тож фіксація частини — це гроші в найімовірнішій точці
  реакції, а хвіст лишається «безкоштовним».

⚠️ **Жорсткі перевірки, без яких поділ безглуздий:**
1. обидва рівні СТРОГО попереду руху ВІД ВХОДУ (не від поточної ціни);
2. TP-1 суворо МІЖ входом і TP-2;
3. TP-1 віддалений від входу ≥ `tp_min_gap_pct` — інакше комісія зʼїсть
   частковий вихід;
4. TP-2 віддалений від TP-1 ≥ `tp_min_gap_pct` — два рівні впритул не мають сенсу;
5. немає придатної проміжної цілі → **TP-1 НЕ вигадуємо** (лише TP-2).
Кожен рівень несе `from_entry_pct` і **`r`** (кратність ризику від стопа) — саме
R показує, чи вартий рівень виходу. Без стопа `r=None` (не вигадуємо).

**Виконання.** `manual_tp1` — ОКРЕМЕ поле позиції (не плутати з `manual_tp`):
- `_check_manual_tp1` у ОБОХ моніторах стоїть **ПЕРЕД** перевіркою повного TP —
  інакше TP-2, що лежить далі, ніколи не дав би частковому спрацювати;
- спрацьовує **РІВНО ОДИН раз** (`tp1_done`), інакше кожен тік різав би позицію далі;
- закриває `pilot_tp1_close_pct`% (деф. 50) через наявні `_partial_close` /
  `_partial_close_shadow`;
- рівень лишається видимим у таблиці з ✓ — щоб було зрозуміло, ДЕ зафіксували.

**Автозаповнення** (`pilot_autofill_tp`, деф. **OFF**): рахується ОДИН раз на
угоду (`pilot_tp_set`), рівні прибиті до плану входу й не «пливуть» за ціною.
Рівень, уже введений ОПЕРАТОРОМ, автопілот НЕ чіпає. TP-2 йде через
`update_manual_sl_tp` (та сама валідація боку + позначка 🤖 бот).

**UI:** колонки `Manual TP-1` / `Manual TP-2` в обох таблицях (real 14 колонок,
paper 13); `submitManualTp1` — окремий сабміт, бо поле окреме. Налаштування —
тумблер `tm-pilot-autofill-tp` + «TP-1 закриває N%» + «мін. зазор %».
Тест: `test_trade_pilot.py` (30).

## 🎯 TP-1: ІНВЕНТАР ЦІЛЕЙ · ВІКНО ШЛЯХУ · СИЛА ОБʼЄКТА (не зламати!)

> ⚠️ **ЧИТАТИ РАЗОМ із розділом «TP-1 = ВЛАСНЕ ЧИСЛО 🎯 Автопілота» нижче.**
> Вікно шляху і похідний рівень, описані тут, тепер працюють ЛИШЕ в ЗАПАСНІЙ
> гілці — коли магніту немає. Коли TP-2 прийшов із 🧲 магніту, TP-1 = власне
> число автопілота, і НІ вікно, НІ похідний рівень до нього не застосовуються.

**Заміряно на проді (14 позицій).** TP-2 стояв у всіх, TP-1 — лише в 6.
Два різні дефекти:

**1. Інвентар був затонкий.** `collect_targets` знав ЛИШЕ 4 обʼєкти (2 пули
ліквідації + swing + POC), тож у 8 із 14 позицій між входом і ціллю не було
ЖОДНОГО рівня — частковій фіксації просто не було де стати. Додано ДВА
джерела, які НЕ коштують жодного зайвого запиту:
- **VAH/VAL** — `compute_poc` уже повертав їх разом із POC, ми просто не брали
  (`_pilot_context` тепер кладе `ctx['vah']/['val']`);
- **EQ — рівновага дилінг-діапазону** `(high+low)/2` з того самого
  `_swing_hl_for_tf`. Канонічний SMC-рівень: саме тут PD-зона перемикається
  між премією і дисконтом.

**2. Вибір «найближчий обʼєкт» був неправильний.** На FIL це дало TP-1 на
1.5% при цілі 13.4% — половина позиції знімалась через **11% шляху**
(DOT — 16%). Тепер TP-1 шукається у **ВІКНІ ЧАСТКИ ШЛЯХУ** до TP-2:
`tp1_min_path_pct`=**30%** … `tp1_max_path_pct`=**75%** (це % ШЛЯХУ, а не %
ціни!). Серед кандидатів виграє **НАЙСИЛЬНІШИЙ** обʼєкт за
`_TP1_WEIGHT` (liq_next 5 > liq_main 4 > poc/va 3 > eq 2 > swing 1), тайбрейк
— ближчий до середини вікна.

**3. Похідний рівень, коли обʼєкта у вікні немає** —
`tp1_fallback_path_pct`=**50%** шляху до цілі, `kind='path'`, підпис «50%
шляху до цілі». Це НЕ «рівень із повітря»: він похідний ВІД ВЛАСНОЇ ЦІЛІ
автопілота, і в лозі прямо сказано, що обʼєкта у вікні не знайшлось.
`0` = не ставити.
⚠️ Похідний рівень теж перевіряється на «строго перед TP-2» — без цього
`tp1_fallback_path_pct > 100` давав TP-1 ДАЛІ за TP-2 (упіймано тестом
`test_tp1_never_passes_tp2`).

UI: нові значки типів у `_PILOT_KIND` — `va: ◫`, `eq: ⚖`, `path: ➗`.
Тести: `test_trade_pilot.py` (45).

## 🧲 TP-2 = МАГНІТ ЛІКВІДНОСТІ · TP-1 = РІВЕНЬ АВТОПІЛОТА (не зламати!)

Вимога користувача: «💧 Фільтр ліквідності не будемо використовувати в Черга-4,
щоб зменшити навантаження на біржу, а використаємо для визначення Manual TP-2
при відкритті угоди — ставимо туди ціну із 🧲 найбільший магніт, а Manual TP-1
ставимо ціну яку визначить 🎯 Автопілот».

**1. Черга-4 більше НЕ перевіряє 💧 фільтр.** `_q4_recheck_filters` кличе
`scanner._signal_allowed(..., skip_liq=True)` (+ `except TypeError` — фолбек на
старіший сканер, щоб оновлення файлів у різному порядку не поклало чергу).
⚠️ На **ІНТЕЙКУ фільтр лишається** — монета проходить його, коли СТАЄ в чергу;
знято лише повторну перевірку на кожному тіку двигуна. У розкладі так і
написано: `Ліквідність[…]:⏭ Черга-4 не перевіряє (пройдено на інтейку)`.

**2. 🧲 Магніт стає ЦІЛЛЮ УГОДИ (`pilot_objective`), а не просто рівнем TP-2.**
Це головне рішення: якби ми поставили ЛИШЕ рівень, автопілот закрив би позицію
на СВОЇЙ (ближчій) цілі через `action='take'`, і TP-2 не спрацював би НІКОЛИ —
декоративний рівень. Ставши ціллю, магніт узгоджує все одразу: колонку
«🎯 Автопілот», R, автозакриття і Manual TP-2. TP-1 далі рахує автопілот
(`plan_targets`: найсильніший обʼєкт у вікні 30-75% шляху ДО магніту).
- `pilot_tp2_from_magnet` (дефолт **True**); біржа/глибина беруться з
  налаштувань сканера (`liq_filter_exchange`/`liq_filter_bars`) — одне місце.
- `TradeManager._magnet_objective(symbol, side, entry)` → ціль або None;
  `SMCScanner.get_liq_magnet(symbol)` — той самий зріз і кеш, що у фільтра.
- ⚠️ **БЛИЖНЯ МЕЖА, а не підпис.** Магніт — це СМУГА `[price..price_hi]`:
  LONG зустрічає НИЖНЮ межу, SHORT — ВЕРХНЮ. Виходимо на першому дотику до
  кластера, а не сподіваємось, що ціна прошиє його наскрізь. Для LONG це
  збігається з підписом у банері, для SHORT — ні, і це навмисно.
- ⚠️ Три перевірки: магніт **попереду ВХОДУ** (драбина симетрична навколо ціни,
  найбільша сходинка легко лежить ПОЗАДУ) · відстань ≥ `tp_min_gap_pct` ·
  дані взагалі є. Не пройшло → ціль рахує автопілот (стара поведінка), угода
  НІКОЛИ не лишається без рівнів.
- ⚠️ **Питаємо РІВНО ОДИН раз на угоду**, і позначку `pilot_magnet_done`
  ставимо **лише коли біржа ВІДПОВІЛА** (прапорець `_magnet_data_ok`, а НЕ
  розбір тексту причини — саме на цьому тест упіймав мою першу версію).
  Біржа лежала → повтор пізніше (зріз кешується, повтор безкоштовний).
- ⚠️ У `_pilot_tick` магніт застосовується, лише якщо `pilot_objective` ЩЕ НЕ
  стоїть. Це лишилось як є — але тепер на Q4-шляху ціль обирається РАНІШЕ і
  вже з магніту (див. наступний розділ), тож суперечності немає.
- UI: значок `magnet: '🧲'` у `_PILOT_KIND`.

### 🧲 МАГНІТ — У ПРІОРИТЕТІ ДЛЯ TP-2 (вимога користувача, кейс STXUSDT)

**Що показав скрін (06.09).** STXUSDT LONG @0.26660 із Черги-4, у лозі:
`R достатній: 1.44R · ціль Weak High +5.55% / стоп 1H OB −3.85%` → Manual TP-2
= **$0.28140**. Магніт ліквідності **не брався взагалі**, хоча на драбині
(720 год) для LONG попереду стояла сходинка **$0.28000–0.28500 ($26K)**.

**Корінь — порядок, а не логіка.** `_pilot_tick` бере магніт ЛИШЕ коли
`pilot_objective` порожня, а R-гейт Черги-4 заповнює її ще ДО відкриття
(`set_pending_objective`). Тобто на КОЖНІЙ Q4-угоді магніт був структурно
недосяжний.

**Фікс: вибір цілі перенесено В ГЕЙТ — `_q4_pick_objective(sym, side, ref, tm,
trade_pilot)` → `(obj, note)`, ЄДИНЕ місце вибору:**
1. **🧲 `tm._magnet_objective(sym, side, ref)`** — найбільша сходинка ПОПЕРЕДУ
   входу в бік угоди, ближня межа, ≥ `pilot_tp_min_gap_pct`;
2. немає магніту → ціль автопілота (`collect_targets`/`pick_objective`).

⚠️ **Переносити було ОБОВʼЯЗКОВО, а не «зручніше».** Якби магніт просто
перекривав ціль ПІСЛЯ гейта, повернувся б баг VETUSDT: гейт пропустив за одним
числом, а в угоді стоїть інше. Тепер гейт судить угоду САМЕ за магнітом. На
STXUSDT це видно в числах: магніт $0.28000 БЛИЖЧИЙ за Weak High $0.28140, тож
**R чесно падає 1.44 → 1.31** — і саме 1.31R стоїть у колонці.
⚠️ **Другого тумблера НЕ заводимо** — керує той самий `pilot_tp2_from_magnet`
(деф. ON): одна річ — один вимикач. OFF → магніт не питається взагалі (зайвого
запиту до біржі немає).
⚠️ **Фолбек НЕ мовчазний:** у розкладі `🧲 фолбек на ціль автопілота
(<причина>)`, інакше «ціль Weak High» виглядала б як вибір, а не як запасний
варіант.
⚠️ **Фіксація цілі винесена З-ПІД гейта.** Раніше весь блок стояв під
`if _min_rr > 0`, тож при ВИМКНЕНОМУ гейті (`queue4_min_rr=0`) ціль не
фіксувалась і TP-2 обирався пізніше, іншим шляхом — те саме налаштування
давало РІЗНІ TP-2. Тепер `_q4_expected_r` рахується ЗАВЖДИ, а поріг лише
застосовується.
⚠️ Вартість: гейт кличеться лише коли шари зійшлись і recheck пройдено (тобто
в момент відкриття), а `_liq_snapshot` кешується 300с — постійного тиску на
біржу немає.
Тести: `test_opposite_and_rr.py` (18), розділ «МАГНІТ У ПРІОРИТЕТІ».

**3. TP-1 НІКОЛИ не перевищує TP-2** (окрема вимога користувача). Обидві гілки
`plan_targets` це вже перевіряли, але додано **фінальний замок перед
`return`**: `tp1` не перед `tp2` → `tp1 = None` + рядок у `reasons`. Порожній
TP-1 — коректний стан (працюємо одним TP-2); «частковий» вихід ДАЛІ за повний —
ні. Правило надто дороге, щоб покладатись на те, що майбутня правка однієї з
гілок його не загубить.
⚠️ Виходів із `plan_targets` тепер ДВА (див. наступний розділ), тож замок
винесено у спільну **`_finish_targets(side, tp1, tp2, reasons)`** — обійти його
новою гілкою не можна: тест-замок `test_every_exit_goes_through_the_shared_lock`
розбирає AST і вимагає, щоб КОЖЕН `return` ішов або через цю функцію, або був
раннім «цілі немає» (літеральний dict).

### 💧 TP-1 = СХОДИНКА ДРАБИНИ ЛІКВІДНОСТІ (ЧИННЕ ПРАВИЛО, вимога 09.09)

**Вимога користувача, дослівно:** «Зміни алгоритм автоматичного визначення
Manual TP-1, на даний момент 🎯 Автопілот не підходить. Став автоматично
значення із "💧 Ліквідність" щось із середню шкалу між 🧲 найбільший магніт і
поточна ціна».

⚠️ **ЦЕ ЗАМІЩУЄ розділ «TP-1 = ВЛАСНЕ ЧИСЛО 🎯 Автопілота» нижче** (він був
чинним лише кілька годин того самого дня). Той опис лишено як ІСТОРІЮ рішень і
як опис гілки, що працює при `tp1_from_liquidity=False`.

- **`pick_tp1_from_ladder(side, entry, price, tp2_price, ladder, cfg)`** —
  ЧИСТА функція: серед сходинок драбини бере ту, що **НАЙБЛИЖЧА до СЕРЕДИНИ**
  шляху «поточна ціна → TP-2». `tp1_liq_mid_pct` (деф. **50**) — де саме та
  «середина». За рівної відстані виграє **ТОВЩА** сходинка (там реакція
  ймовірніша) — тобто частка це ТАЙБРЕЙК, а не головний критерій.
- **Чому сходинка, а не просто «половина шляху»:** сходинка — РЕАЛЬНИЙ кластер
  ліквідації, тобто місце, де рух статистично гальмує; половина шляху — просто
  число. Тому «просто число» лишилось ЗАПАСНИМ варіантом.
- **`TradeManager._liq_ladder_levels(sym, side)`** віддає сходинки як рівні.
  ⚠️ **ТОЙ САМИЙ ЗРІЗ, ЩО ДАВ МАГНІТ:** `get_liq_magnet` тепер повертає ще й
  **`rows`** — уся драбина з ОДНОГО кешованого знімка. Мережі це не коштує
  НІЧОГО (`_liq_snapshot` уже в кеші після вибору магніту), і обидва рівні
  угоди рахуються з ОДНИХ даних — «TP-1 з однієї драбини, TP-2 з іншої»
  структурно неможливо (урок PD-зони).
- ⚠️ **Ближню межу смуги рахує `ladder.magnet_edge`** — ТА САМА функція, що для
  магніту (LONG зустрічає нижню межу, SHORT — верхню). Своєї копії правила в
  `trade_pilot` НЕ заводимо: тому `plan_targets(ladder=…)` приймає вже готові
  рівні, а не сирі рядки драбини.
- **Кандидат мусить бути попереду І ВХОДУ, І ПОТОЧНОЇ ЦІНИ** (перше — тверде
  правило поділу, друге — буквально «між магнітом і поточною ціною»), плюс
  зазори від входу і від TP-2, плюс вікно `tp1_min/max_path_pct`.
- **Немає придатної сходинки → ➗ похідний «% шляху»** (`tp1_fallback_path_pct`),
  а **НЕ обʼєкт автопілота**: користувач сказав, що автопілот тут не підходить.
- ⚠️ **`_derived_tp1(...)` — ОДНА реалізація похідного рівня на ОБИДВІ гілки**
  (💧 і стару). Раніше він був інлайном лише в старій; копіювати його у нову
  означало б завести два «однакових» розрахунки, які з часом розійшлись би.
- **Тумблер `pilot_tp1_from_liquidity` (деф. УВІМК)** повертає попередній
  ланцюг, коли вимкнений; сходинки тоді навіть не питаються (зайвого походу в
  сканер немає). UI: `tm-pilot-tp1-liq` + `tm-pilot-tp1-liq-mid` у секції
  «🎯 РІВНІ УГОДИ».
- ⚠️ **НОВІ ключі** → `merged.update(stored)` віддає дефолт, міграція НЕ потрібна.
- ⚠️ Нова гілка теж виходить через `_finish_targets` — замок «TP-1 не перевищує
  TP-2» діє й тут.
- ⚠️ **Тест-замок AST довелось уточнити:** `ast.walk(plan_targets)` спускається
  у ВКЛАДЕНІ функції, тож `return` усередині `_derived_tp1` виглядав як «вихід
  повз замок». Тепер вкладені `FunctionDef` виключені — інакше тест падав би на
  КОЖНОМУ нормальному рефакторингу у хелпер.

Тести: `test_trade_pilot.py` (59) — розділ «💧 TP-1 = СХОДИНКА ДРАБИНИ».

### 🎯 TP-1 = ВЛАСНЕ ЧИСЛО «🎯 Автопілота» (ІСТОРІЯ · діє при вимкненому 💧)

**Вимога користувача (09.09), дослівно:** «Manual TP-1 буде мати автоматичне
число із 🎯 Автопілот, у випадку якщо число 🎯 Автопілот менше за автоматичне
значення Manual TP-2, то поле Manual TP-1 залишаємо порожнім».

Раніше TP-1 шукався серед **усіх** обʼєктів графіка у вікні частки шляху до
TP-2 — тобто число, яке стоїть у колонці «🎯 Автопілот», і число в полі
Manual TP-1 були РІЗНІ сутності. Тепер вони одна й та сама.

- **`_own = pick_objective(targets)`** — найдальший змістовний обʼєкт графіка,
  тобто РІВНО те, що автопілот показує як свою ціль. Якщо воно ВІДРІЗНЯЄТЬСЯ
  від TP-2 (а це означає, що TP-2 прийшов із 🧲 **магніту**) — воно й стає TP-1.
- **Не підійшло → поле ПОРОЖНЄ, похідний рівень НЕ підставляється.** Користувач
  просив саме порожнє поле, а не вигаданий замінник. Чотири причини порожнього
  (кожна названа в `reasons`): позаду входу · **не перед TP-2** · ближче за
  `tp_min_gap_pct` до входу · впритул до TP-2.
- **Вікно частки шляху (30-75%) до власного числа НЕ застосовується** — інакше
  вимога «TP-1 = число Автопілота» діяла б через раз. `path_pct` і далі
  рахується, але лише ДЛЯ ПОКАЗУ.

⚠️ **«МЕНШЕ» ТУТ НАПРЯМКОВЕ, А НЕ АРИФМЕТИЧНЕ** (головне рішення розділу).
Порівняння йде через `_ahead(side, ...)`: рівень мусить лежати МІЖ входом і
TP-2 — для LONG це «нижче за TP-2», для **SHORT — ВИЩЕ**. Буквальне `<` на
шорті означало б протилежне і ставило б частковий вихід ДАЛІ за повний.
Тест-замок: `test_short_mirrors_the_rule`.

⚠️ **ЗАПАСНИЙ ШЛЯХ ЗБЕРЕЖЕНО.** Коли магніту немає, власне число автопілота
ЗБІГАЄТЬСЯ з TP-2 — взяти TP-1 звідти фізично неможливо. Тоді працює стара
механіка: найсильніший обʼєкт у вікні шляху, а якщо його немає — похідний
`tp1_fallback_path_pct`. Видалити її означало б лишити без TP-1 усі угоди без
магніту. Тест-замок: `test_without_a_magnet_the_window_search_still_works`.

⚠️ Кейс STXUSDT навпаки (магніт БЛИЖЧИЙ за Weak High) тепер дає порожній TP-1 —
і це правильно: проміжного рівня між входом і магнітом там просто немає.

Тести: `test_trade_pilot.py` (51) — розділ «TP-1 = ВЛАСНЕ ЧИСЛО»;
`test_tp_from_magnet.py` (23) — обидва halves на справжньому `plan_targets`.

### 🧭 МАГНІТ ДЛЯ TP-2 — ЗА НАПРЯМКОМ УГОДИ, а не «найбільший узагалі»

**Кейс зі скріна:** `▲ Маса ліквідності ВИЩЕ ціни 66.2% · тягне ВГОРУ виразно
(+32.4 п.п.)` і водночас `🧲 найбільший магніт $0.64000 · 14.2% · ↓5.58%`.
Протиріччя тут НЕМАЄ (66.2% — обсяг, розсіяний по багатьох сходинках; 14.2% —
одна щільна сходинка внизу), але для TP-2 це був **прямий дефект**: магніт
брався ГЛОБАЛЬНИЙ, тож для LONG він опинявся ПОЗАДУ входу →
`_magnet_objective` повертав None → `pilot_magnet_done=True` → магніт більше не
питався, і угода в напрямку, який 💧 фільтр щойно СХВАЛИВ (66.2% ≥ поріг),
лишалась **без магнітного TP-2**. Ті самі дані давали протилежні висновки у
двох місцях — та сама помилка, що з PD-зоною.

**Фікс — розділені два різні питання:**
- **банер** («куди тягне ціну взагалі») → найбільша сходинка драбини, як було;
- **ціль угоди** → `ladder.pick_magnet_ahead(rows, ref, side)` — ЧИСТА функція:
  найбільша сходинка **ПОПЕРЕДУ входу** в бік угоди; тайбрейк — ближча
  (спрацює першою). «Попереду» міряється по **ближній межі** смуги
  (`ladder.magnet_edge`: LONG — нижня, SHORT — верхня).
- `get_liq_magnet(symbol, side=None, price_ref=None)`: без `side` — стара
  поведінка; із `side` — вибір за напрямком + `global_row`/`note`, щоб у 🧾 Лозі
  було ВИДНО, що взято не ту сходинку, що в банері, і чому.
- `_liq_snapshot` тепер несе `rows` і просить `rows=12` (було 6): із шести
  найбільших може не знайтись жодної попереду. Мережі це не коштує нічого —
  драбина рахується з тих самих уже завантажених рівнів.
- `_magnet_objective` кличе сканер із `side`+`entry`; `except TypeError` —
  фолбек на старіший сканер (файли оновлюються в різному порядку).

⚠️ Сходинка, ВСЕРЕДИНІ якої стоїть вхід, ціллю НЕ є: «перший дотик» до
кластера вже відбувся.
⚠️ Немає жодної сходинки попереду → магніту немає, ціль рахує автопілот
(вигаданого рівня не даємо).
⚠️ Порогу мінімальної частки для напрямкового магніту НЕМА (підлога — та сама
`MIN_ROW_PCT=1%` драбини). Частка йде в підпис цілі, тож видно, наскільки
магніт вагомий.
Тести: `test_tp_from_magnet.py` (22), розділ «МАГНІТ ЗА НАПРЯМКОМ УГОДИ».

`ladder.build_ladder` тепер віддає `magnet_row` (сирі числа сходинки-магніту),
`liq_scan.summarise` його прокидає далі. ⚠️ `magnet_price` — ФОРМАТОВАНИЙ рядок
для показу; парсити його заради числа не можна.
Тест: `test_tp_from_magnet.py` (15).

## 📨 TELEGRAM: TP-1 / TP-2 — КОРОТКО, з % біля ціни

`_tp_lines(pos)` — ЄДИНЕ джерело рядків рівнів для ОБОХ шляхів відкриття
(real і 🧪 paper), щоб формат не розʼїхався.

⚠️ **Немає даних — немає рядка.** На момент відкриття автопілот ще не рахував
рівні (він працює з монітора), і раніше там стояло «⏳ рахується». Користувач
попросив **не писати про TP нічого**, поки чисел немає: повідомлення про
відкриття має лишатись коротким. Тепер `_tp_lines` повертає ПОРОЖНІЙ рядок, і
блок TP просто не додається.

Щойно рівні реально стали — `_pilot_apply_tp` шле окреме коротке
`_notify_pilot_levels`: **заголовок + два рівні, і все**. Службового рядка
«вхід … · TP-1 (+1.50%)» НЕМАЄ (прибрано на прохання) — **відсоток стоїть
одразу після ціни кожного рівня**:
`🎯 TP-1 (50%): 0.69313 (+1.50%)` / `🎯 TP-2 (повний): 0.60960 (+13.37%)`.
Нема чого показувати → повідомлення не шлеться взагалі.
`is_shadow` передається явно → paper іде з міткою 🧪 у свій топік.
Тести: `test_autosl_chain.py` (розділ «TELEGRAM»).

## 🐞 АВТОПІЛОТ: СТАН НА КОЖНУ КНИГУ + ЛІЧИЛЬНИК = ФАКТ (кейс TRXUSDT)

Скрін paper-таблиці 03.09: `TRXUSDT LONG · вхід $0.32510 · SL 0.3205 · ціль
$0.34220`, а в колонці «🎯 Автопілот» — **114.38R** і **🛡432**. Обидва числа
неправдиві, і це ДВА РІЗНІ дефекти.

**1. R = 114.38 арифметично неможливий.** Вручну: `(0.34220−0.32510) /
(0.32510−0.32050)` = **3.72R**. Для 114.38R стоп мусив би стояти за 0.046% від
входу. Причина: **`_pilot_state` і `_pilot_at` були ключовані ЛИШЕ символом**, а
по монеті можуть одночасно стояти РЕАЛЬНА і ПАПЕРОВА позиції з різними входом,
стопом і ціллю. Наслідки (усі три реальні):
- `/api/tm/state` підмішував ОДИН знімок в ОБИДВІ таблиці → у paper-рядку
  світилось R реальної угоди (там стоп стояв майже в беззбитку);
- спільний троттл `_pilot_at` глушив пілот другої книги на цілий `PILOT_TTL` —
  одна з позицій фактично лишалась **без супроводу**, і в UI це не видно;
- закриття реальної позиції стирало стан паперової.

**Фікс:** `_pilot_key(symbol, is_shadow)` → `SYMBOL|R` / `SYMBOL|S` — ЄДИНЕ
джерело ключа для `_pilot_state`, `_pilot_at` і обох `pop` при закритті.
`get_pilot_state(symbol, is_shadow=False)`; `/api/tm/state` ітерує два списки
ОКРЕМО і передає книгу. Знімок несе `is_shadow`.
⚠️ Дефолт `is_shadow=False` лишено лише щоб старі виклики не падали — він дає
саме РЕАЛЬНУ книгу, а не «якусь».

**2. 🛡432 — лічильник рахував НАМІРИ.** Приріст стояв у блоці запису стану
(`trails += 1 if act == 'trail'`), який виконується **ДО** самої спроби трейлу.
А трейл нижче міг не застосуватись: операторський замок (`manual_sl_src ==
SRC_USER`) або відмова `update_manual_sl_tp`. На скріні SL бурштиновий = веде
оператор → стоп не зрушив ЖОДНОГО разу, а за 2.4 год (такт 20с) намоталось 432,
і тултип писав «Стоп підтягнуто 432 раз(и)». Іконка 🛡 «стоп підтягнуто» теж
світилась щотакту.

**Фікс:** знімок пишеться з `trails` = попереднє значення і `trail_block=''`;
приріст робить САМ трейл через `_pilot_mark(pkey, ...)` — **лише на
`r.get('ok')`**. Замок і відмова пишуть у `trail_block` причину.
UI: `_PILOT_BLOCKED` = **🔒 бурштиновий** перекриває значок дії, коли
`trail_block` не порожній (той самий принцип, що ⚖️ БЗ перекриває «бот» у полі
SL); причина — у тултипі, першим рядком.
⚠️ Знімок і далі пишеться ДО дії (щоб колонка не «замерзала», якщо дія впаде) —
тому результат дії доповнюється через `_pilot_mark`, а не вгадується наперед.

**3. Одне число — два написання.** `roundSlTp` робив `String(Number(...))` →
обрізав кінцеві нулі: поля показували `0.3205 / 0.33274 / 0.3422` (4/5/4 знаки,
рвана колонка), а той самий TP-2 у колонці автопілота — `$0.34220` (5 знаків
через `fmtPriceJS`). Точність у `_sltpDp` і `fmtPriceJS` **збігається**, тож
досить було лишити `n.toFixed(_sltpDp(n))` без обгортки `Number()`.
Плюс `lang="en"` на трьох полях рівнів: браузер локалізує `<input
type="number">` і малював кому замість крапки (функціонально безпечно — `.value`
повертає крапку, — але поле випадало з таблиці).

Тест: `test_pilot_books.py` (12). Перший тест дослівно відтворює скрін: дві
позиції з тією самою ціллю дають **paper 3.72R** і **real 94.67R**, і вони
більше не змішуються.

## 🔇 СПАМ ТИМ САМИМ РІВНЕМ SL (кейс ETHUSDT 09.09) — не зламати!

**Скарга:** «Що це за спам? Нічого не змінюється, а спам лізе, для чого?»
У 🧾 Лозі щотакту (20-35с) стояли ДВА ОДНАКОВІ рядки, і так годинами:
```
22:45:32 🤖 Бот (Автопілот · структура): Manual SL → $2543.63 (paper)
22:45:32 🎯 Автопілот: SL → $2,543.63 · стоп підтягнуто: структура Strong High …
22:46:08 (те саме)     22:46:45 (те саме) …
```
Стоп при цьому **НЕ РУХАВСЯ**.

**Корінь: РАТЧЕТ ПОРІВНЮВАВ СИРЕ ЧИСЛО З ОКРУГЛЕНИМ.** `plan()` рахує
кандидата з повною точністю (`swing×(1+buf)` = **2543.62973**), а на позиції
лежить УЖЕ ОКРУГЛЕНИЙ рівень (`_round_sltp_value` → **2543.63**, бо ≥$100 → 2
знаки). Для SHORT «краще» = НИЖЧЕ, і `2543.62973 < 2543.63` → `better_stop`
каже ТАК → пишемо… те саме 2543.63. Наступний тік — знову. **Вічний цикл на
дві сотих цента** (для LONG дзеркально). Відтворено точно, з тим самим числом
зі скріна.

**Фікс 1 — гейт «нічого не змінилось» ПЕРЕД записом.** У `_pilot_tick`
порівнюємо ТЕ, ЩО РЕАЛЬНО ЗАПИШЕМО, з тим, ЩО ВЖЕ СТОЇТЬ:
`_cur = _round_sltp_value(pos['manual_sl'])`; `_cur == lvl` → `return False`
(ні запису, ні рядка в лозі, ні приросту 🛡).
⚠️ Фікс саме в **TM**, а не в `trade_pilot`: округлення — вузол TM (`_sltp_dp`
знає точність рівня), чиста функція про нього не знає й не мусить.
⚠️ Порівняння ОКРУГЛЕНИХ значень закриває **будь-яке** джерело мікрошуму, а не
лише це конкретне.

**Фікс 2 — «один рядок = одна подія».** Автопілот пише СВІЙ, змістовніший
рядок (рівень + причина + ціль), тож загальний рядок `update_manual_sl_tp`
на цьому шляху лише дублював подію — і подвоював той самий флуд. Додано
**`update_manual_sl_tp(..., quiet: bool = False)`**: гасить РІВНО
`log_activity`, а позначку 🏷 походження (`manual_sl_src`/`manual_sl_by`) і сам
запис рівня НЕ чіпає. `quiet=True` передають ДВА шляхи автопілота — трейл SL і
TP-2.
⚠️ Щоб CSV-експорт не втратив поля, які раніше нікуди не дівались із рядка TM,
власний рядок автопілота тепер несе `extra={'manual_sl','origin','is_shadow'}`.
⚠️ Дефолт `quiet=False` — усі інші виклики (UI, Черга-4, Q3-VOB, POC) пишуть у
лог як раніше.

Тести: `test_autosl_chain.py` — розділ «🔇 СПАМ ТИМ САМИМ РІВНЕМ»
(`test_ratchet_compares_the_rounded_level_not_the_raw_one` відтворює пастку
числами зі скріна).

## 📐 R = ПЛАНОВИЙ (від ПОЧАТКОВОГО стопа) · ⚖ «ризик прибрано» — окремо

**Кейс VIRTUALUSDT (05.09).** У колонці «🎯 Автопілот» стояло **168.53R**.
Перевірка вручну: вхід `0.67160`, ціль `0.72890`, стоп `0.67194` →
`0.0573 / 0.00034` = **168.53**. Тобто число АРИФМЕТИЧНО ПРАВИЛЬНЕ — і
беззмістовне: TP-1 спрацював (✓ у полі), `tp1_move_to_be` переніс SL у
БЕЗЗБИТОК (на 0.05% ВИЩЕ входу), знаменник схлопнувся. У таблиці це читалось
як «геніальний сетап», хоча означало протилежне: **ризику вже немає**.

⚠️ **Це НЕ той самий баг, що TRXUSDT «114.38R»** (там була колізія книг
real/paper). Той фікс лишається; тут — інший корінь: R рахувався від
**ПОТОЧНОГО** стопа, тож будь-який трейл чи беззбиток його роздував.

**Правило: R — ПЛАНОВИЙ.** Канонічне значення («1R = скільки я ризикнув на
вході») міряється від **ПОЧАТКОВОГО** стопа:
- `pos['pilot_r_stop']` — якір, ставиться ОДИН раз у `_pilot_tick`, щойно в
  угоді зʼявився стоп, і НЕ переписується ні трейлом, ні беззбитком, ні рукою;
- `trade_pilot.is_risk_free(side, entry, stop)` — ЧИСТА функція: LONG — стоп на
  вході або ВИЩЕ, SHORT — навпаки. Стан ризику став ОКРЕМИМ полем
  (`risk_free`), а не маскувався роздутим числом.
- UI: `⚖` (зелений) поруч із R + пояснення в тултипі; сам R лишається плановим
  і фарбується як раніше (<1 червоний · <1.5 бурштин · ≥1.5 зелений).

⚠️ **Якір НЕ ставиться на беззбитковий рівень.** Угода, відкрита ДО появи
якоря, приходить у пілот уже з переїханим стопом — записати ЙОГО як
«початковий» означало б зберегти той самий роздутий R назавжди. Початковий
ризик відновити нізвідки → **чесно без R** (у тултипі сказано чому). На НОВІЙ
угоді ризик-фрі стоп при відкритті неможливий (TM вимагає рівень із
правильного боку ціни), тож якір ставиться першим же тіком.
⚠️ `_q4_expected_r` (гейт ПЕРЕД входом) НЕ чіпали — там стоп і Є початковий.
Тести: `test_pilot_books.py` (17), розділ «R = ПЛАНОВИЙ».

## ❓ ПОРОЖНІ Manual TP — ЧОТИРИ ПРИЧИНИ, І КОЖНА МУСИТЬ ГОВОРИТИ (NEOUSDT)

**Питання користувача (09.09): «Де Manual TP-1 / Manual TP-2? Що сталось?»**
На скріні paper SHORT NEOUSDT @ $2.1860: `Manual SL 2.3033` стоїть, **обидва
поля TP порожні**, а колонка «🎯 Автопілот» ПРАЦЮЄ — `🧲 $2.0000 · 6.8% · 1.59R`.

**Перше, що дав діагноз: РОЗРАХУНОК БУВ ПРАВИЛЬНИЙ.** `plan_targets` на РІВНО
цих числах віддає `TP-2 = 2.0000` і `1.59R` — тобто те саме R, що в колонці
(тест-замок `test_the_numbers_from_the_screenshot_do_produce_a_tp2` фіксує це
числами). Отже порожні поля НЕ могли бути результатом розрахунку — шлях просто
**до розрахунку не дійшов**. А мовчав він тому, що **жоден** зі чотирьох
виходів «рівня не буде» про себе не повідомляв: ні рядка в 🧾 Лозі, ні
позначки в тултипі.

**Чотири стани → чотири поля знімка `_pilot_state` (читає тултип колонки):**
1. **`tp_off`** — тумблер `pilot_autofill_tp` ВИМКНЕНО. У лог не пишемо (це
   налаштування, а не подія), але в тултипі сказано прямо, де тумблер.
2. **`tp_locked`** — рівень зняв **ОПЕРАТОР** (`pilot_tp_cleared`). Правило
   «людина має приоритет» лишається без змін, але раніше воно виглядало як
   «поля просто зникли назавжди». У тултипі — і як повернути авто.
3. **`tp_err`** — **ЗБІЙ розрахунку**. Головна дірка: стояв РІВНО `print(...)`
   у stdout, якого на проді не видно. Тепер іде рядок у 🧾 Лог
   (`source='PILOT'`, `🎯 Автопілот: ⚠️ збій автозаповнення TP — <тип>: <текст>
   · рівні НЕ виставлено`) + позначка `pilot_tp_err` на позиції. Той самий
   урок, що «Черга-4 ⚠️ збій після відкриття»: **невидимий збій читається як
   „бот просто не працює“**.
4. **`tp_skip`** — пілоту нема де взяти рівень (це вже працювало).

⚠️ **АНТИ-ФЛУД обовʼязковий:** такт пілота 20с, тож незмінна причина збою
пишеться ОДИН раз (порівняння з `pilot_tp_err`), інакше за годину було б 180
однакових рядків. Збій минув → позначка ЗНІМАЄТЬСЯ (`pos.pop`), інакше ⚠️
світилось би до кінця угоди.

⚠️ **ФОЛБЕК НА СТАРІШИЙ `trade_pilot` (`except TypeError`)** — найімовірніший
корінь САМОГО кейсу. Новий kwarg `ladder=` (💧 TP-1 зі сходинок) у ще не
оновленому модулі кидає `TypeError` на КОЖНОМУ такті → обидва TP не ставляться
**мовчки**. Файли розпаковуються/деплояться в РІЗНОМУ порядку, тож це той самий
прийом, що вже стоїть на `get_liq_magnet(side=…)` і `_signal_allowed(skip_liq=)`.
Фолбек у лог НЕ пишеться — це не збій, а сумісність.

⚠️ **КОЛІР РОЗРІЗНЯЄ ПРИРОДУ СТАНУ:** `tp_err` → **червоний** `#f87171` (це
поломка), `tp_off`/`tp_locked`/`tp_skip` → бурштин `#fbbf24` (це РІШЕННЯ).
Один значок на всі чотири стани знову зробив би причину здогадкою.
⚠️ Бекенд дає ЧИСТІ поля, кольори — справа фронта (той самий принцип, що
`verdict.parts` у драбині); тест звіряє, що сторінка показує ВСІ чотири.
Тест: `test_pilot_tp_visible.py` (8).

## 🖥 Колонка «🎯 Автопілот» — РІВНІ СТОВПЧИКИ + редаговані поля рівнів

- **Вирівнювання.** Сегменти йшли підряд, тож ціна різної довжини
  ($2,411.95 проти $0.07561) зсувала все наступне — %, R і значки не
  вишиковувались по вертикалі. Тепер КОЖЕН сегмент — `inline-block`
  ФІКСОВАНОЇ ширини зі своїм вирівнюванням (`seg(w, align, html)`):
  значок 14 · смужка 40 · ціль 86 (ліворуч) · залишок 44 (праворуч) ·
  R 50 (праворуч) · ⚠/🛡 30. Плюс `font-family:monospace` на всю комірку.
  Ширини підібрані під найдовші реальні значення — саме вони тримають
  стовпчик рівним.
- **Порожнє поле Manual SL/TP мусить ЧИТАТИСЬ як поле.** Воно завжди було
  `<input>`, але з ледь помітною рамкою `#333`: поряд із заповненими (яскраво
  обведеними) виглядало як звичайний прочерк, і складалось враження, що рівень
  не вписати руками. Тепер порожній стан — **пунктирна рамка `#4b5563`** +
  підказка «клікніть і впишіть рівень (Enter або клік поза полем зберігає)».
  Механіка не змінювалась: `submitManualTp1` / `submitManualSlTp` →
  `POST /api/tm/positions/manual-sltp`, захист редагування `_tmEditingIn`
  не дає 5-с полу стерти те, що друкуєш.

## 💧 ДРАБИНА ЛІКВІДНОСТІ BTC — біля банера настрою (у ВІДСОТКАХ)

Користувач читає в каналах зріз виду «на $82k — 4 150 стопів, на $78k — 2 011»
і попросив, щоб бот збирав те саме. **Виявилось, що 80% уже є:**
`detection/liquidation_map/` — повний рушій оцінки рівнів ліквідації
(провайдери OI Binance/Bybit/Hyperliquid → приріст OI між тиками → плечі
25×/40% · 50×/35% · 100×/25% → реальна формула ціни ліквідації → кошики $25 на
BTC → сітка 0.25% з часовим згасанням і мітигацією). Бракувало лише подання.

**`detection/liquidation_map/ladder.py`** (ЧИСТІ функції) згортає `levels` із
`get_state()` у КРОКИ ПО $N і переводить у **ЧАСТКИ** від усієї ліквідності:
`{above/below: {pct,usd}, pull: up|down|flat, pull_pct, rows[]}`, рядок —
`{price, price_hi, side, usd, pct, dist_pct, dir}`.

⚠️ **ЧОМУ ЧАСТКИ, А НЕ «КІЛЬКІСТЬ СТОПІВ» (рішення, не міняти без потреби).**
Реальних стоп-наказів трейдерів не публікує ЖОДНА централізована біржа. «4 150
стопів» — чужа оцінка: долари ÷ припущений середній розмір позиції, два
припущення поспіль, які неможливо перевірити. Частку ж ми справді знаємо зі
своїх даних. Єдине чесне джерело кількості — **Hyperliquid** (on-chain, ~16%
OI по BTC), але це обхід позицій користувачів; провайдер є, поки бере агрегат.
Тест-замок `test_module_does_not_promise_stop_counts` не дає завести поле
`stops`/`count`/`orders` у рядку драбини.

Інші рішення: `pull` = 'flat', поки перевага ≤ **10 п.п.** (52/48 — не
напрямок); відстань міряється від СЕРЕДИНИ сходинки; рядки < `MIN_ROW_PCT`
(1%) відкидаються як шум; авто-крок ≈1.25% ціни, округлений до людського
($1000 на BTC, $50 на ETH), явний `step` має пріоритет.

API: `GET /api/liquidity-ladder?symbol&step&lookback&top` — читає готовий стан
liq-map, біржу НЕ смикає. UI: блок `#sm-liq` під банером настрою (та сама
типографіка: monospace, 1px-бордер, стовпчики), оновлюється тим самим тактом.

**У рядку драбини праворуч — СУМА, не відсоток** (рішення користувача): частку
вже показує довжина смужки, а гроші дають відчуття масштабу («$95M проти $12M»
читається краще за «29% проти 4%»). Відсоток лишився в підказці рядка.

**📍 ПОРЯДОК ПОКАЗУ = ЦІНА, з ЛІНІЄЮ ПОТОЧНОЇ ЦІНИ посередині.** Драбина
читається як стакан: згори те, що над ціною, знизу — під нею, а між ними
пунктирна лінія `sm-liq-price` із самою ціною. ⚠️ Два РІЗНІ порядки, не
плутати: **відбір** рядків (`top_n`) — за ЧАСТКОЮ (значущістю), **показ** —
за ЦІНОЮ спаданням. «Магніт» для вердикту береться ДО пересортування, інакше
в нього потрапив би просто найвищий за ціною рядок (тест-замок
`test_selection_by_share_display_by_price`). Рядок із прапорцем `at_price`
(одна з меж = сама ціна) стоїть впритул до лінії; у підказці — «впритул до
поточної ціни».

**🐞 СХОДИНКА, ЩО НАКРИВАЄ ЦІНУ, ДІЛИТЬСЯ ПО НІЙ (знайдено на проді).**
Кошик $78 000 = `[78000..79000]` при ціні **$78 563** діставав ОДИН напрямок
за своєю серединою (78 500 < ціни → «↓ нижче»), хоча 44% його ширини лежить
ВИЩЕ ціни. У ньому сиділо **16% усієї ліквідності** — і банер сам собі
суперечив: підсумок «⬆50.5% / ⬇49.5%» (рахується по РІВНЯХ) проти рядків, які
сумарно давали **32% / 68%**. Фікс: якщо `lo <= mp < hi`, сходинка ріжеться
САМЕ по `mp` на дві. Плюс ключ кошика тепер **діапазон, а не (діапазон, сторона)**
— інакше виходило ДВА рядки з однаковим підписом «$78 000», які неможливо
розрізнити. Тест-замки: `test_step_containing_price_is_split_at_it`,
`test_rows_never_contradict_the_summary`, `test_no_duplicate_price_labels`.

**🏷 ПІДПИС РЯДКА = СМУГА «$76 000–78 000», а не одна межа (не зламати!).**
Кейс BTC (05.09, 📡 Tickr): ціна $80 038, крок $2 000. Рядок підписувався
`price` (нижньою межею), а відсоток поруч рахувався від СЕРЕДИНИ — тобто
підпис і відстань вказували на РІЗНІ точки. Наслідки, видні прямо на екрані:
- `🧲 магніт $76 000 · ↓3.8%` — насправді $76 000 лежить за **5.05%**; 3.8% це
  $77 000 (середина смуги);
- `$80,038 · ↑1.23%` — сходинку, РОЗРІЗАНУ поточною ціною, підписувало САМЕ
  ЧИСЛО ЦІНИ, тож поруч із лінією ціни стояв рядок «ціна за 1.23% від ціни»,
  і зрозуміти, що це за рівень, було неможливо.
⚠️ Самі ЧИСЛА були правильні (перевірено: сума рядків = `total_usd`, частки =
підсумок ⬆34.6/⬇65.4, магніт 206.3/725.4 = 28.4%) — брехав саме ПІДПИС.
**Фікс:** `ladder.fmt_band_ua(lo, hi)` + JS-дзеркала `_lqBand` (tickr) і
`_liqFmtBand` (smart_money); долар лише на початку. Підпис описує ОБʼЄКТ, а
відсоток — відстань до його середини, і це вже не суперечить одне одному.
`summarise` віддає ще й `near_price_hi` (та сама проблема в колонці «найближчий»).
⚠️ Семантику `dist_pct` (від середини) НЕ міняли: вона документована і живить
магніт/`near_dist`; змінився саме підпис. Колонки `.lp` розширені (96→150 у
tickr, 78→132 у smart_money). Тест-замки: `test_step_split_by_price_is_not_
labelled_with_the_price_alone`, `test_magnet_label_and_distance_point_at_the_
same_object` (точка на заявленій відстані мусить лежати ВСЕРЕДИНІ підписаної
смуги), `test_both_pages_mirror_the_band_helper`.

**🎨 КОЛІР = НАПРЯМОК ТЯГИ, а не сторона позицій (не перевертати!).**
Спершу драбина фарбувалась за тим, ЧИЇ позиції ліквідують: шорти стоять над
ціною → червоний, лонги під ціною → зелений. Формально вірно, але банер читався
ПРОТИЛЕЖНО до всього іншого інтерфейсу (де зелений = LONG/вгору): «тягне вниз»
світилось зеленим. Тепер колір показує, КУДИ ТЯГНЕ ЦІНУ: вище ціни → **зелений**,
нижче → **червоний**. Це стосується смуги спліту, рядків драбини, підсумку
«⬆/⬇» і вердикту з магнітом — усе з одного правила.

**🧭 ВЕРДИКТ замість опису методики.** Замість постійного напису «оцінка з
відкритого інтересу…» (він переїхав у тултип заголовка) внизу блоку стоїть
`ladder.make_verdict(...)` — ВИСНОВОК ПО ЦИХ ДАНИХ, що змінюється РАЗОМ із
ними: напрямок від перекосу часток, сила словом (`_strength_word`: ≤10 п.п.
рівновага · ≤30 помірно · ≤60 виразно · далі сильно), плюс «найбільший магніт»
= найбільша сходинка з ціною, часткою і відстанню. ⚠️ При перекосі ≤10 п.п.
пишемо «Рівновага», а не витискаємо напрямок із 52/48. Немає даних → вердикт
усе одно є і чесно каже, що даних немає. Тест-замок
`test_verdict_flips_with_the_data`: дзеркальний набір мусить перевернути висновок.

**Вердикт віддається ШМАТКАМИ (`verdict.parts`)**, а не суцільним рядком:
`{icon, lead, lead_val, action, strength, skew, magnet{price,pct,dist,dir}}`.
Це дозволило UI (а) винести «найбільший магніт» у ДРУГИЙ рядок і (б) фарбувати
ЧИСЛА окремо від пояснювальних слів — раніше все зливалось в один зелений
абзац. ⚠️ Розмітку модуль НЕ робить: `parts` — чисті дані, кольори лишаються
справою фронта (тест це перевіряє). `text` зберігається як фолбек для логу/TG,
і тест `test_verdict_parts_mirror_the_text` не дає їм розійтися.

## 🧭 РЯДОК АНАЛІЗУ: ліворуч ліквідність, праворуч ОДИН «Потенціал»

Спроба вкласти банер рішення в ліву смугу рядка Trade Direction — **скасовано
користувачем** («дуже все заляпано»): банер повернуто на своє місце ОДИН-В-ОДИН
(усі id і порядок звірені з попередньою версією), компактний CSS прибрано.
⚠️ Не повторювати: банер рішення живе окремим блоком на повну ширину.

Натомість переставлено `.sm-analysis-row`:
- **ліворуч** — блок настрою Binance + 💧 драбина ліквідності BTC
  (`#sm-analysis-side`), перенесений із `sm-side-gates-right` (та колонка
  прибрана, у рядку Trade Direction лишились самі кнопки);
- **праворуч** — РІВНО ОДНА панель «Потенціал», та, що несе інформацію зараз.
Обидві половини `flex:1` → рівні за шириною.

**Хто «активний»** — `_movePickActive(moveLong, moveShort, verdict)`:
1) є вердикт LONG/SHORT і по ньому є дані → він;
2) WAIT/нейтраль → бік із БІЛЬШИМ ЗАПАСОМ: `_MOVE_RANK` FRESH 0 < MATURE 1 <
   EXHAUSTED 2, за однакового стану — менша `exhaustion`;
3) дані лише з одного боку → він.
Другу панель ХОВАЄМО (`display:none`), а не пригашуємо: раніше вона займала
половину рядка без користі. Кейс зі скріна (WAIT · LONG ДОЗРІЛИЙ 44.4 ·
SHORT СВІЖИЙ 28.7) → показується SHORT.

## 🚫 РЯДОК АНАЛІЗУ ВИМКНЕНО ДЛЯ ПОКАЗУ (`SM_ANALYSIS_ROW_ON = false`)

Вимога користувача: «щоб не навантажувати сторінку, вимкни повністю все, що
зображено на скріні… Розрахунок і алгоритм залиш, вимкни лише для сторінки
відображення». Ідеться про ВЕСЬ `.sm-analysis-row` — три банери разом:
🏦 настрій Binance · 💧 драбина ліквідності BTC · 📈/📉 Потенціал LONG/SHORT.

**Один вимикач** — `const SM_ANALYSIS_ROW_ON = false;` на початку скрипта
`templates/smart_money.html`. Керує ЧОТИРМА місцями:
- `loadSentiment()` — вихід ДО `loadSentimentFor` → **немає запиту**
  `/api/sentiment`;
- `loadLiquidityLadder()` — вихід ДО `fetch` → **немає запиту**
  `/api/liquidity-ladder`;
- `renderMovePanel()` — вихід ДО `renderMoveSide` → жодного DOM-малювання;
- обгортка `#sm-analysis-row` прихована інлайном, а при `true` init ставить їй
  `display:flex` назад.
**Повернути = поставити `true`.** Розмітку і всі id СВІДОМО лишено на місці.

⚠️ **Що НЕ вимкнено і чому.** `move_long`/`move_short` рахує бекенд у
`compute_bias`, і їх читає ТОРГОВА логіка — `fuel_filter._exhaustion`
(«Виснаженість» → ворота відкриття) і `confluence_direction`. Вирізати цей
розрахунок разом із банером означало б тихо змінити ТОРГІВЛЮ. Тому:
- настрій і драбина економлять **і запит, і серверний розрахунок**;
- «Потенціал» економить **лише малювання** — його числа приїжджають усередині
  `/api/smc/bias`, який сторінці потрібен для вердикту.

⚠️ Ендпоінти `/api/sentiment` і `/api/liquidity-ladder` НЕ видалено — вимкнено
саме виклик зі сторінки.
⚠️ Рядок `#sm-bias-updated` («BTCUSDT · оновлено … · авто кожні 60с») НЕ
чіпали: він стоїть ПОРУЧ, але належить банеру вердикту, який лишається.
Тест: `test_analysis_row_off.py` (8) — стереже ОБИДВІ половини вимоги.

## 🏦 Банер настрою — BINANCE із прозорим фолбеком

`/api/sentiment` дефолт **binance** (був bybit) + `?fallback=bybit`. Binance
періодично блокує IP дата-центрів (418/451), і банер тоді просто гас. Тепер за
потреби мовчки береться друга біржа, але **підпис малюється з поля `exchange`
у відповіді** (`#sm-sentiment-src`), а не з розмітки — тож банер ніколи не
бреше про походження цифр; фолбек підсвічується бурштиновим.

## 🔄 ПРОТИЛЕЖНИЙ СИГНАЛ — У ЧЕРГУ, а не в нікуди (кейс ARBUSDT)

**Що показав лог (31.08).** О 01:51 бот відкрив ARBUSDT SHORT (стоп із 1H OB —
**8.23%** від входу, ціль 1.60% → **0.19R**). О **15:02** прийшов LONG, який
пройшов **УСІ** фільтри (OB(1h)✓ · Прогноз[AND] 1H LONG 75% + 4H LONG 50%✓ ·
Decision LONG 82%✓). Але «Reverse on opposite signal» вимкнено, і TM написав
«тримаємо SHORT» — **сигнал зник назавжди**, його навіть не поклали в чергу.
Бот досидів у шорті до **−8.58%**, а рух пішов саме в бік того LONG.
За добу таке сталось **8 разів на 8 монетах**.

**Правило (`queue_opposite_signal`, дефолт УВІМК).** Протилежний сигнал при
відкритій угоді СТАЄ В ЧЕРГУ-4 з міткою **`opp_wait`** і чекає закриття
позиції. Автореверс лишається окремим тумблером — позицію ми не перевертаємо.

⚠️ **Три місця, без яких мітка не працює** (кожне закрито тестом):
1. `intercept(..., opp_wait=True)` знімає блок `_already_open` — інакше запис
   не поставився б саме тоді, коли він і потрібен;
2. **`note_trade_closed` НЕ прибирає `opp_wait`-записи.** Чистка викидає все,
   що старше за закриття, — а цей запис за визначенням старіший і його змело б
   рівно в момент, коли він став актуальним;
3. `_is_new_situation(..., opp_wait=True)` пропускає перевірку «сигнал
   старіший за закриття». ⚠️ Гейт «сигнал УЖЕ ВІДПРАЦЮВАВ» (`burned`)
   лишається в силі — виняток точковий.

📨 **Telegram:** `_notify_opposite_queued` — «🔄 ПРОТИЛЕЖНИЙ СИГНАЛ · #SYM ·
у позиції SHORT @ ціна (PnL) · новий сигнал LONG у Чергу-4, чекає закриття».
Раніше ця подія проходила непоміченою; тепер рішення (закрити руками зараз чи
дати боту відкрити після закриття) ухвалюється вчасно.

## 📐 ГЕЙТ ЗА R ПЕРЕД ВІДКРИТТЯМ (`queue4_min_rr`, дефолт 1.0)

Останній крок Черги-4 перед `_open`: рахуємо **очікуваний R** — те саме число,
що потім стоїть у колонці «🎯 Автопілот». Угода з R < порогу не відкривається.

**`_q4_expected_r(sym, side, s)` → `(r, detail)`** бере обидві складові з ТИХ
САМИХ джерел, що дадуть реальні рівні:
- **стоп** — `_q4_pick_sl` (винесено з `_q4_set_vob_sl` НАВМИСНО): той самий
  ланцюг джерел, буфер і стеля `autosl_max_pct`;
- **ціль** — `trade_pilot.pick_objective` над `TradeManager._pilot_context`.

⚠️ **Це НЕ повернення скасованого `tp_min_r`.** Той різав ВИСТАВЛЕННЯ TP уже
відкритій угоді — користувач це скасував, і воно лишається скасованим. Цей
рахує R **до входу** й вирішує, чи взагалі відкривати.
⚠️ Запис при відмові **НЕ виселяється**: R рухомий (ціна підійде до стопа —
ризик впаде; зʼявиться дальша ціль — виграш зросте), тож монета пробує на
наступному тіку. Анти-флуд `_q4_rr_logged` — як у 🔁 повторної перевірки.
⚠️ Немає стопа або цілі → `(None, причина)` і гейт **не блокує**: вигаданої
відмови не даємо.

На кейсі ARB: `0.19R · ціль Weak Low +1.60% / стоп 1H OB −8.22%` — відсіявся б.

**🐞 ЦІЛЬ ГЕЙТА ФІКСУЄТЬСЯ В УГОДІ (кейс VETUSDT 01.09 — не зламати!).**
Гейт пропустив шорт із вердиктом «**1.0R** · ціль головний пул ліквідності
+2.75% / стоп 1H OB −2.74%», а через **90 секунд** автопілот обрав ІНШУ ціль —
Strong Low +1.25% — і та сама угода стала **0.43R**. Тобто число, ЗА ЯКИМ
пропустили, не було числом, яке потім реально стоїть в угоді, і вимога «не
відкривати нижче 1.0R» обходилась сама собою.

Причина: ціль спирається на `runway`, а `_pilot_context` віддає його ЛИШЕ коли
напрямок МММ збігається з боком угоди. МММ фліпнув — пул зник із контексту,
найдальшою стала інша ціль. **Фікс:** `_q4_expected_r` запамʼятовує обрану ціль
у `_q4_rr_objective[sym]`; перед відкриттям вона йде в
`TradeManager.set_pending_objective`, лягає на позицію як `pilot_objective` і
стає ЗАФІКСОВАНОЮ ціллю автопілота (механізм `objective_lock` уже був).
Тепер R рішення = R у колонці «🎯 Автопілот».
⚠️ Ціль кладеться в ОБИДВА шляхи відкриття (real + paper) — тест це перевіряє.

⚠️ **Спостереження, яке НЕ виправлено (рішення користувача).** R механічно
РОСТЕ, коли ціна підходить до стопа: по VET стоп ішов 3.10% → 3.22% → 3.14% →
**2.74%**, а ціль 2.32% → 2.75%, тож поріг 1.0R «дозрів» не від кращого
сетапу, а від того, що ціна пішла ПРОТИ ідеї шорта на 0.4%. Тобто очікування в
черзі саме по собі рано чи пізно виробляє пропуск. Варіанти, якщо це заважає:
міряти R від ціни СИГНАЛУ (`added_price`), а не від поточної; або додати
окрему вимогу мінімальної відстані до цілі.
Зі стелею SL 2.5% той самий сетап дав би 0.64R (усе одно нижче 1.0).
UI: `ff-queue4-min-rr` у гармошці «⚙️ Налаштування (Черга-4)».
Тести: `test_opposite_and_rr.py` (11).

## 💧 СКАН ЛІКВІДНОСТІ ПО БІРЖІ (сторінка 📡 Tickr) — `detection/liq_scan.py`

Список монет, відсортований за ПЕРЕКОСОМ ліквідності: куди тягне маса рівнів
ліквідації, де найбільший магніт і де найближчий. Разовий запуск за кнопкою,
з вибором біржі.

⚠️ **ЦЕ НЕ ЖИВА liq-map — не плутати (принципово).** `liquidation_map` будує
рівні з ПРИРОСТУ OI між своїми тіками: точніше, але лише для монет, які демон
веде (BTC/ETH + замовлені), і потребує годин накопичення. Для разового скану
сотні монет таких даних НЕМАЄ і бути не може.
Тут — **МОМЕНТАЛЬНИЙ ЗРІЗ**: поточний сукупний OI (один bulk-запит на всю
біржу) розкладається по історії цін за обсягом («позиції відкривались там, де
торгувався обсяг»), далі — ТА САМА формула `liquidation_price` і ТА САМА
драбина `build_ladder`/`make_verdict`. Тест-замок
`test_summary_uses_the_same_ladder_and_verdict` не дає вердиктам розійтися.

**Що чесно:** напрямок перекосу і розташування магнітів надійні (їх задає
геометрія рівнів); абсолютні суми грубіші за живу карту; лонг/шорт беремо
50/50, бо співвідношення по КОЖНІЙ монеті пачкою не віддає жодна біржа.

**Ключові рішення:**
- **Мітигація обовʼязкова:** якщо після свого бару ціна вже проходила крізь
  рівень — позиції там немає, рівень викидаємо. Без цього список показував би
  давно знесені кластери.
- **Два РІЗНІ магніти в рядку:** `magnet_*` — найбільший за масою (куди тягне),
  `near_*` — найближчий за відстанню (що спрацює першим). Плутати не можна.
- **Біржі без bulk-OI відмовляють ЧЕСНО.** `NO_BULK_OI` = Binance, BingX: вони
  не віддають відкритий інтерес пачкою (лише по одній монеті — сотні запитів).
  Кажемо це прямо, а не показуємо нулі чи порожній список.
- Відсів перед скануванням: обіг ≥$20M і OI ≥$5M, інакше «перекіс 90%» буде
  побудований на трьох доларах. Кількість монет — стеля `MAX_SYMBOLS`=120.

**Вартість:** 1 bulk + 1 запит свічок на монету (1h × 168). При 40 монетах —
~41 HTTP і 15-30 с, у 8 потоків. На постійне навантаження не впливає.
API: `POST /api/tickr/liquidity-scan` {exchange, top_n, min_vol_usd,
min_oi_usd, bars, sort_by}. Сортування: `pull` / `magnet` / `near`.
Тести: `test_liq_scan.py` (12).

⚠️ **Урок із тесту:** один бар дає рівні в ОБИДВА боки (лонги ліквідуються
нижче входу, шорти — вище), тож «важчий обсяг знизу → маса знизу» НЕ працює.
Асиметрію створює геометрія: з входів далеко ПІД ціною у вікно потрапляють
лише шорт-ліквідації, з входів далеко НАД ціною — лише лонгові.

## 💧 BULK-OI: обмеження було в МАСШТАБІ, а не в біржі + режим ОДНІЄЇ монети

**Питання користувача:** «Чи можна вийти з положення "Binance не віддає
відкритий інтерес пачкою"?» — і власна відповідь: «можна по одній монеті
робити запити… і можливість вводити назву монети (за замовчуванням BTC)».
Слушно: **«пачкою» заважає лише тоді, коли монет СОТНІ.** Поштучний
`/fapi/v1/openInterest` Binance віддає без проблем — я помилково зробив із
відсутності bulk-ендпоінта повну відмову.

- **`_OI_ONE[exchange](session, symbol) → (ціна, OI у $)`** для всіх чотирьох
  бірж (Binance `openInterest`+`ticker/price`, BingX `quote/openInterest`+
  `quote/ticker`, Bybit/MEXC — свій тікер по символу). `_KLINES` теж доповнено
  Binance і BingX, тож підтримані всі чотири.
- **Скан СПИСКУ** більше не відмовляє: `BULK_OI={'bybit','mexc'}`, решта
  добирає OI поштучно. Порядок кроків інший і це принципово — без bulk-OI
  кандидати відбираються за **ОБІГОМ** (він у тікерах є завжди), OI питається
  лише для них, і аж ПІСЛЯ цього застосовується поріг `min_oi_usd`. Стеля
  `PER_SYMBOL_OI_CAP=60` (запитів удвічі більше: OI + свічки). Відповідь несе
  `bulk_oi` і `warnings` — UI пише, що скан довший, а не мовчить.
- **`scan_one(exchange, symbol, bars, rows)`** — аналіз ОДНІЄЇ монети: 2-3
  запити, повна драбина + вердикт, працює на ВСІХ біржах. `norm_symbol` зводить
  `btc`/`BTC-USDT`/`btc_usdt` до `BTCUSDT`, `_ex_symbol` — у формат біржі
  (`BTC_USDT` MEXC, `BTC-USDT` BingX). Порожнє поле → BTC.
  API: `POST /api/tickr/liquidity-one` {exchange, symbol, bars, rows}.
- **UI (`templates/tickr.html`):** під скан-таблицею — поле «МОНЕТА» (дефолт
  BTC, Enter запускає) + вибір глибини історії; результат малюється **тим
  самим виглядом**, що блок 💧 на Smart Money: смуга спліту, драбина за ціною
  з **лінією поточної ціни** посередині, вердикт шматками (числа окремо від
  слів, магніт другим рядком). **Біржа за замовчуванням — Binance** (рішення
  користувача), і для списку, і для однієї монети.

**⏳ ГЛИБИНА ІСТОРІЇ: 10 варіантів, стеля 1000 год (не піднімати наосліп!).**
Було лише 72/168/336. Тепер `24 · 48 · 72 · 120 · 168(деф) · 240 · 336 · 504 ·
720 · 1000`. ⚠️ **1000 — не кругле число «щоб було».** Свічки тягнуться ОДНИМ
запитом БЕЗ пагінації, а найтісніший ліміт серед чотирьох бірж — **Bybit
(1000 барів/запит)**; Binance дає 1500, BingX 1440. Дати більше означало б, що
на Bybit глибина МОВЧКИ обріжеться і те саме налаштування давало б різні дані
на різних біржах. `scan_one`/`scan_liquidity` тримають `max(24, min(bars,
1000))` — обидві межі списку збігаються з clamp, тож жодна опція не
перетворюється мовчки на інше число. **Хочеш глибше — спершу пагінація в
`_KLINES`, і тільки потім нові опції.**
Тест-замок `test_history_dropdown_offers_only_depths_the_backend_honours`
перевіряє КОЖНУ опцію проти clamp.

**📋 СКАН СПИСКУ: своя «ІСТОРІЯ, ГОД» + до 200 монет.**
Раніше глибина у скані списку була ЗАШИТА (`bars` узагалі не передавався → завжди
`DEFAULT_BARS`=168), а «МОНЕТ» доходило лише до 100.
- `liq-bars` — **ТОЙ САМИЙ список із 10 варіантів**, що в режимі однієї монети
  (це той самий `bars` того самого `build_levels`; два різні списки розійшлись
  би). Тест-замок звіряє обидві випадайки МІЖ СОБОЮ.
- `MAX_SYMBOLS` **120 → 200**, і **`PER_SYMBOL_OI_CAP = MAX_SYMBOLS`**: стеля 60
  для Binance/BingX МОВЧКИ різала вибір удвічі раніше за решту — а Binance тут
  біржа ЗА ЗАМОВЧУВАННЯМ, тобто «200» перетворювалось на 60 непомітно.
- ⚠️ **Будь-яке обрізання тепер НАЗВАНЕ вголос:** `обрано N монет, але стеля
  скану — M` іде в `warnings` → у статус-рядок. Мовчазний clamp — це та сама
  вада, що з глибиною історії. Тест: `test_clamped_coin_count_is_said_out_loud`.
- У підсумку статусу додано `· глибина N год` — інакше числа немає з чим звірити.

**💸 `_liqCostHint()` — ЖИВА ціна запуску замість статичного напису.**
Відколи можна обрати 200×1000, «один запит на монету» перестало бути дрібницею.
Підказка рахує `1 + N×(2 для Binance/BingX, інакше 1)` запитів, час у 8 потоків
і **вагу за лімітом Binance** (свічки: >500 барів ≈ 10, ≥100 ≈ 2; +1 за OI).
При вазі > 1500 з 2400/хв пише прямо: «зменш глибину або кількість монет».
⚠️ Це не косметика: **200 монет × 1000 год на Binance ≈ 2200 ваги з 2400** —
реальний ризик 418/429. Оновлюється на зміну біржі/монет/глибини і на старті
сторінки; під час скану мовчить (`window._liqScanning`).

**🎨 РОЗГОРНУТИЙ СПИСОК `<select>` — суцільні кольори + `color-scheme: dark`.**
Скарга «нічого не видно»: стилізували ми лише КНОПКУ select, а САМ список
малює браузер — і напівпрозоре `rgba(255,255,255,0.06)` лягало на **БІЛЕ**
системне меню, тож світло-сірий текст ставав нечитабельним (видно було тільки
підсвічений рядок). Фікс у `.tk-field select`: `color-scheme: dark` (перемикає
НАТИВНЕ меню в темну тему) + СУЦІЛЬНІ `option{background:#141821;color:#dde3ee}`
та `option:checked/:hover{#1e2a3d/#93c5fd}` як фолбек.
⚠️ Напівпрозорість тут НЕ годиться: під випадним меню немає нашого тла.
⚠️ Правило одне на ВСІ 8 випадайок сторінки — тому кожен `<select>` мусить
лежати в `.tk-field` (тест-замок `test_dropdown_list_is_readable_on_dark_page`
це перевіряє; інакше новий select лишиться зі світлим меню, а помітять це
лише очима).

**🐞 Два дефекти, які виліз­ли одразу, щойно драбина пішла не по BTC:**
1. `step_for` мав СПИСОК готових кроків, що починався з **$1** → на монеті за
   $0.47 уся драбина сходилась в одну-дві сходинки. Тепер крок рахується як
   ~1.25% ціни, округлені до **1/2/5 × 10^k** — масштабується вниз так само
   вільно, як угору (BTC $1000 і ETH $50 лишились ті самі).
2. `_fmt_price_ua` робив `int(round(v))` → магніт дешевої монети підписувався
   **«$0»**. Нижче $10 показуємо знаки після коми (3/5/7 за масштабом).
Тест-замок: `test_cheap_coin_magnet_is_not_rounded_to_zero`.

⚠️ **Пастка тестування:** `scan_liquidity` робить `from detection import
tickr_core` ДО всіх перевірок, тож перший же виклик прив'язує РЕАЛЬНИЙ модуль
атрибутом пакета — підміни лише `sys.modules['detection.tickr_core']` замало,
треба ще `sys.modules['detection'].tickr_core = fake`. Інакше тест мовчки йде
в мережу.
Тести: `test_liq_scan.py` (37).

## 🎯 СКАН ЛІКВІДНОСТІ ПО WATCHLIST + ФОЛБЕК ПО ОКРЕМІЙ МОНЕТІ (10.09)

**Вимога користувача:** «Додай до цього скану можливість сканувати WATCHLIST,
при цьому, якщо вибрано WATCHLIST, не потрібен вибір кількості МОНЕТ і
ОБІГ 24H ≥, OI ≥… Якщо на момент скану монету не знайдено на біржі, потрібно
автоматично вибрати іншу біржу для перевірки саме цієї монети: вибрано
Binance — підміною буде Bybit і навпаки.»

**`scan_liquidity(..., universe='top'|'watchlist', symbols=None)`.**
- `'top'` — як було: тікери самої біржі + відсів за обігом/OI, кількість `top_n`.
- `'watchlist'` → `_scan_watchlist(...)`: **ЖОДНОГО ВІДСІВУ**. Ні `top_n`, ні
  обіг, ні OI — список склала ЛЮДИНА, і мовчки викинути з нього монету означало
  б показати не те, про що просили. Єдина межа — `MAX_SYMBOLS` (200), і про її
  спрацювання пишеться ВГОЛОС у `warnings`.
- ⚠️ **Тікери біржі в цій гілці НЕ качаються взагалі** (список уже відомий) —
  тест-замок `test_watchlist_mode_never_touches_exchange_tickers` розбирає AST
  і не дає `tickr_core` чи порогам просочитись назад.
- ⚠️ **Watchlist читає МАРШРУТ, а не модуль.** `liq_scan` лишається без
  залежності від сканера (його беруть ізольовані тести), а список бере
  `flask_app` тим самим `scanner.get_watchlist()`, що малює сторінку.

**🔁 ФОЛБЕК — `FALLBACK_EXCHANGE` + `_resolve_symbol(session, ex, sym)`.**
`{'binance':'bybit','bybit':'binance','mexc':'bybit','bingx':'bybit'}` — Bybit
партнер за замовчуванням із тієї самої причини, що в `_liq_snapshot`: це біржа,
на якій бот ТОРГУЄ, отже монета watchlist там є за визначенням (кейс MNTUSDT —
монети просто немає на Binance Futures).
- ⚠️ **ЦЕ ТОЧКОВА ЗАМІНА НА ОДНУ МОНЕТУ**, а не зміна біржі скану: решта списку
  лишається на обраній.
- ⚠️ **СВІЧКИ БЕРУТЬСЯ З ТІЄЇ САМОЇ БІРЖІ, ЩО Й OI** — інакше рівні ліквідації
  рахувались би по ДВОХ РІЗНИХ РИНКАХ (та сама помилка, що з PD-зоною).
  Тест-замок перевіряє саме це: `('bybit','MNTUSDT') in calls['klines']`.
- ⚠️ **Фолбек НЕ мовчазний:** рядок несе `exchange` (хто РЕАЛЬНО дав числа),
  `requested_exchange`, `fallback`, `note`; у таблиці — бурштиновий бейдж
  `⇄BYBIT` біля символу, у підсумку — «через партнерську біржу N».
- ⚠️ **Немає на ЖОДНІЙ → причина називає ОБИДВІ спроби**, а не «немає даних».
- ⚠️ У watchlist OI **завжди поштучний** на будь-якій біржі: інакше не було б
  куди застосувати фолбек по окремій монеті (`bulk_oi: False` у відповіді).

**UI (`templates/tickr.html`):** випадайка `liq-universe` («Топ біржі» /
«🎯 Watchlist бота»); `_liqUniverseSync()` **ХОВАЄ** `liq-f-topn` /
`liq-f-minvol` / `liq-f-minoi` — активний контрол, який ні на що не впливає,
вводить в оману (той самий принцип, що з полями TTL при ♾ «Без терміну»).
`_liqCostHint` бере кількість монет із живого `/api/smc/watchlist`, а не
вигадує число. Тест: `test_liq_scan.py` (+7).

### ⚖ РІВНОВАГА НЕ ЙДЕ У ЗВІТ СПИСКУ (вимога 10.09)

Дослівно: «у звіті відсікай записи у яких "рівновага", їх не потрібно
показувати». Скан і сортується за ПЕРЕКОСОМ, тож монета без перекосу
(`pull == 'flat'`, перевага ≤ `ladder.FLAT_PP` = 10 п.п.) не несе тут
інформації. `drop_flat(rows)` → `(рядки, скільки прибрано)`, застосовується
в ОБОХ гілках (`top` і `watchlist`).
- ⚠️ **Рядки БЕЗ ДАНИХ (`ok is False`) НЕ чіпаємо:** «немає даних» і
  «рівновага» — РІЗНІ речі; мовчки викинути перше означало б приховати, що
  монету не вдалось порахувати.
- ⚠️ **`scan_one` (одна монета) НЕ фільтрує** — там ви питали САМЕ про цю
  монету, і «рівновага» коректна відповідь. Тест-замок розбирає AST.
- ⚠️ **Кількість прибраного НАЗВАНА** (`dropped_flat` → статус
  `· без перекосу (⚖ рівновага) N`): мовчазне зникнення рядків читалось би як
  збій скану — той самий принцип, що з clamp-ом монет і глибини історії.
- ⚠️ **Пастка фікстур:** рівний ряд свічок навколо ціни дає САМЕ `flat`, тож
  два тести після цієї зміни почали бачити порожню таблицю «на рівному
  місці». Фікстура `_fake_net` тепер будує бари з реальним перекосом
  (`_bars((88.0, 60, 3000))` при ціні 100 → `up`), а тест OI-порогу перевіряє
  `scanned`, а не вміст таблиці.

## 🖐 ✋ СИГНАЛ: ручний вибір напрямку + підпис, що не ламає кнопки (10.09)

**Дві вимоги користувача по одному рядку кнопок:**
1. «Текст розмісти так, щоб кнопки не втрачали свою форму. Його не потрібно
   багато, не більше ніж на дві стрічки.»
2. «Зроби сигнал також щоб був ручний вибір LONG чи SHORT.»

**1. Форма кнопок.** `.sm-decision-actions` — flex-рядок, і довгий підпис
зʼїдав ширину сусідів: «⬆ LONG» ламалось на два рядки. Фікс — три речі разом:
`.sm-decision-btn{flex:0 0 auto; white-space:nowrap}` (кнопка не стискається і
не переносить підпис) · `.sm-decision-actions{flex-wrap:wrap}` (підпис
переноситься ЦІЛИМ блоком) · `.sm-decision-actions-hint{flex:1 1 240px;
-webkit-line-clamp:2; overflow:hidden}` — **обмеження у два рядки тримає CSS**,
а не надія на коротку фразу. Сам текст скорочено до
`⚠️ ⬆LONG/⬇SHORT вимкнено — Trade Manager OFF. ✋ Сигнал працює (йде в чергу).`
(≤90 символів, тест-замок на довжину), а повне пояснення переїхало в `title`
(`hintFull`) — нічого не втрачено.

**2. Напрямок ✋ Сигналу.** Раніше він ЗАВЖДИ брався з вердикту банера. Тепер
поруч стоїть селектор `sm-signal-dir`: **«за вердиктом»** (дефолт = стара
поведінка) · **⬆ LONG** · **⬇ SHORT** — власний вибір, навіть ПРОТИ вердикту.
- **`_smSignalSide()` — ЄДИНЕ місце вибору.** `sendManualSignal` не читає
  `_smDecisionDir` сам: інакше кнопка показувала б один напрямок, а на сервер
  летів би інший (тест-замок).
- **Кнопка ПІДПИСАНА напрямком** (`✋ Сигнал LONG`, зелений/червоний) —
  `_smSignalDirSync()` кличеться на зміну селектора, на КОЖНОМУ рендері банера
  (у режимі «за вердиктом» напрямок пливе разом із ним) і на init. Без підпису
  натискання було б наосліп.
- ⚠️ `btn.dataset.busy` — щоб рендер банера не перебив «⏳ …» під час запиту.
- Вибір памʼятається в `localStorage['smSignalDir']` (UI-налаштування).
⚠️ Ворота напрямку (головні кнопки Trade Direction) ✋ сигнал НЕ обходить —
це лишається як було.
Тест: `test_manual_signal_hint.py` (12).

## ✋ РУЧНИЙ СИГНАЛ ≠ «Trade Manager disabled» (кейс WIFUSDT 10.09)

**Питання користувача:** «Чому не відправляється в ручному режимі сигнал? До
чого тут "⚠️ Trade Manager disabled"? Якщо сигнал мав би як мінімум потрапити
у Чергу.»

**Мав — і потрапляє.** ✋ Сигнал кличе `on_signal(opened_by='manual')` →
`intercept` → черга; майстер-тумблер TM гасить **лише ПРЯМЕ відкриття**
(⬆LONG / ⬇SHORT). Тест-замок `test_endpoint_does_not_gate_on_the_tm_master_switch`
не дає завести в цей маршрут перевірку `is_enabled()`.

**Зламаний був ПОКАЗ, і зразу в трьох місцях:**
1. **Підпис стосувався не тих кнопок.** Один спільний
   `sm-decision-actions-hint` стояв ВПРИТУЛ до ✋ Сигналу, а текст належав лише
   двом кнопкам прямого відкриття. Тепер він прямо каже, ЯКІ кнопки вимкнено і
   що ✋ Сигнал працює далі.
2. **Відповідь СТИРАЛАСЬ.** `sendManualSignal` писала результат у ТОЙ САМИЙ
   елемент, а `updateManualEntryButtons` перезаписує його на КОЖНОМУ
   перемальунку банера (~10с) → підтвердження зникало і кнопка виглядала
   мертвою. Фікс: **власний** `sm-decision-signal-hint` (замок:
   `updateManualEntryButtons` не має його навіть згадувати).
3. **Відповідь нічого не казала.** Результат `on_signal` **ІГНОРУВАВСЯ**, тож
   «відправлено в обробку» друкувалось і на `queued`, і на `duplicate`, і на
   `rejected`. Тепер маршрут віддає `status` + людський `outcome`
   («у ЧЕРГУ» / «ВІДКРИТО угоду» / «дубль» / «ВІДХИЛЕНО · причина»).
⚠️ Плюс `if (!r.ok)` — без окремої перевірки HTTP-статусу 404/500 виглядав би
як «збережено» (той самий урок, що з `submitManualTp1`).
Тест: `test_manual_signal_hint.py` (6).

## 🌊 ПОТОП СИГНАЛІВ У ЛОЗІ (кейс 10.09) — ДВА дефекти, не зламати!

**Скарга:** «Що сталось із ботом? Йому знесло дах… Що за жахіття в логу?»
Вимога: «потрібно лише при появі нового OB 1H CHoCH відправити сигнал і
проконтролювати Deduplicate Signals (1 per trend) і все».

**Експорт: 500 рядків за 5.6 хв, УСЬОГО 4 монети.**

    ASTERUSDT LONG 44 сигнали · FILUSDT 42 · BCHUSDT 39 · ADAUSDT 35
    ті самі монети SHORT — рівно по 1
    OB-фільтр зарізав 165 · дедуп зарізав 161 · у чергу пройшло 4

**АСИМЕТРІЯ 44:1 — ЦЕ Й Є ДІАГНОЗ.** Напрямок, що ПРОЙШОВ фільтри, дав ОДИН
сигнал і далі чесно різався дедупом. Напрямок, що фільтр ВІДХИЛИВ, фаєрився
знову й знову — отже позначку дедупу отримував ЛИШЕ успішний шлях.

### ДЕФЕКТ 1 — позначка дедупу стояла ПІСЛЯ раннього `return`

У `_send_alert` рядок `self._last_signal_dir[symbol] = side_label` лежав ~70
рядків НИЖЧЕ за `return` у гілці «фільтр не пропустив». Відхилений напрямок
НІКОЛИ не ставав «опрацьованим» → повторювався щопрохід, вічно.
**Фікс:** позначка + `_persist_dedup_state()` тепер стоять ДО того `return`.
- ⚠️ **«Дедуплікація 1/тренд» — про ТРЕНД, а не про «1 угоду»:** структурний
  сигнал СТАВСЯ, фільтр лише вирішив ним не торгувати.
- ⚠️ **Скид лишився там, де й був — на ФЛІПІ 1H-OB** (`_update_smc_ob`), тобто
  «новий 1H-OB → новий сигнал», рівно як просив користувач. Тест-замок стежить,
  щоб скид не переїхав.
- ⚠️ **НАСЛІДОК, який треба знати:** напрямок, зарізаний ШВИДКИМ фільтром
  (`Require Decision match` — вердикт рухливий), тепер «горить» до фліпу 1H-OB.
  Це саме те, що просив користувач («1 per trend»), але якщо потрібен ретрай на
  зміну вердикту — вимикати треба сам Decision-фільтр, а не дедуп.
- Тест-замки — AST: позначка МУСИТЬ бути до `return`, і поруч має бути персист.

### ДЕФЕКТ 2 — ОДИН прохід фаєрив ПАЧКУ подій

BCHUSDT: **40 «сигналів» за 0.8с з інтервалом 0.02с** — це ОДИН виклик
`_process_alerts` із десятками подій у `new_events`, а НЕ 40 циклів скану
(прохід по 230 монетах стільки не встиг би). При `Свіжість сигналу: без ліміту`
(`recency_minutes = 0`) вікно віку не відсіювало нічого, тож КОЖНА ланка старого
ланцюга CHoCH давала власний алерт.
**Фікс:** `_alertable_ids(new_events)` — ЧИСТА функція: множина `eid`
**найновіших** подій КОЖНОГО напрямку → максимум ДВА алерти за прохід.
- ⚠️ **Новизна за `to_t`, а НЕ за `from_t`:** `from_t` — це СТАРИЙ півот, що
  ламається; за ним «найновішою» стала б не та подія, що на графіку.
- ⚠️ **Обмежуємо САМЕ АЛЕРТ, а не цикл.** `_pending_choch`, `tm.on_bos_event` і
  `ff.queue2_on_choch` мусять бачити КОЖНУ подію — інакше зламалась би логіка
  CHoCH+BOS і виходи TM. Тест-замок перевіряє, що гейт не стоїть на цих рядках.
- ⚠️ Гейт застосовано в ОБОХ гілках алерту (`choch` і `choch_or_bos`) — інакше
  другий режим лишився б із потопом.
- ⚠️ Порожній/сміттєвий вхід → ПОРОЖНЯ множина (нічого не фаєримо, а не «все»).

**Порада щодо налаштувань (не код):** `Signal recency: Off (no limit)` знімає
природне вікно віку — саме воно перетворило історичний ланцюг на потік.
Розумне значення (15-60 хв) лишає в роботі лише справді свіжий CHoCH.
Тест: `test_signal_flood.py` (10).

## ❓ «ЩО ЦЕ ЗА OB? ЇХ НЕМАЄ НА ГРАФІКУ» (кейс 10.09) — не зламати!

**Питання користувача:** «Подія "Сигнал" це має бути одне і те, що і 🆕 Новий
OB? Тобто це одні і ті ж OB просто по різному визначаються. То чому вони не
співпадають?» + «OB-фільтр заблокував (Order Block проти напрямку), що це за
напрямок? Що це за OB? Їх немає на графіку.»

**ВІДПОВІДЬ ПО СУТІ: це РІЗНІ події на РІЗНИХ TF, збігатися не мусять.**
- **🆕 Новий OB** (`event='ob_new'`) — **1H Order Block** (`ob_filter_timeframe`),
  детекція на ЗАКРИТИХ 1h-барах;
- **Сигнал** (`event='signal'`, «Свіжий сигнал choch») — **CHoCH на 15m**
  (`timeframe` + `internal_size` сканера).
Один — це ЗОНА на старшому TF (ворота входу), другий — ПОДІЯ СТРУКТУРИ на
молодшому. Дані в лозі це підтверджують: AAVE `ob_new SHORT OB 1H` (11:00) і
через 6 год LONG-сигнал, зарізаний `BEARISH` — узгоджено, а не суперечливо.

**АЛЕ ЛОГ ПІДШТОВХУВАВ ДО ХИБНОГО ВИСНОВКУ — три дефекти ПОКАЗУ:**

### 1. Причина відмови не називала НІ бік, НІ блок
`OB-фільтр заблокував (Order Block проти напрямку)` — жодного орієнтира: ні
боку сигналу, ні боку блоку, ні свічки, ні цін. Поруч подія «🆕 Новий OB» про
**ТОЙ САМИЙ рядок БД** несла і свічку, і ціну — два повідомлення про одне й те
саме описували його ПО-РІЗНОМУ.
**Стало:** `OB-фільтр заблокував: 1H-блок BEARISH ПРОТИ сигналу LONG (потрібен
BULLISH) · свічка 10.09.26 о 05:00 UTC · $122.50–$123.40`.

### 2. `_ob_state_label` давав лише `BEARISH/BOS`
**Стало:** `_ob_state_info(symbol)` → `{label, bias, tag, bar_time, low, high,
where}`; `label` несе ще й «де шукати». `_ob_state_label` — тонка обгортка.
- ⚠️ **Джерело ОДНЕ** — той самий рядок `sob_smc_ob_state`, який читає
  `_ob_filter_allows`. Нічого не перераховуємо: інакше повернувся б баг
  «мітка каже 1H, а числа з іншого TF».
- ⚠️ **Форматування ОДНЕ** — `ob_alert.fmt_utc` / `fmt_price`, ті самі функції,
  що друкують «🆕 Новий OB». Свій формат = два написання одного числа.
- ⚠️ Тег пишемо **«CHoCH»**, не «CHOCH» (тест-замок).
- ⚠️ Немає рядка / немає блоку → чесний текст, а не вигаданий орієнтир.
- ⚠️ ТРИ причини лишаються РОЗРІЗНЕНИМИ: «нема блоку» / «ПРОТИ напрямку» /
  «створено BOS» (тест-замок на попарну відмінність).

### 3. ЕКСПОРТ СКЛЕЮВАВ `ob_new` із ЧУЖИМ ланцюгом
`/api/activity-log/export?linked` різав сесію **ЛИШЕ на `signal`**, без жодної
межі за часом. Наслідок в експорті: ORDIUSDT `blocked` об 11:01 і `ob_new` о
17:00 — **через 6 годин** — в ОДНІЙ сесії, а `outcome` усього ланцюга став
`ob_new`. Саме це й створювало враження «Сигнал і Новий OB — те саме».
**Стало:** `_SOLO = {'ob_new'}` (та сама подія, що вже solo в таблиці логу) +
`_MAX_GAP = 3 год`.
- ⚠️ **Після solo-події наступна теж починає СВІЙ ланцюг** (`cur = None`) —
  інакше вона приклеїлась би вже до рядка «Новий OB» (той самий найтонший
  момент, що в UI-таблиці).
- ⚠️ Тест-замок `test_ui_and_export_agree_on_what_is_solo`: два подання ОДНОГО
  логу не мають суперечити одне одному.

⚠️ **КОНТРАКТ РОЗШИРЕНО — два наявні тести довелось оновити** (не регресія):
`test_ob_choch_only.py` звіряв мітку на ТОЧНУ рівність `'BEARISH/BOS'` і шукав
рядок «проти напрямку»; тепер — префікс і СУТЬ (`ПРОТИ` + обидва боки).
Стаби `SimpleNamespace` мусять мати і `_ob_state_info` (його кличе
`_signal_allowed`), інакше падають з `AttributeError` «на рівному місці».
Тест: `test_ob_reason_clarity.py` (11).

## 🐞 URL ФРОНТА ≠ МАРШРУТ FLASK — мовчазна втрата рівня (не повторити!)

**Симптом:** у Manual TP-2 значення зберігалось, а введене руками в
**Manual TP-1 зникало через кілька секунд**.

**Корінь — моя друкарська помилка.** `submitManualTp1` слав POST на
`/api/tm/positions/manual-sltp`, а маршрут зареєстровано як
**`manual-sl-tp`** (той, що використовує робочий `submitManualSlTp`).
404 → `x.json()` кидав виняток → його ковтав `catch` із `console.error` →
жодного повідомлення користувачу, а на наступному 5-с поллі поле
перемальовувалось зі старого (порожнього) стану. Бекенд був НІ ПРИ ЧОМУ:
перевірено прямим викликом `update_manual_sl_tp(manual_tp1=…)` — рівень
пишеться, персистить і доїжджає в `get_state` (там `{**pos}`, без білого списку).

**Два висновки, зашиті в код:**
1. `submitManualTp1` перевіряє **HTTP-статус ОКРЕМО** (`if (!res.ok)`) і кричить
   у alert. Без цього будь-який 404/500 виглядає як «збережено».
2. **Тест-замок `test_every_frontend_position_route_exists`**: усі
   `/api/tm/positions/*`, які кличе `smart_money.html`, звіряються з
   `@app.route(...)` у `flask_app.py`. Вигадати URL більше не можна.

## ⚖️ TP-1 → БЕЗЗБИТОК (тумблер `tp1_move_to_be`, дефолт ВИМКНЕНО)

Коли спрацював TP-1 і тумблер УВІМКНЕНО, Manual SL переїжджає на вхід + запас
на комісії. **Дефолт OFF** — рішення про ризик за користувачем: беззбитковий
стоп після часткової фіксації нерідко вибиває позицію на звичайному відкаті до
входу. Вимкнено → стоп лишається де стояв, у лог нічого не пишеться.

- `trade_manager.breakeven_with_fees(side, entry, buf_pct)` — ЧИСТА функція.
  Стоп РІВНО на вході беззбитком НЕ є: раунд-трип комісії (Bybit перпи ≈0.06%
  відкриття + 0.06% закриття) зробили б із нього мінус. Буфер —
  `be_commission_buffer_pct` (деф. **0.12%**), той самий ключ, що вже живить
  BE-Move (другий однаковий за змістом параметр не заводимо).
- `_tp1_move_to_breakeven(...)` кличеться з `_check_manual_tp1` ОДРАЗУ після
  успішного часткового закриття (обидві книги: real + paper).
- ⚠️ **НІКОЛИ не послаблює стоп** (`trade_pilot.better_stop`): чинний рівень,
  кращий за беззбиток, лишається. Стосується і рівня, введеного ОПЕРАТОРОМ.
- ⚠️ Результат `update_manual_sl_tp` ЧИТАЄТЬСЯ: ціна могла повернутись до
  входу → TM відхилить → у лог «НЕ прийнято», зелена позначка НЕ ставиться.
- **🎨 Третій колір поля Manual SL:** `sl_breakeven` → `_SLTP_SRC_STYLE.be` =
  **зелений `#4ade80`**, перекриває блакитний «бот» (стан УГОДИ важливіший за
  походження рівня). Ручна зміна/зняття SL позначку скидає; автотрейл зберігає.
  Легенда в `<th>` обох таблиць: 🤖 авто · ✏️ вручну · ⚖️ БЗ.
UI: тумблер `tm-tp1-move-be` під налаштуваннями TP-1/TP-2.
Тести: `test_autosl_chain.py` (розділ «БЕЗЗБИТОК ПІСЛЯ TP-1»).

## 📐 R — ДОВІДКА, а НЕ гейт (скасовано користувачем — не повертати!)

Була спроба відсікати цілі, ближчі за власний стоп (`tp_min_r`, дефолт 1.5):
на проді 5 із 9 позицій мали R < 1 (ARB 0.39R … ORDI 0.82R), і TP-2 їм не
виставлявся. **Користувач це СКАСУВАВ дослівно:** «Не потрібно відсікати ніякі
угоди, все має бути як і до введення Автопілот… розрахуй Manual TP-1/TP-2 по
відсотках, які ти вираховуєш по алгоритму Автопілот».

**Правило:** автопілот РАХУЄ рівні з обʼєктів графіка і ВИСТАВЛЯЄ їх. Судити
угоду за R — не його справа. Налаштування `pilot_tp_min_r` і ключ `tp_min_r`
ВИДАЛЕНІ з коду й UI (а не просто вимкнені дефолтом — інакше збережений у БД
`1.5` продовжував би різати; див. урок про блоб нижче).
- **TP-2 виставляється ЗАВЖДИ**, коли ціль є попереду ВХОДУ. `tp_min_gap_pct`
  тепер керує ЛИШЕ ПОДІЛОМ на два рівні, а не правом TP-2 існувати.
- **Стоп на рівні НЕ впливає.** Він передається в `plan_targets` виключно щоб
  порахувати `r` для ПОКАЗУ (колонка «🎯 Автопілот» + рядок логу).
- `trade_pilot.risk_reward(entry, stop, objective)` лишився: R рахується від
  ВХОДУ, фарбується (червоний <1R · бурштиновий <1.5R · зелений ≥1.5R) — це
  інформація для людини, жодної логіки за нею немає.
Тест-замок: `test_low_r_target_is_still_set` (+ `'tp_min_r' not in DEFAULTS`).

## ⚠️ ЗМІНА ДЕФОЛТУ НЕ ДІЄ НА ВЖЕ ЗБЕРЕЖЕНИЙ БЛОБ (не наступати вдруге!)

Після вмикання `pilot_autofill_tp` дефолтом поля Manual TP-1/TP-2 лишились
ПОРОЖНІ навіть там, де R був 3.53 / 2.23 / 1.76 (тобто поріг проходив).
**Корінь:** `TradeManager._load_settings` робить `merged.update(stored)`, а
сторінка налаштувань шле УСІ ключі одним блобом — тож `pilot_autofill_tp:false`
осів у БД ще з часів старого дефолту й перекривав новий. Правка дефолту в коді
на такій установці не робить НІЧОГО.

**Фікс — одноразова міграція** (той самий прийом, що `interval_migrated_v2`):
`pilot_autofill_migrated_v1` у `_load_settings` вмикає тумблер РАЗ і одразу
персистить; далі вибір користувача поважається. `_fresh_settings()` (чиста
установка / немає БД) ставить позначку ОДРАЗУ — інакше міграція спрацювала б на
ДРУГОМУ старті й перекрила б вибір, який користувач уже встиг зробити.

**Друга причина того ж симптому — стара позначка.** Попередня версія ставила
`pilot_tp_set` БЕЗУМОВНО, навіть коли не виставила жодного рівня; такий блоб
«замуровував» угоду без TP назавжди. Тепер гейт питає `_pilot_tp_done(pos)`:
робота вважається зробленою, ЛИШЕ коли рівень реально стоїть (`manual_tp` /
`manual_tp1` / `tp1_done`). ⚠️ Виняток — `pilot_tp_cleared`: рівень, знятий
ОПЕРАТОРОМ (`origin='user'` + clear), автопілот НЕ відновлює, інакше
самолікування сперечалося б із людиною.

**Правило на майбутнє:** міняючи дефолт налаштування, яке вже могло бути
збережене, ЗАВЖДИ додавати міграцію — інакше зміна побачить лише чисту БД.
Тести: `test_trade_pilot.py` (40).

## 🛡 ГЛОБАЛЬНІ налаштування — ОКРЕМА гармошка, не всередині черги

**Скарга користувача:** «Налаштування (Черга-2) — до чого тут Черга-2? Вона
вимкнена. Працює Черга-4.» Слушно: у гармошці Черги-2 лежав цілий рядок
параметрів, які до неї стосунку НЕ мають — вони діють на КОЖНЕ автоматичне
відкриття і на КОЖНУ відкриту угоду:
`q2_auto_ob_sl`(+буфер,+TF) · `autosl_fallback_on`(+%) · `autosl_max_pct` ·
`require_new_situation` · `open_max_signal_age_min` · `manual_close_lock_min`.

Винесено у ВЛАСНУ гармошку верхнього рівня **«🛡 Авто-SL та ворота відкриття»**
(`togglePanel('autosl')`, `#autosl-body`/`#autosl-caret`, `_PANEL_IDS[0]`), яка
стоїть НАД Чергою-1 і прямо підписана «діють на ВСІ черги (1/2/3/4) і на ручні
відкриття». Розгорнута за замовчуванням — саме через неї їх і не могли знайти.

⚠️ **Ключі НЕ перейменовані** (`q2_auto_ob_sl*` лишились як були): перейменування
зламало б уже збережені налаштування в БД. **Історична назва ключа ≠ область
дії** — не робити з префікса `q2_` висновків про те, на що параметр впливає.

## 🎚 СТЕЛЯ SL (`autosl_max_pct`) — ОДНА на ВСІ авто-SL (не зламати!)

Налаштування «🎚 Стеля SL ≤ N %» існувало, але діяло ЛИШЕ в
`trade_manager._auto_ob_manual_sl`. Угоди Черги-4 ставлять стоп СВОЇМ шляхом
(`fuel_filter._q4_set_vob_sl`) — стеля їх не бачила, тож на проді стопи стояли
там, де стоїть блок: **2.4-5.9% від входу при цілях 2-13%**, тобто ціль часто
була ближчою за власний стоп (R < 1) і TP-2 не виставлявся взагалі.

**Фікс:** ЧИСТА функція `trade_manager.clamp_sl_distance(side, entry, sl,
max_pct)` → `(рівень, була_відстань|None)` — ЄДИНЕ джерело правила. Її кличуть
ОБИДВА шляхи: `_auto_ob_manual_sl` і `_q4_set_vob_sl` (через
`_tm_entry_price(sym)` — ціна входу реальної/тіньової позиції).
⚠️ Стеля лише ПІДТЯГУЄ задалекий рівень; ближчий стоп ніколи не відсувається.
⚠️ Вибір самого блоку НЕ змінюється — тільки максимальна відстань.
⚠️ Діє в момент ВИСТАВЛЕННЯ стопа (тобто на НОВІ угоди) — «SET ONCE» лишається,
заднім числом уже відкриті позиції не чіпаємо.
У 🧾 Лозі видно `· підтягнуто до стелі N% (було X%)`.
Дефолт **0 = вимкнено** (політика ризику — рішення користувача).
Тести: `test_q4_controls.py` (розділ «стеля SL»).

## ⚠️ ЗНИКЛИЙ ГЕЙТ Черги-3: `queue3_require_ob_match` (не реалізовано)

Аудит тестів показав: `_ob_match_ok` і налаштування `queue3_require_ob_match`
**відсутні в коді ПОВНІСТЮ** — жодного посилання ніде, крім
`test_readiness_strategy.py::test_ob_match_gate` (єдиний падючий тест файлу).
Тобто задокументований запобіжник «Q3 не відкриває ПРОТИ 1H-OB» зараз **не
працює**, і в `_engine_tick_readiness` немає ані OB-перевірки, ані
`_signal_allowed`-recheck (на відміну від Черги-4, де є `queue4_recheck_filters`).
Вплив обмежений: `queue3_enabled` дефолт **OFF**. Тест лишено падати СВІДОМО —
як запис про відсутню функцію. Відновлювати — за рішенням користувача.

⚠️ Стаби `_open` у тестах мусять приймати ВСІ kwargs справжнього методу
(`by_hand`/`signal_at`/`origin_trace`/`**kw`) — інакше двигун ловить TypeError,
мовчки нічого не відкриває, і тест падає «на рівному місці» (саме так відстав
`test_hot_opens_and_logs`).

⚠️ `test_bybit_reconcile.py` падає на `test_reconcile_skipped_when_tm_disabled`
— це ДАВНІЙ збій, відтворюється і без цих змін (перевірено `git stash`).

⚠️ **ПОВНИЙ ПРОГІН (09.09): 35 файлів OK, 3 ДАВНІХ падіння** — усі три
відтворюються на чистому дереві (перевірено `git stash`), тож їх можна
відрізняти від своїх регресій:
`test_bybit_reconcile.py::test_reconcile_skipped_when_tm_disabled` ·
`test_readiness_strategy.py::test_ob_match_gate` (свідомо, див. вище) ·
`test_reconcile_when_disabled.py::test_reconcile_loads_existing_when_disabled`
(`entry_price` не синхронізується: 100.0 замість 101.0).

## 🖼 БЛОКИ НА ГРАФІКУ = ЯК У TRADINGVIEW (обидві сім'ї · ворота не чіпали!)

**Скарга дослівно (BTCUSDT, 07.09):** «В TradingView на графіку вже давно є на
1Н OB SHORT, чому тут ще досі 1Н LONG?» → «чому бот не малює на графіку SHORT
OB 1Н?» → «зроби щоб вже працювало як у TradingView».

**Розрахунок був ПРАВИЛЬНИЙ — губились ДАНІ, і аж у двох місцях:**
1. `detect_last_order_block` віддавала РІВНО ОДИН блок (`internalOrderBlocks[0]`).
   Живий блок ПРОТИЛЕЖНОГО боку рахувався в тому ж проході й **викидався на
   `return`**. LuxAlgo ж малює ВСІ живі блоки одночасно — тож на TV висів
   ведмежий, а бот показував «🟢 OB Long 1H ★» (ведмежий лежав під індексом 1).
2. `_update_smc_ob` кликав `detect_smc_structure(...)`, який ОДНИМ проходом
   рахує і `internal`, і `swing` — а брався лише `result['internal']`. Тобто
   **Swing Order Blocks** (`swing_size=50`, у LuxAlgo ОКРЕМА сім'я зі своїм
   тумблером) бот не мав У ПРИНЦИПІ, хоча півоти й події вже лежали поруч.

**Зроблено:**
- `ob_detector._live_blocks(...)` — **ЄДИНЕ місце самого обчислення** (той самий
  прохід Pine). Над ним ДВІ функції з РІЗНИМ призначенням:
  • `detect_last_order_block` → **ВОРОТА**, один блок = `obs[0]`, форма
    відповіді БАЙТ-У-БАЙТ як була;
  • `detect_order_blocks(..., limit=8)` → **ПОКАЗ**, увесь список живих блоків,
    найновіший першим.
  ⚠️ Саме тому це ОКРЕМА функція, а не зміна старої: змінити повернення
  `detect_last_order_block` означало б поїхати показом І торгівлею разом.
- `smc_scanner._ob_zones[symbol] = {'tf','ts','internal':[…],'swing':[…]}` —
  знімок обох сімей, пишеться в `_update_smc_ob` з ТИХ САМИХ `klines_closed` і
  того самого `result`. Мережі не коштує НІЧОГО. `OB_ZONES_SHOW=8`,
  `OB_ZONES_CAP=300`. **IN-MEMORY, не в БД** (як `_vob_diag`): похідні від
  скану дані, відновляться першим циклом, міграція БД не потрібна.
- `get_chart_data` → `'ob_zones': dict(self._ob_zones.get(symbol) or {})`.
  ⚠️ **НЕ рахувати блоки інлайн на цьому ендпоінті** — саме так колись
  зʼявився баг «бейдж каже 1H, а числа з чарт-TF» (див. аудит ★ watchlist).
- Фронт: `VolOBPrimitive` тепер тримає **СПИСОК** (`setOBs(list)`; `setOB(ob)`
  лишився фасадом для Volumized-боксу), малює задом наперед — найновіший
  ЗВЕРХУ. Бокси будуються з `d.ob_zones` обох сімей, дублікати відсіює `seen`
  за ключем `bar_time|bar_high|bar_low` (той самий блок буває в обох сім'ях),
  свінг-блоки густіші (fill 0.16 проти 0.08 — «старша» сім'я), підпис
  `1H internal OB` / `1H swing OB` + `CHoCH ★` / `BOS →`. Фолбек на
  `d.last_ob`, поки скан не заповнив зони.

⚠️ **ВОРОТА ВХОДУ НЕ ЗМІНЮВАЛИСЬ.** `db.upsert_smc_ob_state(symbol, ob_tf, ob)`
і далі пише РІВНО поточний internal-блок; `Require OB Match`, «1H OB лише з
CHoCH» і такт `vob_one_per_ob` (`_current_ob_bartime`) читають ТОЙ САМИЙ рядок.
**Бейдж теж лишився станом воріт** (`d.last_ob`) — інакше «намалювали обидва
боки» перетворилось би на «бейдж каже одне, ворота роблять інше». Скільки
блоків бот реально бачить — у тултипі бейджа: `Живих блоків на 1H: internal
5 (🟢3 / 🔴2) · swing 1 (🟢0 / 🔴1)`.

⚠️ **ПОКАЗ ВІДВʼЯЗАНО ВІД ТУМБЛЕРА ВОРІТ** (зміна поведінки!). Раніше бокси
малювались лише при `ob_filter_enabled` — тобто з вимкненим фільтром графік
мовчав про структуру, яка на ньому Є. У LuxAlgo блоки не залежать від того, чи
хтось за ними торгує. Тумблер `Require OB Match` керує ВХОДОМ, а не малюнком.

⚠️ **Свінг-блоки — ЛИШЕ показ.** Дозволяти по них вхід я НЕ став: це змінило б
торгову логіку (послабило б OB-фільтр), а такого рішення користувач не ухвалював.
Якщо знадобиться — окремим тумблером із дефолтом OFF.

⚠️ **Пастка фікстури в тесті:** монотонний ряд НЕ дає ЖОДНОГО півота
(`detect_smc_structure` шукає бар, чий high вищий за максимум наступних `size`
барів), тож перша версія тесту давала **0 подій і 0 блоків** — на «рівному
місці». Бари мусять бути ПИЛКОЮ, а ноги для свінг-сімʼї — ДОВШІ за
`swing_size=50`. Хелпер `_leg(...)` у тесті це й робить.
Тест: `test_ob_zones_like_tv.py` (11).

## 🆕 АЛЕРТ «НОВИЙ OB НА ГРАФІКУ» — момент появи + ціна + 4H (не зламати!)

**Вимога користувача (09.09), дослівно:** «моментальна реакція на появу на
графіку нового OB 1H і моментальна відправка повідомлення в "Лог роботи бота"
про появу нового OB в який саме час і поточна ціна монети на графіку… потрібно
вияснити який на даний момент останній і актуальний OB 4H і також записати цю
інформацію до інформації про OB 1H… максимально швидко». Плюс: «розмалюй цю
інформацію кольорами, щоб не зливався текст».

`detection/ob_alert.py` — **ЧИСТІ функції** (рішення + формат), жодного I/O.
Подія в 🧾 Лозі: `event='ob_new'`, `source='OB'`, рядок
`LONG OB 1H (CHoCH) · LONG OB 4H · зʼявився 09.09.25 о 21:25 UTC · ⚡ виявлено
за 8с · $78,539.90`.

⚠️ **«ЗʼЯВИВСЯ» = `created_at_t` + ТРИВАЛІСТЬ БАРУ, а НЕ `bar_time`.** У блоку
ДВА різні часи: `bar_time` — свічка самого блоку (лежить «назад у часі»),
`created_at_t` — бар, на якому спрацював BOS/CHoCH. Детекція йде по ЗАКРИТИХ
барах, тож блок стає ВИДИМИМ, коли ТОЙ бар закрився. Підписати рядок `bar_time`
означало б показати час, якого на графіку ніхто не бачив.
⚠️ Часу появи немає → `stale` (тихо), бо написати «зʼявився», не знаючи коли —
брехня в самому рядку.

⚠️ **«НОВИЙ» = ЩЕ НЕ ОПРАЦЬОВАНИЙ (пастка breaker!).** `_ob_alert_seen[sym]` —
**СПИСОК** `bar_time` (`SEEN_CAP=16`), а не «водяний знак». Коли поточний OB стає
breaker, він випадає зі списку і «останнім» стає СТАРІШИЙ блок із МЕНШИМ
`bar_time` → умова `bt > prev` не спрацювала б НІКОЛИ (той самий урок, що вже
задокументований для VOB). Персистимо в `DB_KEY_OB_ALERT='smc_ob_alert_v1'`
(разом із TF: перемкнули `ob_filter_timeframe` → стара база не застосовується).

⚠️ **СТАРИЙ БЛОК → ТИХА БАЗА, у лог НЕ пишемо.** Вікно свіжості
`ob_alert_max_lag_sec` (**0 = АВТО = один бар TF** — той самий принцип «0=авто»,
що у `vob_alert_max_age_bars`): у межах поточного бару новішого блоку фізично не
існує. Без цього кожен рестарт (`botupdate` робиться часто) виливав би в лог
поточний OB УСІХ монет watchlist — тобто СТАН, а не подію: рівно той флуд, через
який лог VOB уже довелось чистити.

**⚡ ШВИДКА СМУГА — ЗВІДКИ «МОМЕНТАЛЬНО».** Вузьке місце було НЕ в детекції, а в
ЧЕРЗІ: `_update_smc_ob` кликався в ПОСЛІДОВНОМУ проході, тож монета №33
дізнавалась про свій новий блок у кінці циклу (хвилини).
- `_ob_fast_lane(md)` стоїть **ПЕРЕД ПОВНИМ ПРЕФЕТЧЕМ** — і це головне: повний
  префетч тягне ~10 HTTP на монету (3000 барів × кілька TF) і сам з'їдає
  десятки секунд; після нього «швидка» смуга вже не швидка. Смуга тягне РІВНО
  `(ob_tf, 700)` — один запит на монету, паралельно.
- **`_ob_lane_due(tf, now)` — ключова економія:** OB рахується по ЗАКРИТИХ барах,
  отже змінитись може ЛИШЕ коли закрився бар TF воріт. Смуга працює НЕ щоцикл, а
  РІВНО на першому циклі після закриття бару (для 1h — раз на годину); решту
  циклів коштує один `if`.
- **Логіка НЕ дублюється:** смуга кличе ТОЙ САМИЙ `_update_smc_ob`, а
  `_ob_done_cycle[sym] == _scan_count` не дає звичайному проходу зробити ту саму
  роботу вдруге (гейт стоїть ДО мережі).
- ⚠️ **І набір барів не качається двічі:** смуга `pop`-ає `(ob_tf, 700)`, а
  `_update_smc_ob` далі пропускається — тож на циклі зі смугою цей набір
  ВИКИДАЄТЬСЯ зі специфікацій повного префетчу. Інакше 33 зайвих HTTP, які
  ніхто не забере. Ліміт 700 унікальний для OB-споживача (решта бере 3000) —
  тест-замок стежить, щоб `KLINES_LIMIT` не став 700.

**📏 ЗАМІРЯНО (а не «здається швидше»).** CPU детекції на монету (700 барів:
structure + 3 проходи OB) = **5 мс** → 33 монети ≈ **0.2с**, 100 ≈ 0.5с. Тобто
смуга — це майже чиста мережа: 33 запити у 8 потоків ≈ 1-3с. Разом із
`interval_secs` (деф. 10с) реакція ≈ **до ~12с після закриття бару** замість
попередніх «до повного циклу» (1-4 хв).
⚠️ **ФІЗИЧНА МЕЖА, яку не обійти:** 1H-OB **не існує**, доки 1h-бар не
закрився. Швидше за закриття бару не буває — і це не налаштування, а природа
детекції по закритих барах. Тому в рядку стоїть `⚡ виявлено за Nс`: швидкість
ВИДНО в лозі, її не треба брати на віру.

**🔎 OB 4H — рахується ЛИШЕ В МОМЕНТ ПОДІЇ.** `_ob_on_htf(sym, md, tf, ob_tf,
ob_same)`; `ob_alert_htf` (деф. `'4h'`, порожньо = не питати).
- Новий OB трапляється кілька разів на добу, тож це одиничні запити. Рахувати 4h
  для 33 монет КОЖЕН цикл означало б +33 HTTP заради рядка, який майже ніколи
  не пишеться — той самий принцип, що у 💧 фільтра ліквідності («останнім у
  ланцюгу і лише якщо справді потрібно»).
- Кеш — до закриття свого 4h-бару; рядок 4h теж іде в `upsert_smc_ob_state`.
- ⚠️ **HTF == TF воріт → біржу НЕ питаємо вдруге**, беремо вже порахований блок:
  два різні числа для того самого TF були б прямим протиріччям.
- ⚠️ Невдача НЕ мовчить: у рядку `OB 4H: немає` + причина (мало історії / бари
  не завантажились / не порахувався).

⚠️ **АЛЕРТ — ЛИШЕ ПОВІДОМЛЕННЯ.** Нічого не дозволяє і не блокує:
`_ob_filter_allows` (ворота входу), «1H OB лише з CHoCH», такт `vob_one_per_ob`
(`_current_ob_bartime`) і джерела Manual SL читають ТОЙ САМИЙ рядок БД і
поводяться РІВНО як раніше. Тест-замок перевіряє, що у воротах немає ЖОДНОЇ
згадки про алерт — інакше «додали рядок у лог» тихо змінило б ТОРГІВЛЮ.
⚠️ **TF алерту НЕ дублюємо окремим ключем** — беремо `ob_filter_timeframe`
(«той OB, що зазначено в налаштуваннях»). Два джерела розійшлись би.

**🎨 КОЛЬОРИ (вимога «щоб не зливався текст»).** Решта подій логу малюється
одним сірим `#cbd5e1`, і рядок читався як суцільна смуга — а в ньому аж чотири
РІЗНІ за природою речі. `_obNewHTML(parts, fallback)` у `smart_money.html`:
напрямок **зелений/червоний**, мітка `OB 1H` — `#93c5fd`, `OB 4H` —
**бурштиновий** `#fbbf24` (щоб 1H і 4H не зливались), тег CHoCH/BOS — `#a78bfa`,
час — світлий monospace, `⚡` — `#22d3ee`, ціна — біла жирна monospace.
- ⚠️ Малюємо зі СТРУКТУРНИХ `extra['parts']`, а **НЕ розбором українського
  тексту** (той самий прийом, що `verdict.parts` + `text` у драбині): бекенд дає
  чисті дані, кольори — справа фронта, `detail` лишається фолбеком для CSV/TG.
- ⚠️ Ціну на сторінці форматує `fmtPriceJS` — ЄДИНИЙ формат ціни; Python
  `ob_alert.fmt_price` його ДЗЕРКАЛИТЬ (для CSV/логу), і тест-замок звіряє
  пороги з шаблоном.
- Подія є у `_actMeta` (`🆕 Новий OB`) і у випадайці фільтра логу.

⚠️ **Тег пишемо «CHoCH», а не «CHOCH»** — `.upper()` на тегу зробив би з нього
іншу сутність; у всьому проєкті він пишеться саме так.
⚠️ **Пастка, яку впіймав тест:** `seen_add` зберігає `int(секунди)`, а перша
версія `is_processed` звіряла з допуском `abs(a-b) < 0.5` — дробовий `bar_time`
НІКОЛИ не знаходив свій же записаний цілий, тож той самий блок щоразу виглядав
«новим» і писався в лог повторно. Тепер порівняння — по ЦІЛИХ секундах.
⚠️ `_sibling_mod(name, cache_key)` — спільний безпечний завантажувач сусіднього
модуля (`_dg_mod` тепер делегує йому). Причина та сама: пакетний імпорт тягне
`detection/__init__.py → pybit`.

Налаштування: `ob_alert_enabled` (**True**, новий ключ → міграція не потрібна),
`ob_alert_htf` (`'4h'`), `ob_alert_max_lag_sec` (`0` = авто).

### 🐞 ТРИ ДЕФЕКТИ, ЗНАЙДЕНІ НА ПРОДІ ПЕРШИМ ЖЕ ЕКСПОРТОМ (09.09)

**1. ЦІНА БУЛА ПОРОЖНЯ.** У КОЖНОМУ рядку стояв прочерк:
`⚡ виявлено за 4хв 46с · —`. Причина: `market_data.fetch_klines` віддає
`[{p, v, b, s, h, l, o, t}, …]` — ціна закриття у **`p`**, а код читав
`bar['close']`. Бари були на місці, ціна просто не діставалась.
**Фікс:** `ob_alert.close_of(bar)` — `p` → `close` → `c` (ohlc чарта справді
несе `close`, тож фолбек потрібен). Тест-замок звіряє ще й ДОКСТРІНГ
`fetch_klines`: зміниться контракт — тест упаде.

**2. `⚡ виявлено за 4хв 43с` БРЕХАЛО.** Після рестарту 6 монет одночасно дали
таке число — але бот просто ПІДНЯВСЯ через 4хв після закриття бару; `lag`
міряв ВІК БЛОКУ, а не нашу реакцію. **Фікс:** `build_parts(first=…)` — коли
база монети порожня (старт / новий символ), у рядку стоїть
`👀 перший показ після старту (вік 4хв 43с)`, а `⚡ виявлено за` — ЛИШЕ в
робочому стані, коли число справді про швидкість бота.

**3. ЗАПИС ТОНУВ У КУПІ — «окремою стрічкою» (вимога користувача).** Таблиця
логу ЗШИВАЄ події однієї монети+сторони в межах 180с в ОДИН рядок. На проді за
8 год: `signal` 156 + `rejected` 134 + `sltp` 130 — тож «🆕 Новий OB»
гарантовано приклеювався до ланцюга угоди і його «нічого не можна розібрати».
**Фікс:** `SOLO_EVENTS = new Set(['ob_new'])` — така подія створює ВЛАСНИЙ
ланцюг (`solo: true`) і візуально відбита лівим бірюзовим кантом
(`box-shadow:inset 3px 0 0 #22d3ee` + ледь помітне тло).
⚠️ **Найтонше місце:** у гілці solo **НЕ можна чіпати `lastByKey`** — інакше
наступна подія монети приклеїлась би вже до рядка «Новий OB». Тест це стереже.
⚠️ Це подія ІНШОЇ ПРИРОДИ: не крок життєвого циклу угоди, а факт зміни
СТРУКТУРИ на графіку — зшивати її з ланцюгом угоди неправильно й по суті.

### ⏱ «OB був на графіку з 12:00, а бот побачив о 21:00?» (кейс LITUSDT 09.09)

**Питання користувача:** «OB 1H, який промальовувався на графіку орієнтовно о
12 годині дня, визначився аж у 21? Це нормально? А швидше є можливість?»

**Спершу арифметика зі скріна — 9 годин там НЕМАЄ.** Заголовок рядка логу
малює `toLocaleTimeString()`, тобто **ЛОКАЛЬНИЙ** час, а «зʼявився … UTC» —
UTC. `21:00 UTC + 3хв07с = 21:03:07 UTC`, а в рядку `23:03:07` → зсув **+2 год**.
Тобто реальна реакція — **3хв 07с** після закриття бару.

**ДВА РІЗНІ ЧАСИ — ось звідки «з 12:00».** Бокс МАЛЮЄТЬСЯ на свічці блоку
(`bar_time`), яка лежить «назад у часі», а ІСНУВАТИ блок починає, коли
закрився бар із BOS/CHoCH. О 12:00 його на графіку ще НЕ БУЛО.
**Фікс подання:** у рядок додано `свічка блоку DD.MM.YY о HH:MM UTC` ПЕРЕД
`зʼявився …` — обидва часи поруч, і питання відпадає само. Немає `bar_time` →
сегмента немає (нічого не вигадуємо). Тест: `test_both_times_are_shown…`.

**🐞 ЧОМУ 3 ХВИЛИНИ, А НЕ СЕКУНДИ.** Смуга кликалась **ЛИШЕ на початку
циклу**, тож закриття бару ПОСЕРЕД проходу чекало наступного циклу.
**Фікс:** `_ob_fast_lane(md)` тепер кличеться ще й **МІЖ МОНЕТАМИ** в самому
проході — коштує один `if` на монету (`_ob_lane_due` — чиста арифметика
`now // tf_secs`, без I/O), а очікування зводить до однієї монети (~7с).
⚠️ **НАЙТОНШЕ МІСЦЕ:** монети, опрацьовані ДО закриття бару, мають
`_ob_done_cycle == _scan_count`, і гейт у `_update_smc_ob` мовчки пропустив би
САМЕ ЇХ — найгірший випадок (бар закрився одразу після монети №1) лишився б
без реакції на цілий цикл, попри другий виклик. Тому смуга **ЧИСТИТЬ
`_ob_done_cycle`**, коли бар справді закрився (`_ob_lane_due` → `clear()`, у
цьому порядку). Тест: `test_new_bar_invalidates_work_already_done_this_cycle`.

⚠️ **ФІЗИЧНА МЕЖА лишається:** 1H-OB **не існує**, доки 1h-бар не закрився —
детекція по ЗАКРИТИХ барах. Швидше за закриття бару не буває. Перейти на
ЖИВИЙ бар (як у VOB) НЕ можна без рішення користувача: уся архітектура
(такт `vob_one_per_ob`, джерело Manual SL, `Require OB Match`) спирається на
задокументоване «1H-OB стабільний у межах бару» — на живому барі блок
зʼявлявся б і зникав, і «1 угода на 1H-OB» перестала б бути гарантією.

Тест: `test_ob_alert.py` (33).

### 🆕 «НОВИЙ OB» = ОКРЕМИЙ ТИП СИГНАЛУ: ЗБІГ ДВОХ TF + дедуп 1-на-тренд

**Вимога користувача (10.09), дослівно:** «Додай до налаштувань вибір другого
таймфрейму для Нового OB… Другий таймфрейм зроби щоб його можна було вимикати
при нагоді (за замовчуванням другий таймфрейм 4Н). Тобто, якщо вибрано другий
таймфрейм, то Новий OB має тепер спрацювати, коли зійдуться обидва таймфрейми
(1Н і 4Н тільки тоді вважається, що Новий OB зʼявився), інакше якщо вибрано
основний таймфрейм, а це 1Н, то мають іти всі Нові OB 1Н» · «Додай також
алгоритм Deduplicate Signals (1 per trend) для Новий OB… якщо вибрано лише
основний таймфрейм 1Н то SHORT має право перебити лише протилежний Новий OB
LONG. А якщо задіяно два таймфрейми наприклад 1Н+4Н то 1Н+4Н SHORT має право
перебити лише 1Н+4Н LONG» · «Організуй все це як ще один вид сигналу. З
можливістю вмикати і вимикати.»

**Три ЧИСТІ функції в `detection/ob_alert.py`** (рішення, як завжди, окремо від
I/O): `combo_label(tf1, tf2, htf_on)` → `'1H'` / `'1H+4H'` ·
`converge(side1, side2, htf_on)` → `(ok, note)` · `dedup_allows(prev, side,
combo)` → `(ok, note)`.

**🔎 ДРУГИЙ TF = ВИМОГА ЗБІГУ** (`ob_alert_htf_enabled`, деф. **True**, TF —
наявний `ob_alert_htf`, деф. `'4h'`):
- УВІМК → подія «новий OB» вважається такою, що СТАЛАСЬ, лише коли напрямки
  ОБОХ TF ЗІЙШЛИСЬ; ВИМК → ідуть ВСІ нові OB основного TF, а старший **не
  питаємо взагалі** (ні запиту до біржі, ні рядка 4H у повідомленні).
- ⚠️ **Тумблер ОКРЕМО від самого TF:** «вимкнути при нагоді» не має стирати
  вибір користувача (той самий принцип, що чекбокс+TF в OB-фільтрі).
- ⚠️ **«Немає OB на старшому TF» ≠ «узгоджено».** Якби відсутність даних
  пропускала подію, увімкнений другий TF мовчки НЕ робив би нічого на монетах,
  де 4H-блок не порахувався. Це навмисно ІНАКШЕ, ніж fail-open у 💧 фільтра
  ліквідності: там джерело ЗОВНІШНЄ (біржа блокує IP), а тут дані НАШІ і
  зʼявляться з наступним баром старшого TF.
- ⚠️ **РОЗБІГ НЕ КОВТАЄ БЛОК** (найтонше місце). Старший TF оновлюється СВОЇМ
  баром, тож збіг може настати за кілька хвилин — і це буде ТА САМА поява.
  Тому при розбіжності блок **не** позначається опрацьованим: `outcome` вертає
  `wait_htf`, і поки блок у вікні свіжості, ми спробуємо знову. Вічного
  перебору немає — випав із вікна → звичайний `stale` (тиха база). Та сама
  помилка вже траплялась у VOB («скидання ковтало блок, що виник після 1H-OB»).
- ⚠️ Режим «показати 4H, але НЕ гейтити» БІЛЬШЕ НЕ ІСНУЄ — вибір другого TF
  тепер і означає вимогу збігу (рішення користувача).

**🔁 ДЕДУП «1 на тренд»** (`ob_alert_dedup`, деф. **True**): той самий напрямок
у ТІЙ САМІЙ комбінації TF глушиться, перебити може ЛИШЕ протилежний.
- ⚠️ **Тренд міряється КОМБІНАЦІЄЮ** (`_ob_alert_fired[sym] = {side, combo}`):
  інакше після вмикання збігу перший же 1H+4H-сигнал мовчки загубився б через
  позначку від часів «лише 1H». Інша комбінація = інший за визначенням сигнал.
- ⚠️ **ПЕРСИСТ** разом із `seen` у `DB_KEY_OB_ALERT` — `botupdate` робиться
  часто, і без цього той самий тренд давав би другий сигнал після рестарту.
- ⚠️ Придушений блок усе одно позначається ОПРАЦЬОВАНИМ — інакше перевірявся б
  щоцикл до кінця вікна свіжості.
- Окремого скиду НЕ треба: «новий OB протилежного боку» і є зміна тренду (на
  відміну від CHoCH-дедупу, якому скид дає фліп 1H-OB).

**📨 ТИП СИГНАЛУ** (`ob_alert_signal`, деф. **False**): 🆕 Новий OB іде в
ТОРГІВЛЮ — `_ob_alert_signal()` → **`_signal_allowed`** → `tm.on_signal(
opened_by='ob_alert')` → черги FF.
- ⚠️ **ЧЕРЕЗ СПІЛЬНІ ВОРОТА, І НІЯК ІНАКШЕ** (урок ASTERUSDT: VOB-alert колись
  кликав `on_signal` напряму й обходив усі фільтри). Тест-замок через AST
  вимагає, щоб `_signal_allowed` стояв ДО `on_signal`.
- ⚠️ Дефолт OFF: новий тип сигналу не має мовчки розширити потік угод на
  робочій установці (та сама причина, що у `vob_alert_enabled`).
- Мітка — `'ob_alert': '🆕 Новий OB'` у `signal_labels.py` + ОБА JS-дзеркала
  (`smart_money.html`, `infosite/app.js`).

⚠️ **ПРИДУШЕНІ СТАНИ У 🧾 ЛОГ НЕ ПИШУТЬСЯ.** `wait_htf` / `dedup` — це СТАНИ, а
не події (новий 1H-блок без збігу трапляється на кожній монеті щокілька годин →
був би рівно той флуд, через який лог VOB уже довелось чистити). Причина живе в
`self._ob_alert_diag[symbol]` і віддається `get_state()['ob_alert_diag']`
(+ `ob_alert_fired`) — саме там відповідь на «чому по монеті немає реакції».
⚠️ **ВОРОТА ВХОДУ НЕ ЗМІНИЛИСЬ:** `_ob_filter_allows`, «1H OB лише з CHoCH»,
такт `vob_one_per_ob` і джерела Manual SL читають ТОЙ САМИЙ рядок БД. Тест-замок
перевіряє, що у воротах немає згадки ні про алерт, ні про збіг/дедуп.
⚠️ НОВІ ключі → `merged.update(stored)` віддає дефолт, міграція НЕ потрібна.
UI: окремий блок `#sm-obalert-group` у ряду типів сигналів (4 контроли, один
обробник `updateObAlert()` шле їх РАЗОМ; `_smObAlertSync()` гасить мертві
контроли); у шапці налаштувань — `· 🆕 OB 1H+4H` (+`📨`, якщо йде в торгівлю).
Тест: `test_ob_signal_type.py` (25).

## 🧮 МММ-МОНІТОР — жива таблиця напрямків + групове ✋ відкриття (15.09)

**Вимога користувача, дослівно:** «Я хочу відслідковувати в реальному часі,
який на даний момент по монетах МММ стан. І сортувати їх окремо на LONG чи
SHORT… Додай до кожної монети можливість в таблиці її помітити для групового
відкриття угод.» Плюс пропозиція зробити це в «Черзі на вхід».

⚠️ **ЧОМУ НЕ В ЧЕРЗІ-1 (розбір, щоб не переробляти вдруге).** `_pending`
наповнюється ВИКЛЮЧНО з `intercept` — монетами, які ДАЛИ СИГНАЛ і пройшли
ворота. Це черга подій, а не список спостереження: усього watchlist там не буде
НІКОЛИ (на скріні користувача — 0 рядків). Плюс рядок черги живе під двигуном
(TTL, виселення, авто-відкриття), тож галочка на ньому означала б вибір на
записі, який може зникнути або відкритись сам. Тому — ОКРЕМА секція, а черги не
чіпали взагалі (тест-замок: монітор у `_pending*` не пише).

**🧮 ПОКАЗНИК — САМЕ «СТАРИЙ» МММ** (вимога 15.09 дослівно: «Мені потрібно МММ
саме старого зразка показник»). Це `_fuel_dir_legacy` — сирий `(fa−fb)/den` по
розташуванню кластерів liq-map, ТОЙ САМИЙ, що живить шар «Старий МММ» у
Черзі-4. НЕ `_fuel_dir_smoothed` (нова «бабло»-модель з EMA і гістерезисом).
- ⚠️ **Мережі це не коштує НІЧОГО:** legacy читає `_liq_state` — той самий
  кешований (TTL 20с) знімок, який щойно взяв новий МММ; тут лише арифметика
  над уже завантаженими рівнями.
- ⚠️ **Ціна — з нового зрізу:** `_fuel_dir_legacy` не повертає `mark_price`
  взагалі, а число там одне й те саме (знімок liq-map + `_live_price`).
- ⚠️ Немає legacy по монеті → рядка НЕМАЄ. «Даних нема» ≠ «тиску нема»:
  вигадувати ⚖ рівновагу не можна.
- ⚠️ Напрямок ГРУПОВОГО ВІДКРИТТЯ теж береться зі старого МММ — рівно те, що
  людина бачить у рядку.
- Тест-замок: новий МММ у знімку подається ПРОТИЛЕЖНИМ старому, тож кожен тест
  заразом доводить, що показуємо саме старий.

**Джерело чисел — ЗНІМОК ДВИГУНА, не власний розрахунок.** `_tick` рахує
`_fuel_dir_smoothed(sym, update=True)` для всіх «relevant» монет; одразу після
цього циклу `_mm_capture(fuels, settings)` бере по кожній монеті СТАРИЙ МММ і
кладе результат у `self._mm_snapshot` (+ `_mm_prev` для тренду сили).
`mm_monitor_state()` лише ЧИТАЄ. Це той самий урок B2, що з шарами Черги-4:
якби монітор рахував сам, у таблиці стояли б інші числа, ніж ті, за якими
двигун ухвалює рішення.
- ⚠️ **Сила = |dir| × 100** — беремо ГОТОВЕ поле `strength` із самого legacy, а
  не рахуємо вдруге: два «однакових» числа розійшлись би.
- ⚠️ Віддаємо `strength_prev`, а НЕ готове «up/down»: стрілку малює спільний
  віджет `ffFuelCell` (той самий, що в колонці «МММ» черг). Друге правило
  тренду на фронті розійшлося б із першим.
- `get_state()['mm_monitor']` = `{rows, ts, enabled, limited, counts}`; секція
  `'mm'` додана в `HEAVY_SECTIONS` → сервер віддає її ЛИШЕ коли гармошка
  розгорнута (до 200+ рядків на полл), а фронт ОБОВʼЯЗКОВО читає `served.mm`.

**🔌 ТУМБЛЕР СЕКЦІЇ `mm_monitor_enabled` (деф. УВІМК)** — «як у всіх Черг»
(вимога 15.09). **ВИМКНЕНО → знімок НЕ будується взагалі**: це і є економія
(старий МММ по 200+ монетах щотакту не рахується), а не просто приховування
таблиці — інакше тумблер брехав би про роботу.
- ⚠️ Вимкнення **ЧИСТИТЬ** старий знімок: «заморожені» числа, які вже ніхто не
  оновлює, гірші за порожню таблицю — вони виглядають як живі.
- ⚠️ `group_open` при вимкненому моніторі відмовляє ОКРЕМОЮ причиною; без цього
  кожна монета дістала б «МММ без напрямку», тобто тумблер читався б як
  «ринок у рівновазі».
- Дефолт УВІМК свідомо: монітор нічого не відкриває сам, тож потік угод не
  розширює (на відміну від нових ТИПІВ СИГНАЛУ, які дефолтяться в OFF).

⚠️ **РЕЖИМ ЕКОНОМІЇ НАЗВАНИЙ ВГОЛОС.** При `mmm_limited_mode` (дефолт **True**)
МММ рахується лише по монетах «у роботі» (₿ · угоди · черги 1-4 · фандинг ·
додані вручну), тобто монітор покаже КОРОТКИЙ список. Мовчазно це читалось би
як «бот сліпий», тож у відповіді є `limited`, а на сторінці — бурштинове
попередження з кнопкою «Рахувати весь WATCHLIST» (вона чіпає ТОЙ САМИЙ чекбокс
`ff-mmm-limited` і зберігає звичайним `saveFuelFilterSettings()` — другого
шляху запису ключа не заводимо). Той самий принцип, що з мовчазним clamp-ом у
скані ліквідності.

**✋ ГРУПОВЕ ВІДКРИТТЯ** — `group_open(symbols)` → `_mm_open_one` на кожну:
- напрямок КОЖНОЇ монети — її власний МММ **зі знімка**, тобто рівно те, що
  людина бачила в рядку;
- шлях ТОЙ САМИЙ, що у ✋ відкриття з Черги-4: `_open(..., by_hand=True,
  skip_safeguard=True, skip_ctr_safeguard=True)`, мітка `manual → MMM`
  (**✋ Ручний → 🧮 МММ-монітор**), далі звичайний `_q4_set_vob_sl`;
- ⚠️ **🚦 головні кнопки напрямку НЕ обходяться** — їх тримає сам `_open`;
  тест-замок не дає завести у груповий шлях власний обхід воріт;
- ⚠️ результат — **ПО КОЖНІЙ монеті** (`results[]`): часткова невдача не має
  виглядати як загальний успіх. Монета в угоді / ⚖ рівновага / відмова `_open`
  → окрема причина в рядку відповіді й у 🧾 Лозі (`source='MMM'`).
- Дублікати в запиті згортаються (`dict.fromkeys`), порожній вибір нічого не
  робить, вимкнений Fuel Auto-Filter блокує все.
- API: `POST /api/fuel-filter/mm-monitor/open` {symbols}.

⚠️ **ВИБІР ЖИВЕ ПОЗА DOM** (`_mmSel`, Set). Таблиця перемальовується на кожному
поллі (~10с), тож якби галочки трималися лише в розмітці, їх стирало б просто
під час вибору — групове відкриття було б неможливим. Рендер відновлює галочки
з набору, а монету, що зникла зі знімка (відкрилась), знімає з вибору сам.
Плюс сигнатура `tb.dataset.sig` — нічого не змінилось, DOM не чіпаємо.
⚠️ «Обрати всі» стосується ЛИШЕ ВИДИМИХ і придатних рядків (фільтр напрямку +
«Сила ≥»), інакше клік тихо обирав би монети, яких на екрані немає.
⚠️ Монета вже в угоді → чекбокс `disabled` (друга позиція по тому самому
символу все одно не відкриється); ⚖ рівновага → теж не обирається.
«У черзі» — це ІНФОРМАЦІЯ, а не заборона: ✋ ручне свідомо йде повз ворота черг.

UI: секція `togglePanel('mm')` (`#mm-body`, у `_PANEL_IDS`) НАД Чергою-1,
розгорнута за замовчуванням; у шапці — тумблер `ff-mm-monitor-enabled`
(`tm-toggle-switch` + `event.stopPropagation()`, щоб клік по ньому не згортав
гармошку); вкладки Всі / 🟢 LONG / 🔴 SHORT / ⚖ Рівновага зі стилем 1-в-1 як у
вкладок Черги-1; сортування — сила ↓, тайбрейк символ (щоб рядки не
«стрибали»). Мітка `'MMM': '🧮 МММ-монітор'` — у `signal_labels.py` + ОБИДВА
JS-дзеркала.
⚠️ **КОЛОНКИ-ДУБЛІКАТА «СИЛА» НЕМАЄ** (прибрано на прохання 15.09): віджет
`ffFuelCell` уже показує і напрямок, і 0-100%, і стрілку тренду — окрема
колонка дублювала те саме число. Саме поле `strength` ЛИШИЛОСЬ у даних: воно
живить сортування і фільтр «Сила ≥». ⚠️ НЕ плутати з колонкою «Сила **росте**»
(нижче) — то ІНША метрика. Тест-замок звіряє кількість `<td>` у рядку з
кількістю `<th>` у заголовку (а не з магічним числом).

### 📈 «СИЛА РОСТЕ» ЧИСЛОМ + ⏱ ТАЙМЕР РОСТУ · 📊 TradingView по назві (15.09)

**Три вимоги користувача:** «при натисканні на монету відкривався TradingView,
так як і в WATCHLIST» · «показник "Сила росте" розрахуй у вигляді відсотків і
додай колонку в таблиці по цьому показнику і сортування також по ньому» ·
«включай таймер при кожному старті показника "Сила росту" і обнуляй, коли
перестає рости; також відображай цей таймер у таблиці».

**📊 TradingView** — назва монети обгорнута в **`tvSym(r.symbol)`**, ТУ САМУ
функцію, що й рядок WATCHLIST: одна вкладка `tvchart`, символ `BYBIT:<SYM>.P`.
Свого лінка НЕ заводимо. `tvSym` сам робить `stopPropagation`, тож клік по
назві не смикає галочку рядка.

**📈 ПРИРІСТ — У ПУНКТАХ, А НЕ У ВІДНОСНИХ %** (головне рішення).
Сила — це вже ЧАСТКА у відсотках, тож її зміна міряється в **пунктах**:
45% → 57% = **+12 п.п.** Відносний % тут пастка: 1% → 5% дало б «+400%», і
сортування за зростанням підняло б нагору ШУМ замість монет із реальним тиском.
Відносне число віддається ОКРЕМО (`delta_rel`) і живе ЛИШЕ в підказці рядка.
- `delta` = `strength − strength_prev`. ⚠️ **ВІКНО ЗМІНЕНО** — спершу це був
  один такт двигуна, і показник через це мерехтів; чинне правило див. у розділі
  «🩹 «СИЛА РОСТЕ» МЕРЕХТІЛА» нижче.
- Немає бази → `delta = None` і в таблиці «—»; нуля не вигадуємо.
- Сортування: порожні рядки йдуть у КІНЕЦЬ при БУДЬ-ЯКОМУ напрямку («немає
  даних» ≠ «нуль»), тайбрейк — символ (щоб рядки не «стрибали»).

**⏱ ТАЙМЕР БЕЗПЕРЕРВНОГО РОСТУ** — `_mm_grow_since[sym]` (in-memory):
- ПЕРШИЙ такт росту → `setdefault(sym, now)`; далі таймер НЕ перезапускається;
- перестала рости (плато АБО падіння) → `pop` → таблиця показує «—»;
- новий ріст після паузи → НОВИЙ старт (саме «при кожному старті»);
- вимкнення монітора і зникнення монети зі знімка теж чистять таймер.

⚠️ **«РОСТЕ» ВИЗНАЧЕНО ОДИН РАЗ.** Поріг — `MM_GROW_MIN_DELTA = 1`, тобто
`delta > 1`, що ТОЧНО дорівнює умові `now > prev + 1`, за якою спільний віджет
`ffFuelCell` малює стрілку ↑. Інший поріг дав би рядок, де стрілка каже «→», а
таймер біжить — суперечність прямо на екрані. Клітинка приросту фарбується за
ТИМ САМИМ правилом (>+1 зелений ↑ · <−1 червоний ↓ · інакше сірий →).
Тест-замок стежить і за константою, і за тим, що поріг у `ffFuelCell` не змінили.

⚠️ **СЕКУНДИ НЕ ПЕРЕБУДОВУЮТЬ ТАБЛИЦЮ.** Бекенд віддає `grow_since` (МОМЕНТ
старту, epoch), а не «скільки секунд»; фронт малює `<span class="ff-timer"
data-since=…>`, і його веде ОДИН глобальний 1с-тікер сторінки — той самий
прийом, що з `held_sec` у Черзі-4. `grow_since` у сигнатурі Є (щоб старт і
обнулення перемалювались), живого часу в ній НЕМАЄ.

**⇅ Сортування** — клікабельні заголовки `data-mmsort` (символ / Старий МММ /
Сила росте / Ціна) зі стрілками `.ff-sort-arrow`, як у Черг; дефолт — сила ↓
(той самий порядок, що віддає бекенд). Вибір сортування входить у сигнатуру,
інакше клік по заголовку нічого б не перемалював.
⚠️ **`enabled` — ЧАСТИНА сигнатури таблиці** (`tb.dataset.sig`). Без цього
перемикання тумблера при порожній таблиці нічого не перемальовувало б, і
«Монітор вимкнено» висіло б замість «Немає даних» (і навпаки). Упіймано
тестом, який ганяє реальний JS сторінки. Три причини порожньої таблиці
(вимкнено / немає даних / не підходить під фільтр) мусять лишатись РІЗНИМИ.
⚠️ **Тест `test_q4_perf.py::test_heavy_sections_are_declared` РОЗШИРЕНО** (не
регресія): він фіксував ТОЧНУ рівність `{'q4','fund'}`, тож будь-яка нова важка
секція ламала його на рівному місці. Тепер стереже СУТЬ — базові дві на місці і
КОЖНА оголошена секція реально має свій `_want_<ім'я>` + прапорець у `served`.
Тест: `test_mm_monitor.py` (53, з них 8 — реальний JS зі сторінки під node).
⚠️ **Пастка фейк-DOM у тесті:** `querySelectorAll` віддавав вкладки на БУДЬ-ЯКИЙ
селектор, тож `_mmHeaderArrows` (шукає `th[data-mmsort]`) падав на
`getAttribute is not a function` — «на рівному місці». Фейк тепер селектор-
залежний. І ще: символ трапляється в рядку ДВІЧІ (обробник галочки + назва),
тож різати HTML треба на `</tr>`, а не по символу.

### 🩹 «СИЛА РОСТЕ» МЕРЕХТІЛА — ВІКНО ЗАМІСТЬ «ПОПЕРЕДНЬОГО ТАКТУ» (15.09)

**Скарга користувача:** «Чому "Сила росте" дуже часто пропадає? Він такий
непостійний? Чи він перезаписується часто?» → «Визнач професійне рішення, щоб
не було частого мерехтіння, і показник щоб не вважався застарілим одночасно.»

**ДІАГНОЗ (відтворено прогоном справжнього `_mm_capture`, не на здогад).**
Приріст рахувався між ДВОМА СУСІДНІМИ тактами, і це давало три вади разом:
1. **разовий пропуск коштував ДВА такти показника.** liq-map мить не віддала
   стан → монета випала зі знімка → наступного такту «попередньої сили» не
   існувало → «—», хоча дані вже повернулись;
2. **сила — ЦІЛЕ число** (`|dir|×100`): рух 0.520 → 0.525 дає ті самі «52» →
   приріст 0 і ОБНУЛЕНИЙ таймер;
3. **такт 30с проти оновлення джерела 60с** — кожен другий такт порівнював
   фактично ті самі рівні liq-map.

**РІШЕННЯ — база не «попередній такт», а НАЙСТАРІШИЙ семпл у ВІКНІ.**
`_mm_str_hist[sym] = [(ts, strength)…]` + `_mm_track_growth(snap, now)`;
результат (`strength_prev` / `delta` / `delta_rel` / `delta_span`) пишеться В
ЗНІМОК, читачі лишаються читачами.
- `MM_GROW_WINDOW_SEC = 180` — три оновлення джерела: разовий пропуск бази не
  стирає, дрібне тремтіння цілого числа не перемикає стрілку, і це все ще
  «зараз», а не вчорашній стан.
- `MM_GROW_MIN_SPAN_SEC = 60` — поки історії менше за ОДНЕ оновлення джерела,
  приросту НЕ показуємо («—» + «набираємо історію Nс»): у межах одного
  оновлення рівні ідентичні, і «приріст» там був би шумом ціни. Це друга
  половина вимоги — не видавати за виміряне те, що виміряти не встигли.
- ⚠️ **НАЙВАЖЛИВІШЕ МІСЦЕ:** монету, якої немає в поточному знімку, **не
  прибираємо одразу** — історію просто ОБРІЗАЄМО вікном. Пережила разовий
  пропуск → база збереглась; не було довше за вікно → семплів не лишилось,
  історія і таймер зникають самі (памʼять обмежена тим самим вікном).
- `delta_span` віддається і ПОКАЗУЄТЬСЯ (як `price_span`): «+5 п.п. за 40с» і
  «+5 п.п. за 3 хв» — різні за вагою твердження. Фронт малює те, що прийшло,
  свого «3 хв» не зашиває. `delta_span` **у сигнатурі таблиці** — інакше
  перехід «—» → число не перемалював би рядок.

**⏱ ТАЙМЕР — ДВІ УМОВИ (і саме тут «золота середина»).** Ріст ТРИВАЄ, поки
(а) `delta > MM_GROW_MIN_DELTA` за вікном **і** (б) сила не відкотилась від
ПІКУ вікна більше ніж на той самий поріг.
- (а) прибирає мерехтіння: один рівний такт більше не гасить таймер;
- (б) не дає таймеру тягнути «росте» ще до 3 хв після розвороту — без неї
  вікно створювало б «застарілість» з іншого боку.
- ⚠️ Третього магічного числа немає: допуск відкату — ТОЙ САМИЙ
  `MM_GROW_MIN_DELTA`, що малює стрілку ↑ у `ffFuelCell`.
- ⚠️ Наслідок, який треба знати: на рівному плато таймер гасне не миттєво, а
  коли старий низький рівень ВИПАДЕ з вікна (до ~3 хв). Це свідома ціна за
  відсутність мерехтіння, і вона обмежена вікном.

⚠️ **`_mm_prev` ВИДАЛЕНО** — база тепер лише з історії. `mm_snapshot_for`
(колонка в таблицях угод) бере ту саму базу зі знімка, тож стрілка тренду в
угодах і в моніторі не можуть розійтись.
⚠️ **`mm_window_change(hist, now, window)`** — ОДНА чиста функція
(`first/last/peak/low/abs/span/points`) на ОБИДВІ метрики монітора: рух ціни
(`mm_price_move` тепер її обгортка) і приріст сили. Дві «однакові» реалізації
з часом розійшлись би.
⚠️ `_mm_capture(..., now=None)` — інʼєкція годинника ЛИШЕ для тестів: приріст
тепер прив'язаний до ЧАСУ, тож без віртуального годинника тести перевіряли б
неіснуючу поведінку (усі такти в одну мить). Хелпери `_cap` / `_caps`
сунуть годинник на `CYCLE_SECS`.

Тест: `test_mm_monitor.py` (87) — розділ «🩹 БОРОТЬБА З МЕРЕХТІННЯМ».

### ⏱ ТАЙМЕР ОКРЕМОЮ КОЛОНКОЮ · 💹 ЦІНА · 🔮 ПРОГНОЗ · 🧠 РІШЕННЯ · 🧮 МММ в УГОДАХ (15.09)

**Чотири вимоги користувача:** «Таймер зроби окремою колонкою» · «Показник по
ціні зроби, щоб було розуміння чи ціна росте чи падає» · «Додай поля з
показниками що на скріні 1 і 2» (банер Decision «LONG 71% СИЛЬНИЙ» + бейджі
прогнозу «1H: 🟢 LONG +100% · 90%» / «4H: …») · «Додай колонку Старий МММ в
таблиці відкритих угод».

⚠️ **ЧАСТКОВО ЗАМІЩЕНО** розділом «🎛 ЧИСТКА ТАБЛИЦІ» нижче: колонок тепер
**8**, «🧠 Рішення» і «Стан» ПРИБРАНО, «🔮 Прогноз» розбито на дві.
Решта цього розділу (таймер, ціна, колонка в угодах) — чинна.

**⏱ ТАЙМЕР — ВЛАСНА КОЛОНКА** (`_mmTimerCell`, сортування `data-mmsort="grow"`).
Логіка `_mm_grow_since` НЕ змінювалась; з комірки «Сила росте» таймер прибрано
(вона розтягувалась). Тест-замок перевіряє, що таймер стоїть у комірці
**одразу за** приростом і що це РІЗНІ `<td>`.

**💹 ЦІНА — «росте / падає» за 15-хв вікном.** `mm_price_move(hist, now)` —
ЧИСТА функція над `_mm_price_hist[sym] = [(ts, price)…]`; напрямок пишеться в
ЗНІМОК (`price_dir`/`price_chg`/`price_span`), `mm_monitor_state` лише читає.
- ⚠️ **Вікно і мертва зона — ТІ САМІ числа, що в колонці Price у 💰 Funding**
  (`MM_PRICE_WINDOW_SEC = 15×60`, `MM_PRICE_DEADZONE = 0.10`): дві колонки на
  одній сторінці не мають називати «росте» різні речі. Константи **дублюються,
  а не імпортуються** (джерело семплів інше: знімок МММ раз на `CYCLE_SECS`
  проти 1-хв опитувань фандинг-сканера, а пакетний імпорт
  `detection.funding_monitor` тягнув би півпроєкту в ізольовані тести) —
  тому стоїть тест-замок, що звіряє числа МІЖ ФАЙЛАМИ.
- ⚠️ **`price_span` віддається обовʼязково і показується в комірці.** Одразу
  після рестарту вікно ще неповне, і «+0.4% за 40с» не можна видавати за
  «+0.4% за 15 хв». Фронт малює РІВНО те число, що прийшло, — свого «15хв» він
  не зашиває.
- ⚠️ Історія обрізається вікном (+ один такт запасу) і забуває монети, яких
  немає у знімку: інакше монета, що повернулась, рахувала б рух від ціни
  годинної давності.
- Сортування колонки «Ціна» — **за РУХОМ** (`pchg`), а не за самою ціною: заради
  «яка монета зараз іде вгору» колонку й додали. Рядок без історії — у КІНЕЦЬ.

**🔮 ПРОГНОЗ 1H/4H — БЕЗКОШТОВНО.** `_mm_forecast(fe, sym)` читає РІВНО той
самий `forecast_engine.get()`, що малює бейджі над графіком, тож число в
таблиці й на бейджі не можуть розійтись; формат комірки теж 1:1 з бейджем.
- ⚠️ **`ensure_fresh` у цьому шляху ЗАБОРОНЕНИЙ** (тест-замок): він ходить по
  свічки, а тут 200+ монет щотакту. Двигун прогнозу беремо ОДИН раз на такт.
- ⚠️ `side = 0` («ней») — це ЗМІСТОВНА відповідь, і в таблиці вона читається
  інакше, ніж порожній кеш («—»).

**🧠 РІШЕННЯ — ЄДИНИЙ РОЗРАХУНОК таблиці, тому за тумблером.**
`mm_monitor_decision` (деф. **ВИМКНЕНО**) · `MM_DECISION_TTL = 120с` ·
`MM_DECISION_MAX_PER_TICK = 8`.
- ⚠️ Джерело — `TradeManager.compute_decision`, ТОЙ САМИЙ виклик, що малює
  банер. Свого вердикту не рахуємо (тест-замок: у `_mm_decisions` немає ні
  `evaluate_entry`, ні `build_decision`).
- ⚠️ **Порціями, найстаріші першими:** `compute_decision` = 2× `evaluate_entry`
  на монету; 200 монет за такт поклали б двигун. Таблиця заповнюється за
  кілька тактів, і це ЧЕСНО видно — «⏳», а не порожнеча.
- ⚠️ Три РІЗНІ стани комірки: «—» вимкнено (це РІШЕННЯ) · «⏳» ще не дійшла
  черга · сам вердикт. Слова вердикту (`_MM_V_UA`) — дзеркало банера;
  тест-замок звіряє ВСІ словесні мапи `V_UA` на сторінці між собою.
- ⚠️ Вимкнення тумблера ЧИСТИТЬ кеш вердиктів (інакше після повторного
  вмикання світились би вердикти піврічної давності).

**🧮 «СТАРИЙ МММ» У ТАБЛИЦЯХ ВІДКРИТИХ УГОД (real + paper).**
`ff.mm_snapshot_for(symbols)` → `{sym: {mm, strength, strength_prev, delta,
grow_since}}`; `/api/tm/state` кладе це в `pos['old_mm']`, фронт малює
`oldMmCellHTML` через ТОЙ САМИЙ `ffFuelCell`, що й монітор і черги.
- ⚠️ **ЖОДНИХ розрахунків на `/api/tm/state`** — саме через них колонку «МММ»
  колись і прибрали звідси (ендпоінт опитується кожні 5с під одним
  gunicorn-воркером). Тепер це ЧИТАННЯ знімка, тож таблиця угод і монітор
  показують ОДНЕ І ТЕ САМЕ число (тест це звіряє поле за полем).
- ⚠️ **Знімок будується для монет У ВІДКРИТІЙ УГОДІ НАВІТЬ ПРИ ВИМКНЕНОМУ
  моніторі** (їх одиниці): колонка в таблиці угод до монітора стосунку не має, і
  «вимкнув монітор — зникли числа в угодах» було б безпідставно. Економія
  тумблера від цього не страждає — рахується жменя монет над УЖЕ кешованим
  знімком liq-map.
- ⚠️ Через це `mm_monitor_state` тепер ЯВНО віддає `rows: []` при вимкненому
  тумблері, а не покладається на порожній знімок — інакше монети з угод
  протекли б у таблицю щойно вимкненого монітора.
- ⚠️ `except AttributeError` у маршруті — фолбек на старіший `fuel_filter`
  (файли деплояться в різному порядку): колонка просто порожня, 500-ки немає.
- Colspan обох таблиць: real 14 → **15**, paper 13 → **14**. Тест-замок звіряє
  `colspan` із живою кількістю `<th>`, а не з магічним числом.

Тест: `test_mm_monitor.py` (78, з них 13 — реальний JS зі сторінки під node).
⚠️ **Пастка тестів, на яку вже наступили:** зріз «перші N символів таблиці» і
зріз «функція + 2200 символів» ОБИДВА обрізали не там, щойно підказки виросли —
тепер ріжемо по `</thead>` / `</table>` / кінцю функції. І ще: замок «у коді
немає `ensure_fresh`» падав на власному КОМЕНТАРІ, де це слово пояснювалось, —
`_fn_code()` тепер прибирає докстрінг перед перевіркою.

### 🎛 ЧИСТКА ТАБЛИЦІ + 🟪 VOB→УГОДА З МОНІТОРА (15.09)

**Сім вимог користувача одним пакетом** (+ восьма — про ріст сили, нижче).

**1. Фільтри ЗАПАМʼЯТОВУЮТЬСЯ + лічильники стали чесними.**
Скарга: «"Сила ≥" не запамʼятовується… і при "Сила ≥ 50" кількість монет
рахується невірно» (на скріні «🟢 LONG (36)» над таблицею з кількох рядків).
- `_MM_UI_KEY = 'mmMonitorUi'` у localStorage тримає ВСІ ТРИ UI-фільтри: поріг
  сили, вкладку напрямку і сортування. Це вибір ПОКАЗУ, тож localStorage, а не
  БД (той самий принцип, що `wlSortMode` у watchlist). `_mmLoadUi()` кличеться
  РІВНО один раз, із першого `mmApplyState` — так він гарантовано відпрацює ДО
  першого рендера, незалежно від порядку init.
- ⚠️ **Лічильники вкладок ПЕРЕЇХАЛИ на фронт** і рахуються по
  `_mmRowsByStrength()` — рядках, що пройшли фільтр «Сила ≥». Серверне поле
  `counts` **ВИДАЛЕНО**: фільтр живе на фронті, тож серверне число поруч із
  відфільтрованою таблицею означало б, що «(36)» і кількість рядків під ним —
  різні речі. Два джерела одного числа не тримаємо.
- ⚠️ Фільтр НАПРЯМКУ на лічильники НЕ впливає — інакше кожна необрана вкладка
  показувала б (0) і перемкнутись було б неможливо.

**2. Колонку «🧠 Рішення» ПРИБРАНО ПОВНІСТЮ** — разом із налаштуванням
`mm_monitor_decision`, кешем `_mm_decision`, методом `_mm_decisions` і
константами `MM_DECISION_*`. ⚠️ Прибрано САМ РОЗРАХУНОК, а не лише колонку:
це був єдиний важкий показник таблиці. Вердикт Decision лишається там, де й був
— у банері над графіком.

**3. Колонку «Стан» прибрано, а монети В УГОДІ в таблиці не показуються взагалі**
> ⚠️ **СКАСОВАНО 18.09 — див. розділ «📍 МОНЕТИ В РОБОТІ ЛИШАЮТЬСЯ В ТАБЛИЦІ»
> нижче.** Дослівно: «Із таблиці не потрібно видаляти монети що в роботі —
> поверни поле статус в таблицю». Колонка повернулась, рядки не зникають.
(«монета, яка в угоді, не потрібно відображати у таблиці»). Монітор — список
КАНДИДАТІВ. ⚠️ Зі ЗНІМКА такі монети НЕ зникають: знімок живить колонку
«🧮 Старий МММ» у таблицях угод (`mm_snapshot_for`) — фільтр стоїть САМЕ в
`mm_monitor_state`. Поля `in_trade`/`in_queue` з рядка прибрано; `selectable`
тепер означає лише «є напрямок».

**4. «🔮 Прогноз» → ДВІ колонки** «🔮 1H» і «🔮 4H», у кожній лише напрямок
(`1H: 🔴 SHORT`). Відсотки руху й впевненості переїхали в ПІДКАЗКУ — у вузькій
колонці вони не читались, а джерело те саме, що бейдж над графіком.

**5. «Ціна» — в ОДИН рядок:** `1.5 ▲ +0.42%`; вікно спостереження переїхало в
підказку (раніше воно стояло другим рядком і подвоювало висоту рядка).

**6. 🦓 Зебра.** `#mm-table tbody tr:nth-child(even)` + підсвітка рядка під
курсором + 1px-розділювачі. Стиль локальний для ЦІЄЇ таблиці: рядків багато і
вони щільні.

**7. 🟪 НОВИЙ VOB → АВТО-ВІДКРИТТЯ** (`mm_vob_open`, деф. **УВІМК**).
⚠️ **ДЖЕРЕЛО БЛОКУ ЗМІНЕНО 16.09 — див. розділ «🟦 БЛОК БЕРЕ СКАН» нижче**
(власного TF `mm_vob_tf` більше немає). Вимога: «відслідковування і автоматичне відкриття
угоди, коли зʼявляється саме НОВИЙ VOB по монеті, яка є в таблиці; VOB має
співпадати з напрямком, в якому на даний момент знаходиться монета».
`_mm_vob_tick(snap, s, now)` після запису знімка:
- **кандидати = РІВНО рядки таблиці**: є у знімку, мають напрямок, не в угоді;
- блок бере `_funding_vob(sym, side, tf)` — ТОЙ САМИЙ детектор, що вже живить
  funding-стратегію; він віддає найновіший НЕ-breaker OB **потрібного боку**,
  тож «співпадає з напрямком» виконується за побудовою;
- «НОВИЙ» = інший `formation_time`.
- ⚠️ **ПЕРШИЙ ПОКАЗ МОНЕТИ — ТИХА БАЗА.** Після рестарту (а `botupdate`
  роблять часто) перший знайдений блок виглядав би «новим», і бот відкрив би
  угоду по блоку, якому може бути півдня. Той самий урок, що у VOB-алерті.
- ⚠️ **ПОРЦІЯМИ** (`MM_VOB_MAX_PER_TICK = 15`): це ЄДИНЕ місце монітора з
  мережею (200 свічок на монету), і повний watchlist дав би сотні запитів за
  такт. Затримка нічого не губить — блок лишається «новим», доки не
  опрацьований.
- ⚠️ `by_hand` тут **НЕ** ставимо (на відміну від ✋ групового відкриття):
  рішення АВТОМАТИЧНЕ, тож «нова ситуація», запобіжники і 🚦 головні кнопки
  напрямку мусять діяти. Мітка — `_ob_compose('vob_alert','MMM')` =
  **🟪 Volumized OB → 🧮 МММ-монітор**; далі звичайний `_q4_set_vob_sl`.
- ⚠️ Вимкнений тумблер МОНІТОРА теж зупиняє цей шлях: таблиці немає, а знімок
  лишається лише для колонки в угодах — торгувати з невидимого списку не можна.

**8. 📈 ОЧІКУВАННЯ РОСТУ СИЛИ — СКАСОВАНО КОРИСТУВАЧЕМ 16.09 (не повертати!).**
Був гейт: новий VOB відкривав угоду ЛИШЕ коли `sym in self._mm_grow_since`,
інакше блок ставав у чергу `_mm_vob_pending`, у рядок ішло поле `vob_wait`,
фронт малював ⏳ з живим таймером, а в 🧾 Лог писалось «новий Volumized OB …
є, але сила МММ НЕ росте — відкриття ВІДКЛАДЕНО». Користувач СКАСУВАВ це
дослівно: «відміни цю перевірку».
- Прибрано **ПОВНІСТЮ, а не вимкнено**: немає ні `_mm_vob_pending`, ні
  `vob_wait` у рядку, ні `_mmWaitBadge` на сторінці, ні рядка в лозі. Пріоритет
  «монети в очікуванні — першими» теж зник разом із чергою (сортування порцій
  знову просто за `_mm_vob_at`).
- Ріст сили лишився **ПОКАЗНИКОМ** (колонки «Сила росте» + ⏱ таймер) — він
  нічого не блокує. Решта умов входу без змін: напрямок МММ · новий
  `formation_time` · тиха база на першому показі · порції.
- Тест-замки (`test_mm_monitor.py`, розділ 18): плато і навіть СПАД сили
  відкривають угоду; у стані немає `_mm_vob_pending`, у рядку — `vob_wait`, а в
  коді `_mm_vob_tick` — жодної згадки `_mm_grow_since`; у сторінці немає ні
  `_mmWaitBadge`, ні ⏳ у рядку.

Тест: `test_mm_monitor.py` (98). ⚠️ Пастка гармоні: фейковий `<select>`/вкладки
мусять ТРИМАТИ свій span (інакше прочитати назад записаний лічильник
неможливо), а `localStorage` треба стабити — без нього сторінка не стартує.

### 🟦 БЛОК ДЛЯ АВТО-ВІДКРИТТЯ БЕРЕ СКАН «📦 Volumized OB Trend» (16.09)

> 🗑 **СКАСОВАНО 17.09 — VOB-ШЛЯХ МОНІТОРА ВИДАЛЕНО ПОВНІСТЮ.** Дослівно:
> «Забери взагалі із МММ-монітор алгоритм "VOB→сигнал". Скан не потрібний
> — реалізуємо трішки по іншому. Цей алгоритм себе не виправдав.» Текст
> нижче лишено як ІСТОРІЮ рішень (і як опис пасток, які там ловились);
> у коді цього шляху НЕМАЄ — див. розділ «🗑 VOB-ШЛЯХ МОНІТОРА ПРИБРАНО».


**Вимога дослівно:** «Використовуй VOB алгоритм, що на скріні у вкладенні. Має
бути задіяний цей скан VOB» (скрін — блок **Volumized OB Trend** у ⚙️ Settings:
Enable · TF 5M · Swing Length 5 · Zone Invalidation Wick · Max ATR Mult 3.5 ·
Zone Count Low(3) · Combine Zones).

**Що було не так.** `_mm_vob_tick` кликав `_funding_vob` — детектор
funding-стратегії з ЗАХАРДКОДЖЕНИМИ константами (`_VOB_SWING=5`, `Wick`, 3.5,
`Low`, **200 барів**, `combine_obs` не передавався взагалі) і власним TF
`mm_vob_tf`. Тобто монітор торгував блоком, порахованим ІНШИМИ параметрами на
ІНШІЙ глибині, ніж бокс на графіку — рівно та розбіжність, що в кейсі PD-зони.
Плюс 200 барів проти `VOB_KLINES_LIMIT=3000` у сканера (глибину колись і
підняли 400→3000, бо «на частині монет OB не потрапляв у вікно»).

**Тепер джерело ОДНЕ — сканер:**
- **`SMCScanner.volumized_ob_side(symbol, side, allow_fetch=True)`** → найновіший
  НЕ-breaker OB потрібного боку, порахований `volumized_timeframe` /
  `volumized_swing_length` / `volumized_ob_end_method` / `volumized_max_atr_mult` /
  `volumized_zone_count` / `volumized_combine_obs` на `VOB_KLINES_LIMIT` барах.
  Плюс `volumized_tf()` і `has_fresh_vob(symbol)`.
- **`_vob_newest_live(res)`** — ЄДИНЕ правило «найновіший не-breaker по боку»
  (breaker не сигнал: бокс пунктиром із «✕», зона знецінена).
- **Кеш наповнює САМ СКАН** (`_vob_sides_put(symbol, vol_result, vol_tf)` одразу
  після `get_latest_ob_trend`) — розрахунок уже зроблено на тих самих барах,
  тож це коштує НУЛЬ, а монітор у робочому стані **не робить жодного запиту**.
  `VOB_SIDES_TTL = 300с` — це вік РОЗРАХУНКУ, а не свіжість блоку.
- ⚠️ **Фолбеку на власний детектор НЕМАЄ і бути не може.** Немає сканера / стара
  версія файлу без `volumized_ob_side` → шлях не працює, і про це йде **ОДИН**
  рядок у 🧾 Лог (`_mm_vob_scanner`, прапорець `_mm_vob_src_warned`): тихо
  «порахувати самим» означало б повернути торгівлю по невидимому блоку.
- ⚠️ **Тумблер Enable (`use_volumized_ob`) вимкнено → монітор нічого не
  відкриває**: боксів на графіку немає, отже й торгувати нема по чому.
- ⚠️ **ВЛАСНИЙ TF ПРИБРАНО** (`mm_vob_tf` — з `DEFAULT_SETTINGS`, валідації,
  збереження і UI; `MM_VOB_TFS` видалено). Замість випадайки в шапці монітора
  стоїть підпис `параметри з 📦 Volumized OB Trend` (`ff-mm-vob-src`): другий
  набір параметрів = другий «останній блок».
- ⚠️ **Порція `MM_VOB_MAX_PER_TICK` тепер про ХОЛОДНІ монети.** Є свіжий знімок
  скану (`has_fresh_vob`) → читання безкоштовне, бюджет не витрачається і
  монета обробляється ЗАВЖДИ; бюджет їдять лише ті, по яких доводиться рахувати
  самим. Раніше бюджет їли всі підряд → повне коло по 230 монетах ≈ **8 хв**.
  У циклі використано `continue`, а не `break`: після «холодних» у списку
  можуть стояти «теплі», яких обривати нема причини.
- ⚠️ **Після цієї правки `formation_time` інші** (інші параметри → інші блоки),
  тож на першому такті кожна монета мовчки перебазується (тиха база) — сплеску
  відкриттів немає.

Тести: `test_mm_monitor.py` (98) — замки: у `_mm_vob_tick` немає `_funding_vob`/
`detect_volumized_obs`/`fetch_klines`; `volumized_ob_side` читає всі параметри
користувача і `VOB_KLINES_LIMIT` (не 200); скан наповнює кеш; теплі монети
обробляються всі за такт, холодні — не більше `MM_VOB_MAX_PER_TICK`.
⚠️ Пастка замка: докстрінг `_mm_vob_tick` ПОЯСНЮЄ, чому `_funding_vob` прибрано,
тож `_src()` ріже докстрінг перед перевіркою (той самий випадок, що `ensure_fresh`).

⚠️ **ЩЕ НЕ ЗРОБЛЕНО (аналіз 16.09, чекає рішення користувача):** у шляху немає
ГЕЙТА СВІЖОСТІ блоку; `_mm_vob_seen[sym]` — ОДИН watermark на обидва боки (фліп
МММ відкриває по старому блоку протилежного боку) і не список (пастка breaker →
відкриття по СТАРІШОМУ блоку); пропуск монети зі знімка СТИРАЄ базу і ковтає
реально новий блок; стан не персиститься. Усі три перші пункти ВІДТВОРЕНО
прогоном.

### 📨 VOB МОНІТОРА → СИГНАЛ У ЗАГАЛЬНИЙ АЛГОРИТМ (вимога 16.09) + «де сигнали?»

> 🗑 **СКАСОВАНО 17.09 — VOB-ШЛЯХ МОНІТОРА ВИДАЛЕНО ПОВНІСТЮ.** Дослівно:
> «Забери взагалі із МММ-монітор алгоритм "VOB→сигнал". Скан не потрібний
> — реалізуємо трішки по іншому. Цей алгоритм себе не виправдав.» Текст
> нижче лишено як ІСТОРІЮ рішень (і як опис пасток, які там ловились);
> у коді цього шляху НЕМАЄ — див. розділ «🗑 VOB-ШЛЯХ МОНІТОРА ПРИБРАНО».


**Дослівно:** «МММ-монітор при знайденому VOB не відкриває угоду, а передає
монету у вигляді сигналу далі по алгоритму, тобто монета має потрапити у Чергу,
якщо Черга увімкнена».

**Було:** `_mm_vob_open_one` кликав `_open` НАПРЯМУ — тобто повз спільні ворота
сканера І повз черги. Монітор фактично торгував в обхід усього алгоритму.
**Стало:** `_mm_vob_signal_one` — той самий шлях, що у VOB-алерта сканера:
**`scanner._signal_allowed(at_intake=True)` → `tm.on_signal(opened_by='mm_vob')`**
→ `intercept` → черга → двигун. Куди потрапить монета, вирішують ЧЕРГИ; усі
вимкнені → TM відкриє напряму (штатна поведінка `intercept`).
- ⚠️ **Прямого `_open` у цьому шляху БІЛЬШЕ НЕМАЄ.** Він лишився ЛИШЕ у
  ✋ ГРУПОВОМУ відкритті, де рішення ухвалює людина.
- **Новий код сигналу `mm_vob`** (`🧮 VOB з МММ-монітора`) у `signal_labels.py`
  + іконка 🧮 + ОБИДВА JS-дзеркала + `_kind_lbl` у `intercept`. Окремий код, бо
  з `vob_alert` монета в черзі й в угоді не відрізнялась би від Черги-4; двигун
  допише своє → «🧮 VOB з МММ-монітора → 🎯 Черга-4». ⚠️ Усі порівняння `kind`
  у проєкті — на РІВНІСТЬ, тож новий код нічого не зачіпає.
- UI: тумблер перейменовано «🟪 VOB→угода» → **«🧮 VOB→сигнал»** (підказка
  переписана: монітор сам НЕ відкриває).

**🔎 «ДЕ СИГНАЛИ?» — дві причини, обидві виправлені.**
1. **Шлях МОВЧАВ.** Лог писався ЛИШЕ на успішне відкриття; «блок не новий»,
   «ворота не пропустили», «TM недоступний» не лишали ЖОДНОГО сліду, тож в
   експорті логу по монітору були самі ✋ групові відкриття. Тепер **КОЖЕН**
   сигнал іде в 🧾 Лог (`event='signal'`, `source='MMM'`) РАЗОМ із розкладом
   фільтрів, а відмова — окремим `rejected`.
2. **База «опрацьованих блоків» СТИРАЛАСЬ на першому ж пропуску монети.** Було
   «немає у знімку → забути»; монета, що випала на ОДИН такт (liq-map мить не
   віддала стан), поверталась із ЧИСТОЮ базою і наступний — уже РЕАЛЬНО новий —
   блок ковтався як «перший показ». У лімітованому режимі знімок коливається
   постійно, тож монета могла не дати сигналу НІКОЛИ. Тепер позначка живе за
   ЧАСОМ останньої перевірки (`MM_VOB_SEEN_TTL = 6 год`) — той самий прийом, що
   з `_mm_str_hist` («обрізаємо вікном, а не видаляємо»). Відтворено прогоном;
   тест-замок `test_baseline_survives_a_one_tick_dropout`.

### 👁 СКАН VOB МУСИТЬ БУТИ ВИДИМИЙ — колонка стану (скарга 16.09)

> 🗑 **СКАСОВАНО 17.09 — VOB-ШЛЯХ МОНІТОРА ВИДАЛЕНО ПОВНІСТЮ.** Дослівно:
> «Забери взагалі із МММ-монітор алгоритм "VOB→сигнал". Скан не потрібний
> — реалізуємо трішки по іншому. Цей алгоритм себе не виправдав.» Текст
> нижче лишено як ІСТОРІЮ рішень (і як опис пасток, які там ловились);
> у коді цього шляху НЕМАЄ — див. розділ «🗑 VOB-ШЛЯХ МОНІТОРА ПРИБРАНО».


**Дослівно:** «VOB з МММ-монітора будуть скануватись чи ні? Бо зараз я не бачу
жодних ознак на сканування.»

**Скан ІШОВ — не було ОЗНАК.** Поки не зʼявиться НОВИЙ блок, шлях мовчить за
побудовою: «блок той самий», «блоку немає», «поставлено тиху базу» — це СТАНИ, і
в лог їх писати не можна (той самий флуд, через який чистили лог VOB). Тому
стан переїхав туди, де йому місце, — **у таблицю**.

- `self._mm_vob_diag[sym] = {state, ft, tf, ts}` — заповнює `_mm_vob_tick` на
  КОЖНУ перевірену монету; `mm_monitor_state` віддає це полем `vob`.
- Стани: **⛔ off** (блок «📦 Volumized OB Trend» вимкнено — ми не шукаємо) ·
  **∅ none** (блоку в бік МММ немає) · **⏳ base** (перший показ, тиха база) ·
  **👁 same** (той самий блок) · **🆕 signal** (пішов сигнал) · **⚠️ err**.
- Колонка **«👁 VOB»** (9-та) — значок + **живий час від останньої перевірки**
  (`.ff-timer`, той самий 1с-тікер). Саме таймер і є доказом, що скан іде.
- ⚠️ **`off` ≠ `none`.** «Ми не шукаємо» і «блоку немає» показані ПО-РІЗНОМУ:
  друге на місці першого читалось би як «скан зламався». Для цього в сканері
  зʼявився публічний `volumized_on()` — читати чужий `_settings` не можна.
- ⚠️ У сигнатурі таблиці — лише **СТАН**, не час: інакше DOM перебудовувався б
  щотакту (урок `held_sec`).
- ⚠️ Кількість колонок тепер звіряється зі **СКЛАДОМ** (`_cols`), а не з
  магічним числом, і очікувану кількість `<td>` JS-тест бере з ЖИВОГО
  заголовка: на «8→9» уже наступили двічі. `colspan` порожнього рядка — 9.

⚠️ **Пастка стабу (втретє!):** `_mk()` не мав `_mm_vob_diag`, і `_mm_vob_tick`
падав з AttributeError на ПЕРШІЙ монеті — `_mm_capture` його ковтав, тести
«нічого не робили». Нове поле стану ЗАВЖДИ додавати у `_mk`.

### 🕐 ВЛАСНИЙ TF МОНІТОРА + «СКАНУЄМО ВСІ, КРІМ ВІДФІЛЬТРОВАНИХ» (16.09)

> 🗑 **СКАСОВАНО 17.09 — VOB-ШЛЯХ МОНІТОРА ВИДАЛЕНО ПОВНІСТЮ.** Дослівно:
> «Забери взагалі із МММ-монітор алгоритм "VOB→сигнал". Скан не потрібний
> — реалізуємо трішки по іншому. Цей алгоритм себе не виправдав.» Текст
> нижче лишено як ІСТОРІЮ рішень (і як опис пасток, які там ловились);
> у коді цього шляху НЕМАЄ — див. розділ «🗑 VOB-ШЛЯХ МОНІТОРА ПРИБРАНО».


**Дослівно:** «додай сюди вибір таймфрейму, щоб МММ-монітор мав свою можливість
коригувати метод сканування. Скануватись мають всі монети із таблиці
МММ-монітор, за винятком відфільтрованих.»

⚠️ **ЦЕ ЧАСТКОВО ВІДМІНЯЄ «ВЛАСНИЙ TF ПРИБРАНО» з розділу вище** — але лише
щодо ТАЙМФРЕЙМУ. Причина того видалення (два набори ПАРАМЕТРІВ = два різні
«останні блоки») лишається чинною і сьогодні.

**🕐 `mm_vob_tf` (деф. `''` = «як у скану»).**
- `SMCScanner.volumized_ob_side(..., tf=None)` і `has_fresh_vob(symbol, tf=None)`
  приймають TF; **кеш тепер ключований `(символ, TF)`** — без TF у ключі читач
  отримав би блоки чужого таймфрейму (та сама підміна, що в уроці PD-зони).
- ⚠️ **ЗАМІНЮЄТЬСЯ РІВНО СВІЧКА.** Swing Length / Zone Invalidation / Max ATR /
  Zone Count / Combine і далі беруться з «📦 Volumized OB Trend». Тест-замок
  `test_only_the_timeframe_is_ours_the_method_stays_shared` не дає завести в
  `_mm_vob_tick` жодного власного параметра методу.
- ⚠️ **ЦІНА ВИБОРУ НАЗВАНА ВГОЛОС.** «Як у скану» = читання вже порахованого
  знімка, **нуль запитів**. Свій TF = кеш скану не про нього → КОЖНА монета
  «холодна» і тягне `VOB_KLINES_LIMIT` (3000) барів ≈ 3 запити.
  `_mmVobCostHint` малює це живим рядком біля випадайки (`· 12 монет ×
  ~3 запити`) — той самий принцип, що `_liqCostHint` на 📡 Tickr.
- ⚠️ Старіший сканер без параметра `tf`: при `''` мовчки працюємо як раніше, а
  при ОБРАНОМУ TF шлях чесно падає в `err` — взяти чужий масштаб мовчки означало
  б торгувати не тим, що просили.

**⚑ ФІЛЬТР «Сила ≥» ТЕПЕР КЕРУЄ І СКАНУВАННЯМ** (`mm_str_min`).
- Поріг **ПЕРЕЇХАВ із localStorage у налаштування бота**: число з одного
  браузера не може вирішувати, що робить бот. У localStorage лишились вкладка і
  сортування — це чистий показ. Є **разовий переїзд** старого локального
  значення (`_mmStrLocal`), інакше збережене «Сила ≥ 50» мовчки обнулилось би
  після деплою — рівно та скарга, через яку поріг колись і почали зберігати.
- Кандидати `_mm_vob_tick` = рядки таблиці: напрямок є · не в угоді ·
  **сила ≥ порогу**. Питати біржу про монету, якої на екрані немає, — марна
  робота (і саме вона з’їдала бюджет).
- Фільтр НАПРЯМКУ (вкладки) на скан НЕ впливає: це вибір перегляду, монета в
  необраній вкладці лишається живим кандидатом.

**♾ БЮДЖЕТ ЗА ТАКТ — `mm_vob_max_per_tick`, деф. `MM_VOB_MAX_PER_TICK = 0` =
БЕЗ ЛІМІТУ** (дослівна вимога «скануватись мають ВСІ монети»). Поле лишилось
ЗАПОБІЖНИКОМ на випадок «Сила ≥ 0» по всьому watchlist зі своїм TF.
⚠️ Спрацювання більше НЕ мовчазне: монета, що не влізла, отримує стан
**⏸ wait** у колонці 👁 VOB. Раніше вона просто зникала з черги — і це читалось
як «скан не працює» (та сама скарга, що й учора).

⚠️ **Пастки, на які наступили в цій же правці:**
- стаб сканера в тестах мусить мати **ту саму сигнатуру**, що реальний
  (`tf=None`, `volumized_on`) — інакше виклик падає в `except TypeError`, шлях
  тихо не працює і тест «нічого не робить»;
- у JS для node **немає `window`** — разовий переїзд порогу тримаємо у звичайних
  змінних модуля, а не в `window.*`;
- новий id (`ff-mm-vob-cost`, `ff-mm-vob-tf`) ЗАВЖДИ додавати в білий список
  фейкового DOM (утретє).

Тести: `test_mm_monitor.py` (98).

### 🗑 VOB-ШЛЯХ МОНІТОРА ПРИБРАНО ПОВНІСТЮ (вимога 17.09)

**Дослівно:** «Забери взагалі із МММ-монітор алгоритм "VOB→сигнал… параметри з
📦 Volumized OB Trend… як у скану". Скан не потрібний — реалізуємо трішки по
іншому. Цей алгоритм себе не виправдав.»

Видалено, а не вимкнено (інакше збережений у БД тумблер лишався б живим):
- `fuel_filter`: `_mm_vob_tick` · `_mm_vob_signal_one` · `_mm_vob_scanner` ·
  стан `_mm_vob_seen/_at/_diag` · константи `MM_VOB_MAX_PER_TICK` /
  `MM_VOB_SEEN_TTL` / `MM_VOB_TFS` · ключі `mm_vob_open` / `mm_vob_tf` /
  `mm_vob_max_per_tick` (з дефолтів і валідації) · поле `vob` у рядку і
  `vob_tf`/`vob_tf_scan`/`vob_cap` у стані;
- `smc_scanner`: `volumized_ob_side` · `has_fresh_vob` · `volumized_on` ·
  `volumized_tf` · `_vob_newest_live` · `_vob_sides_put` · `VOB_SIDES_TTL` і
  виклик наповнення кешу зі `_scan`. **Ніхто інший їх не читав** — перевірено
  по всьому репозиторію перед видаленням;
- UI: тумблер «🧮 VOB→сигнал», підпис «параметри з 📦 Volumized OB Trend»,
  випадайка TF, підказка вартості, колонка «👁 VOB» (таблиця знову **8**
  колонок, `colspan` 8).

⚠️ **КОД СИГНАЛУ `mm_vob` У МІТКАХ ЛИШАЄТЬСЯ** (`signal_labels.py` + обидва
JS-дзеркала + `_kind_lbl`). Угоди й рядки логу, уже відкриті тим шляхом, мусять
далі малювати свій бейдж: прибрати мітку означало б заднім числом зіпсувати
історію. Тест-замок: `test_the_old_trade_label_survives_the_removal`.
⚠️ Поріг **«Сила ≥» (`mm_str_min`) ЛИШИВСЯ в налаштуваннях бота** (він переїхав
туди 16.09). Тепер це знову ЧИСТО фільтр показу, але повертати його в
localStorage не стали: одне число на всі браузери — менше сюрпризів.

### ⚖️ БАНЕР «🧮 МММ-МОНІТОР» — ВАЖІЛЬ ЗА ВЕЛИЧИНОЮ ВІДСОТКІВ (вимога 17.09)

**Дослівно:** «Зроби банер типу ₿ BTCUSDT… поки що лише вигляд такий зроби як
на скріні. Не потрібно повторювати алгоритм ₿ BTCUSDT. Для початку лише вигляд
такий і таймер» · «За основу банера візьми показники LONG і SHORT із
МММ-монітор. Але не просто визначай загальний відсоток LONG і SHORT, а
враховуй які відсотки мають монети, низькі чи високі — бери це до уваги і
розраховуй важіль в напрямку за рахунок величини відсотків по кожній монеті.»

**ФОРМУЛА — `_mm_track_bias(snap, now)`** (рахує ДВИГУН у `_mm_capture`,
`mm_monitor_state` лише ЧИТАЄ поле `bias` — той самий урок B2, що з шарами Q4):

    важіль = (Σ сила LONG − Σ сила SHORT) / Σ сила ВСІХ монет монітора

- це **НЕ «скільки монет у кожен бік»**: одна монета з 90% переважує дві по
  15% (тест-замок `test_the_banner_weighs_strength_not_headcount`);
- ⚠️ **⚖ РІВНОВАЖНІ МОНЕТИ СТОЯТЬ У ЗНАМЕННИКУ.** Вони нікуди не тягнуть, але
  лишаються МАСОЮ на терезах: без них одна монета з 12% при двадцяти
  безнапрямкових дала б «100% LONG» — банер кричав би там, де ринок якраз НЕ
  визначився;
- поріг напрямку — **`MM_BIAS_FLAT = 0.10`**, ТЕ САМЕ число, за яким комірка
  МММ пише «⚖ рівновага» (інакше банер казав би LONG над таблицею, де в
  кожному рядку рівновага);
- **монети в угоді не рахуються** — рівно як і в таблиці: банер описує те, що
  людина під ним бачить;
- сила береться **з того самого знімка**, що малює рядки, тож банер і колонка
  «🧮 Старий МММ» розійтись не можуть.

⚠️ **Фільтр «Сила ≥» на банер НЕ впливає** — він ховає рядки, а важіль описує
ринок; до того ж слабкі монети й так важать мало за самою формулою. Це сказано
в підказці обох контролів, інакше виглядало б як розбіжність.

**⏱ ТАЙМЕР** — `_mm_bias_since`: момент, ВІДКОЛИ тримається поточний стан;
зміна напрямку (зокрема в рівновагу і назад) перезапускає. Час іде з СЕРВЕРА,
тож перезавантаження сторінки його не обнуляє.

**🎨 ВИГЛЯД — 1-в-1 з ₿ BTCUSDT, і саме тому СПІЛЬНІ хелпери.** `mmRenderBias`
малює через `dirGrad` / `dirTint` / `fuelBand` / `flipTimerHTML` — ті самі, що
банер ₿; свої копії розійшлися б у кольорах і словах. Ті самі відступи, висота
смуги 18px, підпис «N% <смуга сили>» праворуч у смузі, таймер у комірках цифр,
бейдж статусу 🟢 LONG / 🔴 SHORT / ⏸ FLAT. Без напрямку смуга **сіра**, а
таймера немає взагалі.
⚠️ **Другого `setInterval` не заводимо** — банер підганяє ТОЙ САМИЙ глобальний
1с-тікер сторінки (він же веде `.ff-timer` і ₿). Окремий інтервал у node-тестах
ще й не дає процесу завершитись (наступили одразу).
⚠️ Розклад (скільки монет і скільки ваги з кожного боку) — у підказці банера:
без нього «62% LONG» немає з чим звірити.
⚠️ Вимкнений монітор ГАСИТЬ банер разом зі знімком: застиглі числа виглядають
як живі.
⚠️ Алгоритм ₿ (сеанси, пауза, START/STOP, фліп-чистка черги) тут свідомо НЕ
повторюється — вимога була саме про вигляд і таймер.

Тести: `test_mm_monitor.py` (98) — розділи 17 (замки на видалення) і 18 (важіль).
⚠️ Пастки цієї правки: новий id ЗАВЖДИ додавати в білий список фейкового DOM
(вчетверте); спільні хелпери вигляду лежать ПОЗА зрізом JS, тож `_run_js`
тепер підкладає їх із того самого файлу; таймер порівнювати за ТЕКСТОМ без
розмітки (цифри розкидані по `<span class="fd">`).

### 🧭 TICKR: «за близькістю магніту» → ЗВІТ «КУДИ ТЯГНЕ ЛІКВІДНІСТЬ» (16.09)

**Дослівно:** «Заміни відбір монет "за близькістю магніту" на звіт по монетах,
які покажуть "▲ Маса ліквідності ВИЩЕ ціни 62.3% · тягне ВГОРУ" або навпаки».

- Опція `near` у `liq-sort` **ЗАМІНЕНА** на `pull_report` («🧭 куди тягне
  (звіт)»). Порядок той самий, що у `pull` (сильніший перекіс вище) —
  відрізняється ПОДАННЯ: замість колонок магнітів іде **рядок-вердикт**
  `ladder.make_verdict` по кожній монеті.
- **`_lqVerdictHTML(parts, text, tone, withMagnet)`** — ЄДИНИЙ рендер вердикту
  на сторінці: ним малює і режим однієї монети, і рядок звіту. Дві копії тієї
  самої розмітки розійшлись би (той самий принцип, що `verdict.parts`).
- `_lqApplyReportCols(on)` **ХОВАЄ** колонки ⬆/⬇ · Перекіс · магніти, а «Тягне»
  перейменовує на «Куди тягне ліквідність». Ховаємо, а не лишаємо порожніми:
  стовпчик-привид читається як «дані загубились».
- ⚠️ **Прибрано РЕЖИМ, а не дані:** поля `near_*` лишились і живлять колонку
  «Найближчий» у звичайній таблиці — це інше питання («що спрацює першим»),
  ніж «куди тягне». Тест-замок стежить за обома половинами.
- ⚠️ Невідомий/старий ключ сортування (`near` зі збереженого UI) НЕ падає і не
  дає випадкового порядку — мовчки повертається до перекосу.

⚠️ **Пастка, через яку три нові тести НЕ ЗАПУСКАЛИСЬ:** у `test_liq_scan.py`
`__main__` містив РУЧНИЙ перелік викликів, і доданий тест просто не виконувався
— мовчки. Тепер раннер збирає всі `test_*` сам (37).

### ⚑ «СКІЛЬКИ У ФІЛЬТРІ, А СКІЛЬКИ ПОЗА» (вимога 16.09)

**Дослівно:** «Коли увімкнена фільтр "Сила ≥" — незрозуміло що з іншою
кількістю монет. Потрібно розуміння, скільки у фільтрі, а скільки поза».
«Всі (6)» без другого числа читається як «у боті всього 6 монет».
- Поряд із полем «Сила ≥» — бурштиновий `#mm-str-out`: **`⚑ поза фільтром 17 з 23`**
  (у підказці — що це НЕ «бот їх не бачить», і як побачити всі).
- У підказці КОЖНОЇ вкладки — свій розклад: `у фільтрі N · поза фільтром M`.
- ⚠️ Саме число на вкладці лишилось **ВИДИМИМ** (тобто після фільтра) — воно
  мусить збігатися з кількістю рядків під ним (скарга 15.09).
- ⚠️ Нічого не відсіяно → напису НЕМАЄ: «поза фільтром 0» — шум.

Тести: `test_mm_monitor.py` (98).
⚠️ Пастка, на яку наступили ВТРЕТЄ: `test_selection_survives_the_poll` різав
`mmRender` ФІКСОВАНИМИ 4000 символами — нові рядки лічильників виштовхнули
перевірку за межу. Ріжемо по кінцю функції (`function mmApplyState(`).

### 🧮 КАРТИНКА УГОДИ МОНІТОРА = ДВИГУН, а не сигнал (вимога 15.09)

**Дослівно:** «🟪 заміни на 🧮, щоб видно було, що ця угода із "🧮 МММ-монітор"».

Угоди монітора несуть ТОЙ САМИЙ код сигналу, що й угоди Черги-4
(`vob_alert → MMM` проти `vob_alert → Q4`), тож іконка біля назви монети —
🟪 — їх НЕ РОЗРІЗНЯЛА. Відрізняє їх саме ДВИГУН.

- **`signal_labels.ENGINE_ICON_OVERRIDE = {'MMM': '🧮'}`** — двигун, чия
  картинка ПЕРЕКРИВАЄ картинку сигналу. Плюс `engine_code_of(raw)` і
  **`icon_of(raw)`** — єдине правило вибору картинки.
- ⚠️ **Змінився РІВНО показ.** Повна мітка в підказці лишилась
  «🟪 Volumized OB → 🧮 МММ-монітор», `signal_code_of` і далі дає `vob_alert`,
  тож логіка (substring-перевірки, порівняння кодів) не зрушила.
- ⚠️ **Діє на ОБИДВА шляхи монітора:** 🟪 авто-VOB (`vob_alert → MMM`) і
  ✋ групове відкриття (`manual → MMM`) — обидва тепер 🧮. Решта двигунів
  (Q1-Q4, POC, EXH) картинку сигналу НЕ міняють.
- ⚠️ **Одиничний код — це СИГНАЛ, а не двигун** (`engine_code_of('vob_alert')
  == ''`): записи черг несуть лише `kind`, і без цього сигнал «перекривав» би
  сам себе.
- ⚠️ **Іконки малює ФРОНТ**, тому картинку тепер стереже тест-замок: мапи
  `SIGNAL_ICON_JS` / `ENGINE_ICON_OVERRIDE_JS` в ОБОХ дзеркалах
  (`templates/smart_money.html`, `infosite/app.js`) звіряються з Python, і
  перевіряється ПОРЯДОК у `signalIconHtml` (двигун → потім сигнал; інакше 🧮
  ніколи не переміг би 🟪).
Тест: `test_signal_labels.py` (18).

## 🚦 ГОЛОВНІ КНОПКИ НАПРЯМКУ (Trade Direction) — ЄДИНІ ВОРОТА (не зламати!)

**Вимога користувача дослівно:** «Це головні кнопки бота. Тобто якщо вони
вимкнені, це має означати, що бот взагалі не сканує і не відправляє сигнали,
відповідно, якщо увімкнені обидві кнопки, бот працює у всіх напрямках LONG і
SHORT. Бо працює в тому напрямку, в якому є ввімкненою кнопка.»

**Аудит 08.09: кнопки НЕ ДІЯЛИ на робочій установці ВЗАГАЛІ.** Вони пишуть
`allow_long_entries`/`allow_short_entries` у налаштування TM, а перевірялись у
чотирьох місцях — і жодне не стояло на робочому шляху:
1. **сканер** — не знав про них ЖОДНОГО слова (нуль згадок ключів у файлі);
2. **`intercept`** — пускав у чергу БУДЬ-ЯКИЙ напрямок;
3. **двигун Черги-4** — свідомо їх не дивився (у докстрінгу: «НЕ фільтрує
   кнопками LONG/SHORT»);
4. **`trade_manager._open_position`/`_open_shadow`** — перевірка БУЛА, але під
   `if not bypass_gates`, а Fuel Filter відкриває саме через
   `manual_open(..., bypass_gates=True)` → кожна угода з КОЖНОЇ черги проходила
   повз вимикач.
Разом: з вимкненим LONG бот однаково відкривав LONG.

**`detection/direction_gate.py` — ЄДИНЕ ДЖЕРЕЛО.** Ключі й розбір живуть ЛИШЕ
тут: `gates_from` · `allows` · `both_off` · `reason` · `chip` · `state_label` +
живі читачі `live_settings`/`live_gates`/`live_allows`/`live_reason`.
⚠️ Літерали ключів дозволені у ТРЬОХ файлах: `direction_gate.py` (визначення),
`trade_manager.py` (DEFAULT_SETTINGS + білий список полів) і `auto_gate.py`
(він їх ПИШЕ). Тест-замок `test_single_source_of_keys` обходить увесь репозиторій
і не дає ключам знову розповзтися — саме розповзання й було коренем дефекту.

**Ворота стоять на ЧОТИРЬОХ вузлах (кожен закритий тестом):**
- **`smc_scanner._signal_allowed`** — **НАЙПЕРШИМИ**, до OB/PD/Forecast/
  Decision/POC/Ліквідності. Заблоковано → `return` ОДРАЗУ (решта фільтрів
  ходить у БД/кеші, рахувати їх ні до чого).
- **`fuel_filter.intercept`** — ДО розбору черг: сигнал вимкненого напрямку в
  чергу не потрапляє взагалі.
- **`fuel_filter._open`** — ЄДИНИЙ вузол відкриття всіх черг. Потрібен окремо,
  бо запис міг лягти в чергу ЩЕ ДО того, як кнопку вимкнули.
- **`trade_manager._open_position`/`_open_shadow`** — останній замок, тепер
  **поза `bypass_gates`**.

⚠️ **`bypass_gates` більше НЕ знімає головні кнопки, але лишається чинним для
РЕШТИ воріт** (FF-підтвердження тощо). Прибрати його цілком було б іншим багом.
Тест-замки: `test_bypass_gates_no_longer_defeats_the_master_switch` +
`test_bypass_gates_still_governs_the_other_gates`.

⚠️ **`intercept` віддає ОКРЕМЕ `'blocked_dir'`, а не `''` і не `'dropped'`.**
Порожній рядок означає «жодна черга не взяла — відкривай НАПРЯМУ», тобто сигнал
пішов би повз кнопки просто у відкриття; `'dropped'` у двох місцях TM жорстко
підписаний «Черга-2 відкинула: CTR-нахил» — довелось би брехати про причину.
⚠️ **Обидва місця виклику `intercept` у TM мусять обробити `blocked_dir`
ЯВНО**: після `if _disp == 'dropped'` перевірки на `''` немає, тож незнайоме
значення провалилось би прямо у пряме відкриття.

⚠️ **✋ РУЧНЕ ці ворота НЕ обходить** — на відміну від «нової ситуації» і
блокування після ручного закриття. Це рішення користувача («головні кнопки») і
рівно те, що обіцяє підказка самої кнопки: «включно з ручним відкриттям».
Стосується і ✋ ручного сигналу, і ✋ відкриття з Черги-4.

⚠️ **FAIL-OPEN, коли стан невідомий** (TM не піднявся, помилка читання) —
свідомо: ці ворота вміють ЗУПИНИТИ торгівлю повністю, тож збій читання не має
ставати тихою зупинкою бота. Дефолт обох ключів — `True` (стара БД без ключів
не зупиняє бота).

⚠️ **«Не сканує» = не породжує СИГНАЛІВ.** Потік даних скану (ціни, `sob_smc_ob_state`,
PD, VOB-стан) НЕ зупиняється, і це навмисно: з нього живляться ЗАКРИТТЯ
відкритих позицій (`tm._get_current_price` бере ціну з кешу сканера), правило
Opposite-OB Exit і джерела Manual SL. Зупинка скану зламала б супровід угод —
а підказка кнопки прямо обіцяє, що закриття працює як зазвичай.

⚠️ **Черга-4 при вимкненій кнопці ПРОПУСКАЄ запис, а не виселяє його.** Кнопку
можна увімкнути назад — на відміну від «відпрацьованого» запису, цей ще
актуальний. Виселяють його звичайні виходи: TTL / застій / 🚨 hard-max.

⚠️ **`detection/auto_gate.py` ПИШЕ ці самі ключі** (`sm_auto_gate_enabled`,
дефолт **false**). Тепер це набагато вагоміше: у режимі `simple` вердикт **WAIT
→ обидві кнопки False → бот стоїть повністю**. Раніше на Черзі-4 це не давало
майже нічого. Вмикаючи Auto-gate, треба розуміти, що він може зупинити бота.

⚠️ **Урок про імпорт:** будь-який `from detection import X` виконує
`detection/__init__.py`, а той тягне `sleeper_scanner → core → pybit`, тобто
півпроєкту. Сканер навмисно вміє вантажитись САМОСТІЙНО (так його беруть
ізольовані тести), тож пакетний імпорт у `_signal_allowed` зламав ДВА чужі
тести на рівному місці. Фікс — `_dg_mod()`: звичайний імпорт, а при невдачі
завантаження сусіднього файлу напряму, з кешем у `_DG_CACHE`.

**UI:** підказки обох кнопок переписані під реальну поведінку; поряд стоїть
`#sm-side-gates-state` — 🚦 «лише SHORT — сигнали LONG відхиляються» (бурштин)
або ⏸ «ПАУЗА — сигнали не приймаються в жодному напрямку» (червоний). При обох
увімкнених підпис прихований (вимикач нейтральний → не шумимо).
**Розклад у 🧾 Лозі** несе `Напрямок[LONG]:✗` — але ЛИШЕ коли хоч одна кнопка
вимкнена (`direction_gate.chip`), інакше сегмент з'являвся б у кожному рядку.
Тест: `test_direction_master_gate.py` (22).

## 🎯 РІВНІ УГОДИ (SL · TP-1 · TP-2) — ГЛОБАЛЬНІ, ПОЗА ЧЕРГАМИ (не зламати!)

**Вимога користувача (08.09), дослівно:** «Винеси за межі Черга-4 автоматичний
SL і TP, тобто "🛑 SL з", "📐 Мін. R на відкриття ≥" … виділи ці параметри, щоб
їх було видно. Це має спрацьовувати навіть, коли всі черги вимкнені. Сигнал
відразу летів на відкриття угоди і присвоювались йому автоматично SL і TP.»
Плюс: «Які параметри застосовуються для визначення Manual TP-2, винеси їх
також для налаштування.»

**Що було не так — рівні були прив'язані до ЧЕРГИ, а не до бота:**
1. **`🛑 SL з`** (`queue4_sl_source`) у `trade_manager._auto_ob_manual_sl`
   застосовувався ЛИШЕ коли `opened_by` містив `'Q4'`. На прямому відкритті
   (усі черги вимкнені) налаштування мовчки не діяло — стоп брався з
   `q2_auto_ob_sl_tf` (деф. 15m). Одне поле давало РІЗНИЙ стоп залежно від
   того, хто відкрив угоду.
2. **`📐 Мін. R`** і **фіксація ЦІЛІ (= Manual TP-2)** жили ТІЛЬКИ в двигуні
   Черги-4. При вимкнених чергах сигнал ішов повз них прямо у відкриття: R
   ніхто не рахував, 🧲 магніт ціллю не ставав.
3. **`pilot_tp2_from_magnet`** — головний перемикач TP-2 — **не мав UI ВЗАГАЛІ**
   (0 згадок у шаблоні): змінити його можна було лише через API.
4. **Вікно TP-1** (`tp1_min_path_pct`/`tp1_max_path_pct`) і **похідний фолбек**
   (`tp1_fallback_path_pct`) існували в `trade_pilot.DEFAULTS`, але не мали
   ключів у налаштуваннях TM. `cfg` збирається як `{k[6:]: … if k.startswith
   ('pilot_') and k[6:] in trade_pilot.DEFAULTS}` — тож 30/75/50 було
   ЗАМУРОВАНЕ в коді. Кейс WIFUSDT: ціль +2.23% → вікно 30-75% дало смугу
   шириною ~1%, жоден обʼєкт туди не потрапив, і TP-1 щоразу ставав похідним
   «50% шляху».

**Зроблено:**
- **`fuel_filter.open_plan(symbol, side)` → `(r, detail, objective)`** і
  **`min_open_r()`** — ПУБЛІЧНІ двері до ТОГО САМОГО розрахунку. `open_plan`
  ДЕЛЕГУЄ в `_q4_expected_r`, другої реалізації немає (інакше гейт і угода
  судились би різними числами — урок VETUSDT). Ціль віддається `pop`-ом:
  разова передача, не кеш. ⚠️ Жодних `enabled`/`queue*_enabled` усередині —
  рівні до черг стосунку не мають (тест-замок).
- **`trade_manager.on_signal`** — ГЛОБАЛЬНІ ворота рівнів ПЕРЕД обома треками
  (real і paper): рахує R, застосовує поріг і кладе ціль через
  `set_pending_objective`. Сюди доходять лише ПРЯМІ відкриття (Q4-сигнал
  повертається раніше з `'queued'`), тож подвійного розрахунку немає.
  ⚠️ Ціль кладеться ЗАВЖДИ, навіть коли поріг вимкнено: вибір цілі не має
  залежати від того, чи судимо ми угоду за R.
  ⚠️ Помилка гейта НЕ блокує (fail-open) — вигаданої відмови не даємо.
  ⚠️ На дублікаті (позиція того самого боку вже є) розрахунок пропускається:
  магніт — це запит до біржі, а `_pilot_context` важкий.
- **`_auto_ob_manual_sl`** — обране `🛑 SL з` тепер ПЕРШЕ в ланцюгу для
  БУДЬ-ЯКОЇ угоди; `q2_auto_ob_sl_tf` («OB TF») став ФОЛБЕКОМ; гарантія
  «% від входу» лишилась останнім кроком.
- **UI:** «🛑 SL з» і «📐 Мін. R» ПЕРЕНЕСЕНО з гармошки Черги-4 у ВЛАСНУ
  секцію верхнього рівня **«🎯 РІВНІ УГОДИ: SL · TP-1 · TP-2»** разом із:
  🧲 `pilot_tp2_from_magnet` (уперше видимий), біржа+глибина магніту, вікно
  TP-1 і фолбек TP-1.
  ⚠️ **ІСТОРІЯ ПЕРЕНОСІВ — щоб не робити цього вп'яте.** Блок мандрував
  ЧОТИРИ рази, і кожен крок був відповіддю на реальну скаргу:
  1) гармошка **Черги-4** — виглядав як ЇЇ налаштування, а при вимкнених
     чергах не діяв узагалі;
  2) усередині **«🛡 Авто-SL та ворота відкриття»** — «Де ці параметри?»:
     опинявся ПІД довгим рядом інших полів;
  3) **першою секцією** — але це ВСЕ ЩЕ був блок Черг;
  4) **⇒ ТЕПЕР:** власна секція `togglePanel('levels')`
     (`#levels-body`/`#levels-caret`, у `_PANEL_IDS`) **ПОЗА чергами,
     безпосередньо ПЕРЕД 💼 Trade Manager** (вимога 09.09).
  Тест-замки: `test_controls_are_in_their_own_top_level_section`,
  `test_levels_section_sits_right_before_trade_manager` (перевіряє і «після
  всіх черг», і «нічого стороннього між секцією та Trade Manager»).

⚠️ **«📐 Мін. R на відкриття» ПОВЕРНУТО в налаштування Черги-4** (окреме
рішення користувача 09.09). Переїхало ЛИШЕ ПОЛЕ: ключ `queue4_min_rr` і його
**глобальна дія** (гейт рахується і на ПРЯМОМУ відкритті через
`on_signal` → `ff.min_open_r()`) НЕ змінились. У тултипі поля це сказано
прямо, інакше повернулась би та сама плутанина «лежить у Черзі-4 → отже діє
лише там». Тест-замок `test_min_r_went_back_to_queue4_settings` стереже
ОБИДВІ половини: поле в Q4, гейт глобальний.

⚠️ **🛑 ТУМБЛЕР «SL з» (`sl_source_enabled`, деф. УВІМК)** — вимога
користувача «додай тумблер для можливості вимкнути при нагоді».
**ВИМКНЕНО → вибір не нав'язується НІКОМУ:** `_q4_set_vob_sl` стоп не ставить
взагалі (пише це в 🧾 Лог), а `_auto_ob_manual_sl` іде звичайним ланцюгом
`OB TF → ★TF → Volumized → % від входу` — тобто рівно та поведінка, що була
до глобалізації вибору.
⚠️ **ОДНА поведінка на ВСІ шляхи.** Гасити лише половину (лишити Черзі-4 її
джерело) означало б повернути саме ту розбіжність, через яку вибір і став
глобальним: одне поле — різний стоп залежно від того, хто відкрив угоду.
⚠️ Угода НЕ лишається без стопа: гарантію тримає «🛡 Стоп ЗАВЖДИ» (% від входу).
⚠️ Читач ОДИН — `fuel_filter.sl_source_on(s)`; TM читає той самий ключ зі
знімка FF-налаштувань, які він уже тягне.
⚠️ UI: вимкнений тумблер **ГАСИТЬ випадайку** (`_ffLevelsSync()`, `disabled` +
`opacity`) — активний контрол, що ні на що не впливає, це та сама вада, що з
полями TTL при ♾ «Без терміну».

⚠️ **КЛЮЧІ НЕ ПЕРЕЙМЕНОВАНІ** (`queue4_sl_source`, `queue4_min_rr`) —
перейменування зламало б збережені налаштування. Той самий прецедент, що з
`q2_auto_ob_sl*`: **історична назва ключа ≠ область дії**.

⚠️ **ЗМІНА РИЗИКУ, про яку треба знати.** Не-Q4 угоди тепер беруть блок
з обраного джерела (деф. **1H OB**) замість `q2_auto_ob_sl_tf` (15m), тобто
стоп зазвичай ДАЛІ від входу. Регулюється «🎚 Стеля SL» або вибором
«15m Volumized OB». Старий тест `test_non_q4_trade_keeps_its_own_tf` фіксував
саме стару поведінку — його замінено на
`test_every_trade_now_uses_the_chosen_source` із поясненням.

⚠️ **Біржа/глибина магніту — ОДИН ключ, ДВА місця показу.** `ff-mag-exchange`/
`ff-mag-bars` пишуть ТІ САМІ `liq_filter_exchange`/`liq_filter_bars`, що й
секція 💧 Liquidity Filter; `_syncMagnetControls()` тримає обидва набори
однаковими. Окремих `magnet_*`-ключів НЕ заводити — поріг фільтра і ціль угоди
мусять рахуватись з одних даних (урок PD-зони). Тест-замок це стереже.

⚠️ **`_numOr(id, d)` замість `parseFloat(v) || d`** для полів рівнів: **0 —
ЗМІСТОВНЕ значення** («фолбек TP-1 = не ставити»), а `||` мовчки перетворив би
його на дефолт 50 і вимкнути фолбек було б неможливо.

⚠️ **Вікно TP-1 не може вивернутись:** `max < min` = порожнє вікно, TP-1 не
знайдеться НІКОЛИ і мовчки — валідація зводить межі до коректного порядку.

⚠️ **ЗАЛИШОК (не виправляв — політика ризику).** Буфер стопа різний за
походженням: пряме відкриття бере `q2_auto_ob_sl_buffer_pct` (0.2%), Черга-4 —
`queue3_vob_sl_buffer_pct` (0.10%, лежить у налаштуваннях **Черги-3**). Обидва
підписані в UI. Уніфікація змінила б чинний ризик — рішення за користувачем.
Тест: `test_global_levels.py` (21).

## ✋ ЗНЯТИЙ Manual TP-2 = ЗНЯТИЙ АВТО-ВИХІД (кейс 17.09 — не зламати!)

**Скарга дослівно:** «Чому, коли я вручну видаляю Manual TP-1 Manual TP-2, вони
все одно спрацьовують?»

**Діагноз: поле очищалось ПРАВИЛЬНО.** `update_manual_sl_tp` на `''` → `0` →
`'clear'` чесно робить `pos.pop('manual_tp')` + ставить `pilot_tp_cleared`, а
`_check_manual_sl_tp` без ключа мовчить. Угоду закривали **ДВА ІНШІ шляхи на
ТІЙ САМІЙ ціні**, і саме тому це читалось як «видалений рівень спрацював»:
1. **🎯 Стратегічний TP** (`use_tp` + `tp_pct`, деф. 5%) — `tp_price`,
   порахований ПРИ ВІДКРИТТІ. Причина закриття «🎯 Take Profit».
2. **🎯 Автопілот** — **TP-2 і Є його ЦІЛЛЮ** (🧲 магніт), тож `act='take'`
   фіксував результат на тій самій ціні з причиною `pilot_target`.
3. **🏦 А в РЕАЛЬНІЙ книзі тейк ще й стоїть НА БІРЖІ** (`place_order(
   take_profit=tp_price)`) — і скасувати його було **ФІЗИЧНО НЕМОЖЛИВО**:
   `bybit_connector.set_trading_stop` мав `if take_profit:`, тобто ковтав
   НУЛЬ, яким Bybit і скасовує тейк. Тобто біржа закрила б позицію повз усю
   нашу логіку.

**ПРАВИЛО: знятий TP-2 = знятої АВТОМАТИЧНОЇ ФІКСАЦІЇ ПРИБУТКУ по цій угоді.**
ЄДИНЕ джерело — ЧИСТА `TradeManager._tp2_cleared_by_user(pos)` =
`pilot_tp_cleared and not manual_tp`. Стоїть у ТРЬОХ вузлах: `_pilot_tick`
(перед `act == 'take'`), стратегічний TP у РЕАЛЬНІЙ і в ПАПЕРОВІЙ книзі.

⚠️ **ЗАХИСНІ ВИХОДИ НЕ ЧІПАЄМО.** SL, беззбиток, правила виходу за вердиктом,
Opposite OB, час — усе лишається. Правило знімає ФІКСАЦІЮ ПРИБУТКУ, а не
ЗАХИСТ; ризик не знімаємо ніколи. Тест-замок дивиться, що біля `stop_loss`
жодної згадки про гейт немає.
⚠️ **Міряємо САМЕ TP-2 (`manual_tp`), а не «обидва поля порожні».** TP-1 —
ЧАСТКОВА фіксація, і зняти її, лишивши повний вихід, — нормальний сценарій.
(Наявне поле знімка `tp_locked` рахує ІНШЕ питання — «чому поля порожні», тобто
автозаповнення, — і навмисно лишилось як було.)
⚠️ **`pilot_tp_cleared` ставиться ЛИШЕ на `origin='user'`** — порожнє поле,
якого ще ніхто не рахував, це НЕ рішення оператора.
⚠️ **Замок НЕ вічний:** вписали Manual TP-2 → правило знімається само (умова
перестає виконуватись); на `set` ще й скидаються `pilot_tp_cleared` та обидві
позначки «про це вже сказано», щоб пояснення зʼявилось і для наступного разу.
⚠️ **МОВЧАТИ НЕ МОЖНА** (урок «невидимий збій читається як „бот не працює“»):
ціна СТОЇТЬ на рівні, монітор тікає раз на 4с. Автопілот пише ОДИН рядок
(`_pilot_take_user_lock`), стратегічний TP — ОДИН (`_log_tp_cleared_once` →
`_tp_cleared_logged`). Без анти-флуду це були б сотні рядків.
⚠️ **БІРЖА:** `set_trading_stop` тепер перевіряє `take_profit is not None`
(нуль = скасувати), а `update_manual_sl_tp` на `clear` від ОПЕРАТОРА в РЕАЛЬНІЙ
книзі шле `take_profit=0`. **Результат ЧИТАЄМО:** відмова означає, що тейк
ЛИШИВСЯ на Bybit, і про це йде окремий рядок у 🧾 Лог. Виклики з
`take_profit=None` (усі наявні) поводяться РІВНО як раніше.
⚠️ **UI:** `take_block` у знімку автопілота → значок **✋ бурштиновий**
(`_PILOT_TAKE_BLOCKED`), який перекриває і 🔒 «трейл заблоковано» (закриття
угоди — сильніший стан за трейл), причина й підказка «як повернути» — у тултипі.

⚠️ **ЩО НЕ ЗМІНЕНО (рішення користувача).** Сам стратегічний TP (`use_tp`,
`tp_pct` 5%) лишається УВІМКНЕНИМ і працює для всіх угод, де оператор рівень НЕ
знімав. Хто хоче лишити ЛИШЕ Manual TP-1/TP-2 — вимикає `use_tp` у Trade
Manager; глобально за нього це рішення не ухвалюємо.
Тест: `test_autosl_chain.py` (розділ «✋ ЗНЯТИЙ Manual TP-2»).

## 🧮 ПРАВИЛО «Старий МММ ⚖ → вихід» ГАСИТЬ АВТОМАТИКУ 🎯 АВТОПІЛОТА (17.09)

**Вимога дослівно:** «Якщо увімкнено "🧮 Старий МММ ⚖ → вихід" — потрібно
автоматично вимкнути все, що стосується автоматичного "🎯 Автопілот". Якщо
Manual TP-1 або Manual TP-2 виставлені вручну, бот має реагувати на ручні дані.
І тумблер ⚖️ TP-1 переводить SL у беззбиток має працювати, якщо його вмикнули.»

Сенс: коли виходом керує 🧮 показник, другий «водій» угоди не потрібен —
автопілот не має ні закривати на СВОЇЙ цілі, ні вести стоп, ні підставляти
СВОЇ рівні поверх ваших.

**ЄДИНЕ джерело умови — ЧИСТА `TradeManager._pilot_auto_off(s)`** =
`use_mm_flat_exit`. Гейт стоїть **ОДИН**, найпершим у `_pilot_tick` (одразу за
`pilot_enabled`), і саме тому вимикає ВСЕ автоматичне за раз: вибір цілі ·
🧲 магніт · трейл стопа · `take` (закриття за ціллю) · автозаповнення
Manual TP-1/TP-2 · якір планового R. Сім окремих перевірок розійшлися б.
Тест-замок перевіряє, що гейт стоїть ДО `import trade_pilot`, ДО магніту,
ДО `act == 'take'` і ДО `pilot_autofill_tp`.

⚠️ **РУЧНЕ ПРАЦЮЄ ДАЛІ — це друга половина вимоги.** `_check_manual_tp1` і
`_check_manual_sl_tp` викликаються з моніторів ОКРЕМИМИ кроками, а
⚖️ `_tp1_move_to_breakeven` — зсередини `_check_manual_tp1`. Жоден із них про
гейт не знає (тест-замок: у їхніх тілах немає `_pilot_auto_off`).
⚠️ **Тумблер користувача НЕ переписуємо.** Гасимо ПОВЕДІНКУ, а не
`pilot_enabled` у БД: вимкнув правило — автопілот повернувся рівно таким, як
його налаштували. Мовчки правити чужі налаштування не можна.
⚠️ **КОЛОНКА ГОВОРИТЬ.** `_pilot_note_auto_off` пише знімок із полем
`auto_off`, фронт малює **🧮 сірий** (`_PILOT_AUTO_OFF`), який перекриває і ✋,
і 🔒 — такту взагалі не було, це найсильніший стан. Без цього комірка просто
застигла б на останньому рішенні, тобто виглядала б як живий супровід.
Знімок пишеться ОДИН раз (доки стан не змінився) — інакше `ts` смикав би
таблицю щополла; ключ — свій на КОЖНУ книгу (урок TRXUSDT).
⚠️ **UI гасить РІВНО мертві контроли** (`_PILOT_AUTO_IDS` + `_tmPilotAutoSync`,
дзеркало бекендної умови): автопілот, вікно цілей, TF структури/POC, буфер
стопа, беззбиток за R, автозаповнення TP, вікно TP-1 і 💧 TP-1 зі сходинок.
**НЕ гасимо:** `tm-tp1-move-be` і «TP-1 закриває N%» (обслуговують РУЧНІ рівні)
та 🧲 `tm-pilot-tp2-magnet` (живить ще й гейт за R ПЕРЕД відкриттям). Напис
`#tm-pilot-auto-off-note` зʼявляється лише при увімкненому правилі — мовчки
погашені поля читались би як збій. Синхронізація йде і на зміну тумблера, і
після завантаження налаштувань.
⚠️ Стратегічний TP (`use_tp`) і правила виходу за вердиктом гейт НЕ чіпає — він
про автопілот, а не про решту виходів.
Тест: `test_autosl_chain.py` (розділ «🧮 ПРАВИЛО … ГАСИТЬ АВТОМАТИКУ»).

## 📍 МММ-МОНІТОР — ВЛАСНА СЕКЦІЯ ПОЗА ВІКНОМ ЧЕРГ, НАД 🎯 POC (17.09)

**Вимога дослівно:** «Винеси МММ-монітор із вікна Черг, розмісти над POC-сетап».

Монітор ніколи не був чергою (рядки не чекають сигналу, двигун їх сам не
відкриває — це записано ще 15.09), але лежав УСЕРЕДИНІ панелі
`#fuel-filter-panel`, між гармошкою «🛡 Авто-SL» і Чергою-1 — тобто читався як
ще одна черга.

Тепер це окрема панель верхнього рівня **`#mm-monitor-panel`**, і порядок на
сторінці: **🧮 МММ-монітор → 🎯 POC-сетап → ❤️ Fuel Auto-Filter (черги)**.
- ⚠️ Переїхала САМА розмітка, жодного id не змінено: `mm-body` / `mm-caret` /
  `mm-tbody` / `ff-mm-monitor-enabled` / `mm-bias-banner` ті самі, тож
  гармошка (`togglePanel('mm')`, `_PANEL_IDS`), `_ffWantedSections()` і
  `served.mm` працюють без правок.
- ⚠️ Заголовку прибрано власний верхній відступ (`margin:14px 0 6px` →
  `margin:0 0 6px`) — тепер його дає сама панель.
- Тест-замки: `mm-monitor-panel` стоїть ПЕРЕД `poc-setup-panel` і ПЕРЕД
  `fuel-filter-panel`, усі ключові id лежать усередині нової секції, гармошка
  збереглась. `test_mm_monitor.py` (103) — розділи 19-20.

### 💾 БАНЕР МОНІТОРА ПЕРЕЖИВАЄ РЕСТАРТ — таймер і показники (вимога 17.09)

**Дослівно:** «Банер МММ-монітор не має втрачати свій таймер і показники після
кожного рестарту бота. Всі дані мають зберігатись і відновлюватись.»

`botupdate` роблять часто, а таймер міряє, скільки **РИНОК** тримає напрямок —
обнуляти його рестартом означало б показувати вік ПРОЦЕСУ замість віку СТАНУ.

- `mm_bias` + **`mm_bias_since`** пишуться в ТОЙ САМИЙ блоб `fuel_filter_state`
  (`_persist_state`), який тік зберігає щоразу — окремого ключа не заводимо.
- ⚠️ **`since` зберігаємо ОКРЕМИМ числом**, а не лише всередині знімка: саме
  воно порівнюється в `_mm_track_bias` і саме від нього залежить, ПРОДОВЖИТЬСЯ
  таймер чи почнеться заново. Відновлюємо ОБИДВА — інакше перший же такт із тим
  самим напрямком скинув би відлік (`not self._mm_bias_since` → `since = now`).
- ⚠️ **ФЛІП ПІСЛЯ РЕСТАРТУ ТАЙМЕР ПЕРЕЗАПУСКАЄ** — відновлення не «приморожує»
  стан, воно лише не дає йому обнулитись на рівному місці. Обидва боки закриті
  тестами.
- ⚠️ **↻ «ВІДНОВЛЕНО» НАЗВАНО ВГОЛОС.** До першого `_mm_capture` (≤ один такт)
  числа з минулого запуску — це ще не свіжий розрахунок. `_load_state` ставить
  `restored: True`, банер показує сіре `↻` біля таймера + пояснення в підказці.
  Позначка зникає САМА: `_mm_track_bias` будує знімок наново, і поля в ньому
  просто немає (окремого «скидання» не заводимо — воно б розсинхронізувалось).
- ⚠️ Час із МАЙБУТНЬОГО (переведений годинник, зіпсований блоб) дав би
  відʼємний таймер → такий стан відкидаємо і починаємо відлік заново.
- ⚠️ Вимкнений монітор і далі ГАСИТЬ банер у `_mm_capture`; порожній стан теж
  персиститься, тож після рестарту з вимкненим монітором банер не «воскресне».
Тест: `test_mm_monitor.py` (108) — розділ 21.

### 🧲 У РЕЖИМІ 🧮 МАГНІТ ЙДЕ В Manual TP-1, А КОЛОНКА НЕ ПОРОЖНІЄ (17.09)

**Вимога дослівно:** «при виборі 🧮 "Старий МММ ⚖ → вихід" із 🎯 Автопілота
беремо "🧲 найбільший магніт" і ставимо в Manual TP-1. І таким чином щоб не
було пустим поле 🎯 Автопілот в таблицях відкритих угод — підраховуй дані для
Manual TP-1.»

Правило й далі гасить **РІШЕННЯ** автопілота (вибір цілі з графіка, трейл
стопа, `take`, автозаповнення TP-2, план поділу), але ОДНУ його здатність
лишає: знайти магніт і віддати рівень оператору. Це робить **окремий
редукований такт `_pilot_magnet_tp1`**, який викликає той самий гейт у
`_pilot_tick` замість колишнього `_pilot_note_auto_off` (метод видалено —
один писач знімка, без дрейфу).

- **ТОЙ САМИЙ магніт**, що живить TP-2 у звичайному режимі
  (`_magnet_objective` → `get_liq_magnet` за напрямком, ближня межа смуги).
  Другого розрахунку не заводимо — інакше два місця показували б різні рівні.
- **Рівень пише ТОЙ САМИЙ `_pilot_apply_tp`** (як `{'tp1': …, 'tp2': None}`):
  там уже живуть «оператора не чіпаємо», позначка 🤖 бота і рядок у 🧾 Лозі.
- ⚠️ **TP-1 — ЧАСТКОВИЙ вихід** (`pilot_tp1_close_pct`, деф. 50%): на магніті
  закриється половина, решту ведуть 🧮 правило і стоп. Так просив користувач;
  TP-2 у цьому режимі СВІДОМО лишається порожнім.
- ⚠️ **Біржу питаємо РІВНО ОДИН раз на угоду** (`pilot_magnet_done`) і лише
  коли вона ВІДПОВІЛА: мовчання позначку не ставить (повтор безкоштовний —
  зріз кешується). Причина невдачі лягає в `pilot_magnet_note` і показується.
- ⚠️ **КОЛОНКА ЗАПОВНЮЄТЬСЯ ЗАВЖДИ:** ціль, смужка прогресу від входу і
  **плановий R** від ТОГО САМОГО якоря `pilot_r_stop` (кейс VIRTUALUSDT). Немає
  магніту → у тултипі стоїть ПРИЧИНА, а не порожнеча.
- ⚠️ **UI розрізняє два стани:** є магніт → **🧲 бурштиновий** «магніт →
  Manual TP-1»; немає → **🧮 сірий** «автоматику вимкнено правилом». Один
  значок на обидва знову зробив би причину здогадкою. Рядок тултипа «Ціль
  (TP-2)» у цьому режимі підписаний «Рівень Manual TP-1 (🧲 магніт)».
- ⚠️ **Два тумблери ПОВЕРНУЛИ СЕНС і більше НЕ гасяться:** 🧲 «TP-2 з магніту»
  (джерело рівня) і «Автозаповнення TP» (дозвіл вписати). Обидва вимкнені —
  рівня не буде, і про це сказано в тултипі. Тест-замок переписано, а не
  «полагоджено»: вимога змінилась.
Тест: `test_autosl_chain.py` (розділ «🧲 МАГНІТ → Manual TP-1 у режимі 🧮»).

### ⏱ СКІЛЬКИ МОНЕТА ТРИМАЄ СТАН LONG / SHORT / ⚖ (вимога 17.09)

**Дослівно:** «Додай таймер для кожної монети, яка знаходиться в стані LONG
SHORT Рівновага — скільки саме часу, для МММ-монітор.»

Колонка **«⏱ У стані»** (9-та, одразу ЗА «Старий МММ» — вона уточнює САМЕ цей
стан). Рахує `_mm_track_state(snap, now)` у ДВИГУНІ, `mm_monitor_state` лише
читає поле `state_since` (той самий урок B2).
- ⚠️ **⚖ РІВНОВАГА — ТЕЖ СТАН** (ключ `'FLAT'`): «скільки монета вже без
  напрямку» не менш змістовне за «скільки вона в LONG». Будь-яка зміна стану,
  у тому числі перехід у рівновагу і назад, перезапускає відлік.
- ⚠️ **РАЗОВИЙ ПРОПУСК МОНЕТИ ТАЙМЕР НЕ СКИДАЄ** — той самий урок, що з
  `_mm_str_hist`: liq-map могла на мить не віддати стан, і обнуляти через це
  годинний відлік означало б те саме мерехтіння, через яке переробляли «Силу
  росте». Запис живе, доки монету бачили не давніше за `MM_GROW_WINDOW_SEC`
  (третього магічного числа не заводимо); довша відсутність — відлік заново.
- ⚠️ **ПЕРСИСТ** (`mm_state_since` у тому самому блобі `fuel_filter_state`):
  рестарт не обнуляє. На завантаженні `seen` ставиться ТЕПЕРІШНІМ часом —
  інакше прибирання «давно не бачили» знесло б відновлене ще до першого такту.
  Стан перевіряється першим же знімком: інший напрямок → відлік заново.
- ⚠️ Віддаємо **МОМЕНТ** (epoch), а не секунди: живі секунди малює ТОЙ САМИЙ
  глобальний 1с-тікер (`.ff-timer[data-since]`), тож таблиця не
  перебудовується щосекунди (урок `held_sec`). `state_since` — у сигнатурі.
- Колір комірки — за станом (зелений LONG · червоний SHORT · сірий ⚖), щоб
  рядок читався як ціле. Сортування `data-mmsort="state"` — найдовший зверху.
- Вимкнений монітор чистить таймери разом зі знімком.
⚠️ Колонок у таблиці тепер **9** (`colspan` 9); контракт складу — у
`test_mm_monitor.py::test_ui_has_growth_column_with_timer_and_sorting`.
⚠️ Пастка, на яку наступили: `test_js_price_cell_is_a_single_line` різав рядок
`split('<td')[6]` МАГІЧНИМ числом — нова колонка зсунула його. Тепер номер
береться з ЖИВОГО заголовка.
Тест: `test_mm_monitor.py` (115) — розділ 22.

### 📊 «У WATCHLIST 51, А МОНІТОР ІЗ 49» + 💹 «Ціна»/«Рух» + значки (17.09)

**Три косметичні вимоги одним пакетом.**

**1. Різниця в кількості монет ПОЯСНЕНА, а не лишається здогадкою.**
Монітор бере не «весь watchlist», а набір двигуна: **BTC + угоди + черги 1-4 +
фандинг + WATCHLIST** (у режимі економії — лише монети «в роботі»). З нього
рядками стають не всі: частина ще **БЕЗ даних СТАРОГО МММ** (liq-map не зібрала
рівнів — нова монета «дозріває» кілька хвилин), частина вже **В УГОДІ** (монітор
— список КАНДИДАТІВ, рішення 15.09).
- `_mm_capture` пише `self._mm_stats = {targeted, data}`, `mm_monitor_state`
  віддає **`coverage`** = `{targeted, no_data, in_trade, rows}`.
- ⚠️ **Розклад мусить СХОДИТИСЬ:** `targeted − no_data − in_trade == rows`
  (тест-замок). Інакше «пояснення» саме стало б джерелом сумнівів.
- Шапка секції: **«49 з 51 монет»**, повний розклад — у підказці.
- ⚠️ Вимкнений монітор віддає `coverage = {}` — фальшивих чисел не малюємо.

**2. «Ціна» розбита на ДВІ колонки: «Ціна» і «Рух».**
Дослівно: «значення розбий рівними стовпчиками, відсотки в інший стовпчик».
В одній комірці числа різної довжини зсували одне одного. Тепер обидві
праворуч + monospace → розряди вишиковуються.
- ⚠️ **Сортування «Ціна» тепер за ЦІНОЮ** (було — за рухом, бо колонка була
  одна). «Яка монета зараз іде вгору» — це колонка **«Рух»** (`pchg`), порядок
  той самий, що був. Обидві сортуються, рядок без історії — у КІНЕЦЬ.
- Колонок **10** (`colspan` 10); склад — у контракті
  `test_ui_has_growth_column_with_timer_and_sorting`.

**3. Значок ДІЇ більше не дублює значок ЦІЛІ** (скарга «картинка два магніта»).
🧲 стоїть ЛИШЕ біля цілі; ліворуч — значок самого ПРАВИЛА **🧮**, а два його
стани розрізняє **КОЛІР**: бурштин «правило веде лише Manual TP-1» (рівень
живий) · сірий «автоматику вимкнено правилом» (магніту немає).
- ⚠️ Тест-замок `test_the_action_icon_never_repeats_the_objective_icon` звіряє
  **ВСІ** значки дій (`_PILOT_ACT`, 🔒, ✋, 🧮×2) з мапою `_PILOT_KIND`: жоден
  не має збігатися зі значком цілі — у комірці вони стоять поруч.
Тест: `test_mm_monitor.py` (120) — розділи 23-24.

### ⏳ «ПІСЛЯ РЕСТАРТУ МОНІТОР ДОВГО ПОРОЖНІЙ» + що коштує в такті (18.09)

**Скарга дослівно:** «Після рестарту бота, МММ-монітор довго знаходиться в
такому стані і не оновлюється, що так сильно тормозить сторінку?» На скріні:
банер малює ↻ ВІДНОВЛЕНІ 54% LONG із живим таймером, а таблиця — «Немає даних
МММ» і в шапці «ще немає знімка».

**Що це означало насправді.** `mm_monitor.ts == 0` = `_mm_capture` **НЕ
відпрацював ЖОДНОГО разу від старту**. `_mm_capture` кличеться в **КІНЦІ**
`_tick`, тож нуль виникав у ТРЬОХ зовсім різних ситуаціях — і всі три
показувались ОДНИМ нейтральним написом:
1. **❤️ Fuel Auto-Filter ВИМКНЕНО** — `_tick` виходить ПЕРШИМ рядком
   (`if not settings.get('enabled'): return`), тобто знімка не буде **НІКОЛИ**,
   і чекати марно. Тумблер самого монітора при цьому зелений.
2. **перший такт іще рахується** — нормальний прогрів (кеші холодні).
3. **такт ПАДАЄ до `_mm_capture`** — раніше про це знав ЛИШЕ `print` у stdout,
   і монітор виглядав як «прогрівається», хоча був зламаний (той самий урок,
   що «Черга-4 ⚠️ збій після відкриття»).

**Фікс — причина НАЗВАНА.** `self._mm_pending = {reason, at, detail}`
(`boot` / `ff_off` / `error`), віддається полем `pending` у `mm_monitor_state`
**лише поки `ts == 0`** (є знімок → поле порожнє, пояснення не висить вічно).
Фронт малює три РІЗНІ написи і в шапці, і в порожній таблиці; `error` —
червоний, `ff_off` — бурштиновий.
⚠️ **Причина — У СИГНАТУРІ таблиці**: без неї перехід «⏳ рахується» →
«⛔ Fuel Auto-Filter вимкнено» не перемалював би напис (рядків як не було,
так і немає) — той самий випадок, що з `enabled` у сигнатурі.

**⏱ ПРОФІЛЬ ТАКТУ** `[FF] tick #N: K coins in Xs — pre Ys · fuel Zs (Ws/coin)
· mm Vs` (конвенція `[SMC] Scan #N` / `[FF-Q4] tick`). Тепер «чому довго» — це
ЧИСЛО, а не здогад. Найдорожчий крок названо прямо: цикл
`_fuel_dir_smoothed` по ВСІХ монетах «у роботі».

**ЩО САМЕ ВАНТАЖИТЬ СТОРІНКУ (заміряно по коду, не на відчуття).** На КОЖНУ
монету `_liq_state` → `lm.get_state()` = `liqmap_get_latest_oi` × провайдери
(2) + **`liqmap_get_events(limit=3000)`** — окрема сесія SQLAlchemy і до 3000
ORM-обʼєктів із `.to_dict()`. При ~50 монетах це **~150 сесій БД і до ~150 тис.
ORM-обʼєктів КОЖНІ 30с**, у ТОМУ САМОМУ процесі, що обслуговує сторінку (один
gunicorn-воркер). Після рестарту до цього додається холодний старт усіх кешів,
префетч сканера (~10 HTTP на монету) і перереєстрація символів у liq-map.

**🔁 ЛОСЛЕС-ЕКОНОМІЯ: не перезбирати знімок, поки ДЖЕРЕЛО не тікало.** Рівні й
мітигацію в БД пише **ВИКЛЮЧНО** демон liq-map, а він тікає раз на
`SCAN_INTERVAL_SEC` = 60с, тоді як `CYCLE_SECS` = 30с. Тобто **рівно половина**
збірок читала БАЙТ-У-БАЙТ те саме. Тепер запис кешу — **трійка**
`(ts, знімок, позначка тіку джерела)`, і після TTL знімок віддається повторно,
якщо `lm.last_tick_at()` не зрушив.
- ⚠️ Це **НЕ послаблення свіжості**: щойно демон тікне, позначка зміниться і ми
  перерахуємо. `LIQ_STATE_TTL` (20с) лишився як був.
- ⚠️ **`LIQ_STATE_SRC_MAX = 180с`** — стеля на випадок, коли демон ЛІГ: його
  позначка не зрушить ніколи, і без межі знімок заморозився б назавжди.
- ⚠️ Позначку читаємо **ПУБЛІЧНИМ `LiquidationMapDaemon.last_tick_at()`**, а не
  приватним `_last_tick_at` (той самий принцип, через який у сканері зʼявився
  `volumized_on()`). Старіший модуль без методу → `None` → поведінка РІВНО як
  раніше (лише TTL). Тест-замок стежить за обома половинами.
- ⚠️ Форма запису кешу змінилась 2→3 поля — `test_q4_perf.py` розпаковував
  двійку і впав; оновлено з поясненням.

⚠️ **ЩО НЕ ЗМІНЕНО (потребує рішення користувача).** Сама вартість
`liqmap_get_events(limit=3000)` на монету лишилась: різати ліміт / вікно
(24 год) / глибину означало б змінити ЧИСЛА МММ, а це торгова логіка.
Другий важіль — `mmm_limited_mode` (рахувати лише монети «в роботі»).
Тест: `test_mm_monitor.py` (129) — розділ 25.

### 🐞 «МММ-МОНІТОР ВЗАГАЛІ ПЕРЕСТАВ ПРАЦЮВАТИ» — ТРИ ДЕФЕКТИ ЗІ СКРІНА (18.09)

**Скарга дослівно:** «МММ-монітор взагалі перестав працювати. Навіть в таблиці
відкритих угод не відображається.» На скріні: ❤️ Fuel Auto-Filter **OFF**,
монітор увімкнений, банер малює ↻ відновлені 54% LONG, таблиця порожня,
колонка «🧮 Старий МММ» у 6 paper-угодах — суцільні прочерки.

**1. КОРІНЬ: при `enabled=False` ПОТІК НАВІТЬ НЕ СТАРТУВАВ.**
`init_fuel_filter` робив `if _instance.is_enabled(): _instance.start()`, тож із
вимкненим майстер-тумблером `_tick` не виконувався ЖОДНОГО разу — а `_mm_capture`
живе саме в ньому. Разом із чергами мовчки вмирали ТРИ речі, дві з яких до черг
стосунку не мають:
- 🧮 МММ-монітор (окрема секція з ВЛАСНИМ тумблером);
- колонка «🧮 Старий МММ» у таблицях ВІДКРИТИХ УГОД;
- **правило виходу `use_mm_flat_exit`** — `mm_snapshot_for` віддавав порожньо,
  що читається як «немає даних», і правило МОВЧАЛО. Тобто тумблер ДВИГУНА
  ВІДКРИТТЯ тихо вимикав правило ЗАКРИТТЯ вже відкритих угод.

**Фікс:** `init_fuel_filter` **стартує потік ЗАВЖДИ**, а `_tick` при
`enabled=False` іде в новий **`_mm_only_tick`** — РІВНО набір монет →
`_register_with_liqmap` → `_fuel_dir_smoothed` → `_mm_capture`.
- ⚠️ **ТОРГІВЛЮ ЦЕ НЕ ВМИКАЄ:** усі чотири двигуни (`_engine_tick`,
  `_engine_tick_q2/_readiness/_queue4`) і цикл алертів ПЕРШИМ РЯДКОМ перевіряють
  `enabled`. Тест-замок `test_every_engine_still_gates_on_the_master_switch`
  стереже саме це — бо тепер потік працює завжди.
- ⚠️ У редукованому такті НЕМАЄ ₿-сеансу, `_enforce_btc_flip_close` (він
  ЗАКРИВАЄ угоди), TTL/виселень, персисту черг — тест-замок на кожен рядок.
- ⚠️ Набір монет вужчий (BTC + відкриті угоди + WATCHLIST; черги й фандинг не
  наповнюються) — і це НАЗВАНО вголос: `queues_off` у стані, `· ❤️ черги OFF`
  у шапці секції + пояснення в підказці. Мовчазно коротший список читався б як
  «бот загубив монети».
- ⚠️ Вибір WATCHLIST-частини винесено у **`_mm_watchlist_part`** — ОДНЕ джерело
  на обидва такти (два «однакових» набори розійшлись би).
- ⚠️ `set_enabled(False)` потік НЕ зупиняв і раніше — діра була саме в
  СТАРТІ після рестарту.
- ⚠️ Причина `pending='ff_off'` (учорашня) **ВИДАЛЕНА разом із гілкою UI**:
  вимкнені черги знімок більше не зупиняють, тож вона недосяжна. Замки
  переписано, а не «полагоджено» — вимога змінилась.

**2. 🐞 `coverage` І `pending` НЕ ДОЇЖДЖАЛИ НА СТОРІНКУ — дві мертві фічі.**
`mmApplyState` будував `_mmMeta` РІВНО з трьох полів (`limited`/`ts`/`enabled`),
тож `_mmMeta.coverage` і `_mmMeta.pending` були `undefined` ЗАВЖДИ. Наслідок,
видимий на скріні: шапка ніколи не показувала «49 з 51 монет» (фіча 17.09), а
порожня таблиця писала «Немає даних МММ» замість причини (фіча 18.09) — при
тому, що шапка вже малювала фолбек «⏳ перший знімок рахується · 0с».
- **Тест-замок `test_every_state_field_the_page_uses_reaches_mmmeta`** збирає
  регуляркою ВСІ `_mmMeta.<key>` зі сторінки і вимагає, щоб КОЖЕН ключ був у
  присвоєнні. Старі замки перевіряли лише НАЯВНІСТЬ РЯДКІВ у HTML — саме тому
  пропустили обидві фічі.
- Початковий літерал `_mmMeta` вирівняно за формою (там ще лежав `counts`,
  прибраний із сервера 15.09).

**3. 🔢 ПОЛЯ РІВНІВ ПОКАЗУВАЛИ КОМУ.** На скріні `0,38000` / `136,00` / `4330,07`
— а та сама ціна в колонці «🎯 Автопілот» стояла з КРАПКОЮ (`$0.38000`). Одне
число у двох написаннях в ОДНОМУ рядку. `<input type="number">` малює значення
за локаллю БРАУЗЕРА, і `lang="en"` (додане 03.09) цього не лікує: Firefox його
для числових полів ігнорує взагалі.
**Фікс:** `type="text" inputmode="decimal"` — рендеримо МИ, крапка гарантована,
числова клавіатура на телефоні лишається. Кому у ВВЕДЕНОМУ тексті нормалізує
**`_numFromInput`** (ЄДИНЕ правило на обидва сабміти): порожньо → 0 («зняти
рівень»), кома → крапка, пробіли геть.
⚠️ **NaN більше не їде на сервер:** `parseFloat('абв')` → NaN → JSON `null` →
маршрут прочитав би це як «не чіпати», і користувач думав би, що зберіг.
Тепер обидва сабміти на NaN кричать у alert і НЕ шлють запит.
⚠️ Старий замок `lang="en"` × 3 переписано з поясненням, а не «полагоджено».

⚠️ **ЩО СВІДОМО НЕ ЧІПАВ.** Банер **₿ BTCUSDT** із вимкненим FF лишається
замороженим (на скріні «0% рівновага · 🔴 SHORT TRADING», тоді як ринок і
монітор кажуть LONG). Це НЕ той самий баг: ₿ — банер СЕАНСУ черг
(`_update_btc_verdict` + START/STOP двигуна), і з вимкненими чергами він
законно стоїть. Оживити його означало б запустити сеансну механіку (зокрема
OP-4 «фліп чистить чергу») при вимкненому боті — це окреме рішення.
Тест: `test_mm_monitor.py` (134) — розділи 25-26; `test_pilot_books.py` (17).

### ₿ БАНЕР У «🔁 ДУБЛЮВАННІ» ПОКАЗУВАВ СТАРЕ ЧИСЛО (скарга 18.09)

**Дослівно:** «він має повторювати "МММ-бабло ↑ зверху → тягне в LONG" бо
увімкнено "дублювання", а він показує "рівновага", це взагалі як так?»
На скріні: банер рішення каже LONG 100% і «▲ МММ-бабло ↑ зверху → тягне в
LONG», watchlist-консенсус 63/71 LONG, а ₿ BTCUSDT — «0% рівновага · ⚪».

**МОДЕЛЬ ТУТ НІ ДО ЧОГО — це вже приводили до спільного знаменника.** Джерело
банера `_btc_liqfuel_raw` рахує РІВНО той самий `(fa−fb)/den`, що й рядок
«МММ-бабло» у `compute_bias`: та сама liq-map (24 год, `liqmap_decay_profile`),
те саме зважування `usd/(1+dist/2)`, те саме вікно 15% і той самий поріг ±0.1.

**КОРІНЬ — СТАРІ ДАНІ.** Живі поля банера (`_btc_live_dir` / `_btc_live_since` /
`_btc_fuel_strength`) оновлював ЛИШЕ `_update_btc_verdict`, а він живе в
ПОВНОМУ такті. З вимкненим ❤️ Fuel Auto-Filter такт не виконувався взагалі →
на банері застигали нулі, тоді як рядок Smart Direction рахувався наново на
кожен `/api/smc/bias`. Тобто банер показував не «іншу думку», а **позавчорашню**.

**Фікс:** живий показ винесено в **`_btc_banner_live_refresh()`** — ЄДИНИЙ писач
цих трьох полів (тест-замок рахує присвоєння поза `__init__`: рівно одне). Його
кличе і `_update_btc_verdict`, і новий `_mm_only_tick`.
- ⚠️ **РІВНО ПОКАЗ.** Сеанс (`_btc_verdict_dir/_since`, пауза, START/STOP,
  OP-4 «фліп чистить чергу», TG) лишився у `_update_btc_verdict` і при
  вимкнених чергах НЕ виконується: оживити його означало б перезапускати сеанси
  й чистити черги у ВИМКНЕНОМУ боті. Тест-замок дивиться і на поведінку, і на
  текст `_mm_only_tick` (без згадок сеансних полів).
- ⚠️ Таймер скидається ЛИШЕ на ЗМІНІ напрямку (у т.ч. в рівновагу й назад).
- ⚠️ **КРОС-ФАЙЛОВИЙ ЗАМОК** `test_the_banner_and_the_decision_line_share_the_
  same_formula`: формула (вікно 15%, вага `usd/(1+dist/2)`, поріг ±0.1) звіряється
  МІЖ `web/flask_app.py` і `fuel_filter._btc_liqfuel_raw`. Два інлайни однієї
  метрики під однією назвою — це рівно кейс PD-зони, і без замка вони розійдуться.
- ⚠️ У режимі **СЕАНСУ** (тумблер «🔁 Дублювання» ВИМКНЕНО) банер із вимкненими
  чергами і далі стоїть — це не баг: сеанс і є машинерія черг.
Тест: `test_mm_monitor.py` (138) — розділ 27.

### 🐢 БАНЕР МОНІТОРА: ПОВІЛЬНИЙ ПЕРЕХІД СТАТУСУ (антиспам, 18.09)

**Дослівно:** «Перехід в банері МММ-монітор між статусами має бути повільним,
а НЕ різким і частим, має бути антиспам.»

Захисти ДВА, і вони РІЗНІ за природою (обидва в `_mm_track_bias`):
1. **ГІСТЕРЕЗИС** — `MM_BIAS_EXIT = 0.06` проти `MM_BIAS_FLAT = 0.10`: зайти в
   напрямок можна за 10% важеля, а ВИЙТИ — лише нижче 6%. Один поріг на вхід і
   вихід давав би перемикання рівно там, де важіль зависає найчастіше (той
   самий прийом, що `_fuel_hyst` для самого МММ).
2. **ПІДТВЕРДЖЕННЯ ЧАСОМ** — `mm_bias_confirm_sec` (деф. **120с**): новий стан
   спершу стає КАНДИДАТОМ (`_mm_bias_cand`) і показується, лише коли
   протримався вікно. 0 = миттєво (стара поведінка).

⚠️ Гаситься САМЕ СТАТУС. `pct`, смуга і розклад лишаються ЖИВИМИ — просили
прибрати миготіння ПІДПИСУ, а не показувати застарілий ринок.
⚠️ **КАНДИДАТ ВИДИМИЙ** (`raw_dir`/`cand_dir`/`cand_since`/`confirm_sec` →
`#mm-bias-cand`: `⏳ 🔴 SHORT 45/120с`). Мовчазне «банер не перемикається»
читалось би як зависання, а не як антиспам.
⚠️ **ПЕРШИЙ стан після чистого старту беремо ОДРАЗУ** — підтверджувати нічого,
попереднього статусу не існувало (інакше після першого запуску банер 2 хв
стояв би ⏸ FLAT).
⚠️ Кандидат СВІДОМО не персиститься: вікно коротке, і після рестарту чесніше
почати відлік заново, ніж «дорахувати» чужий.
⚠️ **Наслідок для 💧 Сканера:** він бере `mm_bias()['dir']`, тобто вже
ПІДТВЕРДЖЕНИЙ статус, тож перескан на фліпі йде раз на реальний розворот.
⚠️ Два наявні тест-замки переписано (не «полагоджено»): вимога змінилась —
тепер фліп у них або вимикає вікно (`0`), або чекає два такти.
UI: поле «⏱ Перехід ≥ N с» (`ff-mm-bias-confirm`) у ряду фільтрів монітора.
Тест: `test_mm_monitor.py` (149) — розділ 28.

### 🪗 СПИСОК МОНЕТ МОНІТОРА — ВЛАСНА ГАРМОШКА, ВДВІЧІ НИЖЧЕ (18.09)

**Дослівно:** «Зроби вікно таблиці 🧮 МММ-МОНІТОР меншим по висоті десь в
двічі і заверни у гармошку. Гармошку починай із "Обрано". Запамʼятовуй стан
гармошки (за замовчуванням звернуто).»

- Висота вікна таблиці **680 → 340px** (секція стоїть над усіма чергами й
  відсувала їх за екран).
- Гармошка `mmlist` (`#mmlist-body` / `#mmlist-caret`, у `_PANEL_IDS`) починає
  ховати РІВНО з рядка «Обрано»: усередині спершу панель групового відкриття,
  потім таблиця. Банер, режим економії і фільтри лишаються ЗОВНІ — це і є
  «монітор одним поглядом».
- **`_PANEL_CLOSED_BY_DEFAULT`** — новий список секцій, згорнутих за
  замовчуванням (поки що лише `mmlist`). Збережений вибір користувача завжди
  сильніший за цей дефолт.
- ⚠️ Згорнутий список **НЕ МАЛЮЄТЬСЯ ВЗАГАЛІ** (`_qCollapsed('mmlist')` у
  `mmRender`), але лічильники вкладок, «поза фільтром» і `#mmlist-n`
  оновлюються ДО виходу — шапка лишається чесною. На розгортанні `togglePanel`
  кличе `mmRender()` одразу (інакше таблиця була б порожня до 30с).
- ⚠️ `_run_js` у тестах підкладає ще й `_qCollapsed` (спільний хелпер лежить
  поза зрізом JS монітора).
Тест: `test_mm_monitor.py` — розділ 29.

## 💧 СКАНЕР ЛІКВІДНОСТІ — ОКРЕМИЙ РУШІЙ ПІД МММ-МОНІТОРОМ (18.09)

**Дослівно:** «організуємо сканер із своїм вікном і основним тумблером і
гармошкою налаштувань, під вікном "МММ-монітор", який буде відбирати монети за
певним фільтром і додавати в свою таблицю. За основу беремо алгоритм [блок 💧
на 📡 Tickr]… періодичність сканування (за замовчуванням 15хв) і фільтр саме
які монети беремо (за замовчуванням 65%) — "Маса ліквідності НИЖЧЕ ціни 65% ·
тягне ВНИЗ" за напрямком, який вказує на даний момент банер "МММ-монітор".
Кнопку "Сканувати" залиш… Дані при кожному скані оновлюються, але потрібно
[перевірити] чи немає вже конкретної монети в роботі (відкрита угода або
черга)… Відображай відсоток "Маса ліквідності" і Найсильніший магніт —
напрямок.» Плюс: «При зміні напрямку банера — перескановуй.»

`detection/liq_hunter.py` (`LiqHunterDaemon`, синглтон `init_liq_hunter` /
`get_liq_hunter`), ключі БД `liq_hunter_settings` / `liq_hunter_state`.

**АЛГОРИТМ ВЗЯТО ГОТОВИЙ** — `liq_scan.scan_liquidity`, той самий, що живить
блок 💧 на 📡 Tickr, і **дефолти ТІ САМІ, що на тій сторінці**: Binance · Топ
біржі · 40 монет · 168 год · за перекосом · обіг ≥$20M · OI ≥$5M. Своєї копії
розрахунку немає: інакше «68% вниз» у двох місцях означало б різне (урок
PD-зони). Тест-замок звіряє `DEFAULTS` з `selected`-опціями `tickr.html`.

- **БІК ВІДБОРУ ЗАДАЄ БАНЕР**, не сканер: 🟢 LONG → `above_pct` ≥ порогу,
  🔴 SHORT → `below_pct`. ТА САМА конвенція, що в 💧 фільтрі входу («за
  напрямком» = маса, яка тягне ціну ТУДИ, куди входимо). Чиста `mass_pct` —
  ЄДИНЕ місце, де боки зіставляються з полями драбини; тест-замок дзеркальний.
- **`pick_rows(rows, side, min_pct, in_work)` — ЧИСТА функція** і віддає
  РОЗКЛАД (`weak` / `in_work` / `nodata` / `scanned`): «чому монет мало»
  ніколи не має бути здогадкою.
- ⚠️ **МОНЕТА «В РОБОТІ» ПОВТОРНО НЕ ДОДАЄТЬСЯ.** Набір дає ЄДИНИЙ публічний
  `ff.symbols_in_work()` (відкриті угоди + `_pending`..`_pending4`); лазити в
  приватні черги самотужки не можна — це був би другий «однаковий» список
  (тест-замок на тіло `_in_work`). Пропуск НАЗВАНИЙ у статусі («у роботі N»).
- ⚠️ **⚖ БАНЕР БЕЗ НАПРЯМКУ → БІРЖУ НЕ ЧІПАЄМО ВЗАГАЛІ** і таблицю ЧИСТИМО:
  рядки відбирались під напрямок, якого вже немає, тож вони СУПЕРЕЧИЛИ б
  банеру над ними. Причина — у статусі й у порожній таблиці.
- ⚠️ **ФЛІП БАНЕРА → ПЕРЕСКАН** (`rescan_on_flip`, деф. ON). Спаму немає за
  побудовою: банер уже перемикається повільно (розділ 🐢 вище), плюс власний
  `MIN_GAP_SEC = 60с`. Фліп упритул до скану не ГУБИТЬСЯ — він зсувається на
  кінець проміжку і спрацьовує наступним тіком.
- ⚠️ **ЦЕ СПОСТЕРЕЖЕННЯ, А НЕ ТОРГІВЛЯ:** жодного `_open` / `on_signal` /
  `intercept` (тест-замок по AST). Відкриття лишається за чергами й ✋ ручними
  діями.
- Монета, що ТРИМАЄТЬСЯ у списку між сканами, не «зʼявляється» заново —
  `since` переноситься, інакше таймер ⏱ обнулявся б кожні 15 хв.
- Рядок несе `mass_pct`/`mass_dir` і **магніт** (`magnet_price/pct/dist/dir`) —
  рівно те, що просили показувати.
- API: `GET /api/liq-hunter/state` · `POST /api/liq-hunter/settings` ·
  `POST /api/liq-hunter/scan`. UI — панель `#liq-hunter-panel` (гармошка `lh`)
  МІЖ 🧮 МММ-монітором і 🎯 POC-сетапом: тумблер `lh-enabled`, `<details>`
  налаштувань, кнопка «💧 Сканувати», таблиця з 4 колонок. Полл — 15с, і він
  лише ЧИТАЄ готовий стан (біржу не смикає).
- ⚠️ Дефолт тумблера **OFF** — новий рушій не має мовчки почати ходити на
  біржу на робочій установці.
Тест: `test_liq_hunter.py` (27).

## ⏳ СПІЛЬНА ЧЕРГА СКАНІВ БІРЖІ (`detection/scan_queue.py`, 18.09)

**Дослівно:** «Організуй всі скани біржі по графіку, щоб не навантажувати
біржу, кожен скан в свою чергу.»

Проблема була не в окремому скані, а в ЇХ ЗБІГУ: ручний скан 💧 зі сторінки
📡 Tickr, періодичний скан 💧 Сканера і періодичний скан можливостей — кожен
десятки HTTP за секунди. **Правило: за раз працює РІВНО ОДИН скан**, решта
стоїть у черзі, між сусідніми — пауза `MIN_GAP_SEC = 5с`.

- `scan_queue.run(name, fn, source, timeout)` — синхронно (кнопки/маршрути);
  `submit` — асинхронно; `state()` — що працює, скільки чекає, чим закінчився
  кожен скан.
- ⚠️ **ДЕДУП ЗА ІМЕНЕМ:** другий запит того самого скану, поки перший ще
  чекає, НЕ стає окремим завданням — він чекає на ТОЙ САМИЙ результат.
  Подвійний клік по «Сканувати» більше не коштує подвійного залпу.
- ⚠️ Виняток у скані НЕ ламає чергу: він віддається тому, хто чекав, і йде в
  історію; наступне завдання виконується як звичайно.
- Через чергу вже йдуть: `tickr:liq-list` · `tickr:liq-one:<SYM>` ·
  `tickr:opportunity` · `liq-hunter`. **Додаючи новий скан біржі — ставити
  його ТУДИ Ж** (тест-замок перевіряє кожен наявний шлях).
- ⚠️ **СВІДОМО НЕ В ЧЕРЗІ** — безперервні цикли SMC-сканера і демона liq-map.
  Це не «скани на вимогу», а робочий такт бота: з нього живляться ціни для
  закриття позицій і правила виходу. Поставити їх у спільну чергу означало б
  віддати торгову логіку в заручники разовим сканам.
- Глибина черги видима в підказці статусу 💧 Сканера.
Тест: `test_liq_hunter.py` — розділ 6.

### 📍 МОНЕТИ В РОБОТІ ЛИШАЮТЬСЯ В ТАБЛИЦІ — КОЛОНКА «СТАН» (вимога 18.09)

**Дослівно:** «Із таблиці "🧮 МММ-МОНІТОР" не потрібно видаляти монети що в
роботі — поверни поле статус в таблицю, пиши де на даний момент монета.»

⚠️ **ЦЕ СКАСОВУЄ рішення 15.09** («монета, яка в угоді, не потрібно
відображати у таблиці»). Обидва наявні тест-замки ПЕРЕПИСАНО, а не
«полагоджено»: вимога змінилась.

- Колонка **«📍 Стан»** — 2-га, ОДРАЗУ за символом (вона уточнює саме цю
  монету): **💼 в угоді** (блакитний) · **🎯 Черга-4** і т.п. (бурштин, із
  НАЗВОЮ конкретної черги) · **—** вільна (сірий). Колонок тепер **11**,
  `colspan` 11.
- Бекенд віддає ЧИСТІ прапорці `in_trade` / `queues`, підпис і колір робить
  фронт (`_mmStatusCell`) — той самий принцип, що `verdict.parts`.
- **`_mm_queue_map()`** — ЄДИНЕ місце, де перебираються `_pending`..`_pending4`
  заради «у ЯКІЙ саме черзі». `symbols_in_work()` (для 💧 Сканера) відповідає
  на ІНШЕ питання — «чи зайнята взагалі» — і лишається окремим набором.
- ⚠️ **Галочка:** монета В УГОДІ не обирається (друга позиція по тому самому
  символу все одно не відкриється) — і причина в підказці ВЛАСНА, окрема від
  «⚖ рівновага». Монета В ЧЕРЗІ обирається як і раніше: «у черзі» — це
  ІНФОРМАЦІЯ, а не заборона (✋ ручне свідомо йде повз ворота черг).
- ⚠️ **Стан — У СИГНАТУРІ таблиці**: без нього перехід «вільна → в угоді» не
  перемалював би рядок (той самий випадок, що з `enabled` і причиною
  порожньої таблиці).
- ⚠️ **ВАЖІЛЬ БАНЕРА РАХУЄ ВСІ МОНЕТИ, у тому числі ті, що в роботі**
  (уточнення того ж дня, дослівно: «за показник банера мають відповідати ВСІ
  монети, для цього і повернули щоб всі монети були у таблиці»). Виняток
  «лише кандидати» тримався на скасованому правилі, тож пішов разом із ним:
  банер знову описує РІВНО те, що під ним видно, і `coins` у розкладі
  дорівнює кількості рядків (тест-замок). ⚠️ Вимкнений монітор і далі ГАСИТЬ
  банер — при вимкненому тумблері знімок лишається тільки для монет в угодах,
  і рахувати «ринок» із трьох рядків не можна.
- ⚠️ **Розклад покриття змінився:** тепер `targeted − no_data == rows`, а
  `in_trade` — це ПІДМНОЖИНА рядків, а не «загублені» монети (тест-замок).
Тест: `test_mm_monitor.py` (154) — розділ 30.

### 🎨 ВІДСТУПИ ПАНЕЛІ 💧 СКАНЕРА + 🧲 ФІЛЬТР МАЛОГО МАГНІТУ (18.09)

**Скарга:** «Поля ховаються, переглянь візуальні відступи» (зі скріном) +
«В полі "🧲 Найсильніший магніт" відсотки вирівняй в рівну колонку і виділи
кольором і додай в параметри фільтр обмежуючий малий відсоток (за
замовчуванням 3%), тобто менше цього відсотка від ціни, монети не додавати в
таблицю».

**1. ЧОМУ ПІДПИС ГАРМОШКИ ВІДЛІТАВ ПРАВОРУЧ.** `.sm-settings-summary` — це
flex зі `space-between`, і в решти гармошок поруч стоїть ДРУГИЙ вузол
(`.sm-settings-summary-right`). У сканера був ОДИН текстовий вузол → він
притискався до правого краю, лишаючи порожню смугу. Тепер ліворуч підпис, а
праворуч **живий розклад параметрів** (`#lh-sum`: `BINANCE · 🎯 watchlist ·
720 год · 💧 маса ≥ 65% · 🧲 магніт ≥ 3% · ⏱ 15 хв`) — згорнута гармошка
мусить казати, ЗА ЧИМ зараз відбираються монети.
**2. ПОЛЯ — ОДНА СІТКА.** Замість інлайнових стилів у кожному полі — клас
`.lh-f` (однакова висота 28px, однакові підписи) у `.lh-grid` з рівним
`gap:12px 16px`. Чекбокс 🔁 переїхав у **власний рядок** за роздільником
(`.lh-flip`, `flex-basis:100%`) — на скріні він тиснувся збоку до вузького
поля. ⚠️ Поля «Монет / Обіг / OI» і далі ХОВАЮТЬСЯ у режимі watchlist (це
навмисно — контрол, що ні на що не впливає, вводить в оману), і тепер про це
сказано в підказці «Список».
**3. 🧲 РІВНА КОЛОНКА.** Комірка магніту зібрана з сегментів ФІКСОВАНОЇ ширини
(104 ціна ліворуч · 62 відстань праворуч · 56 частка праворуч, усе monospace)
— той самий прийом, що тримає рівним стовпчик у колонці «🎯 Автопілот».
Відстань і ціна фарбуються за напрямком (зелений ↑ / червоний ↓), частка —
приглушена: це РІЗНІ речі, і плутати їх не можна.
**4. 🧲 `min_magnet_dist_pct` (деф. 3.0, 0 = вимкнено)** — мінімальна ВІДСТАНЬ
до магніту у % від ціни. Магніт за пів відсотка — не ціль, а шум.
- ⚠️ Число беремо СИРИМ із `magnet_row['dist_pct']`: `magnet_dist` — це
  ФОРМАТОВАНИЙ рядок («↓5.58%»), і парсити його заради числа не можна
  (задокументована пастка). Рядок несе `magnet_dist_pct`, тож колонка й поріг
  — ОДНЕ число.
- ⚠️ **«Невідомо» ≠ «близько»:** монета, по якій драбина не дала сходинки,
  цим фільтром НЕ ріжеться (fail-open, як у 💧 фільтра входу) — у колонці
  чесно стоїть «—».
- ⚠️ Відсів НАЗВАНИЙ у статусі: `· магніт ближче 3%: N` (окремим лічильником
  `near`, а не змішаний зі «слабкими»).
Тест: `test_liq_hunter.py` (35) — розділ 8.

### 🐞 ДЕДУП ЧЕРГИ СКАНІВ НЕ НАКРИВАВ ТОГО, ХТО ВЖЕ ПРАЦЮЄ

Перша версія `scan_queue.submit` шукала однойменне завдання ЛИШЕ серед тих,
що ЧЕКАЮТЬ. Подвійний клік по «Сканувати» встигав проскочити: перший запит
воркер уже забрав (черга порожня) → другий ставав НОВИМ завданням, тобто
давав рівно той подвійний залп, від якого чергу й заводили. Тепер дедуп
дивиться і на `_cur`. Упіймано власним тестом (він став флакі саме через цю
гонку) — тест тепер ЧЕКАЄ, поки перший скан справді почне працювати.
⚠️ Наслідок для ТЕСТІВ: фонові цикли рушіїв у тестах піднімати не можна —
десяток таких потоків подає у СПІЛЬНУ чергу завдання з тим самим імʼям, і
дедуп (правильно) віддає чужий результат. У `test_liq_hunter._mk` потік
заглушено (`h.start = lambda: None`), а `MIN_GAP_SEC` на час тестів — 0.

### 🧮 ⚖ РІВНОВАГА і РОЗВОРОТ — РІЗНІ ПРИЧИНИ ЗАКРИТТЯ (скарга 18.09)

**Дослівно (зі скріном рядка логу):** «Монета закрилась саме по протилежному
значенню, а не рівновага — потрібно точно розрізнити причини закриття.»

**Діагноз:** обидві гілки `_mm_flat_exit_reason` віддавали ОДИН код
`mm_flat_exit`. Розгорнутий текст причини відрізнявся, але **бейдж у таблиці
угод і JS-мапа сторінки** мали для цього коду ОДИН підпис — «🧮 Старий МММ
втратив напрямок (⚖ рівновага)». Тобто закриття на ЗУСТРІЧНОМУ тиску
показувалось як «рівновага»: подія одна, а на екрані інша.

- Розворот тепер має ВЛАСНИЙ код **`mm_against_exit`** із власними підписами в
  УСІХ трьох місцях показу: розгорнутий `_close_detail` («🧮 Старий МММ
  РОЗВЕРНУВСЯ ПРОТИ позиції»), бейдж TM `_reason_label` і JS-мапа
  `smart_money.html` («🧮 Старий МММ ПРОТИ»).
- ⚠️ `mm_flat_exit` ЛИШАЄТЬСЯ за ⚖ рівновагою (і в уже закритих угодах), тож
  історія не псується заднім числом.
- ⚠️ Самі ПРАВИЛА не змінились: розворот закриває ЛИШЕ в режимі
  `'flat_or_against'` і ЗАВЖДИ одразу (витримка належить рівновазі).
- ⚠️ Раннер `test_exit_rules.py` — РУЧНИЙ перелік у `__main__`: новий
  `test_the_two_reasons_are_never_mixed_up` довелось додати туди явно (на цю
  пастку в проєкті вже наступали).

### 📏 РЯДОК СКАНЕРА — В ОДНУ СТРІЧКУ + 🦓 ЗЕБРА (скарга 18.09)

**Дослівно:** «Навіщо в дві стрічки робити записи, розмісти все в одну стрічку
і зроби зебру в таблиці, щоб не зливались.»

- Причина переносу — СМУГА магніту («$0.22000–0.22500»): сегмент під неї був
  104px. Тепер 152px, `white-space:nowrap` і на сегментах, і на всіх
  `#lh-table th/td`.
- 🦓 Зебра + підсвітка рядка + 1px-розділювачі — ТІ САМІ правила, що в таблиці
  МММ-монітора (клас на `tbody`, щоб порожній рядок-заглушка не смугався).
- 🐞 **Попутно: «23.0%%».** `ladder.make_verdict` віддає `magnet.pct` УЖЕ з
  «%», а комірка додавала свій — на екрані стояло два знаки. Тест-замок не дає
  повернути `${r.magnet_pct}%`.
Тест: `test_liq_hunter.py` (37) — розділ 9.

## 📨 НОВИЙ VOB + 🧮 БАНЕР + ТАБЛИЦЯ 💧 СКАНЕРА → СИГНАЛ (вимога 19.09)

**Дослівно:** «Якщо увімкнено 📦 Volumized OB Trend і увімкнено "Сканер
ліквідності", то кожен новоутворений VOB звіряємо з таблицею "Сканер
ліквідності", і якщо співпадає напрямок VOB + напрямок банера "МММ-монітор" і є
така ж монета у таблиці "Сканер ліквідності", одразу маємо сигнал і
відправляємо монету далі по алгоритму в Чергу (якщо активно) або відразу
відкриваємо угоду, перед цим провіривши чи не існує вже відкрита така угода.»

**ДЕ ЖИВЕ.** Подію («новоутворений VOB») ловить `smc_scanner._liq_vob_check` —
там, де VOB і рахується, у ТОМУ САМОМУ блоці скану, що малює бокс на графіку.
💧 Сканер лише ВІДПОВІДАЄ про таблицю: чиста `liq_hunter.vob_confluence(row,
vob_side, bias_dir)` + `LiqHunterDaemon.vob_match(symbol, side)`. Він і далі
нічого не відкриває сам (тест-замок по AST лишився чинний).

**ТРИ УМОВИ РАЗОМ** (`vob_confluence`, чиста функція): банер має НАПРЯМОК ·
напрямок VOB = напрямок банера · монета Є в таблиці **і її рядок зібрано в ТОЙ
САМИЙ бік**. Останнє не формальність: одразу після розвороту банера таблиця ще
стара (перескан попереду), і її рядки зібрані під ПРОТИЛЕЖНИЙ бік — узяти такий
рядок означало б назвати «збігом» пряме протиріччя. Причина повертається
ЗАВЖДИ: «сигналу немає» без пояснення читається як поломка.

- **БЛОК — САМЕ ТОЙ, ЩО НА ГРАФІКУ** (`_vob_chart_candidates`), та сама
  функція, що у 🟪 VOB-алерта; `breaker` сигналом не є.
- **«НОВИЙ» = ще НЕ опрацьований** — ВЛАСНИЙ список `_liqvob_seen[sym][side]`
  + `_vob_outcome` (пастка breaker: водяний знак завис би назавжди).
  ⚠️ База **окрема** від `_vob_alert_seen`: це два НЕЗАЛЕЖНІ споживачі однієї
  події, і спільна база означала б, що увімкнений 🟪 VOB-алерт мовчки зʼїдає
  блоки цього шляху (і навпаки).
- **ВІКНО СВІЖОСТІ — те саме** (`vob_alert_max_age_bars`, 0 = авто
  `swing_length + 2`): «новоутворений» — це момент появи, і другого числа для
  того самого поняття не заводимо. Старий блок → тиха база, у 🧾 Лог нічого.
- ⚠️ **РОЗБІГ НЕ КОВТАЄ БЛОК** (той самий прийом, що `wait_htf` у 🆕 Новому
  OB): немає збігу ЗАРАЗ (монети ще немає в таблиці, банер щойно розвернувся,
  угода відкрита) → блок НЕ позначаємо опрацьованим і спробуємо ще, поки він
  свіжий. Таблиця оновлюється раз на 15 хв — інакше реальний збіг за хвилину
  вже нічого не дав би.
- ⚠️ **ЧЕРЕЗ СПІЛЬНІ ВОРОТА `_signal_allowed`, І НІЯК ІНАКШЕ** (урок
  ASTERUSDT). «Далі по алгоритму в Чергу або відразу відкрити» робить
  `tm.on_signal(opened_by='liq_vob')` → `intercept`: черги вирішують самі, а
  коли всі вимкнені — TM відкриває напряму. Тест-замок на порядок викликів.
- ⚠️ **«Чи не існує вже відкрита така угода»** — ПУБЛІЧНИЙ
  `TradeManager.has_open_position(symbol)` (обидві книги: real + 🧪 paper).
  Новий публічний метод, а не заглядання у чужі `_positions` (той самий
  принцип, що `volumized_on()` / `last_tick_at()`).
- ⚠️ **ОДИН БЛОК — ОДИН СИГНАЛ.** Коли увімкнений ще й 🟪 VOB-алерт, той самий
  блок міг би дати ДВА сигнали по одній монеті. Алерт-шлях позначає
  `_vob_fired_ft[symbol] = ft` (обидві його гілки), і цей шлях такий блок
  пропускає.
- **Код сигналу `liq_vob`** = «💧 VOB + Сканер ліквідності», картинка 💧 —
  у `signal_labels.py`, в ОБОХ JS-дзеркалах і в `_kind_lbl` черги. Окремий
  код, бо в черзі й в угоді має бути видно, що це конфлюенс ТРЬОХ умов, а не
  звичайний VOB-алерт.
- **Тумблер `vob_signal_on` (деф. УВІМКНЕНО)** у налаштуваннях 💧 Сканера.
  Дефолт ON свідомо: умову ввімкнення користувач задав САМИМИ наявними
  тумблерами, тож третій вимикач із дефолтом OFF означав би, що вимога не
  працює, доки його не знайдуть. Потік угод це мовчки не розширює — сам
  💧 Сканер дефолтом ВИМКНЕНИЙ.
- ⚠️ **КЕШ НАЛАШТУВАНЬ СКАНЕРА (`SETTINGS_TTL = 5с`) ОБОВʼЯЗКОВИЙ:** шлях
  питає `vob_signal_on()` по КОЖНІЙ монеті КОЖНОГО скан-циклу, а без кешу це
  сотні сесій SQLAlchemy на цикл (урок `get_mm_settings`). `update_settings`
  кладе свіже значення в кеш ОДРАЗУ — зміна з UI діє миттєво.
- **ВИДИМІСТЬ.** Поки немає нового VOB, шлях мовчить ЗА ПОБУДОВОЮ, і без
  подання це читалось би як «не працює» (урок «де сигнали?»). Тому: кожен
  сигнал і кожна відмова фільтрів ідуть у 🧾 Лог (`source='LIQ-VOB'`); СТАНИ
  (`wait`/`dup`/`in_trade`/тиха база) — лише в `scanner.get_state()
  ['liqvob_diag']`; у панелі 💧 — рядок `#lh-sig` (лічильник + останні рішення)
  і окреме попередження, коли 📦 Volumized OB Trend вимкнено.
Тест: `test_liq_vob_signal.py` (18).

## 🔻 КОРЕКЦІЯ: ВЕРДИКТ НА БАНЕРІ + ВОРОТА ВІДКРИТТЯ (вимога 19.09)

**Дослівно:** «Ринок чітко показує по банеру "МММ-монітор", що в напрямку LONG,
але на даний момент на графіку корекція і всі монети пішли в SHORT… Потрібно
щоб бот моніторив графіки і відслідковував саме корекцію по монетах (наприклад
на молодших таймфреймах VOB, OB або FVG почали зʼявлятись у протилежному
напрямку від банера). Бот має професійно аналізувати графіки і візуалізувати
загальний вердикт на банері "МММ-монітор" стосовно чи почалась або закінчилась
корекція.» + «В період корекції потрібно обмежити відкриття угод.»

**Кейс зі скріна/логу (20.09):** банер 🟢 LONG 66% «сильний тиск», а в лозі —
`ZROUSDT LONG closed · 🧮 Старий МММ розвернувся у SHORT · −2.44%`, `UNIUSDT
LONG ✋ групове · 🤪 SHORT 74% (сильний)` → SL −0.42%. Тобто бот входив ЗА
банером рівно в той момент, коли ринок ішов ПРОТИ нього.

**ЩО ТАКЕ КОРЕКЦІЯ ТУТ.** Не «одна монета впала», а **ширина ринку**: скільки
монет монітора ПРЯМО ЗАРАЗ дивляться проти банера. Рахує ЧИСТИЙ модуль
`detection/mm_correction.py`, контекст збирає `fuel_filter._mm_track_correction`
(двигун рахує — `mm_monitor_state` читає, урок B2).

**ТРИ ОЗНАКИ, і кожна коштує НУЛЬ запитів до біржі:**
1. **📦 VOB проти** (`mm_corr_vob_pct`, деф. 60%) — частка монет, чий ПОТОЧНИЙ
   Volumized OB на молодшому TF (той самий блок, що на графіку) дивиться проти
   банера. Джерело — **`SMCScanner.volumized_trends()`** (новий ПУБЛІЧНИЙ
   метод; той самий кеш, що малює ▲/▼ у watchlist).
2. **💹 Ціна проти** (`mm_corr_price_pct`, деф. 60%) — частка монет, чий свіжий
   рух (те саме 15-хв вікно, що в колонці «Рух») іде проти банера. Монети без
   руху (▬) у частку НЕ входять — інакше рівний ринок читався б як корекція.
3. **📉 Важіль просів** (`mm_corr_lever_drop`, деф. 15 п.п.) — важіль банера
   У БІК СВОГО НАПРЯМКУ впав від піку за `WINDOW_SEC` (30 хв). Міряємо ЗІ
   ЗНАКОМ: якщо важіль устиг перевернутись, просідання чесно більше за пік.
Потрібно `mm_corr_min_layers` (деф. **2 з 3**).

⚠️ **ЧОМУ НЕ FVG І НЕ 1H-OB** (обидва були в прикладі користувача): FVG живе в
ОКРЕМОМУ рушії (`fvg_detector` + `ctr_fvg_*`) зі своїм скануванням — заради
нього довелось би ганяти ще один скан по 200 монетах, маючи рівноцінний і
безкоштовний 📦 VOB на тому самому молодшому TF. 1H-OB — СТАРШИЙ TF: його
розворот це зміна ТРЕНДУ, а не корекція в тренді.

**МАШИНА СТАНІВ** (`next_state`, чиста): `trend → pending → on → ending →
ended → trend`.
- ⚠️ **ПІДТВЕРДЖЕННЯ СИМЕТРИЧНЕ** (`mm_corr_confirm_sec`, деф. 120с): і
  «почалась», і «завершилась» — ПОДІЇ. Оголосити кінець на одному спокійному
  такті означало б повернути бота в ринок на відкаті ВСЕРЕДИНІ корекції.
- ⚠️ **ГІСТЕРЕЗИС**: входимо за СУВОРИМИ порогами (`lit`), тримаємо за
  ПОСЛАБЛЕНИМИ (`lit_hold`, −`EXIT_MARGIN_PCT`=10 п.п. / −`EXIT_MARGIN_PP`=5).
- ⚠️ **МАЛА ВИБІРКА — НЕ ОЗНАКА**: шар, під яким менше за `MIN_SAMPLE`=5 монет
  із даними, лишається НЕВИЗНАЧЕНИМ («немає даних» ≠ «корекції немає»).
- ⚠️ **⚖ БАНЕР БЕЗ НАПРЯМКУ → КОРЕКЦІЇ НЕМАЄ** за визначенням (немає тренду —
  нема чого коригувати); причина названа в `reason`.
- ⚠️ **ФЛІП БАНЕРА ОБНУЛЯЄ ВЕРДИКТ** (і пік важеля): це зміна ТРЕНДУ, а
  «корекція проти LONG» під банером SHORT блокувала б відкриття за вердиктом
  про ринок, якого вже немає.
- ⚠️ **ПЕРСИСТ** (`mm_corr_st` у блобі `fuel_filter_state`): «корекція триває
  1г 20хв» — стан РИНКУ, і `botupdate` не має його обнуляти (як і таймер
  банера). Розклад шарів НЕ персиститься — перерахується першим тактом.
- 🧾 У 🧾 Лог ідуть лише ПОДІЇ (`on` / `ended`, `source='MMM'`); стани — ні
  (урок флуду VOB).

**🚫 ВОРОТА ВІДКРИТТЯ** (`mm_corr_block_open`, деф. **УВІМК**) — публічний
`ff.correction_blocks_open()` → `(bool, причина)`. Стоїть у **ДВОХ** вузлах,
бо автоматичні відкриття йдуть двома шляхами (один накрив би половину — саме
так колись «проспали» 🚦 головні кнопки):
- `fuel_filter._open` — усі черги (1/2/3/4) і реверс;
- `trade_manager.on_signal` — ПРЯМІ відкриття (усі черги вимкнені), одразу
  ПЕРЕД гейтом рівнів (він важкий — магніт + `_pilot_context`).
- ⚠️ Блокує ЛИШЕ **підтверджена** корекція (`state == 'on'`): відліки
  (`pending`/`ending`) — це ще не подія.
- ⚠️ Блокуються **обидва напрямки**: вхід ЗА банером у корекції ловить ніж, а
  вхід ПРОТИ банера — торгівля проти власного тренду бота.
- ⚠️ **✋ РУЧНЕ ОБХОДИТЬ** (`by_hand` / `opened_by='manual'`) — корекція це
  ОЦІНКА РИНКУ ботом, а не заборона користувача (той самий принцип, що в
  «нової ситуації»; на відміну від 🚦 головних кнопок).
- ⚠️ **Записи з черг НЕ виселяються** — корекція мине, і монета відкриється
  штатно (виселяють лише TTL/застій/🚨 hard-max).
- ⚠️ **АНТИ-ФЛУД**: двигун смикає `_open` щотакту по кожній монеті черги, а
  корекція триває годинами → рядок у лог пишеться РІВНО ОДИН раз на монету НА
  ЕПІЗОД (`_mm_corr_skip_logged`, ключ — `since` корекції).
- ⚠️ **FAIL-OPEN**: збій/недоступність детектора НЕ зупиняє торгівлю.
- ⚠️ Вимкнений 🧮 монітор гасить і вердикт (немає живого розрахунку — немає й
  блокування).

**UI:** рядок `#mm-corr-row` ПІД банером (стан · таймер · розклад ознак ·
`🚫 відкриття зупинено`) + гармошка «🔻 Корекція — детектор і ворота
відкриття» з усіма сімома контролами; живий розклад параметрів у згорнутій
шапці (`#mm-corr-sum`). Малює `mmRenderCorr`, який смикає ТОЙ САМИЙ глобальний
1с-тікер (другого інтервалу не заводимо).

### ⏱ ТАЙМЕР БАНЕРІВ — ОДИН СТИЛЬ (вимога 19.09)

**Дослівно:** «Зроби професійний таймер "2д 09:57:19" для банера, щоб все
виглядало в одному стилі.»
Сегмент ДНІВ був єдиним, що випадало з ряду: окремий **жовтий** текст
(`.fday`, 0.9rem) без плитки поруч із білими плитками 1.25rem. Тепер доба — ТА
САМА плитка (`fd fdd`: те саме тло, висота, шрифт і колір), а одиниця «д» —
маленький приглушений суфікс усередині неї. Двокрапки після доби НЕМАЄ
(«2д 09:57:19», а не «2д:09:57:19») — відступ дає CSS.
⚠️ `flipTimerHTML` спільний, тож у тому самому стилі став і банер ₿ BTCUSDT.
⚠️ Додано компактний варіант `.ff-flip-sm` (вердикт корекції стоїть у рядку з
текстом, а не окремим банером).

### 🧾 СИРИЙ ЛОГ КОРЕКЦІЇ — ДЛЯ КАЛІБРУВАННЯ ПОРОГІВ (вимога 20.09)

**Дослівно:** «Зроби логування по "Корекції" для подальшого аналізу і
коригування налаштувань.»

Потрібен був не ще один рядок для очей (події `on`/`ended` у 🧾 Лозі вже є), а
**РЯД ЗНАЧЕНЬ трьох ознак у часі** + пороги, що діяли, + що бот тоді зробив.
Окрема таблиця **`sob_mm_corr_log`** (модель `MmCorrectionLog`), ПЛОСКІ
колонки — щоб CSV аналізувався одним проходом.

- **ЄДИНИЙ ПИСАЧ — `_mm_corr_log_write(kind, **extra)`**: і періодичний зріз, і
  події, і блокування монети йдуть через нього, інакше рядки різних видів мали
  б різні набори полів.
- **Джерело — ГОТОВИЙ знімок** (`_mm_corr` + `_mm_bias`), той самий, що малює
  банер: нічого не перераховуємо (урок PD-зони).
- ⚠️ **ПОРОГИ ПИШЕМО В КОЖЕН РЯДОК** (`vob_need`/`price_need`/`lever_need`/
  `need_layers`/`confirm_sec`). Їх і крутитимуть за підсумками аналізу — без
  знімка порогів старі рядки стали б нечитабельними («60% це багато чи мало
  було тоді?»). Тест-замок.
- ⚠️ **ПИШЕМО І «КОРЕКЦІЇ НЕМАЄ»** (`state='trend'`). Негативні семпли для
  підбору порогів так само обовʼязкові, як позитивні: без них видно лише те, де
  детектор спрацював, і НЕ видно, де він спрацював БИ з іншим числом.
- **`kind`**: `sample` (зріз) · `start` · `end` (несе `lasted`) · `state`
  (перехід pending/ending) · `block` (відкриття по КОНКРЕТНІЙ монеті зупинено:
  `symbol`/`side`/`price`).
- ⚠️ **ПОДІЯ — ЗАВЖДИ, СТАН — ЗА ІНТЕРВАЛОМ** (`mm_corr_log_every_sec`, деф.
  **300с** ≈ 288 рядків/добу; такт двигуна 30с дав би 2880). Перехід не має
  губитись через те, що семпл щойно писався. Нижня межа валідації — 30с
  (частіше фізично нових чисел немає).
- ⚠️ **ЦІНА В МОМЕНТ БЛОКУВАННЯ** — головне число post-hoc аналізу («а що
  зробив ринок після того, як ми не зайшли»). Немає переданої → беремо зі
  знімка монітора (нуль запитів). Плюс `blocked_n` — скільки монет ворота вже
  зупинили В ЦЬОМУ епізоді, тобто ЦІНА блокування.
- ⚠️ **ОДИН ПИСАЧ НА ОБИДВА ВУЗЛИ ВОРІТ** — `ff.note_correction_block(...)`
  (анти-флуд за епізодом + рядок `block`). Його кличуть і `fuel_filter._open`,
  і `trade_manager.on_signal`; дві копії анти-флуду розійшлися б і `blocked_n`
  рахував би половину. ⚠️ Рядок у 🧾 Лог роботи бота лишається за викликачем:
  у `_open` він анти-флудиться результатом, у `on_signal` пишеться ЗАВЖДИ
  (прямий сигнал трапляється рідко — глушити його було б втратою події).
- ⚠️ **FAIL-SAFE:** помилка запису НЕ підіймається в такт (лог не має права
  зупинити торгівлю) і НЕ зсуває таймер троттлу — інакше невдала спроба зʼїла б
  наступний семпл.
- **Тумблер `mm_corr_log_enabled` (деф. УВІМК)** гасить САМ ЗАПИС (а не показ):
  без логу калібрувати нема на чому, тож дефолт ON.
- **API:** `GET /api/fuel-filter/mm-corr-log?limit&kind&state&symbol&format=csv`
  (CSV: найстаріші ЗВЕРХУ — ряд у часі читається згори вниз; тест-замок вимагає
  ВСІ колонки моделі в експорті).
- **UI:** у гармошці «🔻 Корекція» за роздільником — `ff-mm-corr-log`,
  `ff-mm-corr-log-every` і кнопка **⬇️ CSV логу**; у згорнутій шапці видно
  `🧾 лог 300с` / `🧾 лог вимкнено`.
- **Чистка:** `'sob_mm_corr_log'` у `_SERVICE_TABLES_TIME` (ручна «🗄️ Службові»
  + DB-autoclean) + дії `mm_corr_log_old`/`mm_corr_log_all` +
  `db.clear_old_mm_corr(days)`.
- ⚠️ Нові ключі → `merged.update(stored)` віддає дефолт, міграція НЕ потрібна.

### 🐞 ДВА ДЕФЕКТИ ПОКАЗУ В РЯДКУ ОЗНАК (скрін 20.09)

1. **`82.72762799263/120с`** — `cand_since` це серверний `time.time()`
   (ДРОБОВИЙ), а фронт віднімав його від цілих секунд. Тепер відлік
   округлюється (`Math.round`) — у самому стані число лишається точним.
2. **`📉 Важіль просів 24.7%/15%`** — просідання важеля міряється в **п.п.**, а
   не у %. Одиницю тепер дає **БЕКЕНД** (`layer['unit']`: `'%'` для часток,
   `'п.п.'` для важеля), а не вгадує фронт — той самий принцип, що
   `verdict.parts`. Фолбек `'%'` — для старішого бекенда.
Тест-замки: `test_js_countdown_is_whole_seconds_not_a_raw_float`,
`test_js_shows_the_unit_each_layer_really_uses`.

Тест: `test_mm_correction.py` (41) — розділ 6.
⚠️ Пастка (вп'яте): нове поле стану `_mm_corr_log_at` довелось додати в `_mk`
ОБОХ файлів тестів (`test_mm_correction.py` і `test_mm_monitor.py`).
⚠️ У тестах шар БД підмінюється ще НА ІМПОРТІ (`_install_db`): справжній
`storage.db_operations` тягне конфіг і зʼєднання заради тесту детектора.
⚠️ Пастка, на яку наступили ВЧЕТВЕРТЕ: нове поле стану (`_mm_corr_st`,
`_mm_corr`, `_mm_lever_hist`, `_mm_corr_skip_logged`) ЗАВЖДИ додавати в `_mk`
тестів монітора — інакше `_mm_capture` ковтає AttributeError і тести «нічого
не роблять». І ВТРЕТЄ: `test_ui_table_is_striped…` різав HTML фіксованими
«900 символами перед таблицею» — новий блок стилів виштовхнув перевірку за
межу; тепер ріжемо по межах секції.

## 📨 ТЕМА В TELEGRAM «₿ BTCUSDT» → «🧮 МММ-МОНІТОР» + УГОДИ В ГРУПУ (21.09)

**Три вимоги користувача одним пакетом, дослівно:**
1. «Перероби все оповіщення в Телеграм, яке стосувалось ₿ BTCUSDT тепер на
   "🧮 МММ-МОНІТОР", зміни в групі назву і напрявляй туди як і було раніше
   ₿ BTCUSDT якщо увімкнено відправку, а основним тепер сюди напрявляй
   оповіщення стосовно змін банера "🧮 МММ-МОНІТОР".»
2. «Також проаналізуй повідомлення, які ідуть в основний телеграм бот стосовно
   відкритих угод поза потом "External", напрляй їх в основну групу, а не в
   телеграм бот.»
3. «Стосовно корекцій також зроби оповіщення. Додай можливість вмикати та
   вимикати, тобто можливість контролювати відправку всіх оповіщень.»

### 1. ТЕМА ПЕРЕЙМЕНОВАНА — і в коді, і В САМІЙ ГРУПІ

`_CAT_LABEL['btc'] = '🧮 МММ-монітор'`, `_CAT_TAG['btc'] = '#МММ_монітор'`.

⚠️ **КЛЮЧ категорії лишився `btc`** — його знають `TELEGRAM_CHAT_BTC` /
`TELEGRAM_TOPIC_BTC`, збережені id тем (`tg_forum_topics`) і тумблер кабінету
`notify_btc`. Перейменувати ключ означало б загубити і тему, і тумблер (той
самий прецедент, що з `q2_auto_ob_sl*`: **історична назва ключа ≠ область дії**).

⚠️ **ЗМІНИ `_CAT_LABEL` МАЛО.** Тема створюється ОДИН раз, її
`message_thread_id` персиститься — тож у Telegram і далі висіло б «₿ BTCUSDT»
над повідомленнями про монітор. Тому `_forum_rename_if_needed(chat, cat, tid)`
кличе **`editForumTopic`** на кешованому шляху `_forum_thread`.
- ⚠️ Поставлену назву **ЗАПАМʼЯТОВУЄМО в БД** (`tg_forum_topic_names`), інакше
  `editForumTopic` смикався б на КОЖНЕ повідомлення — зайвий запит до API на
  рівному місці. Кеш `_forum_names_cache` + персист → переживає рестарт.
- ⚠️ Невдача (виняток або `ok:false`) НЕ ламає відправку і **НЕ** запамʼятовує
  назву, якої не поставили — спробуємо ще раз наступного разу (best-effort, як
  і саме створення теми).
- Кабінет адміна і онбординг-текст теж називають тему новим імʼям — вимикати
  «не те» не має бути легко.

### 2. ГОЛОВНИЙ ПОТІК ТЕМИ — ЗМІНА СТАТУСУ БАНЕРА 🧮

`_mm_bias_alert(prev, side, now, settings)` викликається з кінця
`_mm_track_bias`, коли **ПІДТВЕРДЖЕНИЙ** статус змінився. Формат:
`🧮 МММ-МОНІТОР → 🔴 SHORT · важіль 62% / було: 🟢 LONG · тримався 1г 12хв /
монет: 47 · 🟢 12 / 🔴 28 / ⚖ 7`.
- ⚠️ **ПЕРШИЙ ТАКТ ПІСЛЯ СТАРТУ — МОВЧКИ.** `_mm_bias` персиститься, тож після
  `botupdate` перший же такт міг би «побачити зміну» і надіслати повідомлення
  про подію, якої не було. `_mm_bias_tg_last` живе В ПАМʼЯТІ (СВІДОМО не
  персиститься) і на першому такті лише запамʼятовується — той самий прийом, що
  в ₿-алерта і в тихій базі VOB.
- ⚠️ **Лише ПІДТВЕРДЖЕНИЙ статус.** Кандидат (⏳) — це ще відлік; сповіщати про
  нього означало б повернути миготіння, від якого банер і захищали (розділ 🐢).
- ⚠️ Best-effort: збій відправки НЕ підіймається в такт двигуна — банер мусить
  порахуватись, навіть коли Telegram лежить.
- ₿-сеанс (START/STOP) іде В ЦЮ САМУ тему, як і раніше, зі СВОЇМ тумблером
  `start_signal_tg_alerts` біля банера ₿.

### 3. ПОДІЇ КОРЕКЦІЇ — ТИМ САМИМ ТЕКСТОМ, ЩО В 🧾 ЛОЗІ

`on` / `ended` з `_mm_track_correction` ідуть у ту саму тему.
- ⚠️ **Текст будується ПОЗА `try` блоку логу**: він потрібен ОБОМ каналам, і
  збій одного не має лишати другий без повідомлення. Два різні тексти про ОДНУ
  подію розійшлися б — тест-замок вимагає, щоб рядок логу був ПІДРЯДКОМ
  повідомлення.
- ⚠️ **Лише події.** Відліки `pending`/`ending` не шлються — той самий принцип,
  що з кандидатом банера.
- ⚠️ Тумблер гасить САМЕ ВІДПРАВКУ: 🧾 Лог роботи бота лишається, інакше подія
  зникла б і з історії.

### 4. ПОВІДОМЛЕННЯ ПРО ВІДКРИТІ УГОДИ — В ГРУПУ, НЕ В ПРИВАТ

Без `category` `_notify` шле адміну в ПРИВАТ, зарезервований під СЛУЖБОВІ
повідомлення. П'ять подій відкритої угоди туди сипались помилково і тепер
несуть `category='trades'`:
⚖️ BE-move (real + paper) · 📈 трейл після BOS-2 (real + paper) ·
🔄 External close · 🔄 Adopted external position.

⚠️ **СВІДОМО НЕ ЧІПАЛИ** (тест-замок): `❌ Sizing error`, `❌ Failed to open`,
`❌ Order rejected`, `⚠️ Close API error`. Це помилки, а не події ВІДКРИТОЇ
угоди — приватний бот саме під них і зарезервований.

### 5. КОНТРОЛЬ НАД УСІМА ПОТОКАМИ — В ОДНОМУ МІСЦІ

Гармошка **«📨 Сповіщення в Telegram — тема «🧮 МММ-монітор»»** у шапці
монітора: `ff-mm-bias-tg` (⚖️ банер) + `ff-mm-corr-tg` (🔻 корекція) + напис,
де стоїть тумблер ₿-сеансу і що майстер-вимикач усієї теми — у кабінеті адміна
(`notify_btc`, гасить ВСІ три потоки).
- `_mmTgSummary(s)` малює в згорнутій шапці `📨 ⚖️ банер · 🔻 корекція · ⛔ ₿
  сеанс` — мовчазно вимкнений потік читався б як «бот перестав повідомляти».
- Дефолти `mm_bias_tg` / `mm_corr_tg` — **УВІМК**: тема існує САМЕ заради цих
  подій. НОВІ ключі → `merged.update(stored)` віддає дефолт, міграція не
  потрібна.
⚠️ `mm_corr_tg` живе в `mm_correction.DEFAULTS` (це про детектор), а
`mm_bias_tg` — у `fuel_filter.DEFAULT_SETTINGS` (це про банер).

Тест: `test_tg_routing.py` (24).

### 🐞 «НАЗВА ТЕМИ СТАРА, СПОВІЩЕНЬ НЕМАЄ» — ДВА ДЕФЕКТИ ПОПЕРЕДНЬОЇ ПРАВКИ

Перша ж перевірка на проді: тема називалась по-старому, і **жодного**
сповіщення не прийшло. Причини РІЗНІ й обидві мої.

**1. ПЕРША РЕАЛЬНА ЗМІНА ПІСЛЯ РЕСТАРТУ КОВТАЛАСЬ (головне).**
`_mm_bias_alert` викликався ПІД `if side != prev`, а всередині нього живе
позначка «перший показ після старту» (`_mm_bias_tg_last`). Тобто позначку
зʼїдала не перша ПЕРЕВІРКА, а перша реальна ЗМІНА статусу. `botupdate` роблять
часто → на проді сповіщень не було взагалі. **Відтворено прогоном** (такт 1 —
тиша, такт 2 з реальним фліпом — теж тиша, і лише третій пішов).
**Фікс:** виклик тепер **БЕЗУМОВНИЙ, на КОЖНОМУ такті** — рішення «подія чи
ні» ухвалює сама функція, порівнюючи зі СВОЄЮ позначкою. Тест-замок дивиться
і на поведінку, і на те, щоб виклик не заводили назад під умову.
⚠️ Попутно: рядок «тримався …» був ПОРОЖНІЙ завжди, бо брав
`_mm_bias['since']`, уже перезаписаний на НОВИЙ статус. Тепер тривалість іде
від `_mm_bias_tg_at` (відколи МИ його бачили), а якщо відлік почався з
рестарту — позначка **↻** (та сама конвенція, що в банері).

**2. ПЕРЕЙМЕНУВАННЯ СТОЯЛО ЛИШЕ НА ОДНОМУ З ДВОХ МАРШРУТІВ.**
`_forum_rename_if_needed` кликався всередині `_forum_thread`, а той виходить
ПЕРШИМ РЯДКОМ, коли немає `TELEGRAM_FORUM_CHAT`. Тему можна задати і напряму —
`TELEGRAM_CHAT_BTC` + `TELEGRAM_TOPIC_BTC`; на цьому шляху перейменування не
відбувалось НІКОЛИ. **Фікс:** виклик переїхав у `_cat_chat` і працює для
ОБОХ маршрутів (`if chat and thread`).
⚠️ Кеш назв тепер ключований **парою (чат, категорія)**: теми різних категорій
можуть жити в різних чатах, і спільний ключ писав би назву однієї в запис іншої.

**3. 🩺 ПЕРЕВІРКА ЗАМІСТЬ ЗДОГАДКИ.** Корінь обох скарг у тому, що всі чотири
причини мовчання невидимі: вимкнений тумблер кабінету (`notify_btc`) · не
налаштований чат/тема · бот не адмін теми · відмова Telegram. Тепер:
- `tg_bot.category_check(category, send_test=)` → маршрут (чат, тема, `route` =
  форум / власний чат / ПРИВАТ адміна), стан тумблера, причина невдалого
  перейменування і **дослівна відповідь Telegram**;
- `notify_category` зберігає причину відмови (`_last_send_err`) і пише її в
  stdout — раніше віддавав голий `bool`, і «Telegram не прийняв» не
  відрізнялось від «ми не слали»;
- `POST /api/tg/topic-check` {category, test} + кнопка **🩺 Перевірити тему**
  у гармошці сповіщень: шле пробне повідомлення ТИМ САМИМ шляхом і друкує
  результат (зелений / бурштин / червоний).
⚠️ Перевірка не «схожа на відправку» — вона і Є відправкою через
`notify_category`, інакше вона могла б проходити там, де бот мовчить.

Тест: `test_tg_routing.py` (30) — розділ 6.

### 🎚 ПОРОГИ КОРЕКЦІЇ ПЕРЕКАЛІБРОВАНО ЗА ЛОГОМ + 🗑 кнопка очищення (21.09)

**Аналіз першого ж логу** (26.3 год, 345 семплів, 70 монет, банер LONG увесь
час — фліп цим логом НЕ перевірено):
- **33 старти = раз на 48 хв**, 🚫 відкриття зупинені **6.8 год = 26% доби**;
  медіана епізоду 17 хв, 7 із 27 коротші за 10 хв;
- **💹 Ціна — тригер у 30 із 33 стартів і НАЙШУМНІШИЙ шар:** медіанний стрибок
  між сусідніми семплами **14.4 п.п.**, 90-й перцентиль **49.8**, максимум
  88.3 (📦 VOB для порівняння — **1.4 п.п.**, важіль — 4.3). Розподіл
  `price_pct` — увесь діапазон 0..100 з медіаною **рівно 50**, тобто поріг 60
  стояв у точці, яку показник перетинає постійно;
- **📦 VOB навпаки — найкорисніший:** стабільний, і саме він ТРИМАЄ корекцію
  (у 22 з 33 згасань лишається останнім засвіченим, коли ціна вже впала до
  медіанних 33%). Його медіана 56 при порозі 60 — майже дотягує;
- **9 із 33 стартів — повернення з `ending`** (миготіння всередині епізоду);
  5 стартів мали `lit < need` — це штатний гістерезис `lit_hold`;
- ✅ ідея вірна: важіль на старті медіанно 62, на кінці 72, **відновився у 20
  з 24 епізодів (83%)** — детектор ловить реальні просідання в тренді.

**Контрфактична симуляція** на ТИХ САМИХ семплах (відтворює факт: 33 старти /
6.9 год проти реальних 33 / 6.8 — тож числам можна вірити):

| конфігурація | стартів | блоковано |
|---|---|---|
| було (price 60 · 120с) | 33 | 26% доби |
| **стало (price 80 · 300с)** | **14** | **9% доби** |
| «3 з 3» | 3 | 2% доби (детектор фактично вимкнено) |

**Змінено ЛИШЕ два дефолти:** `mm_corr_price_pct` 60 → **80** (≈90-й
перцентиль шуму) і `mm_corr_confirm_sec` 120 → **300**. `mm_corr_vob_pct` і
«2 з 3» НЕ чіпали — VOB робить корисну роботу.
⚠️ **Вікно корекції СВІДОМО довше за банерне** (`mm_bias_confirm_sec` = 120):
банер гасить ПІДПИС, а тут ми зупиняємо ТОРГІВЛЮ. Старий тест-замок на 120
ПЕРЕПИСАНО з поясненням, а не «полагоджено».

**⚠️ ОДНОРАЗОВА МІГРАЦІЯ `mm_corr_tuned_v1` — без неї зміна не діяла б.**
Сторінка шле УСІ ключі одним блобом, тож `mm_corr_price_pct: 60` уже осів у БД
і перекрив би новий дефолт (урок `pilot_autofill_migrated_v1`).
`FuelFilterDaemon._migrate_settings()` кличеться з `__init__` одразу після
`_load_state`.
- ⚠️ **Переписуємо ЛИШЕ значення, що дорівнює СТАРОМУ дефолту** (`MM_CORR_TUNE`
  = {ключ: (старе, нове)}): збережені 60/120 — це майже напевно старий дефолт,
  а 65 чи 240 людина поставила руками, і чіпати це не можна.
- ⚠️ **Позначку ставимо і на ЧИСТІЙ установці** (порожній блоб) — інакше
  міграція спрацювала б на ДРУГОМУ старті й перекрила б уже зроблений вибір.
- ⚠️ Збій БД усередині міграції НЕ підіймається (вона в `__init__` — виняток
  там поклав би рушій).
- ⚠️ Хардкоджені фолбеки в HTML (`value=`, `_mmcNum`, `!= null ? … : 60`) —
  це ДРУГЕ написання того самого числа; оновлені всі 6, тест-замок стежить.

**🗑 Кнопка «Очистити лог»** у гармошці 🔻 Корекція, ПОРУЧ із ⬇️ CSV: після
зміни порогів стара вибірка лише заважає (у ній семпли, зняті за іншими
числами). `POST /api/fuel-filter/mm-corr-log/clear` {days} → **наявний**
`db.clear_old_mm_corr(days)`, де `days=0` = все («старше за зараз»). Другої
копії DELETE не заводимо — розійшлись би.
- ⚠️ Дія НЕЗВОРОТНА → `confirm()` із підказкою «спершу вивантажте CSV»; окрема
  перевірка `if (!r.ok)` (урок `submitManualTp1`); відповідь називає **кількість
  видалених рядків** — «натиснув і тиша» читалось би як збій.

⚠️ **ЩО ЛОГ НЕ ПОЯСНИВ — 0 блокувань.** `kind='block'` — жодного рядка,
`blocked_n` максимум 0: за 6.8 год зі статусом «відкриття зупинено» ворота не
зупинили ЖОДНОЇ монети. Отже користь фічі поки не доведена. Дві гіпотези:
черги в ці вікна не намагались відкривати, або гейт не досягається. **Не
досліджено** — чекає рішення користувача.
⚠️ Пастки цієї правки: новий `api_fuel_filter_mm_corr_log_clear` зламав чужий
тест, що шукав маршрут експорту РЕГУЛЯРКОЮ ЗА ПРЕФІКСОМ імені (тепер `_fn_src`
за точним імʼям через AST); а замок «у маршруті немає SQL» падав спершу на
власному докстрінгу (урок `ensure_fresh`), потім на полі відповіді `deleted`,
яке містить слово DELETE як підрядок — шукати треба `DELETE FROM`.

Тест: `test_mm_correction.py` (50) — розділ 7.

### 🧭 КОРЕКЦІЯ: ПОЧАТОК — ІМПУЛЬС, КІНЕЦЬ — ШИРИНА РИНКУ (22.09, не зламати!)

**Скарга користувача дослівно:** «Мені не подобається як працює корекція. Це
точно не професійному рівні якщо VOB 80% і корекція завершилась. Це як так
завершилась, коли майже всі монети у протилежному стані? А ціна, як можна
визначити за 15хв, що корекція завершилась, коли на проміжку наприклад в один
день корекція продовжується. Почитай як професійно визначати початок та кінець
корекції в інтернеті і давай реалізуємо все на професійному рівні в ботові.»

**ДІАГНОЗ ЗА ЛОГОМ (12 год, 144 семпли) — скарга підтверджена дослівно.**
📦 VOB не опускався нижче **53.3%** ЖОДНОГО разу (медіана 80.3, максимум 91.5,
139 із 144 семплів ≥ 60), а детектор за цей час **11 разів оголосив кінець** —
у т.ч. о 03:32 при `vob 90.1%` (**64 монети з 71**), `price 7.7%`, `lever 6.4`.
Розподіл кроків між сусідніми семплами: 💹 ціна **15.3 п.п.** (p90 = 50.8),
📉 важіль 3.9, 📦 VOB — **1.4**.

**КОРІНЬ — НЕ ПОРОГИ, А СХЕМА ГОЛОСУВАННЯ.** Усі три ознаки були РІВНИМИ
голосами в `lit >= need`, тож кінець ухвалювали два НАЙШВИДШІ шари (💹 вікно
15 хв, 📉 вікно 30 хв), а повільну СТРУКТУРНУ ширину ринку вони просто
перекривали голосами. Підняти пороги (що ми зробили 21.09) тут не допомагає:
ламається сама схема.

**ЯК ЦЕ РОБЛЯТЬ ПРОФЕСІЙНО** (breadth-література): тренд **починається з
імпульсу, а закінчується зі СТРУКТУРИ / ШИРИНИ**. Класика — Zweig Breadth
Thrust з АСИМЕТРИЧНИМИ порогами (<40% → >61.5%) і «percentage of stocks above
MA», де **50% — нейтральна лінія**, >70% перекуплено, <30% перепродано. Корекцію
не вважають завершеною, поки ширина не відновилась; окремо описаний і випадок
«**time correction**» — ціна вже не падає, але внутрішня структура ще зламана.
Це РІВНО наш кейс: 💹 7.7% при 📦 90%.

**РЕАЛІЗАЦІЯ — ВХІД І ВИХІД СТАЛИ РІЗНИМИ ЗА ПРИРОДОЮ.**
- **ПОЧАТОК** — швидкі ознаки (`lit >= need`), але 📦 **ширина ОБОВʼЯЗКОВА**
  (`mm_corr_vob_required`, деф. **True**): корекція це подія ШИРИНИ, а не
  смикання ціни.
- **КІНЕЦЬ** — вирішує **ЛИШЕ ширина** (`mm_corr_breadth_exit`, деф. **True**):
  поки частка монет ПРОТИ банера ≥ `mm_corr_vob_exit_pct` (деф. **50%**),
  завершити корекцію НЕ МОЖНА. 💹 ціна і 📉 важіль можуть корекцію лише
  **ПРОДОВЖИТИ**, скоротити — ніколи (`stay = вето OR (lit_hold >= need)`).

⚠️ **ЧОМУ САМЕ 50.** Це нейтральна лінія ширини («більшість монет уже НЕ проти
тренду») і водночас `mm_corr_vob_pct − EXIT_MARGIN_PCT`. Тобто **гістерезис був
правильний від початку — він просто НЕ ДІЯВ**, бо шар перекривали голосами.
⚠️ **ПОРІГ ВИХОДУ МОЖЕ БУТИ ВИЩИМ ЗА ВХІД** — див. розділ «60/65» нижче
(22.09 клемп СКАСОВАНО рішенням користувача; моє обґрунтування «миготіння на
кожному такті» було перебільшенням і спростоване прогоном).
⚠️ **ШИРИНИ НЕМАЄ (📦 вимкнено / мала вибірка) → ФОЛБЕК на старе правило
голосів.** «Немає даних» ≠ «ширина відновилась»: вигадувати кінець корекції на
порожньому місці не можна.
⚠️ **«КОРЕКЦІЯ НАЗАВЖДИ» неможлива:** якщо ширина тримається проти банера досить
довго, сам банер зрештою фліпне, а фліп ОБНУЛЯЄ вердикт. Завершувати корекцію
**за таймером СВІДОМО не будемо** — це повернуло б бота в ринок, де 9 монет із
10 структурно проти нього (тобто рівно те, на що скаржився користувач).
⚠️ **Рішення живуть у `evaluate`** (`start_ok` / `stay` / `breadth_ok` /
`exit_pct` / `why`), а `next_state` лишається МАШИНОЮ СТАНІВ і просто приймає їх
готовими (`start_ok=` / `stay=`, дефолт `None` = стара арифметика, тож усі
наявні виклики й тести живі). Пуре-функція **`breadth_exit_ok(vob, exit_pct)`**
→ `True` / `False` / `None` — ЄДИНЕ місце правила.

**ПЕРЕВІРЕНО НА ДВОХ РЕАЛЬНИХ ЛОГАХ** (прогін через СПРАВЖНІ `breadth_exit_ok`
+ `next_state`):

| лог | було | стало |
|---|---|---|
| 26 год | 14 «корекцій», медіана **10.5 хв**, 8.6% часу | **3** справжні, медіана **177 хв**, 40% часу, **2 завершились самі** |
| 12 год | 12 «корекцій», медіана 13.5 хв, 25% часу | **1** на **10.7 год** (91% часу) |

Другий рядок і є правильною відповіддю на той ринок: ширина там не опускалась
нижче 53%, отже корекція справді тривала весь час.

⚠️ **РОЛЬ ШАРУ ВІДДАЄ БЕКЕНД** (`role`: `'both'` для 📦, `'start'` для 💹/📉) —
той самий принцип, що `unit`. У рядку вердикту 💹/📉 підписані «старт», а поки
корекція триває, поруч стоїть **`🧭 кінець коли 📦 <50%`**: інакше «💹 7.7%/80%»
біля живої корекції виглядало б як суперечність.
⚠️ **ПОДІЯ «ЗАВЕРШИЛАСЬ» НАЗИВАЄ ЧИСЛО ШИРИНИ** (`why` у 🧾 Лозі й Telegram) —
саме на мовчазному «завершилась» стару версію і спіймали.
⚠️ **`vob_exit` — НОВА КОЛОНКА сирого логу** (модель + білий список
`_MM_CORR_FIELDS` + CSV-експорт + ідемпотентний `ALTER TABLE … ADD COLUMN`):
поріг, що тепер вирішує кінець, мусить стояти в КОЖНОМУ рядку так само, як
`*_need` — інакше старі семпли стануть нечитабельними.
⚠️ Нові ключі → `merged.update(stored)` віддає дефолт, міграція НЕ потрібна
(на відміну від `mm_corr_tuned_v1`, де мінявся ІСНУЮЧИЙ дефолт).
⚠️ Пороги 21.09 (💹 80% · ⏱ 300с) НЕ чіпали: вони калібрували ПОЧАТОК, і саме
на початок ці шари й лишились.

UI: `ff-mm-corr-vobreq` · `ff-mm-corr-breadth` · `ff-mm-corr-vobexit` у гармошці
🔻 Корекція (за роздільником — це не поріг, а те, ЯК пороги застосовуються);
у згорнутій шапці — `📦 60%! → кінець <50%`.

⚠️ **ЩО ЛИШАЄТЬСЯ НЕПОЯСНЕНИМ (третій лог поспіль):** `kind='block'` — **0
рядків**, `blocked_n` максимум 0. За 6.8 + 2.9 год зі статусом «відкриття
зупинено» ворота не зупинили ЖОДНОЇ монети, тож користь самих воріт досі не
доведена. Після цієї правки корекція триватиме ДОВШЕ, отже шанс побачити
реальне блокування зросте — на цьому й перевіримо. Не досліджено.

Тест: `test_mm_correction.py` (65) — розділ 8.

### 📨 ПОДІЯ ≠ ЗМІНА СТАНУ — три дефекти сповіщень (скарга 22.09, не зламати!)

**Дослівно:** «Проаналізуй оповіщення, вони не зовсім нормально відпрацьовують.
Проаналізуй недоліки.» Скрін Telegram за 14 год дав три РІЗНІ дефекти, і всі
три — з ОДНОГО кореня: читач дивився на `state != попередній`, а **зміна стану
і ПОДІЯ — різні речі**. Кожен відтворено прогоном ПЕРЕД правкою.

1. **«✅ ЗАВЕРШИЛАСЬ» ДВІЧІ з ТІЄЮ САМОЮ тривалістю** — 21:20 і 21:33
   «тривала 17хв», 0:16 і 0:31 «23хв», 1:58 і 2:10 «27хв». Проміжок 10-15 хв =
   рівно `ENDED_SHOW_SEC=900`. Причина: у стані `ended` ознаки СМИКНУЛИСЬ
   (`ended → pending`), смик згас (`pending → ended`) — і це читалось як нова
   подія кінця, ще й зі СТАРИМ `lasted`.
2. **ДВА «🔻 КОРЕКЦІЯ» підряд без ✅ між ними** (08:08 і 08:30). Причина:
   `ending → on` — це ПОВЕРНЕННЯ того самого епізоду (`since` не змінився), а
   не новий старт.
3. **«🔻 КОРЕКЦІЯ … ознак 1/2»** — менше, ніж потрібно. Наслідок №2:
   повернення з `ending` ухвалюється за **ПОСЛАБЛЕНИМ** порогом (`lit_hold`), а
   в текст друкувався **СУВОРИЙ** лічильник.

**ФІКС — `next_state` тепер віддає `event`:** `'start'` (новий `since`) ·
`'resume'` (`ending → on`, той самий епізод) · `'end'` · `'abort'`
(`pending → ended`, згаслий несправдливий старт) · `''`. У 🧾 Лог і Telegram
ідуть **ЛИШЕ `start`/`end`**.
⚠️ **`kind` СИРОГО ЛОГУ теж від події** — раніше `resume` писався як `start`, а
`abort` як `end`, тобто калібрувальний лог **ЗАВИЩУВАВ кількість епізодів**
(мої ж підрахунки «33 старти» / «12 стартів» були роздуті саме цим).
⚠️ **Кінець більше не обіцяє того, чого не було:** «відкриття знову дозволені»
пишеться ЛИШЕ коли ворота були увімкнені — інакше кінець суперечив рядку
старту («відкриття НЕ блокуємо»).
⚠️ **Назва теми не дублюється:** власний заголовок `🧮 МММ-МОНІТОР` із тіла
прибрано — `tg_bot` і так додає тег `#МММ_монітор`, і тема названа так само
(на скріні назва стояла в КОЖНОМУ повідомленні ТРИЧІ).
⚠️ Відліки (`pending`/`ending`) подією НЕ є — їх показує UI, але не Telegram
(той самий принцип, що з кандидатом банера).

### 📐 ДРУГЕ СТРУКТУРНЕ ДЖЕРЕЛО ШИРИНИ — звичайний OB (вимога 22.09)

**Дослівно:** «Зверни увагу на VOB, вони не відразу зʼявляються, навіть коли
монета отримала протилежний рух. Додай ще відслідковування OB, вони чітко
реагують на рух монет.»

**ЗАМІРЯНО (а не на віру).** Прогін обох детекторів бар-за-баром на 25 рядах:
109 зіставлених фліпів, із них **104 одночасні**, 5 — 📐 OB на 1 бар раніше,
жодного навпаки. Тобто **систематичної переваги в ШВИДКОСТІ немає** — і це
чесно сказано. АЛЕ: на 120 монетах вони **розходяться в НАПРЯМКУ у 15% замірів
(18 зі 120)**. Ось у чому справжня цінність — це ДВА РІЗНІ сенсори:
- **📦 Volumized OB** народжується зі СВІНГА і живе, лише якщо пройшов фільтри
  висоти (`max_atr_mult`), обрізання зон (`zone_count`) і обʼєму — **на різкому
  русі блок може не зʼявитись узагалі**;
- **📐 Order Block** створює ПОДІЯ СТРУКТУРИ (BOS/CHoCH) — сам факт пробою, без
  фільтрів обʼєму й висоти.
Після переробки 22.09 ширина ЄДИНОЛІЧНО вирішує кінець корекції, тож тримати її
на ОДНОМУ детекторі з власними фільтрами — крихко.

**РЕАЛІЗАЦІЯ.**
- `smc_scanner._update_ob_trend()` — той самий `detect_order_blocks(limit=1)`,
  що малює блоки на графіку, на **ЖИВИХ барах молодшого TF**
  (`vol_klines` — ті самі, що й у VOB). ПУБЛІЧНИЙ читач `ob_trends()` —
  дзеркало `volumized_trends()` за формою.
- ⚠️ **Структуру рахуємо НАНОВО на живих барах**, а НЕ беремо
  `vol_data['structure']`: та порахована на ЗАКРИТИХ, тож шар відставав би на
  цілий бар і вийшов би ПОВІЛЬНІШИМ за VOB — тобто безглуздим. Заміряно:
  +16 мс структура + 6 мс OB на монету при 3000 барах (**≈0.3% такту**, де
  мережа — секунди).
- ⚠️ **ЦЕ НЕ ЧЕТВЕРТИЙ ГОЛОС.** 📦 і 📐 міряють ОДНЕ Й ТЕ САМЕ (частку монет,
  чия структура проти банера), тож зливаються в ОДНУ ознаку «ширина» через
  **`breadth_of` = МАКСИМУМ із двох**. Інакше «2 з 3» мовчки стало б «2 з 4» і
  старт став би легшим. У `layers` їх ЧОТИРИ (для показу), у `lit` — три.
- ⚠️ **МАКСИМУМ, одне правило на обидва кінці:** на СТАРТ досить, щоб проти
  банера стало видно ХОЧ ОДНОМУ сенсору (це й є відповідь на «VOB не відразу
  зʼявляються»); на КІНЕЦЬ корекція тримається, поки проти каже ХОЧ ОДИН.
  Обидва напрямки СУВОРІШІ — свідомо, бо скарга була про надто легкий кінець.
  Середнє тут гірше: воно РОЗМИВАЄ сигнал, коли один детектор фізично не має
  блоку по монеті (а це його штатний стан).
- ⚠️ **ПОРОГИ СПІЛЬНІ** (`mm_corr_vob_pct` / `mm_corr_vob_exit_pct`) — два
  числа для однієї величини розійшлись би. Новий ключ РІВНО один:
  `mm_corr_ob_on` (деф. **True** — користувач просив саме додати джерело).
- ⚠️ **ДВА ФОЛБЕКИ** (файли деплояться в різному порядку): старіший
  `mm_correction` без `ob_trends=` → `except TypeError`; старіший сканер без
  `ob_trends()` → шар НЕ визначений, ширину тримає сам VOB, тобто поведінка
  рівно як до правки.
- **Сирий лог** несе `ob_pct`/`ob_n`/`ob_against` + **`breadth_pct`** (число,
  що РЕАЛЬНО вирішило) і **`breadth_src`** (📦 / 📐 / 📦+📐) — інакше
  неможливо перевірити, чий сенсор ухвалив рішення. Колонки додано в модель,
  білий список `_MM_CORR_FIELDS`, CSV-експорт і ідемпотентний `ALTER TABLE`
  (тепер СПИСКОМ колонок, а не одним рядком).
- UI: тумблер `ff-mm-corr-ob`; у згорнутій шапці — `📦+📐 60%! → кінець <50%`.

Тест: `test_mm_correction.py` (88) — розділи 9-10.

### 🧭 «ПОРІГ КІНЦЯ НЕ ЗБЕРІГАЄТЬСЯ» (ІСТОРІЯ · клемп скасовано того ж дня)

> ⚠️ **ЗАМІЩЕНО розділом «60 НА СТАРТ / 65 НА КІНЕЦЬ» нижче.** Спершу я зробив
> зведення ВИДИМИМ, а користувач резонно спитав, чому воно взагалі є —
> перевірка показала, що обмеження зайве. Текст лишено як історію рішень.

**Дослівно:** «📦 Кінець, коли проти <, % не зберігається, якщо 📦 VOB проти ≥,
% менше цього значення.»

**Поле справді зводилось — і правило при цьому ВІРНЕ.** Валідація не дає
порогу КІНЦЯ бути вищим за поріг СТАРТУ, бо це вивернутий гістерезис. Напрямок
тут неочевидний і саме він збиває з пантелику: поріг кінця — це рівень, **нижче
якого** ширина вважається відновленою, тож **БІЛЬШЕ число = корекція
завершується РАНІШЕ**. Вище за старт означало б «завершити на тій самій ширині,
за якої вона щойно почалась».

**Дефект був у ПОДАННІ:** `get_settings` зводив число МОВЧКИ, а обидва поля
стоять далеко одне від одного (між ними роздільник і три чекбокси), тож звʼязок
був невидимий — введене число просто «губилось». Той самий клас вади, що
мовчазний clamp кількості монет у скані ліквідності.

**`_mmcExitSync(clamp)` на сторінці** (висить на ОБОХ полях: `oninput` попереджає
під час набору, `onchange` зводить перед збереженням):
- стеля поля виходу ЖИВА (`out.max = вхід`) — браузер опирається сам, а не ми
  постфактум;
- зводимо РІВНО те, що показано, тож після збереження поле не «стрибає» саме по
  собі (інакше це знову читалось би як загублене введення);
- бурштиновий рядок `#mm-corr-exit-note` називає **обидва числа**, правило і
  **напрямок** («більше число = завершується РАНІШЕ»), плюс що робити:
  спершу підняти «📦 VOB проти ≥».
- ⚠️ **Синк потрібен на ОБОХ полях:** найчастіший шлях у цю скаргу — не
  «підняли вихід», а **ЗНИЗИЛИ вхід** нижче вже збереженого виходу (тест-замок).
- ⚠️ Коректна пара — ТИША: «все гаразд» окремим рядком це шум.
- ⚠️ Бекендний клемп ЛИШАЄТЬСЯ останньою лінією оборони (API повз сторінку).

Тести ганяють СПРАВЖНІЙ JS сторінки під node на крихітному фейк-DOM
(`_js_fn` + `_run_exit_sync`) — перевірка тексту в HTML довела б лише те, що
рядок написаний, а не що він зʼявляється.

### 🧭 60 НА СТАРТ / 65 НА КІНЕЦЬ — КЛЕМП СКАСОВАНО, ЦЕ ДЕФОЛТ (22.09)

**Дослівно:** «Я хочу виставити 📦 VOB проти ≥ 60, а 📦 Кінець, коли проти <
65, чому так неможна? Зроби ці значення за замовчуванням.»

⚠️ **МОЄ ПОПЕРЕДНЄ ОБҐРУНТУВАННЯ БУЛО ПЕРЕБІЛЬШЕННЯМ.** Я писав, що вихід вище
за вхід дасть «вивернутий гістерезис — вердикт миготітиме на КОЖНОМУ такті».
Прогін через СПРАВЖНІ `breadth_exit_ok` + `next_state` (200 тактів по 30с,
`confirm_sec` 300) показав інше:

| ширина | вихід 50 | вихід 65 |
|---|---|---|
| **90%** (кейс скарги 22.09) | 1 старт, 0 кінців | **те саме: 1 старт, 0 кінців** |
| 62% (у смузі 60-65) | 1 старт, тримає | 5 епізодів за 100 хв |

Тобто **захист від «завершилась при 90% проти банера» повністю зберігається**,
а ціна вибору ЛОКАЛЬНА: у смузі `[вхід .. вихід)` вето ширини не діє і кінець
там знову вирішують швидкі шари (стара поведінка голосів САМЕ В ЦІЙ СМУЗІ).
І темп задають самі шари, а не такт двигуна — «щотакту» там не було ніколи.

**Зроблено:**
- **клемп `exit ≤ entry` ВИДАЛЕНО** з `get_settings`; лишився тільки 0..100 на
  кожен поріг окремо. Обидва наявні тест-замки ПЕРЕПИСАНО (вимога змінилась);
- **дефолт `mm_corr_vob_exit_pct` 50 → 65** (`mm_corr_vob_pct` уже був 60);
- **міграція — НОВА ХВИЛЯ `mm_corr_exit65_v2`.** `_migrate_settings` тепер
  бере СПИСОК `MM_CORR_TUNE_WAVES` = `(позначка, {ключ: (старий, новий)})`.
  ⚠️ Дописати ключ у стару позначку було б МЕРТВИМ кодом: `mm_corr_tuned_v1`
  на робочій установці вже `True`, і міграція не виконалась би НІКОЛИ.
  Правило «переписуємо лише значення, що дорівнює СТАРОМУ дефолту» лишається:
  збережені вручну 55 чи 65 не чіпаються;
- UI: `_mmcExitSync()` більше НЕ зводить число і не обмежує `max`. Натомість
  при `вихід > вхід` показує **блакитну довідку** (не попередження): що так
  МОЖНА, що це мʼякша настройка, у якій саме смузі вето не діє і що вище
  порога кінця захист повний. Класична пара (вихід ≤ вхід) — тиша;
- хардкоджені фолбеки 50 оновлені в усіх чотирьох місцях сторінки.

⚠️ **ЩО ЦЕ ЗМІНЮЄ НА ВАШІЙ УСТАНОВЦІ.** У БД збережено **65/65** (вхід теж 65
— поставлений руками), і міграція його НЕ чіпає. Щоб отримати рівно 60/65,
вхід треба опустити до 60 у гармошці — тепер це зберігається.

Тест: `test_mm_correction.py` (90) — розділ 10 (прогони на справжній машині
станів + обидві хвилі міграції + JS під node).

### 🏷 «ТЕМА ЗНОВУ ₿ BTCUSDT + СПОВІЩЕНЬ НЕМАЄ» — ОДИН КОРІНЬ (скарга 22.09)

**Дослівно:** «Тема в телеграм групі знову називається чомусь "₿ BTCUSDT" і
повідомлень про "✅ КОРЕКЦІЯ ЗАВЕРШИЛАСЬ" не було.»

**Це НЕ дві поломки, а одна — і це моя помилка проєктування.** Перейменування
теми жило ВСЕРЕДИНІ `_cat_chat`, тобто виконувалось лише як **побічний ефект
відправки**. А `notify_category` перевіряє тумблер кабінету (`_cat_enabled` →
`notify_btc`) **ПЕРШИМ РЯДКОМ** і виходить **ДО** `_cat_chat`. Отже:

    немає повідомлень  →  _cat_chat не викликається  →  назва НІКОЛИ не оновиться

Тобто **стара назва теми — це СИМПТОМ того, що повідомлення не йдуть**, а не
окрема проблема. Ззовні вони виглядали як дві різні поломки.

**ФІКС 1 — `tg_bot.sync_topic_names()`**: приводить назви **вже створених** тем
до `_CAT_LABEL` незалежно від відправки і від тумблера. Джерела — персистована
мапа `tg_forum_topics` і явні env `TELEGRAM_CHAT_<CAT>`+`TELEGRAM_TOPIC_<CAT>`.
- ⚠️ **НІЧОГО НЕ СТВОРЮЄ** (`createForumTopic` у ній заборонено тест-замком):
  створити тему для ВИМКНЕНОЇ категорії було б протилежною помилкою — тумблер
  має гасити групу, а не наповнювати її.
- Викликається **на СТАРТІ бота** (`start_tg_bot`, поряд із
  `_purge_admin_only_topics`) і з **🩺 «Перевірити тему»** — причому ДО раннього
  виходу на тумблері, інакше користувач із вимкненою темою не мав би ЖОДНОГО
  способу оновити назву.
- ⚠️ Назва теми — властивість ТЕМИ, а не сповіщення, тож тумблер її не стосується.

**ФІКС 2 — відмова відправки більше НЕ мовчазна.** `_broadcast_users` тепер
повертає **`(ok, причина)`** (+ публічний `tg_bot.last_send_error()`), а обидва
шляхи теми (🔻 корекція і ⚖️ банер) РЕЗУЛЬТАТ ЧИТАЮТЬ і на невдачі пишуть у
🧾 Лог: `📨 Telegram НЕ прийняв … : <дослівна причина> — перевірте 🩺`.
Раніше результат `notify_category` ІГНОРУВАВСЯ, тож усі чотири причини
мовчання (вимкнений `notify_btc` · немає чату/теми · бот не адмін теми ·
відмова Telegram) виглядали однаково — ніяк. Той самий урок, що «Черга-4 ⚠️ збій
після відкриття» і «лог брехав» у `_q4_set_vob_sl`.

⚠️ **ПАСТКА СТАБА (вшосте).** `test_tg_routing._mk` підміняв `_broadcast_users`
лямбдою, що віддавала `None`. Розпакування `(ok, why)` падало в `except
Exception`, нова гілка НЕ виконувалась НІКОЛИ — а тести зеленіли. Стаб мусить
повертати ТЕ САМЕ, що справжній метод.
⚠️ `_fn_src` віддає AST-unparse: `return ok, x` стає `return (ok, …)` — замки
шукають суть, а не дослівний рядок.

Тест: `test_tg_routing.py` (38) — розділи 7-8.

**🩺 ПЕРША Ж ПЕРЕВІРКА НА ПРОДІ ЗНАЙШЛА ЩЕ ДВА ДЕФЕКТИ — В ЦІЙ САМІЙ ПРАВЦІ.**
Звіт кнопки: `тема «🧮 МММ-монітор» · форум-група · чат … · тема #42 · тумблер
кабінету ✓ · ✅ повідомлення надіслано · перейменування: trades: Bad Request:
TOPIC_NOT_MODIFIED`. Тобто **канал працює** — назва правильна, тумблер
увімкнений, повідомлення дійшло. «Помилка» була двічі неправдива:

1. **`TOPIC_NOT_MODIFIED` — це УСПІХ, а не збій.** Telegram відмовляє на
   `editForumTopic`, коли міняти нічого (назва вже така). Рахуючи це збоєм,
   `_forum_rename_if_needed` (а) писав у 🩺 звіт неіснуючу проблему і (б) НЕ
   запамʼятовував назву — тобто бив у API на **КОЖНІЙ** синхронізації, вічно.
   Тепер цей код відповіді проходить у гілку успіху (кешуємо і мовчимо).
2. **Причина помилки була ОДНИМ глобальним рядком на ВСІ категорії**, тож
   перевірка теми `btc` показала чужу помилку від `trades`. Тепер
   `_forum_rename_err` — **dict за категорією** + публічний
   `rename_error(category)`; `category_check` читає лише СВОЮ. Успіх стирає
   причину лише своєї категорії.

⚠️ **Висновок по самій скарзі:** «повідомлень не було» ≠ «канал зламаний».
Перевірка довела, що відправка проходить; отже реальних подій `end` після
деплою просто не сталося, а бейдж «✅ ЗАВЕРШИЛАСЬ» на екрані був ВІДНОВЛЕНИМ
станом (`_mm_corr_st` персиститься, `ENDED_SHOW_SEC` = 900с домальовує його
після рестарту). Після ФІКСУ 2 така ситуація вже не буде німою.
