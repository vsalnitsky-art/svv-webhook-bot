# CLAUDE.md — пам'ять проєкту svv-webhook-bot

> Цей файл Claude Code читає АВТОМАТИЧНО на старті кожної сесії.
> Він — єдиний міст контексту між різними чатами (сесії НЕ діляться пам'яттю).
> Тримай його актуальним: коротко, фактологічно, без води.

## Що це за проєкт

Крипто-трейдинг бот (Bybit/ф'ючерси) зі Smart-Money-логікою. Працює як
веб-застосунок Flask + кілька фонових daemon-потоків. Відповіді користувачу —
**українською**.

## ПРОФЕСІЙНО — ЗАВЖДИ (правило назавжди, весь проєкт)

Проєкт **професійний, а не аматорський** — і в UI, і в логіці/даних.

**UI.** Будь-яка сторінка/блок/таблиця — тільки **професійний, охайний вигляд**:
єдина система відступів і типографіки, стримані кольори з акцентами (не строкато),
1px-бордери, вирівнювання в стовпчик, картки однакової висоти, без «стрибання»
елементів, uppercase-підписи для метрик, monospace для чисел. Стат-картки —
в один ряд (горизонтальний скрол на вузькому). НЕ аматорський підхід.

**Логіка/дані.** Жодних «на око достатньо» зрізань кутів. Та сама метрика,
показана у двох місцях, ЗАВЖДИ рахується з ОДНОГО джерела правди — інакше
з'являються протиріччя (див. кейс PD-зони нижче). Кожен показник рахується на
задекларованому TF/вікні; коротке ролінг-вікно НЕ видавати за структурний
діапазон. Фікси — з тестом, що фіксує поведінку.

**Приклад-урок (PD Premium/Discount).** Банер рішення показував «PD Premium 89%»,
тоді як бейдж графіка — «Discount 17% (1H)» для тієї ж монети. Причина:
`position_evaluator._score_pd_zone` рахував Premium/Discount з ролінг-вікна
20 барів 15m, а бейдж/фільтр — з дилінг-діапазону `pd_zone_timeframe` (1H).
Фікс: `evaluate_entry`/`_score_pd_zone` приймають `pd_pct`(+пороги) і, коли задано,
класифікують САМЕ його (єдине джерело — `scanner.get_pd_pct()` +
`get_pd_thresholds()`); ролінг-вікно лишилось лише як fallback для Health-шляху.
Тест: `test_pd_zone_source.py`.

## VOB-алерт: ВАЛІДНІСТЬ = СВІЖІСТЬ (не зламати!)

Volumized-OB-сигнал (`vob_alert_enabled`) фаєриться в `smc_scanner._scan` на
НОВОМУ OB (edge за `formation_time`). **Природа свінг-OB:** OB стає валідним лише
через ~`volumized_swing_length` барів після своєї свічки (свінг мусить
підтвердитись) — тому бокс на графіку стоїть «назад у часі», хоча сигнал свіжий.
Це НЕ баг.

- **⏱ ГЕЙТ СВІЖОСТІ = «СИГНАЛ ЛИШЕ В МОМЕНТ ПОЯВИ» (вимога користувача, не
  зламати!).** `vob_alert_max_age_bars` **дефолт 0 = АВТО `swing_length + 2`**
  (при swing=5 → 7 барів ≈ 35 хв на 5m). Правило користувача дослівно:
  «зʼявився на графіку VOB — ми його опрацювали і все, більше до нього не
  повертаємось; блок, що утворився пів дня тому, ми лише БАЧИМО на графіку, а
  сигналом він був тоді, а не тепер».
  Чому саме `swing+2`: свінг-OB стає видимим лише через ~`swing_length` барів
  після своєї свічки, тож на ПЕРШОМУ показі його вік уже ≈ `swing_length`; +2
  бари — запас на скан-цикл.
  **Старий блок → `stale`:** позначаємо ОПРАЦЬОВАНИМ (щоб більше не
  повертатись), лічильник НЕ рухаємо, сигналу немає — чекаємо НАСТУПНИЙ блок.
  Гейт діє в ОБОХ гілках: і в `vob_one_per_ob` (раніше його там НЕ БУЛО — саме
  тому SUIUSDT-блок від 03:55 вистрілив о 14:18, вік 125 барів ≈ 10.4 год), і в
  catch-all. Явне `max_age=None` вимикає вікно (лише за потреби).
  ⚠️ Була спроба зробити «0 = без ліміту віку» — користувач це СКАСУВАВ.
  Тест: `test_signal_only_at_moment_of_appearance`.
- **🔎 «БЛОК НА ГРАФІКУ = СИГНАЛ» + ЧИСТИЙ ЛОГ (вимога користувача).** Дві
  проблеми, які виправлено разом:
  1) **Поточний блок ковтався як «базова лінія».** `_vob_alert_seen` не
     персиститься → після рестарту ПЕРШИЙ показ кожної монети давав first_sight
     (тиха база, БЕЗ сигналу) — тобто блок, який user БАЧИТЬ на графіку ЗАРАЗ,
     не фаєрився, бот чекав НАСТУПНИЙ. **Фікс:** `_vob_edge_outcome` тепер на
     ПЕРШОМУ показі, якщо блок СВІЖИЙ (вік ≤ поріг) → повертає **'fresh'** (це
     сигнал, одразу у фільтри), а не 'first_sight'. Старий блок на першому показі
     → 'first_sight' (тиха база, бо старий блок не сигнал). У процесі роботи:
     duplicate (ft≤base) / fresh (новіший+свіжий) / stale (новіший+старий).
  2) **Лог засмічувався** (57 рядків «базова лінія» тощо). **Фікс:**
     `_vob_log_decision(...)` пише ЛИШЕ в `self._vob_diag[symbol]` (in-memory,
     для get_state/UI) і **НІКОЛИ в activity_log**. У 🧾 Лог роботи бота йдуть
     ЛИШЕ реальні події з основного шляху: **fired** (`signal`) та **filtered**
     (`signal`+`rejected`). Статуси stale/duplicate/first_sight/epoch/no_candidate
     — тільки в `vob_diag` (структуровано, для UI-колонки «стан VOB»), без флуду.
  `get_state()['vob_diag']` = {symbol→{outcome,label,age,max_age,ft,tf,detail,ts}}
  (+`vob_alert_enabled`). Рішення edge/свіжості — ЧИСТА `_vob_edge_outcome(prev,
  ft,age,max_age)` (юніт-тест). Тест: `test_vob_transparency.py`.
- **CATCH-ALL: обробляємо ОБИДВА напрямки за один цикл (жоден VOB не губиться).**
  РАНІШЕ брався лише ОДИН кандидат — `max(newest_bull.ft, newest_bear.ft)`, тож
  коли за цикл з'являлись і бичачий, і ведмежий OB, протилежний зникав «між
  перевірками». ТЕПЕР `_vob_alert_seen[symbol]` — **ПЕР-НАПРЯМКОВИЙ dict**
  `{'LONG':ft,'SHORT':ft}`, а `_vob_pick_candidates(newest_bull,newest_bear)`
  повертає ОБИДВА (без breaker), відсортовані за часом (найновіший — останній,
  тож його статус лишається у `vob_diag`). Кожен бік проходить свій
  `_vob_edge_outcome` зі СВОЄЮ базою → протилежний новий VOB більше не
  маскується. `vob_one_per_ob`-епоха лишається СПІЛЬНОЮ на 1H-OB (перший фреш її
  витрачає). **Каденція:** VOB перевіряється раз на скан-цикл = повний прохід
  watchlist + `interval_secs` (деф. 60с). Короткоживучі OB (утв.+мітигація в
  проміжку) все одно можна недобачити → лік: менший `interval_secs`. Тести
  `_vob_pick_candidates` + пер-напрямкова база: `test_vob_transparency.py`.
  **UI:** на панелі монети (`templates/smart_money.html`) поряд з бейджем «SHORT
  5M» стоїть бейдж `sm-vob-status-badge` (глобали `wlVobDiag`/`wlVobAlertEnabled`
  з get_state) → показує, ЧОМУ по монеті є/нема реакції: ✅fired/⛔filtered/
  ⌛stale N/поріг/↺duplicate/👁first_sight. Так «бокс висить, а сигналу нема»
  завжди пояснено прямо в UI (стале = блок не свіжий, це не баг).
- **АУДИТ 3 значків watchlist (★ 1H-OB · ▲/▼ 5m-Volumized · ● 15m-тренд) —
  ВСІ до живого джерела (не зламати!).** Скарга: «значки живуть своїм життям».
  Причини й фікси:
  • **●**: сідився з `localStorage` → на завантаженні показував МИНУЛУ сесію ~10с.
    **Фікс:** `wlTrends={}` на старті, БЕЗ `localStorage` (ні init, ні setItem).
  • **★**: бекенд `get_chart_data` для свіжої монети рахував OB **інлайн на
    чарт-TF (5m)**, а бейдж підписував «OB 1H» → мітка ≠ дійсність. **Фікс:**
    інлайн-фолбек ПРИБРАНО — `last_ob` лише з реального рядка `sob_smc_ob_state`
    на `ob_filter_timeframe`.
  • **Усі три для ВІДКРИТОЇ монети** = живі значення панелі (`wlObLive`/
    `wlTrendLive`/`wlVolTrendLive`): панель на кожен перерахунок пише їх і МИТТЄВО
    оновлює елементи рядка (`#obst-<sym>`/`#dot-<sym>`/`#vt-<sym>`); `renderWatchlist`
    для `currentSymbol` бере ЗВІДТИ; 10с-полл НЕ перебиває їх кешем. Спільні
    рендери `wlObStarInner()`/`wlVolTriInner()` → рядок і панель малюють ІДЕНТИЧНО.
  Лишок (не критично): `_build_trends(use_restart_fallback=True)` може дати
  персист-тренд для ще НЕ сканованої монети після рестарту — коригується першим
  сканом (~60с). Правило: та сама метрика у двох місцях — ЗАВЖДИ одне значення,
  оновлення на кожну зміну.
  • **КОРІНЬ ▲/▼ «активна зелена / неактивна червона» (не зламати!):** було ДВА
    різні входи в ОДИН детектор. **Головна знахідка:** `_get_tf_data` НЕ клав
    ключ `'klines'`, тож `vol_data.get('klines') or klines_closed` мовчки давало
    **ЗАКРИТІ 3000 барів**, а панель/бокс рахували з `fetch_klines(400)` **із
    живим баром** → різний «останній OB» (і VOB-сигнал спізнювався до 1 TF,
    попри задокументовану вимогу «детекція на ЖИВОМУ барі»). **Фікс:**
    (1) `_get_tf_data` тепер зберігає `'klines'` = СИРІ бари з живим;
    (2) `_latest_volumized_ob(symbol, klines=None)` — ЄДИНА функція для ▲/▼,
    бейджа «5M» і боксу; ліміт `VOB_KLINES_LIMIT=3000` (було 400 → на частині
    монет OB не потрапляв у вікно і трикутник ЗНИКАВ);
    (3) скан кличе її зі СВОЇМИ барами (`klines=vol_klines`) → БЕЗ зайвого
    мережевого запиту, результат пишеться в той самий 30с-кеш, що читає панель.
    Результат: watchlist == панель == бокс для ВСІХ монет + швидша реакція VOB.
    Сигнали VOB лишились на `vol_result.newest_bull/bear` (той самий live-набір).
- **▲/▼ Volumized-тренд у watchlist = ОДНЕ ДЖЕРЕЛО з бейджем «5M» на панелі
  (не зламати!).** Був розхлябаний баг: у рядку watchlist ▼(червоний/SHORT), а на
  панелі «🟢 LONG 5M» — бо трикутник брався зі стейт-знімка фонового скану (~60с),
  а бейдж рахувався наживо при кліку. **Фікс:** `wlVolTrendLive[sym]` — панель на
  КОЖЕН перерахунок пише живий тренд і МИТТЄВО оновлює `#vt-<sym>`; `renderWatchlist`
  для ВІДКРИТОЇ монети бере тренд ЗВІДСИ, спільний рендер `wlVolTriInner()` (панель
  і рядок малюють ІДЕНТИЧНО). Правило: та сама метрика у двох місцях UI ЗАВЖДИ з
  одного значення, оновлення — на кожну зміну, а не «коли задумається».
- **Швидкість:** детекція лишається на ЖИВОМУ барі (`vol_data['klines']`) — НЕ
  переходити на закриті бари (це додало б до 1 TF затримки). Сигнал іде в ту ж
  мить, щойно OB став валідним.
- **⏱ ПРОФІЛЬ ЦИКЛУ + КЕШ БАРІВ (не зламати!).** Кожен цикл пише
  `[SMC] Scan #N: X symbols in Ys (Zs/coin) — main · tf · ob · vob`. Заміряно на
  проді: 33 монети ≈ 235с (7.1с/монету), з них `tf` 76с + `main` 37с + `ob` 11с =
  **мережа ~53%**. Причина: на монету тягнеться 3000 барів 15m + 3000 5m + 3000 1h
  (PD) + 700 1h (OB), а біржа віддає max 1000/запит → ~10 HTTP на монету.
  **Фікс без втрати якості:** `_get_tf_data(tf, use_cache=True)` кешує РЕЗУЛЬТАТ
  (klines+structure) МІЖ циклами до моменту закриття поточного бару — бо ці
  споживачі (PD, opposite-exit OB) беруть ЛИШЕ закриті бари, отже дані фізично не
  змінюються всередині бару (1H-дані качались ~15 разів на годину даремно).
  **Volumized TF викликається з `use_cache=False`** — там потрібен ЖИВИЙ бар.
  ⚠️ НЕ «оптимізувати» пропуском PD-розрахунку: `get_pd_pct()` живить банер
  Decision → повернеться баг «банер vs бейдж». І НЕ зменшувати глибину барів для
  Volumized: емпірично при 400 барах трикутники зникали на частині монет.
- Далі — `_signal_allowed` (OB/PD/Forecast) → `on_signal` → черга/угода.
- Тест: `test_vob_freshness.py`.
- **«1 VOB на 1H OB» = НУМЕРАЦІЯ VOB** (`vob_one_per_ob`, дефолт OFF). Проста
  детермінована логіка користувача: 1H-OB — це ТАКТ, у межах якого VOB
  НУМЕРУЮТЬСЯ; **VOB #1 = СИГНАЛ, #2,3,… — лише номер (НЕ сигнали)**.
  • `_ob_bt = _current_ob_bartime(sym)` (bar_time OB на `ob_filter_timeframe`);
    None → `no_1h_ob` (без 1H-OB сигналу нема).
  • `_vob_epoch_reset_needed(prev,ob_bt)` (інший bar_time) → **обнуляємо
    `_vob_counter[sym]=0`** і базуємо НАЯВНІ 5m-VOB (`_vob_alert_seen[sym]={side:ft}`
    → вони #0). Тобто наявний блок при появі 1H-OB **не сигнал** — ЧЕКАЄМО НОВИЙ
    (рішення користувача, розвилка B).
  • Кожен НОВИЙ VOB (обидва боки, `prev None or ft>prev`, **БУДЬ-ЯКИЙ вік — без
    вікна свіжості**, жодного не пропускаємо) → `_vob_counter[sym]+=1`, номер `_n`.
  • **«1 РЕЗУЛЬТАТИВНИЙ сигнал на 1H-OB» (вимога користувача).** Такт витрачає
    ЛИШЕ той VOB, що **пройшов УСІ фільтри й пішов на відкриття**, а не «будь-який
    перший». `_vob_is_signal_candidate(fired_epoch, ob_bt)` = кандидатом є КОЖЕН
    новий VOB, доки `_vob_epoch_fired[sym] != ob_bt`. Відсів фільтром такт **НЕ**
    витрачає → наступний VOB (#2, #3…) знову йде у фільтри. Після fire →
    `_vob_epoch_fired[sym]=ob_bt`, решта VOB цього 1H-OB → outcome `numbered`.
    У логах номер видно: «Свіжий Volumized OB (5m) LONG · VOB #3 · <розклад>».
  • Наступний свіжий 1H-OB → лічильник знову 0 → перший новий VOB знову #1.
  • **ТАКТ = `bar_time` КОЖНОГО 1H-OB, а НЕ напрямок (рішення користувача —
    НЕ міняти без його слова!).** LONG→LONG: наступний БИЧАЧИЙ 1H-OB теж
    СКИДАЄ такт і бот знову чекає новий 5m-VOB у відповідному напрямку; так
    само SHORT→SHORT. Тобто «один результативний VOB на КОЖЕН 1H-OB», а не
    «на фазу напрямку». Була спроба перевести такт на напрямок (щоб новий OB
    того самого боку НЕ поновлював) — користувач це ВІДХИЛИВ, зміну
    відкочено. Тест-замок: `test_every_new_1h_ob_resets_even_same_direction`.
  • **⚠️ СКИДАННЯ НЕ КОВТАЄ БЛОК, ЩО ВИНИК ПІСЛЯ 1H-OB (кейс NEARUSDT).** 1H-OB
    детектується на ЗАКРИТИХ 1h-барах → сканер бачить його із запізненням до
    ГОДИНИ. Скидання базувало ВСІ наявні 5m-блоки — разом із тим, що утворився
    ВЖЕ ПІСЛЯ 1H-OB, тобто саме той, який мав стати #1. Наслідок: на графіку
    висів 🟢 LONG-блок + бичачий 1H-OB, а фаєрились ЛИШЕ зустрічні SHORT (їх
    різав OB-фільтр) — «100% сигнал» мовчки пропускався. **Фікс:** базуємо ЛИШЕ
    блоки з `_vob_before_ob(ft, ob_bt)` (`ft <= ob_bt`); блок, що виник ПІСЛЯ
    1H-OB, лишається неопрацьованим → стає VOB #1 і йде у фільтри.
    Тест: `test_reset_does_not_swallow_block_formed_after_1h_ob`.
  • **ПЕРСИСТ (не зламати!):** увесь стан такту зберігається в БД під
    `DB_KEY_VOB_STATE='smc_vob_state_v1'` (`_persist_vob_state`/`_load_vob_state`):
    `seen`(пер-напрямкова база) + `epoch` + `fired` + `counter`. Пишеться на
    НОВОМУ такті та після РЕЗУЛЬТАТИВНОГО fire, читається в `__init__`. Без цього
    кожен рестарт (а `botupdate` робиться часто) обнуляв такт → UI показував
    «VOB 0 · чекаємо #1», і бот міг відкрити ДРУГУ угоду на ТОМУ САМОМУ 1H-OB.
    Тест round-trip: `test_vob_transparency.py`.
  **OFF-гілка = catch-all** (`_vob_edge_outcome` по кожному боку, дефолт без вікна
  віку — кожен новий VOB фаєрить). Пуре-хелпери: `_vob_epoch_reset_needed`,
  `_vob_is_signal_number`, `_vob_pick_candidates`, `_vob_edge_outcome`. Старі
  `_vob_epoch_decision`/`_vob_epoch_already_fired`/`_vob_epoch_fired` лишились як
  тест-хелпери, у нумерації НЕ використовуються (їх замінив лічильник). **ВАЖЛИВО:** 1H-OB стабільний у межах бару (детекція на ЗАКРИТИХ
  1h-барах) — тому re-open у межах години НЕ може бути «новим OB»; якщо угода
  re-open після видалення в межах бару — шукати ІНШИЙ шлях (черга Черга-4/
  reconcile), а не епоху. Тест: `test_vob_one_per_ob.py`.
- **ГЕЙТ НА РІВНІ ВІДКРИТТЯ (переживає close/delete) — фікс «фантомного re-open»
  GRAMUSDT.** Кейс: угоду закрили → за ~54с вона ВІДКРИЛАСЬ ЗНОВУ (мітка
  `Volumized OB → Черга-4`), «хоча в черзі її не було». Причина: scanner-глушіння
  `vob_one_per_ob` придушує лише АЛЕРТ, а `_vob_ob_epoch` — in-memory у сканері;
  але шлях відкриття інший: НОВИЙ 5m VOB (інший `formation_time`, ТОЙ САМИЙ 1H-OB)
  → `on_signal` → `intercept` → `_pending4` (бо після закриття `_fuel_managed`
  звільнено reconcile-ом і `_already_open`=False) → `_engine_tick_queue4` знову
  відкрив. Черга не показувала монету, бо `_pending4` попається ПРИ відкритті.
  **Фікс (fuel_filter, гейт лише коли scanner.vob_one_per_ob=ON):**
  `_opened_ob_epoch[sym]=bar_time` — позначка «вже відкривались на цьому 1H-OB»,
  ПЕРСИСТЕНТНА (переживає рестарт), тримається ПОПРИ закриття/видалення,
  скидається ЛИШЕ на НОВОМУ 1H-OB. Джерело bar_time — ТЕ САМЕ, що у сканера
  (`_ob_epoch_now` = `get_smc_ob_state(sym, ob_filter_timeframe)`).
  Перевірка `_ob_epoch_already_opened` стоїть у ДВОХ місцях: (1) `intercept` — НЕ
  ставити монету в Чергу-4 (`... and not _ob_epoch_used`); (2)
  `_engine_tick_queue4` — перед відкриттям (і прибрати з черги). Позначка
  ставиться `_mark_ob_epoch_opened` після успішного Q4-відкриття. Тобто «1 угода
  на 1 1H-OB» тепер гарантується на ЄДИНОМУ вузлі відкриття, а не лише глушінням
  алерту. Дефолт OFF → стара поведінка без змін. Тест: `test_vob_open_epoch.py`.

## SMC-чарт (/smart-money): налаштування відображення

Гармошка **⚙️ Settings** керує чартом. Ключові фічі:

- **🏁 Swing High/Low (Display, `show_swing_hl_enabled`/`swing_hl_timeframe`,
  дефолт OFF/1H).** Дві горизонтальні лінії — ТОЧНО як у TradingView/LuxAlgo.
  Рівні = trailing swing-екстремуми ДИЛІНГ-ДІАПАЗОНУ (`SMCScanner.
  _swing_trailing_range` — ЄДИНЕ ДЖЕРЕЛО з PD-зоною, тому лінії й бейдж
  Premium/Discount ніколи не розходяться). Назви за swing-трендом
  (`_swing_hl_labels`): бичачий → «Weak High»(зверху)+«Strong Low»(знизу);
  ведмежий → «Strong High»+«Weak Low». НЕ брати «останній HH/LH та HL/LL півот»
  — це давало проміжні хибні рівні (кейс ATOM: 1.3937/1.3252 замість 1.566/1.33).
  Стан: `swing_levels`={high:{price,label},low:{price,label}}|None, `show_swing_hl`,
  `swing_hl_tf`. Фронт малює з `swing_levels` (динамічні підписи).
  Тест: `test_swing_hl_levels.py`.
- **🔮 Forecast min-strength (`forecast_min_strength`: off/moderate/strong).**
  Поріг СИЛИ (впевненості) прогнозу ДО увімкнених 1H/4H-фільтрів напрямку: збіг
  напрямку зі слабшою за поріг впевненістю → 'weak' (не-збіг, ріже сигнал).
  strong ≥66%, moderate ≥40%. У `_forecast_filter_allows` (smc_scanner).
- **📦 Volumized OB TF** тепер включає **5m** (ALLOWED_VOL_TFS).
- **Два OB-бокси на графіку одночасно.** `volObPrim` (Volumized OB, зелений/
  червоний суцільний) і `obMatchPrim` (Require OB Match, teal/orange пунктир) —
  ДВА незалежні `VolOBPrimitive`, малюються обидва, коли увімкнені відповідні
  тумблери. **Критично:** старт боксу прив'язувати через `_snapSecToChart(sec,
  ohlc)` — `timeToCoordinate()` повертає null для часу МІЖ барами, тож OB на TF,
  дрібнішому за TF графіка (Volumized 5m на графіку 15m), інакше НЕ малюється.

## ФІЛЬТРИ ВХОДУ — СПІЛЬНІ ВОРОТА для ВСІХ сигналів (не зламати!)

`SMCScanner._signal_allowed(symbol, side)` — ЄДИНІ ворота УСІХ фільтрів, кожен
НЕЗАЛЕЖНИЙ зі своїм тумблером. Викликаються з ОБОХ шляхів входу:
1. **CHoCH/BOS** → `_send_alert` (як і було);
2. **Volumized OB alert** (`vob_alert_enabled`) → перед `_tm.on_signal(...,
   opened_by='vob_alert')` у скан-тіку.

Ланцюг незалежних фільтрів (кожен за своїм тумблером):
- **OB** (`ob_filter_enabled`) · **PD** (`use_pd_zone_filter`) ·
- **Forecast НАПРЯМОК** (1H/4H match + AND/OR) — `_forecast_filter_allows`, ЛИШЕ
  напрямок (силу НЕ враховує — `min_conf=0`);
- **Forecast МІН.СИЛА** — ОКРЕМИЙ `forecast_strength_filter_enabled` +
  `forecast_min_strength` (`_forecast_strength_allows`: пропускає лише якщо є
  прогноз у бік сигналу з впевненістю ≥ поріг на 1H або 4H);
- **Decision-вердикт (осн. напрямок)** — `decision_filter_enabled`
  (`_decision_gate`/`_decision_filter_allows`): банер Decision Center має
  рекомендувати ТОЙ САМИЙ напрямок (LONG/SHORT), що й сигнал (достатньо бути в
  осн. напрямку); NEUTRAL/протилежний → блок. 10с-кеш; at_intake — блок лише на
  протилежний, строго при відкритті (`_open` re-gate). ЦЕ — про банер «LONG 80%
  СИЛЬНИЙ», НЕ про Forecast-бейджі 1H/4H (їх гейтить Мін.сила).
- **POC «краще LONG/SHORT»** — `poc_filter_enabled` (`_poc_filter_allows`:
  напрямковий — ціна нижче POC → LONG, вище → SHORT, на POC → нейтрально). Рахує
  через ТОЙ САМИЙ `detection.volume_profile.compute_poc`/`price_vs_poc`, що й бейдж
  чарту, з ТИМИ САМИМИ параметрами: `poc_filter_market`/`poc_filter_tf`/
  `poc_filter_window_days` (дефолти FUTURES/1H/3д) → вердикт фільтра 1:1 з чартом.
  `compute_poc` має власний TTL-кеш (не б'є біржу на кожен сигнал).
Тести: `test_independent_filters.py`.

**Прозорість + «чекати вердикт»:**
- `_signal_allowed` повертає **(allowed, reason, detail)**; `detail` — РОЗКЛАД
  ЗНАЧЕНЬ кожного увімкненого фільтра (✓/✗ + числа: прогноз 1H/4H сторона%,
  Мін.сила поріг, PD %, OB tf, POC). Пишеться у 🧾 Лог РАЗОМ із сигналом
  (`Свіжий сигнал … · <detail>`) — щоб було видно, що КОЖЕН фільтр відпрацював
  (`_forecast_pair` дає сирі значення прогнозу).
- **Forecast «чекати вердикт», а не викидати:** `_signal_allowed(at_intake=True)`
  на ІНТЕЙКУ по прогнозу блокує ЛИШЕ явну протилежність; nodata/neutral →
  пускаємо в чергу (сигнал ЧЕКАЄ). Строга перевірка (match + Мін.сила) — при
  ВІДКРИТТІ: `fuel_filter._open` re-gate кличе `_forecast_filter_allows`/
  `_forecast_strength_allows` (строго) для scanner-опенів (не funding) → якщо
  вердикту ще нема / проти → `return False` (монета лишається в черзі, ретрай).
- Telegram open/close повідомлення БІЛЬШЕ не несуть рядок `🏷 opened_by` (прибрано
  на прохання) — мітка лишається в таблицях/лозі/модалці, але не в TG.

### 📐 «1H OB ЛИШЕ З CHoCH» (`ob_filter_choch_only`, дефолт УВІМК)

Order Block створюють **ОБИДВІ** події структури — `ob_detector` фаєрить
`storeOrdeBlock` і на BOS, і на CHoCH (точна Pine-семантика), а тег осідає в
`created_by_tag` (`sob_smc_ob_state`). Змістова різниця:
- **CHoCH-блок** — точка РОЗВОРОТУ, перший блок нової структури;
- **BOS-блок** — ПРОДОВЖЕННЯ, тренд уже йде і пробиває черговий свінг.

Раніше `_ob_filter_allows` дивився ЛИШЕ напрямок (умову «тільки CHoCH» колись
прибрали, але застарілий абзац лишався в docstring і суперечив коду — виправлено).
Тепер це **окремий тумблер**: УВІМК → ворота приймають лише CHoCH-створений блок.
- ⚠️ Перевіряється **ПОТОЧНИЙ (останній немітигований)** блок — детектор зберігає
  саме його. Тобто «лише з CHoCH» = «поточний блок мусить бути CHoCH-створений»;
  якщо після нього встиг сформуватись BOS-блок, вхід блокується (рух пішов у
  продовження). Це і є задуманий тісніший сетап, а не обмеження реалізації.
- ⚠️ Це **УТОЧНЕННЯ воріт `ob_filter_enabled`**, а не окремий фільтр: при
  вимкненому OB-фільтрі тумблер не робить нічого.
- ⚠️ На **малюнок ★-блоку**, **Manual SL** (`queue4_sl_source='1h'`) і **такт
  `vob_one_per_ob`** (`_current_ob_bartime`) НЕ впливає — вони й далі беруть той
  самий рядок БД. Тумблер — про ДОЗВІЛ НА ВХІД, а не про те, який блок існує.
- ⚠️ Рядок без тега (стара БД-строка) при УВІМК **блокує** — «будь-яка
  невизначеність = блок» — але скан перераховує рядок щотіку, тож це один цикл.
- **Прозорість:** `_ob_state_label(symbol)` дає `BEARISH/BOS`, і розклад у 🧾 Лозі
  тепер `OB(1h лише CHoCH BEARISH/BOS):✗`. Причини РОЗРІЗНЕНІ: «нема блоку» /
  «проти напрямку» / «створено BOS — продовження, а не CHoCH».
- UI: чекбокс `sm-ob-choch-only` у ряду типів сигналів (поряд із 🔒 1 VOB на 1H OB);
  у шапці налаштувань — `OB 1H (лише CHoCH)`.
- Дефолт — НОВИЙ ключ, тож `merged.update(stored)` віддає саме його; міграція
  (як `pilot_autofill_migrated_v1`) тут НЕ потрібна.
Тест: `test_ob_choch_only.py` (11).

**НІКОЛИ не відкривати сигнал в обхід `_signal_allowed`.** Був дефект: VOB-alert
кликав `on_signal` напряму й ОБХОДИВ усі фільтри — бот відкрив ASTERUSDT SHORT,
попри `Require Forecast 1H+4H match · AND · Сильний` з прогнозом LONG (фільтри
живуть лише у `_send_alert`, а VOB туди не заходив). Урок: КОЖЕН новий шлях
відкриття мусить проходити ці спільні ворота, інакше налаштування користувача —
фікція. Заблокований сигнал → `log_activity('rejected', reason)`.
Тест: `test_signal_gate_unified.py`.

## 🏷️ Мітки угод «Сигнал → Двигун» (не зламати!)

Кожна угода несе мітку ПОХОДЖЕННЯ у форматі **"<signal> → <engine>"**, щоб було
видно, ВІД ЯКОГО СИГНАЛУ вона пішла (раніше двигун черги штампував свою мітку, а
оригінальний сигнал губився — напр. ASTER SHORT від Volumized-OB показувався як
«Fuel Auto-Filter»).

- **Єдине джерело правди:** `detection/signal_labels.py` — `compose(signal,
  engine)`, `pretty_opened_by(raw)`, `signal_code_of(raw)`, мапи
  `SIGNAL_BADGES`/`ENGINE_BADGES`. **JS-дзеркало** ОБОВʼЯЗКОВЕ і має бути
  синхронним: у `templates/smart_money.html` та `infosite/app.js`
  (`prettyOpenedBy`, `signalIconHtml`, `SIGNAL_ICON_JS`).
- **Зберігаємо МАШИННІ коди** у `position['opened_by']` (напр. `vob_alert → Q4`),
  а бейджі (🟪 Volumized OB → 🎯 Черга-4) робимо лише ПРИ ПОКАЗІ. Так підрядки
  `funding`/`POC-сетап`/`external`/`choch` лишаються для логіки (substring-
  перевірки, `signal_code_of` для порівнянь).
- **Кожен двигун FF** тепер відкриває з `opened_by=_ob_compose(info.get('kind'),
  '<ENGINE>')` (Q1='EXH', Q2, Q3, Q4, Q3-VOB(funding)); `kind` = оригінальний
  сигнал із черги. Прямі опени (FF off) лишають сирий код сигналу (без двигуна).
- **Показ СКРІЗЬ** (вимога користувача — «скрізь означає скрізь»): іконка-
  «картинка» сигналу СТОЇТЬ ПОРЯД ІЗ НАЗВОЮ МОНЕТИ у КОЖНІЙ таблиці:
  • угоди — `histSymOpen`/`histSymCell`/архів/інфосайт;
  • черги Черга-1/2/3 — `ffQueueRowHTML` (з `t.kind`); Черга-4 — власний рендер
    (backend timers4 несе `kind`);
  • 🎯 POC-сетап — константний бейдж 🎯 (`signalIconHtml('POC-сетап')`);
  • 💰 Funding — МММ — уже має 💰 біля символу (джерело — funding-сканер).
  Плюс повна мітка у 🧾 Лозі (відкриття+закриття), у Telegram (рядок `🏷`), у
  модалці історії («🏷 Джерело»). DB-колонка `opened_by` розширена 40→80.
  ⚠️ Черги несуть СИРИЙ `kind` (сигнал), а не `opened_by` — двигуна ще нема
  (угоду ще не відкрито); `signalIconHtml` працює і на сирому коді.
- Коди сигналів: `choch`/`choch_bos` (smc_scanner on_signal), `vob_alert`
  (Volumized-OB alert), `vob` (funding-VOB), `poc` (POC-сетап → черга FF),
  `opp` (реверс), `external`, `manual`.
- **ФАНТОМНИЙ «CHoCH» — виправлено.** `poc_setup._route_to_ff` жорстко ставив
  `intercept(kind='choch')` → запис POC-сетапу в черзі показувався як «CHoCH»,
  хоча CHoCH-алерти вимкнені (`choch_alerts_enabled=False`; `_process_alerts`
  чесно виходить рано — реальний CHoCH НЕ фаєриться). Тепер `kind='poc'` →
  правильний бейдж 🎯 POC-сетап скрізь. `intercept._kind_lbl` теж знає 'poc'/'opp'.
  Урок: НЕ використовувати чужий код сигналу як дефолт при маршрутизації в чергу.
- Тести: `test_signal_labels.py`.

## Як запускати

- Точка входу: `main_bot.py` (для gunicorn: `gunicorn main_bot:app`).
- Flask-фабрика: `web/flask_app.py` → `create_app()`; об'єкт `app`.
- Локально dev: `python web/flask_app.py` → `0.0.0.0:5000`.
- Прод-порт береться з env `PORT` (дефолт 10000), хост через `RENDER_EXTERNAL_URL`.
- Перевірка синтаксису після правок: `python -m py_compile <файл>`.

## Архітектура (головне)

- **`web/flask_app.py`** — усі HTTP-маршрути та JSON API. Великий файл (~5600 рядків).
- **`detection/fuel_filter.py`** — ядро стратегії «FF» (liquidation-fuel + черга на
  вхід + двигун відкриття). Daemon-клас `FuelFilterDaemon`. Синглтон:
  `get_fuel_filter()`, ініт `init_fuel_filter(...)`. Стан читається через
  `ff.get_state()`.
- **`detection/trade_manager.py`** — відкриття/закриття позицій, перехоплення
  сигналів у чергу FF, формування Reason.
- **`detection/funding_monitor.py`** — сканер funding-ставок (окрема таблиця 💰).
- **`detection/smc_scanner.py` / `smc_analyzer.py`** — детекція CHoCH / CHoCH+BOS
  (TF 15m). ТІЛЬКИ свіжий CHoCH/CHoCH+BOS за напрямком увімкненої кнопки потрапляє
  в чергу FF.
- **`storage/db_operations.py`** — шар БД. `get_db()`, методи
  `db.get_setting(key, default)` / `db.set_setting(key, value)` (значення —
  JSON-серіалізовні).
- **`templates/smart_money.html`** — головна UI-сторінка стратегії FF.

## Ключові конвенції стратегії FF (не зламати!)

- **СЕАНСИ ММ (головна логіка черги).** Сеанс = зафіксований напрямок ММ BTC
  (LONG/SHORT), тримається в `_btc_verdict_dir`/`_btc_verdict_since`
  (`_update_btc_verdict`). Правила:
  - **WAIT (ML збалансований, |dir| ≤ 0.1) = ПАУЗА:** напрямок сеансу,
    таймер і черга ЗБЕРІГАЮТЬСЯ; `_btc_paused=True`; таймер продовжує лічити;
    банер показує «напрямок · ⏸ ПАУЗА»; двигун під час паузи НЕ відкриває.
  - **Той самий напрямок знову** (LONG→WAIT→LONG) = той самий сеанс.
  - **Протилежний фліп** (LONG↔SHORT) = НОВИЙ сеанс: скид таймера + **чистка
    всієї черги** (`_pending={}`) + скид `engine_attempts`. Це ЄДИНИЙ випадок,
    коли ММ чистить чергу (замість старої per-flip OP-4 чистки протилежних).
- **Черга `_pending` ПЕРСИСТЕНТНА, відновлюється ЗА СЕАНСОМ.** Зберігається в
  БД і відновлюється на старті. Стале не тягнеться, бо на першому тіку після
  старту, якщо live ML протилежний відновленому сеансу → чергу чистить фліп
  сеансу; якщо той самий напрямок/WAIT → монети валідні й чекають далі.
- Монета входить у чергу ЛИШЕ зі свіжого CHoCH/CHoCH+BOS за напрямком увімкненої
  кнопки (`intercept`). Показ у таблиці НЕ фільтрується кнопками (кнопки керують
  лише відкриттям) — інакше при WAIT черга «зникала».
- Паливо (ММ-напрямок) рахується для монет у черзі; на BTC START (сеанс тримає
  напрямок ≥ поріг, не пауза) двигун відкриває угоду, якщо паливо монети
  співпадає з напрямком сигналу.
- 9 операцій видалення з черги пронумеровані й керуються `_QUEUE_OPS_ALLOWED`
  (всі зараз `True`). OP-4 = чистка черги на фліпі сеансу.
- Банер ₿ BTCUSDT = напрямок СЕАНСУ, а джерело live-напрямку — **МММ-БАБЛО**
  (`_fuel_dir_smoothed('BTCUSDT')['status']`), ТОЧНО як комірка «МММ» монет:
  LONG / SHORT / None(`|dir| ≤ 0.1` = ⚖ рівновага «напрямку немає» → WAIT/пауза).
  Контраріанська liqmap-модель (за запитом). Сила банера = |fuel_dir|×100.
  Сеанс-механіка (пауза на WAIT, фліп на протилежний, START після
  `start_signal_minutes`) — без змін; `_btc_trend_dir()` більше не використовується.

## Черга-3 «🎯 Готовність» (стратегія на grade_setup)

Третя, ОКРЕМА черга/двигун (поряд із Чергою-1 і Чергою-2, усі незалежні тумблери).
Живиться тими самими CHoCH/CHoCH+BOS через `intercept()` (гілка `q3` → `_pending3`).

- **Тригер входу:** двигун `_engine_tick_readiness` відкриває монету у напрямку
  кнопки, коли її SMC-«готовність» `grade_setup(...)` **HOT АБО `score ≥
  queue3_open_min_score`** (дефолт **43**; 0 = лише HOT). Строгий HOT
  (score ≥ 70 + усі блоки, без вето) на реальному потоці майже не спрацьовує
  (спостережений max ~66), тож поріг дає стратегії реальні угоди. КАЛІБРУВАННЯ
  53→43: розворотні CHoCH+BOS системно занижуються блоком таймінгу CTR (свіжий
  розворот б'є проти CTR), і хороші угоди падали в «СЕРЕДНІЙ» (38–52) під 53. **БЕЗ ₿ START /
  сеансів ММ** — весь контекст ₿/CTR/зона/ліквідність уже ВСЕРЕДИНІ `grade_setup`
  (див. `detection/setup_grader.py`). Санітарні ворота: кнопки LONG/SHORT, дедуп,
  вже-в-угодах, ціна.
- **Запобіжники Черги-3 (калібровано по activity_log).** Дані показали, що з 116
  «_open відхилив» більшість — жорсткі вето виснаженості (>80%) і слабкого МММ на
  сетапах, що вже пішли в наш бік (напр. NEARUSDT SHORT SCORE 53 ХОРОШИЙ, рух +1%,
  вичерп 81-89% → різалось). Тому:
  - `queue3_ignore_exhaustion` (дефолт **True**) — Q3/Q3-VOB пропускають ЖОРСТКЕ
    вето виснаженості у `_open` (`skip_exhaustion=`); виснаженість ЛИШАЄТЬСЯ м'яко
    в `grade_setup`/SCORE. Знімає і `max_exhaustion_pct`-гейт, і safeguard-вето.
  - `safeguard_mm_price_override` (дефолт **True**, tf `safeguard_mm_price_tf`=15m,
    ГЛОБАЛЬНО) — слабкий МММ (<`safeguard_mm_min`) НЕ ріже вхід, якщо `_candle_
    momentum(tf)` = напрямок угоди (ціна чітко йде в бік). Контраріанський МММ
    часто «мовчить» на чистому тренді.
  - `queue3_require_ob_match` (дефолт **True**) — `_ob_match_ok(sym,side,s)`: при
    увімкненому OB-фільтрі сканера НЕ відкривати ПРОТИ OB на `ob_filter_timeframe`
    (ріже лише ЯВНИЙ контр-OB; немає OB → дозволяємо, щоб не вбити Q3-VOB). Гейт у
    Q3 (`_engine_tick_readiness`, OB міг фліпнути за час очікування) і в Q3-VOB
    (`_vob_open_or_trail` — funding-опен взагалі не проходить сканерний `_send_alert`).
  - Лог Q3 «_open відхилив» тепер несе КОНКРЕТНУ причину з `_engine_skip`.
- **CTR більше НЕ ріже розвороти (2 ворота прибрано).** Було: (а) вето «CTR проти
  входу» в `grade_setup` капало бал ≤43; (б) `safeguard_ctr` у `_open` давав хард-
  відмову «CTR проти». Свіжий CHoCH+BOS за визначенням проти CTR → ці ворота
  різали КОЖЕН розворот. Тепер: `setup_ctr_mode` (дефолт **soft**) — CTR у грейді
  партіал (0.2) без вето; `queue3_ignore_ctr` (дефолт **True**) — Q3 пропускає
  CTR-перевірку safeguard (МММ/виснаженість лишаються). `_soft_safeguard(...,
  skip_ctr=)`, `_open(..., skip_ctr_safeguard=)`. Режими CTR: soft/normal/off.
- Читає готовий кеш `self._setup_cache[sym]` (той самий, що й колонка
  «Готовність»); `_pending3` додано в `targets` для `_refresh_setup_cache`.
- **Повне логування** кожного рішення (opened/hold/skipped + розклад блоків) →
  таблиця `sob_readiness_log` + рядок у Лог роботи бота (`activity_log`, source
  `Q3`). Тумблер `readiness_log_enabled` (дефолт ON). Анти-флуд: незмінний
  hold/skip пишеться не частіше ніж раз на `READINESS_LOG_MIN_GAP` (300с).
- Налаштування: `queue3_enabled` (дефолт OFF), `queue3_open_min_score` (43),
  `queue3_ignore_ctr` (True), `queue3_ttl_hours` (6 — протермінування, щоб монети
  не висіли годинами), `setup_ctr_mode` (soft), `readiness_log_enabled`.
  Q3-лог у `activity_log` несе пласкі `su_*` блоки
  (structure/poi/zone/liq/mm/timing/context) — щоб CSV-експорт був аналізовним.
- API: `GET /api/fuel-filter/readiness-log?limit&symbol&outcome`;
  `POST /api/fuel-filter/queue3/{delete,clear}`. UI: тумблер + таблиця «Черга-3»
  на `/smart-money`. get_state → `timers3`/`pending3_visible`/`queue3_enabled`.
- Тести: `test_readiness_strategy.py`.

## ⚡ Скальперська «Готовність» для funding-монет

Окремий SMC-грейд ЛИШЕ для монет таблиці «💰 Funding — МММ» (`_anomalies`) на
швидкому TF. `_compute_setup` параметризовано за TF (`base_tf`/`htf_tf`/
`write_exit`); скальп-варіант зве його з `base_tf='5m'`, `htf_tf='15m'`,
`write_exit=False` (не чіпає 1H-«Готовність виходу»). Окремий кеш
`_setup_scalp_cache` + `_refresh_setup_scalp_cache` (власний TTL/cap, тільки
funding-монети). Той самий `grade_setup` — інші свічки; на швидких TF
структура/зона шумніші → трактувати як ОКРЕМИЙ показник, не 1:1 з 1H.
- Налаштування: `funding_setup_scalp_on` (OFF), `funding_setup_tf` (5m),
  `funding_setup_htf` (15m), `funding_setup_ttl` (45с), `funding_setup_max_per_cycle` (8).
- get_state → funding-рядки несуть `setup_scalp`; UI — колонка «⚡ Скальп» у
  funding-таблиці + блок налаштувань у акордеоні. Інфосайт дзеркалить колонку.
- Навантаження мінімальне: klines кешуються (TTL), тротл cap/цикл; funding-
  підмножина мала (одиниці–десятки монет).
- TG на «хороший сигнал» по колонці Скальп: `scalp_tg_on`/`scalp_tg_min_score`
  (HOT або score≥поріг) / `scalp_tg_cooldown_min` / `scalp_tg_dir`. Метод
  `_scalp_setup_alert` (edge-trigger+кулдаун) у `_run_alerts` → топік 💰 funding.
- Панель `/api/fuel-filter/panel/<sym>` віддає `setup_scalp`; TradingView-оверлей
  (`tools/tradingview_mm_overlay.user.js`, v1.7.3) показує ⚡ Скальп-бейдж.

## 🎯 Шаровий конфлюенс (1..5) для funding-монет + консолідований TG

`_funding_layers(sym, a)` рахує 5 шарів У БІК напрямку монети: 1) МММ ≥ легкий
(сила ≥10) + тренд сили АСИМЕТРИЧНО за напрямком: **SHORT — НЕ слабшає (↑ або →;
гасне ЛИШЕ на ↓)**, бо на встановленому спаді сила часто на плато (→) і строге ↑
гасило шар майже завжди; **LONG — СТРОГО РОСТЕ (↑)**, як було раніше · 2) SCORE ≥
СЕРЕДНІЙ (≥40) · 3) Готовність(1H) ≥ СЕРЕДНІЙ (≥38) · 4) Скальп ≥ СЕРЕДНІЙ (≥38) ·
**5) ЦІНА у бік — свіжий рух (~15 хв) з 💰 Funding Rate Scanner: LONG=РОСТЕ (up),
SHORT=СПАДАЄ (down)**. Кожен шар несе `dir` (напрямок монети, КОЛИ засвічений) →
у колонці кольори за напрямком: **усі зелені (LONG) або всі червоні (SHORT)**.
Повертає {count, `base`(=усі 5), base4, layers[]}. get_state → funding-рядки несуть
`layers`; UI — колонка «🎯 Шари» (`ffLayersCell` фарбує крапки за `l.dir`).
- **5-й шар «Ціна» — ПОСТІЙНИЙ (світиться в колонці)**, живиться
  `self._funding_price` (кеш з `_get_funding_price_dirs()` → `FundingMonitor.
  get_price_dirs()`; оновлюється в тіку поряд з `_funding_trends`). Джерело —
  вікно `PRICE_WINDOW`=15хв, мертва зона `PRICE_DEADZONE`=0.10% (funding_monitor).
- **VOB (1m) = ОКРЕМИЙ ОДНОРАЗОВИЙ ТРИГЕР сигналу (не колонка-шар).**
  `_funding_vob(sym,d)` ПОСТІЙНО моніторить 1m-OB для КОЖНОЇ funding-монети (кеш
  8с) → `detect_volumized_obs(swing=5, ob_end_method='Wick', max_atr_mult=3.5,
  zone_count='Low')` → найновіший НЕ-breaker OB у бік. На НОВОМУ OB
  (`formation_time` ≠ `_vob_seen`) перевіряємо всі 5 шарів САМЕ ЗАРАЗ: якщо
  `base ≥ layer_tg_min` (cap 5) → сигнал; інакше чекаємо наступний OB.
- На НОВОМУ OB (інший formation_time) + кулдаун → ОДИН TG «🎯 Рекомендація бота»
  (`_layer_signal_alert`/`_send_layer_alert`, топік 💰 funding). Це ЗАМІНЯЄ старе
  «рекомендована ботом» повідомлення.
- Налаштування: `layer_tg_on` (OFF), `layer_tg_min` (скільки з 5, дефолт 5),
  `layer_tg_cooldown_min` (30). VOB-параметри — константи `_VOB_*` (1m/5/Wick/3.5/Low).
- Тести: `test_readiness_strategy.py` (funding_layers 5-шарів + ЦІНА-напрямок +
  VOB-alert + all-layers-gate).
- **Сортування таблиці «💰 Funding — МММ» — ЗА ШАРАМИ** (get_state): більше
  засвічених шарів вище; тайбрейк — довше тримається (`held_sec`).

## 🎯 Черга-4: пер-шарові тумблери (економія сервера)

Кожен із 4 шарів Черги-4 має ВЛАСНИЙ тумблер: `queue4_new_mm_on`,
`queue4_old_mm_on`, `queue4_setup_on`, `queue4_runway_on` (усі дефолт True =
стара поведінка). **ВИМКНЕНИЙ шар:** (1) НЕ рахується — `_queue4_layers` не
смикає `_fuel_dir_smoothed`/`_fuel_dir_legacy`, не читає setup-кеш, а
`_refresh_setup_cache` НЕ додає pending4 (важкий grade_setup) → менше load;
(2) колонка ховається у таблиці (класи `q4c-new/old/setup/runway` на th+td,
`_ffApplyQ4Cols()`). `_queue4_layers` повертає `required` = к-сть УВІМКНЕНИХ;
двигун відкриває коли `base ≥ required` (усі увімкнені збіглись; required=0 →
не відкриває). `Новий МММ` і `Запас` беруть дані з того самого `fn`
(`_fuel_dir_smoothed`) — рахуємо `fn` лише якщо хоч один із них увімкнено.
**Поріг «Новий МММ» = «рівновага» (0):** шар зараховується, якщо МММ НЕ ПРОТИ
(нейтраль АБО в бік), блок лише коли явно ПРОТИ (бо при силі ≤10% напрямку немає,
і суворе `nd==side` при «рівновага» ніколи не збиралось). Інші пороги — суворо
(напрямок у бік + сила ≥ порогу). Лічильник у 🎯 = `n/required` (к-сть увімкнених).

## 🎯 Черга-3: авто-відкриття за «VOB + 5 шарів» (funding-монети)

Той самий моніторинг VOB у `_layer_signal_alert` живить ДВА незалежні виходи на
НОВОМУ Volumized OB (edge за formation_time): (1) TG-сигнал (`layer_tg_on`);
(2) авто-відкриття угоди `queue3_vob_open` (дефолт **ON**). Логіка —
`_vob_open_or_trail(sym,a,d,ob,lay,s,now)`:
- **Нова угода:** усі 5 шарів зійшлись + монети ще немає в угоді → `_open(...,
  opened_by='Q3-VOB(funding)', skip_ctr_safeguard=True)` за напрямком. **SL = межа
  блоку OB + буфер**: SHORT → `top*(1+buf)` (над верхом), LONG → `bottom*(1-buf)`
  (під низом); `buf = queue3_vob_sl_buffer_pct/100` (дефолт 0.10%). SL ставиться
  через `tm.update_manual_sl_tp(sym, manual_sl=, is_shadow=)`.
- **Повторний VOB** по вже відкритій ботом монеті → угоду НЕ відкриваємо, лише
  ПЕРЕСУВАЄМО SL на новий блок. Трекер `self._vob_trade` {sym→{side,sl,ftime,
  entry,mode}}; чиститься, коли монета зникла І позиції вже нема.
- **Ворота «проти загального тренду»** (`queue3_vob_block_against_trend`, дефолт
  **ON**): НОВУ угоду не відкриваємо, якщо напрямок проти ЗАГАЛЬНОГО тренду монети
  (`dir_overall` ≈2 год з `_funding_price`): LONG у загальному ↓ / SHORT у ↑ →
  skip (лог `Q3-VOB`). 'flat' — дозволяємо. Трейл SL — без цих воріт.
- **TG «Рекомендація бота»** тепер надсилає `_vob_open_or_trail` (не окремий
  alert-branch, щоб не дублювати), формат мінімальний (`_send_layer_alert(sym,a,
  d,sl,mode)`): `🎯 Рекомендація бота · #SYM 🔴 SHORT` + `🛑 SL: <ціна>` (mode
  'open') / `♻️ Угода вже відкрита — змінено лише SL: <ціна>` (mode 'trail').
  Якщо `queue3_vob_open` OFF, а `layer_tg_on` ON — падає на просту «Рекомендацію»
  (mode 'signal', без SL) за старим порогом `layer_tg_min`.
- Усе (open / sl_moved / skipped) пишеться в 🧾 Лог роботи бота (`activity_log`,
  source `Q3-VOB`) з міткою «монета з фандингу».
- Налаштування: `queue3_vob_open` (True), `queue3_vob_sl_buffer_pct` (0.10),
  `queue3_vob_block_against_trend` (True). UI — у гармошці **«⚙️ Налаштування
  (Черга-3)»** (`templates/smart_money.html`), куди перенесено ВСІ параметри
  Черги-3. Заголовок колонки шарів — лише «🎯».
- **💰-мітка у відкритих угодах** (`histSymOpen` у smart_money + `renderTrades` в
  infosite): монета з фандингу (є в `anomalies` АБО `opened_by` містить funding).
- Тести: `test_vob_open_*` (+ ворота тренду) у `test_readiness_strategy.py`.

## ✦ «Золотий funding» TG — формат + затримка-підтвердження

`_send_gold_alert(sym,a,step)` формат (топік 💰 funding): `✦ FUNDING 🟢LONG /
#SYM / 💰 Funding: -2.000%`. Показуємо СНЕПНУТЕ чисте значення (`step` зі знаком
ставки) → завжди точне (−0.500/−1.500/−2.000%), а не сире (−0.495/−1.503%).
Перед відправкою рівень має протриматись `funding_gold_confirm_sec` (дефолт 30с,
«чітке визначення»); далі повтор раз на `funding_gold_cooldown_min` (60хв), поки
той самий рівень тримається. Тест: `test_gold_funding_confirm_then_repeat`.

## 💰 Funding Rate Scanner — колонка «Price» (dashboard)

Таблиця «💰 Funding Rate Scanner» (`templates/dashboard.html`, дані з
`FundingMonitor.get_watchlist()`) у колонці **Price** показує ЧІТКИЙ свіжий
напрямок ЦІНИ: `price_dir` (up/down/flat) + `price_chg_recent` (% за вікно
`PRICE_WINDOW`≈15хв) — **▲ росте (зелений) / ▼ спадає (червоний) / ▬ рівно**.
Плюс ЗАГАЛЬНИЙ тренд монети (`price_dir_overall`/`price_chg_overall`,
`PRICE_WINDOW_LONG`≈2год, deadzone 0.30%) — рядок «загалом ▲/▼ X%», щоб не входити
проти загального тренду (той самий `dir_overall` живить ворота VOB-відкриття).
Цей самий блок (`price` у get_state anomaly-рядку = `_funding_price[sym]`)
показується ДРУГИМ РЯДКОМ у таблиці «💰 Funding — МММ» (bot + infosite):
«💹 Ціна: ▲ +X% 15хв · 📈 Загалом: ▲ +Y% ~2год», вирівняно в стовпчик.
Це окремо від `price_change` (сумарно від старту стеження, тепер у підказці).
`_recent_price_move(rates)` рахує рух по останніх ~15 семплах (мертва зона
`PRICE_DEADZONE`=0.10%). Ту саму метрику віддає `get_price_dirs()` для 5-го шару.

## Закриті угоди: постійний архів у БД + швидкість закриття

- **Кожна закрита угода (real+paper) БЕЗУМОВНО пишеться в постійну таблицю
  `sob_trade_archive`** (`_archive_closed` → `db.archive_trade`, гейт по
  прихованому `archive_trades` ПРИБРАНО). Ролінг-блоб `tm_closed_trades` (кеп
  `CLOSED_TRADES_LIMIT`=2000) лишається для живої стрічки. get_state віддає ВЕСЬ
  блоб (не 50) — таблиці рендерять усе з захистом-сигнатурою (не «стрибають» під
  час скролу).
- **Таблиці «📜 Recent Closed» / «Recent Paper Closes» — прокрутка + копіювання +
  «за весь час (БД)».** Клас `.tm-vhscroll` (max-height, sticky-заголовок);
  `copyTableToClipboard()` → TSV у буфер; `toggleArchiveView('closed'|'shadow')`
  тягне ВСЮ історію з `/api/trade-archive/export?is_paper=…` і показує read-only
  (жива стрічка тоді не перемальовує — `window._tmArchiveMode`).
- **Швидкість закриття:** цикл монітора виходів — налаштовний
  `monitor_interval_secs` (дефолт **4с**, було жорстко 10с; clamp [2,30]).
  Ціна береться з кешу сканера (без API-хіта), тож частіше = швидше закриття
  без навантаження. Реконсіляція з Bybit — на власному `reconcile_interval_secs`
  (n_ticks рахується від ефективного інтервалу монітора). Сам close-ордер
  reduce-only шлеться синхронно, важке (real-PnL/архів/notify) — у фоні
  (`_finalize_close_async`), тож не блокує монітор.

## Лог Черги-3: рух у черзі + виснаженість

`_log_readiness(..., move_pct=, exhaustion=)` — у `sob_readiness_log` і в UI-лозі
несе **рух ціни від входу в чергу** (`move_pct`, + = у наш бік; `_pending3[sym]
['added_price']`) і **виснаженість** — щоб було видно, що монета вже відпрацювала,
поки стояла в черзі. У рядку логу: «… · рух +2.3% · вичерп 72% · …».

## Telegram-бот (`web/tg_bot.py`) — БЕЗПЕКА розсилки

Відповідь адміна юзеру НІКОЛИ не повинна ставати масовою розсилкою:
- Свайп-Reply на шапку «Повідомлення від користувача» → лише тому юзеру. Звʼязок
  `_reply_map` **персиститься в БД** (`tg_reply_map`, `_load_reply_map` на старті)
  → переживає рестарти. Якщо звʼязок усе одно втрачено — `chat_id` парситься з
  тексту шапки (`_chat_id_from_text`).
- Reply без звʼязку → підказка `/reply <chat_id>`, **не розсилка**.
- Масова розсилка ЛИШЕ за явним `/announce <текст>` (`_admin_broadcast`). Звичайне
  повідомлення адміна → підказка, ніколи не broadcast.
- Тести: `test_tg_reply.py`.

## БД-ключі (storage)

- `fuel_filter_settings` — налаштування FF (вкл. `queue3_enabled`,
  `readiness_log_enabled`).
- `fuel_filter_state` — JSON-блоб стану: timers, fuel_managed, anomalies(=funding),
  engine_attempts, fuel_ema, fuel_hyst, btc_verdict_dir/since, pending, pending2,
  **pending3**.
- `fuel_filter_scan_list` — дозволені для сканування символи.
- Таблиця `sob_readiness_log` — пер-рішення лог Черги-3 «Готовність». Префікс
  `sob_` → автоматично в аналізі Database Administration; входить у
  `_SERVICE_TABLES_TIME` → її ЧИСТИТЬ і ручна «🗄️ Службові», і DB-autoclean.

## Основні JSON API (read-only — для інфо-сайту)

- `GET /api/fuel-filter/state` → весь стан FF (`ff.get_state()`): черга, таймери,
  funding-таблиця, банер BTC, статуси. **Головне джерело для інфо-сайту.**
- `GET /api/stats`, `GET /api/trades`, `GET /api/signals`, `GET /api/sleepers`,
  `GET /api/orderblocks`, `GET /api/health`.
- `GET /api/scheduler/status`, `GET /api/events`.
- Сторінки: `/` (головна), `/smart-money` (FF), `/tickr`, `/trades`, `/settings`.

> Маршрути, що ЗМІНЮЮТЬ стан (POST: toggle, scan, delete-timer, force-open,
> settings, trade/close…) — інфо-сайту НЕ потрібні й не повинні викликатись.

## Git / деплой (важливо для кожної сесії)

- Робоча гілка: `claude/analyze-project-01Gopju9D7AHv4pgvetccBeB`. Розробляти й
  комітити сюди.
- **PUSH ЗАБЛОКОВАНИЙ:** git-relay повертає 403 (egress-політика середовища). Це
  НЕ мережевий збій — retry не допомагає, обходити заборонено.
- **ФОРМАТ ВІДДАЧІ ЗМІН (завжди!):** віддавати користувачу САМІ ЗМІНЕНІ ФАЙЛИ
  (не патч) зі збереженням структури папок, запаковані в **ZIP**. Тобто
  `detection/trade_manager.py` лежить у zip за шляхом `detection/trade_manager.py`.
  Користувач розпаковує в корінь репозиторію й пушить локально.
- **У ZIP — ЛИШЕ файли, змінені в ЦІЙ конкретній правці** (вимога користувача),
  а НЕ весь накопичений набір. Тобто якщо цього разу змінено лише
  `templates/smart_money.html` — у zip лише цей файл (+ `CLAUDE.md`, якщо
  оновлював пам'ять). НЕ перекладати весь бандл щоразу.
- Не створювати PR без явного прохання.

## Окрема сесія для інфо-сайту

Інфо-сайт розробляється в ОКРЕМОМУ чаті. Бриф: див. `INFO_SITE.md`.
Принцип «в парі»: бот пише стан → інфо-сайт лише ЧИТАЄ його через JSON API.
Жодного спільного коду, жодних кнопок керування на інфо-сайті.

## Черга-4: «Старий МММ» — поріг «рівновага» = «не проти» (як у «Новий МММ»)

У `_queue4_layers` режим `require` для **Старого МММ** при `mmo_min <= 0`
(«рівновага») тепер зараховує шар, якщо МММ **НЕ ПРОТИ** (нейтраль АБО в бік);
блок лише коли явно ПРОТИ — ТА САМА логіка, що вже діяла для «Новий МММ».
Причина: при слабкій силі статусу напрямку немає, і суворе `od == side` при
«рівновага» не збиралося НІКОЛИ (кейс: «Старий МММ 7% рівновага» — шар не
зараховувався). Інші пороги (>0) — суворо: напрямок у бік + сила ≥ порогу.

## VOB: «новий блок» = НЕ опрацьований (пастка breaker — не зламати!)

РАНІШЕ новизна VOB визначалась «водяним знаком»: `ft > _seen[side]`. **Пастка:**
коли поточний OB стає **breaker**, він випадає зі списку, і `newest_bull/bear`
стає СТАРІШИЙ блок із МЕНШИМ `formation_time` → умова `ft > prev` НІКОЛИ не
виконується → блок вічно `duplicate`, а UI показує «VOB N · чекаємо #N+1»,
хоча на графіку блок давно змінився (скарга: «висить казна який VOB»).
**Фікс:** `_vob_alert_seen[sym][side]` тепер **СПИСОК опрацьованих ft**
(`VOB_SEEN_CAP=12`), а рішення дає `_vob_outcome(done_list, ft, age, max_age)`:
`duplicate` (ft уже в списку) / `first_sight` / `stale` / `fresh`. Новим є
БУДЬ-ЯКИЙ ще не опрацьований блок — навіть зі старішим часом. Хелпери
`_vob_seen_list` / `_vob_seen_add` роблять міграцію зі старого int-формату
(і в `_load_vob_state`). Тест: `test_vob_outcome_breaker_trap`.

## Watchlist: кнопка ⇅ A-Z

Поряд із лічильником «N / 100» — кнопка `sm-wl-sort-btn` (`toggleWlSort()`):
сортує СПИСОК ПОКАЗУ за алфавітом; монети, що починаються з ЦИФРИ
(1000PEPE, 1INCH…) — у САМИЙ КІНЕЦЬ. Серверний watchlist НЕ змінюється
(`wlSymbols` лишається у серверному порядку, сортується лише копія в
`renderWatchlist`). Стан кнопки — у `localStorage['wlSortAlpha']` (це UI-
налаштування, не дані — на відміну від `wlTrends`, який звідти прибрано).

## ⚠️ Застарілі місця, знайдені при аудиті тестів (не виправлено)

- `test_readiness_strategy.py::test_ob_match_gate` кличе `ff._ob_match_ok(...)`,
  якого в `detection/fuel_filter.py` **вже немає** (метод видалено/перейменовано).
  Тест падає на цьому місці; попередні 12 тестів файлу проходять.
- Виправлено в тому ж файлі: стаби `_funding_vob` відставали від сигнатури
  `(sym, d, vob_tf)`; легасі-тести TG-шарів тепер явно ставлять
  `funding_route_q4=False` (бо дефолт True → VOB іде прямо в Чергу-4).
- **Відновлено мертве налаштування:** у `_layer_signal_alert` змінна `need`
  (`layer_tg_min`) рахувалась, але НЕ застосовувалась → TG-алерт летів на КОЖЕН
  новий VOB. Тепер алерт лише коли `base ≥ layer_tg_min` (як у пам'яті проєкту).

## ⚡ ШВИДКІСТЬ ЦИКЛУ: паралельний префетч барів (не зламати!)

Вимога користувача — «жоден сигнал не втрачений через великі проміжки». Заміряно
профілем: 33 монети ≈ 235с (7.1с/монету), з них **мережа ≈53%** — на монету йде
~7 HTTP-запитів (3000 барів = 3 сторінки по 1000 на КОЖЕН TF; `fetch_klines`
НЕ кешує). Тому:

- **`_prefetch_klines(md, symbols, specs)`** тягне бари для (символ × spec)
  **ПАРАЛЕЛЬНО** (`ThreadPoolExecutor`, `PREFETCH_WORKERS=8`) у `self._prefetch`
  ПЕРЕД послідовним циклом. Обробка лишається ПОСЛІДОВНОЮ — спільний стан,
  сигнали, БД не чіпаємо (жодних гонок).
- **`_prefetch_specs()`** — рівно ті набори, що споживає скан: головний TF
  (`KLINES_LIMIT`), Volumized TF (3000), `ob_filter_tf` (700), `pd_zone_tf`
  (3000). Вимкнений Volumized НЕ вантажиться.
- **`_pf_klines(md, sym, tf, limit)`** — ЄДИНА точка отримання барів у скані:
  бере з префетчу (`pop`, одноразово) або робить звичайний запит. Використана в
  3 місцях: головні бари, `_get_tf_data`, `_update_smc_ob`.
- Пауза між монетами `0.2с → 0.01с` (мережі в циклі вже майже немає; 0.2×33 було
  6.6с чистих втрат). Профіль циклу тепер друкує ще й `prefetch Xs`.
- **`DEFAULT_INTERVAL_SECS` 60 → 10**, нижня межа валідації `30 → 5`. Плюс
  ОДНОРАЗОВА міграція `interval_migrated_v2`: старе збережене значення ≥60 один
  раз переводиться на новий дефолт (далі значення користувача поважається).

⚠️ НЕ паралелити САМУ обробку монет — там спільний стан (`_vob_*`, кеші, БД) і
відкриття угод. Паралельне лише ЗАВАНТАЖЕННЯ. Тест: `test_scan_prefetch.py`.

## 🎚 Пороги «якості» — ЄДИНЕ ДЖЕРЕЛО (setup_grader) · не дублювати числа!

**Кейс-урок (XRPUSDT, Черга-4).** Колонка показувала «38% **СЕРЕДНІЙ**», а поріг
стояв «Готовність ≥ **СЕРЕДНІЙ**» — і шар НЕ зараховувався. Причина: одне слово
означало РІЗНІ числа. Смуги грейда перекалібрували (`_GRADE`: ВІДМІННИЙ 72→**70**,
ХОРОШИЙ 55→**53**, СЕРЕДНІЙ **38**, СЛАБКИЙ **24**), але випадайка в HTML лишилась
зі старими `25/40/55/72`, а `fuel_filter` хардкодив `40`/`38` у шарах funding.

**Правило:** пороги беруться ЛИШЕ з `detection/setup_grader.py`:
- `grade_bands()` → `[{min,label,color}]` (без службового «ВИЧЕРПАНО»);
- `grade_min('СЕРЕДНІЙ')` → 38.

Хто вже переведений: `_funding_layers` (SCORE/Готовність/Скальп → `_grade_min
('СЕРЕДНІЙ')`), дефолт `queue4_setup_min` = **38**, `ff.get_state()['grade_bands']`
→ фронт будує випадайку «Готовність ≥» через `_ffSyncGradeOptions()` (HTML-опції
лишились лише як фолбек і вже вирівняні: 24/38/53/70).
⚠️ НЕ вписувати ці числа руками в нових місцях — брати з `grade_min()`.

## 🧾 ЛОГ VOB: лише РЕАЛЬНІ ПРОПУСКИ (чистка за обсягом)

Заміряно на проді: **561 запис за 8.5 год**, з них VOB — 290 (52%), а корисних
дій (TM/FF) — 1.4%. Розподіл VOB: `duplicate` 165 + `numbered` 93 +
`no_candidate` 29 = **51% усього логу** — це СТАБІЛЬНІ СТАНИ, а не події.

**Правило:** `_vob_log_decision` пише в `activity_log` (`source='VOB'`) ЛИШЕ
реальні ПРОПУСКИ сигналу — **`stale` / `epoch` / `no_1h_ob`** (+ анти-дубль за
ключем `(outcome, ft, side, num)`).
НЕ пишемо: `duplicate` / `numbered` / `no_candidate` / `first_sight` — вони
завжди видні в бейджі `vob_diag` на панелі в реальному часі; і `fired`/`filtered`
— їх пише основний шлях (`signal` + `rejected` з розкладом фільтрів).
Тест: `test_log_only_real_misses_not_stable_states`.
⚠️ Історія рішень: спершу було «лог чистий» → потім «логувати КОЖЕН VOB» →
тепер компроміс: у лозі події, у бейджі — стани.

## 🔁 Черга-4: повторна перевірка фільтрів на ВИХОДІ (`queue4_recheck_filters`)

Дефолт **УВІМКНЕНО**. Перед `_open` монета ще раз проходить ТОЙ САМИЙ ланцюг
сканера, що пускав її в чергу — `scanner._signal_allowed(sym, side,
at_intake=False)` (**СТРОГО**: на інтейку дозволялось «чекати вердикт», на
відкритті потрібен фактичний дозвіл). Причина: монета може стояти в черзі
годинами (TTL 15+ год), за цей час 1H-OB/прогноз/Decision можуть перевернутись.
Не пройшла → **НЕ відкриваємо**, лог `Черга-4 🔁 повторна перевірка фільтрів не
пройдена: <причина> · <розклад>`, монета ЛИШАЄТЬСЯ в черзі й спробує на
наступному тіку (доки не викине TTL/застій). Реалізація —
`_q4_recheck_filters(sym, side)`; помилка перевірки НЕ блокує (fail-open).
UI-тумблер `ff-queue4-recheck` у гармошці «⚙️ Налаштування (Черга-4)».
Тест: `test_q4_recheck.py`.

## 🖼 VOB = САМЕ ТОЙ БЛОК, ЩО НА ГРАФІКУ (обмежено графіком — не зламати!)

Вимога користувача: «що виводиться на графік, то і вважається VOB-сигналом».
Панель малює **РІВНО ОДИН** бокс — `volObPrim.setOB(d.volumized_ob)`, а це
`latest_ob` (найновіший серед ОБОХ боків, та сама функція `_latest_volumized_ob`
і ті самі бари). Тому кандидат для сигналу/нумерації/логу — **лише він**:
`_vob_chart_candidates(vol_result)` → `[]` або `[(side, ob, ft)]`.

**Що було не так:** бралися найновіші блоки ОБОХ боків
(`_vob_pick_candidates(newest_bull, newest_bear)`). Протилежний бік часто був
багатоденної давності — у проді траплялись блоки віком **1500-2800 барів 5m
(5-10 діб)**: на графіку їх НЕ видно, але вони крутили лічильник (ETH «VOB #8»,
LDO «#7»), засмічували лог і навіть фаєрили сигнали. Тепер такі блоки не
беруться взагалі.

`breaker` НЕ є сигналом: такий бокс малюється пунктиром із «✕» — зона вже
знецінена, вхід по ній помилковий.
`_vob_pick_candidates` лишився лише як тест-хелпер (у скані НЕ використовується).
Тест: `test_chart_candidate_is_the_drawn_block_only`.

## 🕒 Лог VOB: чесний підпис (вік + час формування, БЕЗ слова «Свіжий»)

Оскільки вікно свіжості за замовчуванням ВИМКНЕНЕ (`vob_alert_max_age_bars=0`),
фаєрить будь-який ще НЕ опрацьований блок — навіть багатогодинний. Тому слово
«Свіжий» у рядку сигналу БРЕХАЛО (реальний кейс: SUIUSDT «Свіжий Volumized OB»
для блоку віком **125 барів ≈ 10.4 год**). Прибрано.

`_vob_age_label(ft, age, tf)` — ЄДИНЕ джерело підпису: `вік N барів (~X год) ·
утв. DD.MM HH:MM UTC`. Використовується і в рядках сигналу, і в статусах
(`_vob_log_decision`). Голий `ft=1787536500000` більше НЕ пишемо — його
неможливо звірити з графіком очима.
Тест: `test_age_label_is_honest_and_human`.

## 🔇 Анти-флуд Q4-recheck + полегшення сторінки логу

- **Q4-recheck флудив:** гейт `queue4_recheck_filters` писав рядок на КОЖЕН тік
  двигуна, доки монета висить у черзі → один ZECUSDT дав **48 однакових рядків**
  за 8.5 год. Тепер `_q4_recheck_logged[sym] = ((side, reason), ts)`: пишемо лише
  коли ЗМІНИЛАСЬ причина або раз на `Q4_RECHECK_LOG_GAP` (900с). Пройшла
  перевірка → запис знімається.
- **Сторінка гальмувала** не через бота, а через UI: таблиця логу тягнула
  **600 рядків кожні 5 секунд** і щоразу повністю перебудовувала DOM. Тепер:
  ліміт **300**, інтервал **15с**, і головне — **сигнатура** (`tb.dataset.sig`):
  якщо нічого не змінилось, DOM НЕ чіпаємо. Повна історія — кнопками CSV/JSON.

## ✋ РУЧНИЙ СИГНАЛ (кнопка в банері Decision)

Кнопка **«✋ Сигнал»** поряд із ⬆LONG/⬇SHORT (`sm-decision-btn-signal`).
Напрямок = **вердикт банера** (`window._smDecisionDir`, ставиться при рендері).

**ВАЖЛИВО (рішення користувача):** ручний сигнал **НЕ проходить фільтри
сканера** — рішення ухвалює людина. Ендпоінт `POST /api/smc/manual-signal`
{symbol, side} одразу кличе `TradeManager.on_signal(opened_by='manual')`, тобто
йде на обробку **ПІСЛЯ фільтрів**: черга (intercept) → двигун → відкриття.
Гейти самої ЧЕРГИ (шари Черги-4, ₿-сеанс, recheck) лишаються — вони не є
фільтрами сканера. У 🧾 Лог пишеться `✋ РУЧНИЙ сигнал <side> — фільтри
пропущено · <розклад>`; якщо фільтри БУЛИ Б проти — це теж видно в розкладі.
Мітка угоди — `manual` → бейдж **✋ Ручний** (уже був у `signal_labels.py`,
`smart_money.html` і `infosite/app.js` — нову картинку не вигадували).

## 🪗 Гармошки та полегшення сторінки

- **🧾 Лог роботи бота** — гармошка, за замовчуванням **ЗГОРНУТО**
  (`toggleActivityLogPanel`, стан у `localStorage['actLogOpen']`). Поки згорнуто
  — таблиця **НЕ опитується і НЕ малюється** взагалі. Полл також пропускається
  при `document.hidden` (неактивна вкладка).
- **ГАРМОШКИ НА ВСІХ ВАЖКИХ СЕКЦІЯХ** — єдина функція `togglePanel(id)`
  (`toggleQueuePanel` лишився як аліас для заголовків Черг). Кожна секція має
  пару `#<id>-body` + `#<id>-caret`; стан памʼятається окремо в
  `localStorage['qPanel_<id>']`; список — `_PANEL_IDS`.
  Секції: **q1..q4** (Черги 1-4), **poc** (🎯 POC-сетап), **fund**
  (💰 Funding — МММ), **tm** (💼 Trade Manager), **testmode** (🧪 Test Mode).
  ⚠️ У гармошку Черги складено **ВЕСЬ** її вміст (налаштування, вкладки
  напрямку, engine-gate, таблиця) — назовні лишається ЛИШЕ шапка з тумблером і
  кнопкою «Очистити всі». У `tm`/`testmode` використано ГОТОВІ контейнери
  `#tm-body`/`#testmode-body`, які вже були в розмітці.
- **Колонки «МММ» і «Готовність виходу» ПРИБРАНО** з таблиць відкритих угод
  (real + paper). Алгоритм НЕ видалено:
  • `trade_manager.POSITIONS_EXIT_GRADE = False` — гейт розрахунку `exit_grade`
    (він рахувався на КОЖНУ позицію на КОЖЕН тік монітора, деф. 4с — найважче);
  • у `/api/tm/state` закоментовано збагачення `fuel_str/fuel_str_prev/fuel_dir`.
  Щоб повернути — `POSITIONS_EXIT_GRADE = True`, розкоментувати 3 рядки у
  flask_app і повернути `<th>`/`<td>` (colspan 12/11 → 14/13).

## ⚡ ЧЕРГА-4: ПАКЕТ ПРИСКОРЕННЯ (A1+A2+A3+B1+B2+C1+C2) — не зламати!

**Заміряна причина гальм.** На КОЖЕН рядок Черги-4 за один 30с-такт liq-map
збирався **~7 разів**: двигун (`_fuel_dir_smoothed` + шари у СВІЙ бік + шари у
ПРОТИЛЕЖНИЙ) + UI-полл (ще одні шари), а `_queue4_layers` усередині кликав
`_fuel_dir_smoothed` І `_fuel_dir_legacy`, кожен з яких будував `lm.get_state()`
САМ. `lm.get_state()` = `liqmap_get_events(limit=3000)` → до 3000 ORM-рядків +
`.to_dict()` + кластеризація, **без кешу**. Плюс ~20 `db.get_setting()` на монету
(`get_mm_settings` — 8 окремих сесій SQLAlchemy на КОЖЕН `compute_mm`).
При 100 монетах у черзі це ~700 збірок / 30с. І **все це — під глобальним
`self._lock` у `get_state()`**, тобто HTTP-запит сторінки блокував ТОРГОВИЙ daemon.

**A1+A2 — `_liq_state(symbol, force=False)` = ЄДИНА точка знімка liq-map.**
`_fuel_dir`, `_fuel_dir_legacy`, `_btc_liqfuel_raw` і панельний шлях беруть ЗВІДТИ.
TTL `LIQ_STATE_TTL=20с` (< `CYCLE_SECS=30` → кожен такт двигуна все одно читає
свіжий знімок; джерело — daemon liq-map — оновлюється раз на 60с, тож 20с ФІЗИЧНО
нічого не змінює). Стеля памʼяті `LIQ_STATE_CAP=400`. **`lst is None` («liq-map ще
не піднявся») НЕ кешується** — інакше після рестарту бот мовчав би цілий TTL.
Свіжість ЦІНИ не постраждала: обидві МММ-функції накладають `_live_price()` поверх
`mark_price` знімка. `_liq_decay_profile()` — той самий `get_setting` із кешем 60с.

**A3 — `mm_model.get_mm_settings` кешується 60с** (+ `invalidate_mm_settings()`).
Помилка БД НЕ кешується (щоб не «залипнути» дефолтами).

**B1 — розділено I/O і арифметику:**
- `_q4_snapshot(sym, s)` — ЄДИНЕ I/O (fn/fo/su + `on` = стан тумблерів);
- `_q4_layers_from(snap, side, s)` — **ЧИСТА функція, ЖОДНОГО I/O** → ДРУГИЙ
  напрямок (фліп-кандидат) коштує НУЛЬ;
- `_queue4_layers(sym, side, s)` лишився як сумісний фасад (знімок + арифметика)
  для поодиноких викликів (фільтр входу в `intercept`).
⚠️ Тумблери арифметика бере **зі знімка** (`snap['on']`), а НЕ з налаштувань ще
раз — інакше тумблер, перемкнутий між знімком і розрахунком, дав би «визначений»
шар без даних під ним.
⚠️ У двигуні `fuel = _snap['fn'] or self._fuel_dir_smoothed(sym)` — фолбек
ОБОВʼЯЗКОВИЙ: коли ОБИДВА шари «Новий МММ» і «Запас» вимкнені, знімок свідомо не
тягне `fn`, а `mark` двигуну потрібен завжди.

**B2 — шари рахує ТІЛЬКИ двигун; `get_state()` їх ЧИТАЄ.**
`_engine_tick_queue4` кладе обидва напрямки в `_q4_layers_cache[sym]=(ts,{side:lay})`
(`_q4_cache_put`), `get_state` бере через `_q4_layers_cached` (промах/протухле >
`CYCLE_SECS*3` → рахує на місці, це дешево завдяки `_liq_state`). Два виграші:
(1) UI більше НЕ рахує нічого під локом; (2) **таблиця показує РІВНО ті числа, за
якими двигун ухвалив рішення** — розбіжність «в таблиці одне, вирішив інше»
структурно неможлива. Кеш чиститься на кожному виході з черги (`_q4_cache_drop`
у всіх 6 `_pending4.pop`, `delete_timer4`, `clear_all_timers4`) + прибирання
сиріт у кінці такту.

**C1 — сигнатура таблиці Черги-4** (`_ffQ4Sig` → `tb.dataset.sig`): нічого не
змінилось → DOM не чіпаємо. `held_sec` СВІДОМО не входить у сигнатуру — секунди
малює `data-since` + глобальний 1с-тікер. `_ffApplyQ4Cols()` викликається ДО
перевірки сигнатури, тож перемикання колонок працює завжди.

**C2 — сервер рахує лише РОЗГОРНУТІ секції.** `GET /api/fuel-filter/state?sections=q4,fund`;
`get_state(sections=None)` = все (сумісність — **інфо-сайт параметр не шле**).
`FuelFilterDaemon.HEAVY_SECTIONS = ('q4','fund')`. Фронт: `_ffWantedSections()`
дивиться `#q4-body`/`#fund-body`, `togglePanel` на РОЗГОРТАННІ важкої секції
одразу смикає `loadFuelFilterStatus()`. Відповідь несе `served:{q4,fund}` — фронт
ОБОВʼЯЗКОВО його читає, інакше порожній `timers4` (секцію не просили) прочитався б
як «черга порожня» і стер би таблицю. Лічильник `pending4_visible` рахується
ЗАВЖДИ (шапка чесна навіть коли гармошка згорнута); для funding пропускається
ЛИШЕ `_funding_layers` — усі побічні ефекти циклу (rate_min/vol24h/епізоди) і
`ffFundingSyms` лишились.

**⏱ Профіль такту:** `[FF-Q4] tick: N coins in Xs (Ys/coin)` — щоб ефект був
у ЦИФРАХ, а не «здається швидше».

**Тести:** `test_q4_perf.py` (17). Головний замок —
`test_split_gives_identical_result`: знімок+чиста функція мусять дати БАЙТ-У-БАЙТ
те саме, що монолітний `_queue4_layers`. Плюс `test_layers_from_is_pure_no_io`
(будь-яке I/O в чистій функції = падіння тесту).

## 🎯 Черга-4: ♾ без терміну · джерело SL · ручне відкриття · сортування

**♾ «Без терміну» (`queue4_no_ttl`, дефолт OFF).** Запис живе, доки його не
ЗАМІНИТЬ ПРОТИЛЕЖНИЙ сигнал по тій самій монеті (механізм заміни вже був:
`_q4_opp_replace` в `intercept` + `gate_exempt`), або поки не відкриється /
не приберуть вручну. **Вимикає ОБИДВА виселення за часом** — і TTL, і «викид
при застої»: інакше монета зникала б через 30 хв і заміняти було б НІЧОГО.
Санітарні виходи (вже в угоді, 1H-OB уже відпрацьовано) лишаються.
Рішення винесені в ЧИСТІ функції `_q4_ttl_expired(added_at, now, ttl_h, no_ttl)`
та `_q4_stagnant(lit_at, now, nl_min, no_ttl)` (юніт-тести). UI: тумблер
`ff-queue4-nottl`; `_ffQueue4TtlSync()` ГАСИТЬ поля TTL і «застій», щоб було
видно, що вони не діють.

**🛑 Джерело Manual SL (`queue4_sl_source`, дефолт `'1h'`).**
- `'1h'` — межа **1H Order Block**: `db.get_smc_ob_state(sym, ob_filter_timeframe)`
  → `bar_high`/`bar_low`. Це ТОЙ САМИЙ ★-блок, що на графіку. ⚠️ НЕ рахувати OB
  інлайн — це вже давало «мітка каже 1H, а цифри з іншого TF» (кейс ★ watchlist).
- `'15m'` — межа **Volumized OB на 15m** (`_q4_ob_bounds_vob`), форма блоку за
  параметрами сканера (swing/end/atr/zone), щоб збігалось із боксом на графіку.
SL = межа + `queue3_vob_sl_buffer_pct`: SHORT → `top*(1+buf)`, LONG → `bottom*(1-buf)`.
Якщо обраного блоку немає — береться ДРУГЕ джерело (щоб угода не лишилась без
SL), і в 🧾 Лозі пишеться, з ЯКОГО саме джерела став рівень + позначка «фолбек».
Немає жодного — SL не вигадуємо, пишемо в лог.

**✋ Ручне відкриття з таблиці** — кнопка ✋ у колонці «Дія» →
`POST /api/fuel-filter/queue4/open` {symbol} → `ff.force_open_queue4(sym)`.
Напрямок береться З ЗАПИСУ В ЧЕРЗІ (відкривається рівно те, що видно в рядку).
Ворота Черги-4 пропущено (збіг шарів, 🔁 повторна перевірка, «1 угода на 1H-OB»)
— рішення ухвалює людина, як і в ✋ ручному сигналі. Запобіжники `_open`
(ціна/розмір) лишаються. Мітка — `_ob_compose('manual','Q4')` = **✋ Ручний →
🎯 Черга-4**. Після відкриття: чистка черги + кешу шарів, старт таймера,
`_mark_ob_epoch_opened` (щоб авто-двигун не відкрив ДРУГУ на тому самому 1H-OB)
і той самий `_q4_set_vob_sl`. `_open` відхилив → монета ЛИШАЄТЬСЯ в черзі.

**⇅ Сортування таблиці Черги-4** — клікабельні заголовки (`ffSortQueue4`,
`_ffQ4SortVal`, стрілки `_ffUpdateQueue4HeaderArrows`), **за замовчуванням
«🎯 Шари» ↓** (`_FF4_SORT_DEFAULT`), тайбрейк — довше в черзі. Сила МММ/Запасу
сортується ЗІ ЗНАКОМ за напрямком запису: «34% у бік» вище за «71% ПРОТИ»
(інакше сортування показувало б найгірші рядки зверху). Порядок входить у
сигнатуру `tb.dataset.sig`, тож зміна сортування перемальовує таблицю.

Тести: `test_q4_controls.py` (20).

## 🛑 Manual SL Черги-4: 4 виправлені дефекти (не зламати!)

Аудит за скріном (10 paper-позицій + NEOUSDT) виявив у `_q4_set_vob_sl`:

1. **ЛОГ БРЕХАВ.** Результат `tm.update_manual_sl_tp(...)` **ІГНОРУВАВСЯ**, а в
   🧾 Лог писалось «SL з … → X». TM має ВЛАСНУ валідацію
   (`update_manual_sl_tp`: SHORT → рівень мусить бути ВИЩЕ ПОТОЧНОЇ ціни, LONG →
   нижче) і на відмову НІЧОГО не змінює. Тобто угода лишалась **БЕЗ стопа**, а в
   лозі стояло, що стоп поставлено. Q3-VOB (`_vob_open_or_trail`) робив це
   ПРАВИЛЬНО — писав «не прийнято: <причина>». Тепер Q4 так само: `ok` → рядок зі
   стопом **і відстанню `% від входу`**; `not ok` → «ВІДХИЛЕНО Trade Manager:
   <причина> — угода БЕЗ стопа».
2. **1H-OB брався БЕЗ звірки bias із напрямком угоди.** Для SHORT брався
   `bar_high` БУДЬ-ЯКОГО блоку; бичачий 1H-OB лежить ПІД ціною → стоп нижче ціни
   → відмова TM (див. п.1, мовчки). Черга-4 приймає ОБИДВА боки і вміє фліпати за
   показниками, тож «напрямок ≠ bias 1H-OB» — звичайна ситуація, і з дефолтом
   `queue4_sl_source='1h'` вона стала б частою. Тепер `_q4_sl_candidates` ВІДКИДАЄ
   1H-джерело з протилежним bias → фолбек на Volumized (він за побудовою бере
   блок потрібного боку з `bullish_obs`/`bearish_obs`).
3. **Бік рівня не перевірявся ДО виклику.** Додано ЧИСТУ `_q4_sl_side_ok(sl, side,
   ref)` — ТЕ САМЕ правило, що в TM, з ТІЄЮ САМОЮ ціною (`tm._get_current_price`).
   Рівень не з того боку → джерело пропускається, беремо наступне; жодне не
   підійшло → SL не вигадуємо і пишемо в лог УСІ причини.
4. **`is_shadow` ВГАДУВАВСЯ:** `not _tm_has_position(sym, True)` — «немає
   реальної, отже тіньова». Якщо позиції не було ЗОВСІМ, писали в тіньову книгу і
   отримували мовчазне «No open shadow position». Тепер за ФАКТОМ (як у Q3-VOB):
   `_real`/`_shadow`; немає жодної → чесний рядок у лозі.

**Що НЕ виправлено (політика ризику — рішення користувача, не баг):**
- **Немає стелі/підлоги відстані SL.** Зі скріна: ALGO 1.40% … **WIF 15.56%** —
  при однаковому розмірі позиції ризик різниться в 11 разів.
- **Q4 не ставить TP** узагалі (R:R не заданий).
- **SL ставиться ОДИН раз** і не трейлиться (на відміну від Q3-VOB).
- ⚠️ `update_manual_sl_tp` пише власний рядок у лог із `source='user'` («Користувач:
  Manual SL → …») навіть коли рівень поставив ДВИГУН. Вводить в оману; лишено як є.

Тести: `test_q4_controls.py` (розділ «Джерело Manual SL»).

## 🛡 ГАРАНТІЯ СТОПА: авто-SL більше НЕ «чекає» (кейс MNTUSDT — не зламати!)

**Що сталось.** MNTUSDT paper SHORT @0.51360 відкрився з Черги-4, а поле Manual SL
лишилось ПОРОЖНЄ. У 🧾 Лозі: «Авто-SL з OB: OB на 15M протилежний (BULLISH) —
чекаю BEARISH». При цьому на графіку висів ★1H-OB **ВЕДМЕЖИЙ** (бейдж
«🔒 OB Short 1H ★», ~0.5250-0.5285) — ідеальний якір для стопа шорта.

**Корінь.** У проєкті ДВА незалежні авто-SL, і жоден не гарантував стоп:
1. `trade_manager._auto_ob_manual_sl` (тумблер `q2_auto_ob_sl`) — дивився РІВНО
   ОДНЕ джерело: OB на `q2_auto_ob_sl_tf` (деф. **15m**, це НЕ `ob_filter_timeframe`
   сканера!). Bias не збігся → `_diag(...)` і `return`. Угода висіла без стопа
   скільки завгодно, бо «SET ONCE» лише про успіх, а невдача ретраїлась вічно.
2. `fuel_filter._q4_set_vob_sl` — свій ланцюг (виправлений раніше).
Q4-шлях ставить `manual_sl` БЕЗ `_auto_ob_sl_val`, тож TM бачить його як «рівень
від користувача» і не втручається — саме тому в інших монет SL був, а в MNT ні.

**Фікс (`_auto_ob_manual_sl` тепер ЛАНЦЮГ, останній крок — гарантія):**
1. OB на `q2_auto_ob_sl_tf` (як було, пріоритет збережено);
2. **★ OB на `ob_filter_timeframe`** — ТОЙ САМИЙ блок, що намальовано на графіку
   (додається, лише якщо це ІНШИЙ TF);
3. **Volumized OB у бік угоди** — `bullish_obs`/`bearish_obs`, тобто за побудовою
   потрібного боку (bias перевіряти не треба); breaker пропускається;
4. **`autosl_fallback_pct` % від ВХОДУ** (`autosl_fallback_on`, деф. **ON, 2%**) —
   стоп буде ЗАВЖДИ. OFF повертає стару поведінку «краще без стопа».
Кожен кандидат перевіряється на БЕЗПЕЧНИЙ бік від поточної ціни (інакше наступний
тік монітора закрив би угоду). Причини пропуску ВСІХ джерел пишуться в той самий
рядок логу — видно, чому взято саме це джерело.

**`autosl_max_pct`** (деф. **0 = вимкнено**) — стеля відстані у % від входу:
надто далекий блок ПІДТЯГУЄТЬСЯ до стелі (сам вибір блоку не змінюється). Причина:
на проді спостережено розкид ризику **ALGO 1.40% … WIF 15.56%** при однаковому
розмірі позиції.

⚠️ Bias-правило: LONG → потрібен **BULLISH** блок (беремо `bar_low`), SHORT →
**BEARISH** (беремо `bar_high`). Протилежний блок лежить не з того боку ціни.
⚠️ «SET ONCE» і «не чіпати SL, введений руками» — збережено без змін.
UI: тумблер `ff-autosl-fallback` + `ff-autosl-fallback-pct` + `ff-autosl-max-pct`
у гармошці **🛡 Авто-SL та ворота відкриття** (див. розділ нижче).
Тест: `test_autosl_chain.py` (14).

## 🐞 UnboundLocalError `exh` у `_open` — КОЖНЕ відкриття Черги-4 падало

**Симптом (зі скріна):** ✋ ручне відкриття з Черги-4 → «Не відкрито: Помилка:
cannot access local variable 'exh' where it is not associated with a value».

**Корінь.** У `_open` змінна `exh` присвоювалась ЛИШЕ всередині
`if not skip_safeguard:`, а читалась у ПІДСУМКОВОМУ рядку беззастережно.
**Обидва** шляхи Черги-4 (авто-двигун і ручне відкриття) передають
`skip_safeguard=True` — тобто падало ЗАВЖДИ.

⚠️ **Найгірше — де саме падало.** На ОСТАННЬОМУ рядку `_open`, коли угода вже
відкрита і записана в `_fuel_managed`. Двигун ловив виняток у `except` (лише
`print` у stdout) і МОВЧКИ пропускав усе, що йде після відкриття:
- `self._timers[sym]` — таймер не стартував;
- `note_signal_consumed` — **сигнал не вигорав**, тобто захист «запис
  відпрацював» не вмикався;
- `_mark_ob_epoch_opened` — позначка «1 угода на 1H-OB» не ставилась;
- `_q4_set_vob_sl` — **стоп ставила інша, незалежна гілка** (`trade_manager.
  _auto_ob_manual_sl`). Саме тому в лозі стояло `autosl · TM`, а не
  `Черга-4: SL з …`, і налаштування `queue4_sl_source` фактично не діяло.

**Фікс:** `exh = None` ПЕРЕД умовою. Плюс `except` у двигуні тепер пише в
🧾 Лог (`Черга-4 ⚠️ збій після відкриття: … — перевірте позицію`), а не лише
в консоль: збій після відкриття не має бути невидимим.
Тести-замки (AST-перевірка порядку запис/читання): `test_q4_controls.py`.

## 📨 ЧАСТКОВЕ ЗАКРИТТЯ — у ГРУПОВИЙ ТОПІК і в ОДИН РЯДОК

`_partial_close` і `_partial_close_shadow` кликали `_notify` **без
`category`**, а без категорії повідомлення йде в ОСОБИСТИЙ бот адміна (він
зарезервований під службові). Тому TP-1 сипався в приват замість групи.
Тепер `category='trades'` в обох, і замість шести рядків — один:
`✂️ 🔴 #DOTUSDT −50% · $0.82470 · +1.30%` (paper — з міткою 🧪).
Рядки рівнів у `_tp_lines` втратили підписи **«(50%)»** і **«(повний)»**
(на прохання): лишились `🎯 TP-1: ціна (+x%)` / `🎯 TP-2: ціна (+y%)`.
Тести: `test_autosl_chain.py`.

## ✏️ РУЧНИЙ Manual SL — АВТОПІЛОТ НЕ ЧІПАЄ (кейс SOLUSDT, не зламати!)

**Скарга: «чому я не можу вручну виставити Manual SL?»** Лог 01.09 показав, що
рівень зберігався — і його щоразу затирали через ~20 секунд:

```
16:58:14 ✏️ Користувач: Manual SL → $96.82
16:58:38 🤖 Бот (Автопілот · структура): Manual SL → $99.9094
16:59:09 ✏️ Користувач: Manual SL → $96.82
16:59:31 🤖 Бот (Автопілот · структура): Manual SL → $99.9094
```

**Корінь:** `_pilot_tick` передає в `plan()` ручний рівень просто як
`prev_stop`. Ратчет `better_stop` бачить структурний стоп «кращим» (для LONG
вище = ближче до прибутку) → `act='trail'` → `update_manual_sl_tp` перезаписує.
Автопілот не знав, що рівень поставила ЛЮДИНА. Для Manual TP правило «оператора
не чіпаємо» вже діяло (`_pilot_apply_tp`), для SL — ні. Це була неузгодженість.

**Фікс:** перед застосуванням трейлу перевіряємо `manual_sl_src == SRC_USER` →
трейл НЕ застосовується, у лог іде ОДИН рядок (`_pilot_sl_user_lock`, інакше
монітор писав би його щотіку) з підказкою, як повернути керування.
⚠️ Замок НЕ вічний: **очистити поле SL** → `_pilot_sl_user_lock` знімається і
автопілот знову веде стоп сам. Нове ручне значення теж скидає позначку, щоб
пояснення зʼявилось у лозі й для нього.
Тести: `test_autosl_chain.py` (розділ «РУЧНИЙ SL»).

## 🏷 Походження Manual SL/TP: 🤖 бот ↔ ✏️ користувач (не зламати!)

Раніше два різні за природою рівні виглядали ОДНАКОВО, а `update_manual_sl_tp`
ЗАВЖДИ логував «Користувач: Manual SL → …» — навіть коли рівень поставив двигун.

- **`update_manual_sl_tp(..., origin='user', origin_label=None)`.** Дефолт `'user'`
  → усі наявні виклики (UI-ендпоінт `/api/tm/positions/manual-sltp`) поводяться як
  раніше. Бот-виклики передають `origin='auto'` + людський підпис:
  Черга-4 (`Черга-4 · <джерело>`), Q3-VOB (`відкриття`/`трейл`), POC-сетап.
- Пишеться на позицію: **`manual_sl_src`/`manual_tp_src`** (`'auto'`|`'user'`) +
  **`manual_sl_by`/`manual_tp_by`** (підпис). SL і TP — НЕЗАЛЕЖНО. Зняття рівня
  (`0`) прибирає й позначку. Відхилений рівень позначки НЕ лишає (нічого не мутує).
- `trade_manager._auto_ob_manual_sl` пише позначку НАПРЯМУ (`SRC_AUTO`,
  `Авто-SL · <джерело>`) — він мутує `pos` без `update_manual_sl_tp`.
- **🧾 Лог:** `🤖 Бот (<label>): Manual SL → X` (source `TM`) або
  `✏️ Користувач: …` (source `user`). В `extra` іде `origin`.
- **🎨 UI (`_sltpSrcStyle`)**: поле Manual SL/TP фарбується — **блакитний
  `#38bdf8` = виставив БОТ** (той самий колір, що бейдж «Авто-SL з OB»),
  **бурштиновий `#fbbf24` = змінено ВРУЧНУ**, сірий = порожньо. Рівень без
  позначки (стара угода) — нейтральний сірий, НЕ вигадуємо походження.
  Легенда — у `<th>` обох таблиць. Тултип несе підпис джерела.
- ⚠️ Інфосайт (`infosite/app.js`, `sltpCell`) кольори НЕ дзеркалить — окрема сесія.
Тест: `test_autosl_chain.py` (розділ «ПОХОДЖЕННЯ Manual SL/TP»).

## 🏦 Watchlist: сортування за капіталізацією

- **`GET /api/smc/mcap-ranks`** → `{ok, available, ranks:{SYMBOL:rank}, total, ranked}`.
  ОКРЕМИЙ ендпоінт, а НЕ поле в `/api/smc/state`: state опитується що 10с, а ранги
  міняються раз на добу. Джерело — `coingecko_client.get_rank()` (той самий, що
  живить `max_market_cap_rank` радара), TTL кешу — всередині клієнта.
- Фронт: кнопка `sm-wl-mcap-btn` («🏦 Cap») поряд із «⇅ A-Z». Режим ОДИН на список —
  `wlSortMode` = `'none'|'alpha'|'mcap'` у `localStorage['wlSortMode']` (є міграція
  зі старого `wlSortAlpha`); повторний клік по активній кнопці = скинути сортування.
  `wlSortAlpha` лишився як булеве дзеркало для сумісності.
- Ранги тягнуться ЛИШЕ коли режим увімкнено, не частіше ніж раз на 30 хв
  (`_wlEnsureMcapRanks`). Монета без рангу (немає в топ-250 CoinGecko) іде в
  КІНЕЦЬ (серед таких — за алфавітом), а не вигадує собі місце. У режимі `mcap`
  у рядку показується `#<ранг>` (або `#—`); в інших режимах — нічого (без шуму).
- Серверний `wlSymbols` НЕ змінюється — сортується лише копія в `renderWatchlist`.

## 🎯 Tickr: режим «Як у Watchlist» (той самий принцип відбору)

**Питання користувача:** за яким принципом підібрано watchlist і чи можна так
само підбирати монети з УСІЄЇ біржі. Аудит поточних 33 монет (ETH, XRP, BNB, SOL,
DOGE, TRX, LINK, AVAX, DOT, HYPE, LTC, SUI, NEAR, APT, ONDO, UNI, MNT, TAO, ENA,
ALGO, INJ, LDO, HBAR, OP, WIF, 1000PEPE, ORDI, NEO, STRK, ZEC, BTC…) показав: їх
об'єднує **НЕ сектор** (там і L1, і L2, і DeFi, і меми, і приватність), а три речі:
1. **USDT-ПЕРПЕТУАЛ** — спот бот не торгує, USDC-перп дублює ту саму монету;
2. **топ-150 за КАПІТАЛІЗАЦІЄЮ** (CoinGecko);
3. **реальна ЛІКВІДНІСТЬ** — обіг 24h та відкритий інтерес, щоб SMC-структура
   (OB/CHoCH) була чистою, а не намальованою шумом.
Плюс чого там НЕМАЄ: стейблкоїнів і обгорток (WBTC, stETH, PAXG) — власної
структури в них немає.

**Реалізація** — новий `sort_by='watchlist_like'` у `tickr_core.top_active`
(опція «🎯 Як у Watchlist (кап + ліквідність)» у випадайці на `/tickr`):
- пороги в `WL_LIKE` = {`mcap_universe`:150, `min_vol_usd`:50M, `min_oi_usd`:10M};
- виключення — СПИСКОМ `WL_LIKE_EXCLUDE` (стейбли/обгортки/золото), а НЕ
  евристикою «USD у назві»: інакше вилетіли б живі USUAL/USELESS;
- поле «VOL 24H ≥» ПЕРЕКРИВАЄ `min_vol_usd`; порожнє поле → дефолт режиму
  (інакше «як у Watchlist» пропускало б неліквід);
- сортування за РАНГОМ капіталізації (ключ `watchlist_like` = `-rank`, бо
  спільний `sort` спадний);
- у `warnings` іде РОЗКЛАД відсіву (не USDT-перп / поза топ-N / стейбл-обгортка /
  мілкий OI → скільки лишилось) — «чому мало монет» не має бути здогадкою;
- немає CoinGecko → явне попередження, а не мовчки порожня таблиця.
UI: `tkOnSortChange` ставить ТОП-N=100 (це ВІДБІР універсуму, а не «топ-20
найактивніших») і показує пояснювальний блок `#tk-sort-hint`.
Тест: `test_tickr_watchlist_like.py` (12).

## 🧬 «НОВА СИТУАЦІЯ ДЛЯ НОВОЇ УГОДИ» — кінець «угодам з повітря» (не зламати!)

**Кейс APTUSDT (26.08):** угоду закрито ВРУЧНУ о 03:03 → о 03:10:53 бот відкрив її
знову. У 🧾 Лозі — лише «Відкрито paper · Volumized OB → Черга-4», без жодного
контексту, звідки це взялось.

**Корінь — не окремий шлях, а ВІДСУТНІСТЬ вимоги нової підстави:**
1. закриття угоди не лишало ЖОДНОЇ позначки — для логіки входу монета одразу
   ставала «чистою», ніби угоди й не було;
2. запис, що вже лежав у черзі, відкривався наступним тіком двигуна незалежно
   від того, КОЛИ туди потрапив (а з ♾ `queue4_no_ttl` він живе ВІЧНО, тож ефект
   став постійним);
3. рядок «Відкрито» не ніс походження — зв'язок доводилось шукати очима по логу.

**Правило (`require_new_situation`, дефолт УВІМК).** Наступна угода по монеті
відкривається ЛИШЕ на сигналі, що виник ПІСЛЯ закриття попередньої.
- `note_trade_closed(sym)` — TM кличе на КОЖНЕ закриття (`_close_position` і
  `_close_shadow`); стамп у `_last_trade_end[sym]`, ПЕРСИСТЕНТНО.
- `_is_new_situation(sym, signal_at, s)` — ЧИСТА функція: `signal_at <= last_end`
  → відмова. Плюс окремий `open_max_signal_age_min` (0 = вимк).
- Гейт стоїть у **`_open`** — ЄДИНОМУ вузлі всіх автоматичних відкриттів. Кожен
  двигун передає `signal_at`: Q4/Q3/Q2/EXH → `info['added_at']` (час постановки
  в чергу = час сигналу).
- ⚠️ `by_hand=True` (✋ ручний сигнал, ✋ відкриття з Черги-4, `force_open_timer`)
  правило ОБХОДИТЬ — людина перекриває власне рішення.
- ⚠️ Немає `signal_at` або немає попереднього закриття → НЕ блокуємо (відмову не
  вигадуємо).

**✋ Ручне закриття** (`note_manual_close`) додатково **ЧИСТИТЬ УСІ ЧЕРГИ**
(`_pending`..`_pending4` + кеш шарів Q4). Це працює ЗАВЖДИ, незалежно від
налаштувань: запис, що лишився в черзі, відкрив би угоду наступним тіком і без
жодного сигналу. Опційний `manual_close_lock_min` (дефолт **0 = вимкнено**)
блокує авто-вхід на N хв; `clear_manual_close_block(sym)` знімає достроково.

**🧬 Походження в рядку відкриття** — `_origin_trace(sym, info, lay, now)`:
`сигнал: 🟪 Volumized OB · у черзі з 25.08 22:30 UTC (чекала 3г 12хв) · ціна на
вході в чергу X · шари 4/4 [розклад] · такт 1H-OB DD.MM HH:MM`. Тепер відповідь
на «звідки це взялось» стоїть У САМОМУ рядку, а не за кілька екранів угору.
`_fmt_wait` — читабельна тривалість (`45с` / `47хв` / `3г 12хв`).

UI: `ff-require-new-situation`, `ff-open-max-signal-age`, `ff-manual-close-lock`
у гармошці **🛡 Авто-SL та ворота відкриття** (див. розділ нижче).
Тест: `test_new_situation.py` (17).

## 🐞 Tickr «Як у Watchlist»: два виправлені дефекти

1. **BINANCE віддавав «залишилось 0».** `_activity_binance` для swap НЕ заповнює
   `oi_usd` (у Binance Futures немає bulk-ендпоінта OI — лише по-символьний, це
   ~600 HTTP). Гейт `min_oi_usd` викошував УСІХ, хто пройшов решту перевірок:
   «мілкий OI 37 → залишилось 0». **Фікс:** `_oi_available` — якщо в наборі немає
   ЖОДНОГО ненульового OI, гейт по OI ВИМИКАЄТЬСЯ, і в статусі прямо написано
   «OI — BINANCE не віддає його пачкою, фільтр по OI вимкнено». На Bybit OI є
   (bulk-тікери) → гейт працює повністю.
2. **`1000PEPE` вилітала як «поза топ-N».** Біржі лістять помножені контракти
   (`1000PEPE`, `1000BONK`, `1MBABYDOGE`) — у CoinGecko це `PEPE`/`BONK`/`BABYDOGE`.
   **Фікс:** `wl_base_variants(base)` — спершу шукаємо як є, потім без множника
   (`1000000/100000/10000/1000/1M/1K`). 1000PEPE РЕАЛЬНО є в робочому watchlist,
   тож без цього режим не відтворював би сам себе.

## 🚪 ПРАВИЛА ВИХОДУ: Opposite OB = ПОДІЯ · Forecast 1H/4H · Decision

### 🧱 Opposite OB Exit — лише НОВИЙ блок (виправлено!)

**Було:** `on_main_ob_update` перевіряв ЛИШЕ біас поточного рядка
`sob_smc_ob_state` на `opposite_ob_exit_timeframe`. Це СТАН, а не подія:
протилежний OB міг висіти на графіку ще ДО відкриття угоди — і найближчий скан
одразу її закривав. **Кейс MNTUSDT (25.08):** о 23:28:50 авто-SL писав «OB на 15M
протилежний (BULLISH)» (тобто блок УЖЕ був), а о 23:41 це ж правило закрило шорт
із підписом «Ціна вдарилась у протилежний Order Block» — при тому, що **код
ціну взагалі не перевіряв**. Підпис теж брехав.

**Стало:** закриваємо, коли протилежний OB **З'ЯВИВСЯ**:
- `_opp_ob_baseline(sym, pos, tf, row)` фіксує `bar_time` блоку, що стояв на
  момент ВІДКРИТТЯ угоди (`self._opp_ob_base[sym]`, ключ =
  `side|opened_at|exit_tf` → нова угода чи зміна TF перебазовують автоматично);
- перший прохід ЛИШЕ фіксує базу і НІЧОГО не закриває;
- далі закриття лише коли `bar_time` **НОВІШИЙ** за базу І біас протилежний;
- новий блок У НАШ бік **зсуває базу** (щоб наступний протилежний рахувався від
  нього), старіший блок (поточний став breaker) — не подія;
- у 🧾 Лог іде рядок із біасом, тегом і часом утворення блоку;
- підпис причини виправлено: «З'явився НОВИЙ протилежний Order Block
  (новіший за той, що був на вході)».
⚠️ База чиститься в `_close_position`/`_close_shadow` разом із `_signal_exit_at`.

### 🔮 Forecast 1H / 🔮 Forecast 4H / 🧠 Decision — САМОСТІЙНІ виходи

Три НЕЗАЛЕЖНІ правила, кожне зі своїм тумблером, **усі дефолт OFF**:
`use_forecast_1h_exit`, `use_forecast_4h_exit`, `use_decision_exit`.
Протилежний вердикт → закриваємо ОДРАЗУ, без підтвердження CHoCH.
⚠️ НЕ плутати з наявним `use_forecast_1h_close` — то «конфлюенс», йому потрібен
ще й розворот LTF (лишився без змін, дефолт ON).

- `_signal_exit_reason(sym, pos)` → `(reason_code, людський опис)` або None.
  Прогноз читається з `_get_forecast_both` (`f1_side`/`f4_side`, 1=LONG/−1=SHORT),
  Decision — з `compute_decision(...)['recommended']`.
- **Нейтраль / «немає даних» НЕ закривають** — інакше кожна пауза в прогнозі
  вибивала б з ринку. Закриває ЛИШЕ явний зустрічний бік.
- `_check_signal_exits(...)` кличеться з ОБОХ моніторів (`_monitor_position` і
  `_monitor_shadow_position`) ПЕРЕД перевіркою Manual SL/TP — якщо вердикт
  розвернувся, немає сенсу чекати, поки ціна дійде до стопа.
- **Тротл `SIGNAL_EXIT_TTL=20с` на монету** (`_signal_exit_at`): монітор тікає
  раз на 4с, а Decision важкий. Коли всі три тумблери OFF — не читається НІЧОГО.
- Причини: `forecast_1h_exit` / `forecast_4h_exit` / `decision_exit`; підписи
  додано і в `_close_detail`, і в бейдж-мапу TM, і в JS-мапу `smart_money.html`.

UI: тумблери `tm-use-forecast-1h-exit`, `tm-use-forecast-4h-exit`,
`tm-use-decision-exit` у блоці Exit Rules, одразу під Opposite OB Exit.
Тест: `test_exit_rules.py` (17).

## 🧟 Мертвий запис у черзі (кейс LDOUSDT) — гейт мусить ВИСЕЛЯТИ, не ретраїти

**Що показав лог (26.08).** Ланцюг LDOUSDT:
- `25.08 23:47:48` сигнал VOB #3 SHORT (усі фільтри ✓) → `23:47:49` у Чергу-4;
- `26.08 08:27:12` відкрито paper — **чекала 8г 39хв** у черзі;
- `26.08 10:29:34` закрито (`bot_tp_trail`, +1.15%);
- `26.08 11:21…11:31` — **30 однакових рядків** кожні ~22с:
  «🧬 сигнал від 25.08 23:47:48 UTC СТАРІШИЙ за закриття попередньої угоди».

**Корінь (моя недоробка попередньої правки).** Правило «нова ситуація» я поставив
у `_open`, і воно чесно ВІДМОВЛЯЛО — але **запис лишався в черзі**. Умова
`signal_at <= last_trade_end` **НЕЗМІННА** (обидві дати зафіксовані), тож запис не
міг стати валідним НІКОЛИ. З ♾ `queue4_no_ttl` його не виселяв ні TTL, ні застій
→ зомбі висів 11.5 год і флудив лог.

**Правило: гейт, який НІКОЛИ не пропустить, мусить ВИСЕЛЯТИ запис, а не ретраїти.**
1. **`note_trade_closed(sym)`** тепер не лише ставить риску часу, а й ОДРАЗУ
   прибирає з УСІХ черг записи з `added_at <= час закриття` (+ `_q4_cache_drop`,
   `_q4_lit_at`). Записи, новіші за закриття, лишаються. У лог — ОДИН рядок із
   переліком, що саме прибрано і скільки воно чекало.
   ⚠️ Під час відкритої угоди `intercept` у черги не додає, тож на практиці
   прибирається все, що лишилось від попереднього циклу.
2. **Запобіжник у самих двигунах** (Q4/Q3/Q2) — перед відкриттям
   `_is_new_situation(...)`; не пройшло → `pop` із черги + лог. Це рятує, коли
   `note_trade_closed` не спрацював (рестарт, стан із БД). ♾ «Без терміну» цей
   вихід НЕ вимикає — він не про час, а про відпрацьовану підставу.
3. **Анти-флуд у `_open`:** `_new_situation_logged = (symbol, reason)` — незмінна
   причина пишеться ОДИН раз, а не щотіку.

Тест: `test_new_situation.py` (22) — розділ «🧟 МЕРТВИЙ ЗАПИС У ЧЕРЗІ».

## 🔥 ВИГОРІЛИЙ СИГНАЛ + 🚨 ЗАСТРЯГ: запис із черги НЕ повертається в середину

**Правило користувача дослівно:** «Якщо запис вийшов із Черги-4 — монета пішла
далі по алгоритму. Потрапити сюди вона має ЛИШЕ відпрацювавши повністю НОВИЙ
алгоритм (новий сигнал + усі фільтри), а не зʼявитись невідомо звідки й
продовжити працювати ПОСЕРЕД алгоритму. З черги монета видаляється ПОВНІСТЮ.»
Плюс: «Якщо монета застрягає у Черзі-4 — її також потрібно повністю викинути.»

**🔥 `note_signal_consumed(sym, added_at)`** — кличеться з КОЖНОГО успішного
відкриття з черги (Q4 / Q3 / Q2 / EXH / ✋ ручне з Черги-4):
- `_consumed_signal_at[sym] = added_at` (ПЕРСИСТЕНТНО) — сигнал ВИГОРІВ;
- монета прибирається з **УСІХ** черг (`_pending`..`_pending4`) + `_q4_cache_drop`
  + `_q4_lit_at`, а не лише з тієї, що відкрила.
`_is_new_situation` тепер відхиляє БУДЬ-ЯКИЙ запис із `added_at <= _consumed_signal_at`
— **навіть поки угода ще ВІДКРИТА** (не чекаємо закриття). Повернення можливе
виключно через `intercept` з НОВИМ `added_at`, тобто новий сигнал сканера, що
пройшов усі фільтри.

**🧬 `_purge_spent_queue_records()` на СТАРТІ** (`start()` до першого тіку).
Стан приїжджає з БД, і серед відновлених записів міг бути відпрацьований — саме
так «зомбі» повертався в чергу й крутився в середині алгоритму. Прибираємо все,
де `added_at <= consumed_signal_at` або `<= last_trade_end`.

**🚨 `queue4_hard_max_hours` (деф. 24)** — абсолютна межа життя запису, яку
♾ `queue4_no_ttl` **НЕ вимикає**. ♾ означає «живе, доки не замінить протилежний
сигнал», а не «живе вічно за будь-яких умов». Чиста функція `_q4_stuck(added_at,
now, hard_h)`; при спрацюванні монета вилітає з **УСІХ** черг із рядком у лозі
(скільки провисіла, яка межа). 0 = вимкнути запобіжник (не рекомендовано).
UI: поле `ff-queue4-hardmax` поряд із ♾.

Тести: `test_new_situation.py` (28) — розділ «ВИГОРІЛИЙ СИГНАЛ + ЗАСТРЯГ».

### 🔗 Режим комбінування трьох виходів (`signal_exit_mode`)

Forecast 1H / Forecast 4H / Decision можна поєднати:
- **`'or'` (ДЕФОЛТ)** — «кожен окремо»: спрацювало БУДЬ-ЯКЕ увімкнене правило →
  вихід. Це поведінка, яка була до появи режиму (нічого не змінилось).
- **`'and'`** — «усі разом»: вихід ЛИШЕ коли ВСІ УВІМКНЕНІ правила одночасно
  проти позиції.

⚠️ Ключові домовленості:
- **Нейтраль / «немає даних» — це НЕ «проти»**, тож у режимі `and` вона ЛАМАЄ
  збіг і угода тримається (у `or` вона просто не спрацьовує).
- **Вимкнене правило у збігу НЕ бере участі** — інакше `and` не зібрався б ніколи.
- **Одне увімкнене правило** → `and` тотожний `or`, і причина закриття лишається
  ВЛАСНА (`forecast_1h_exit` тощо), а не збіднюється до загального коду.
- Збіг кількох дає окрему причину **`signal_exit_and`** («🔗 AND: усі увімкнені
  вердикти проти позиції»); у лозі перелічені ВСІ вердикти з відсотками:
  `AND: Forecast 1H LONG (70%) + Forecast 4H LONG (65%) + Decision LONG (80%) — усі проти SHORT`.
- Некоректне значення налаштування → дефолт `'or'` (не збій).

UI: випадайка `tm-signal-exit-mode` («кожен окремо (АБО)» / «усі разом (AND)»)
одразу під трьома тумблерами в блоці Exit Rules.

### 🎯 «ЧІТКО ВИЗНАЧЕНЕ» значення — інакше ЧЕКАЄМО (вимога користувача)

Правило дослівно: «Значення має бути чітко визначене LONG/SHORT, тобто чітке
розуміння; а якщо є нейтральне — чекаємо, поки не визначиться чітко.»

Кожен вердикт (Forecast 1H / 4H / Decision) проходить `_verdict(...)` і дає
**три** стани, а не два: `(проти?, ВИЗНАЧЕНО?, опис)`.
- **`визначено = False`** → правило НЕ спрацьовує **в жодному режимі**:
  • нейтраль (`side=0`, `recommended='NEUTRAL'`);
  • «немає даних» (порожній прогноз / Decision не порахувався);
  • бік є, але **впевненість < `signal_exit_min_conf`** → «ще не чітко».
- У режимі **`and`** невизначений вердикт ЛАМАЄ збіг (перевірка `all(det)` стоїть
  ПЕРЕД `all(hit)`), у **`or`** — просто пропускається. В обох випадках угода
  ТРИМАЄТЬСЯ і ми чекаємо, поки вердикт стане чітким.

**`signal_exit_min_conf`** (%, дефолт **0 = вимкнено**) робить «чіткість»
вимірюваною. При 0 будь-який явний LONG/SHORT вважається чітким (стара
поведінка); нейтраль і «немає даних» НЕ спрацьовують незалежно від порога.
⚠️ Поріг заданий, а відсотка у вердикті немає → підтвердити чіткість нічим →
вважаємо НЕвизначеним (не закриваємо).

UI: поле `tm-signal-exit-min-conf` («🎯 Чіткий вердикт ≥ N %») під випадайкою
режиму. Тести: `test_exit_rules.py` (30).

## 🎯 ДЖЕРЕЛО Manual SL = НАЛАШТУВАННЯ УГОДИ (не «як склалось»)

**Скарга:** у Черзі-4 стоїть «🛑 SL з 1H OB», а в 🧾 Лозі — «SL встановлено з
OB 15M». **Корінь:** у проєкті ДВА незалежні авто-SL, і кожен мав СВІЙ TF:
- `fuel_filter._q4_set_vob_sl` → `queue4_sl_source` ('1h' / '15m');
- `trade_manager._auto_ob_manual_sl` → власний `q2_auto_ob_sl_tf` (деф. **15m**).
TM нічого не знав про вибір користувача в Черзі-4. Коли SL ставив ВІН (бо Q4
свого не поставив), виходив 15m — попри налаштування.

**Фікс:** `_auto_ob_manual_sl` будує ланцюг ЗА ПОХОДЖЕННЯМ УГОДИ. Якщо
`opened_by` містить `Q4` — ПЕРШИМ іде саме те, що обрано в Черзі-4:
- `'1h'`  → ★OB на `ob_filter_timeframe` (мітка «Черга-4: 1H OB»);
- `'15m'` → `_from_volumized('15m')` — саме 15m, а НЕ сканерний
  `volumized_timeframe` (у користувача він 5m).
Далі — звичайний фолбек (`q2_auto_ob_sl_tf` → ★TF → Volumized → % від входу), і
в лозі видно, ЧОМУ обране джерело не спрацювало. Угоди НЕ з Черги-4 поведінку
не змінили. Тест: `test_autosl_chain.py` (розділ «ДЖЕРЕЛО SL…»).

## 🎯 АВТОПІЛОТ УГОДИ (`detection/trade_pilot.py`) — супровід по ГРАФІКУ

Вимога: «максимально вигідно тримати угоду і чітко вийти при максимальному
результаті». Не таймер і не фіксований %, а РЕАЛЬНІ обʼєкти графіка, які бот уже
рахує. Модуль — **ЧИСТІ функції без I/O**; контекст збирає TM.

**🎯 Ціль** (`collect_targets` → `pick_objective`) — усе ПОПЕРЕДУ руху:
пули ліквідації (`runway.next`/`runway.main` з liq-map) · екстремум дилінг-
діапазону (той самий `_swing_hl_for_tf`, що дає лінії Strong/Weak) · POC.
Відсів: ближче за `min_target_dist_pct` (шум) і далі за `max_target_dist_pct`.
Головна ціль = **НАЙДАЛЬШИЙ** змістовний обʼєкт (це і є «максимальний
результат»); найближчий лишається як `next_obstacle`.
⚠️ **ЦІЛЬ ФІКСУЄТЬСЯ ОДИН РАЗ** (`objective_lock`, зберігається в
`pos['pilot_objective']`). Без замка вона зникала б зі списку «попереду» рівно
тоді, коли ціна до неї дійшла, найдальшою ставала наступна — і фіксація не
наставала б НІКОЛИ (рухомі ворота). Тест: `test_objective_does_not_drift…`.

**🛡 Стоп** (`structure_stop`) — за ПРОТИЛЕЖНИМ екстремумом діапазону + буфер:
LONG під swing-low, SHORT над swing-high. Рівень з неправильного боку від ціни
(структура вже зламана) НЕ повертається. `better_stop` — **ратчет**: стоп
рухається ЛИШЕ в бік прибутку. Після проходження `be_at_r` × ризик стоп не
гірший за беззбиток (`be_lock_pct` лишає невеликий плюс).

**Рішення** (`plan`) → `hold` / `trail` / `take`, і `reasons` ЛЮДСЬКОЮ мовою.
`take` → закриття з причиною `pilot_target` («🎯 Автопілот: ціль на графіку
досягнута»). `trail` → `update_manual_sl_tp(origin='auto', 'Автопілот · структура')`.
`hold` у лог НЕ пишеться (це стан) — лежить у `_pilot_state`, віддається
`get_pilot_state(sym)`.

**Інтеграція:** `_pilot_tick` кличеться з ОБОХ моніторів після `_check_signal_exits`;
тротл `PILOT_TTL=20с` на монету; при `pilot_enabled=False` (ДЕФОЛТ) не читається
НІЧОГО. Контекст (`_pilot_context`) — ЄДИНЕ місце з I/O.
⚠️ `runway` береться, лише якщо його напрямок ЗБІГАЄТЬСЯ з боком угоди — інакше
«ціль» вказувала б у протилежний бік.
UI: тумблер `tm-pilot-enabled` + вікно цілей + буфер стопа/беззбиток у Exit Rules.
Тест: `test_trade_pilot.py` (17).

### 📊 Візуалізація автопілота в таблицях відкритих угод

Колонка **«🎯 Автопілот»** (real + paper, після Manual TP) показує, що
відбувається з позицією ПРОТЯГОМ усієї угоди, а не лише в момент дії:
- **смужка прогресу** — скільки шляху ВІД ВХОДУ до цілі пройдено
  (`trade_pilot.progress`: `pct` обрізаний у [0..100] щоб не «вилітав», але
  `done_pct` лишається СИРИЙ — видно й мінусовий рух);
- **ціль** зі значком типу: 💧 найближчий пул · 🌊 головний пул · 🏁 екстремум
  діапазону · ⚪ POC + скільки ще лишилось у %;
- **найближча перепона** окремим рядком (коли вона не збігається з ціллю);
- **остання дія**: ⏳ тримаємо · 🛡 стоп підтягнуто (×N разів за угоду) ·
  🎯 фіксація; у тултипі — повний розклад причин і таймфрейми.

⚠️ **Стан пишеться на КОЖНОМУ такті автопілота** (`_pilot_state`), не лише на
`hold` — інакше колонка «замерзала» б між діями. `/api/tm/state` лише ЧИТАЄ
готовий знімок; жодних розрахунків на цьому гарячому ендпоінті (той самий урок,
що з прибраною колонкою МММ). Коли `pilot_enabled=False` — поле `pilot` навіть
не додається в payload.

**Таймфрейми автопілота** (тепер керовані з UI):
- **структура (swing high/low)** — `pilot_swing_tf`; порожньо = «як у графіка»,
  тобто `swing_hl_timeframe` сканера (деф. **1H**). Це джерело і структурного
  стопа, і однієї з цілей — тож рівні збігаються з лініями Strong/Weak на екрані;
- **пули ліквідації** — liq-map, вікно **24 год** (не свічковий TF);
- **POC** — `pilot_poc_hours`, деф. **72 год**;
- частота рішень — монітор 4с із тротлом `PILOT_TTL=20с` на монету.

## 🧬 ПОХОДЖЕННЯ — В ОДНОМУ РЯДКУ З ВІДКРИТТЯМ (кейс OPUSDT)

**Що показав експорт логу (27.08).** Черга-4 писала ланцюг походження ОКРЕМИМ
записом (`log_activity(..., source='Q4')`) — і в експорті **не було ЖОДНОГО
рядка `opened,Q4`**: `awk '$8=="opened"' → 15×FF, 1×TM, 0×Q4`. Лишалось лише
«Відкрито paper · 🟪 Volumized OB → 🎯 Черга-4 · …» без контексту.

**Правило: один рядок = одна подія.** Ланцюг тепер їде РАЗОМ із самим фактом
відкриття, тож загубитись не може:
- `_open(..., origin_trace=...)` → `tm.set_origin_trace(sym, text)` ПЕРЕД
  делегуванням;
- TM кладе його на позицію (`pos['origin_trace']`) у `_open_shadow` і в
  реальному шляху **ДО** запису в лог;
- рядок «Відкрито …» друкує `· 🧬 <ланцюг>`.
Окремий Q4-запис прибрано (інакше дублювався б). Флip-нотатку («ФЛІП: сигнал
був X, показники — Y») перенесено в сам ланцюг.

**Друга прогалина: пройдена 🔁 повторна перевірка була МОВЧАЗНОЮ.** По OPUSDT у
лозі стояло п'ять відмов «прогноз 1H проти», а потім тихе відкриття — не було
видно, з ЯКИМИ значеннями фільтри нарешті склались. Тепер `_q4_recheck_filters`
зберігає розклад успіху в `_q4_recheck_pass_detail`, і при відкритті пишеться
`Черга-4 🔁 повторна перевірка ПРОЙДЕНА · <розклад> — відкриваємо`
(лише якщо ДО того були відмови — щоб не плодити рядки на рівному місці).

## 🎯 TP-1 / TP-2 від автопілота + компактна колонка

**Колонка «🎯 Автопілот» — В ОДИН РЯДОК** (`pilotCellHTML`): значок дії ·
мінісмужка 34px · ціль зі значком типу · залишок % · `🛡N` (скільки разів
підтягнуто стоп). Усе інше — у тултипі, щоб таблиця лишалась щільною.

**АНАЛІТИКА ПОДІЛУ** (`trade_pilot.plan_targets`, ЧИСТА функція):
- **TP-2 = ГОЛОВНА ціль** (найдальший змістовний обʼєкт попереду) → повний вихід.
  Береться ЗАФІКСОВАНА ціль угоди (`objective_lock`), а не перераховується.
- **TP-1 = НАЙБЛИЖЧА змістовна перепона МІЖ входом і TP-2** → частковий вихід.
  Саме там рух статистично найчастіше сповільнюється (пул ліквідності / POC /
  межа діапазону), тож фіксація частини — це гроші в найімовірнішій точці
  реакції, а хвіст лишається «безкоштовним».

⚠️ **Жорсткі перевірки, без яких поділ безглуздий:**
1. обидва рівні СТРОГО попереду руху ВІД ВХОДУ (не від поточної ціни);
2. TP-1 суворо МІЖ входом і TP-2;
3. TP-1 віддалений від входу ≥ `tp_min_gap_pct` — інакше комісія зʼїсть
   частковий вихід;
4. TP-2 віддалений від TP-1 ≥ `tp_min_gap_pct` — два рівні впритул не мають сенсу;
5. немає придатної проміжної цілі → **TP-1 НЕ вигадуємо** (лише TP-2).
Кожен рівень несе `from_entry_pct` і **`r`** (кратність ризику від стопа) — саме
R показує, чи вартий рівень виходу. Без стопа `r=None` (не вигадуємо).

**Виконання.** `manual_tp1` — ОКРЕМЕ поле позиції (не плутати з `manual_tp`):
- `_check_manual_tp1` у ОБОХ моніторах стоїть **ПЕРЕД** перевіркою повного TP —
  інакше TP-2, що лежить далі, ніколи не дав би частковому спрацювати;
- спрацьовує **РІВНО ОДИН раз** (`tp1_done`), інакше кожен тік різав би позицію далі;
- закриває `pilot_tp1_close_pct`% (деф. 50) через наявні `_partial_close` /
  `_partial_close_shadow`;
- рівень лишається видимим у таблиці з ✓ — щоб було зрозуміло, ДЕ зафіксували.

**Автозаповнення** (`pilot_autofill_tp`, деф. **OFF**): рахується ОДИН раз на
угоду (`pilot_tp_set`), рівні прибиті до плану входу й не «пливуть» за ціною.
Рівень, уже введений ОПЕРАТОРОМ, автопілот НЕ чіпає. TP-2 йде через
`update_manual_sl_tp` (та сама валідація боку + позначка 🤖 бот).

**UI:** колонки `Manual TP-1` / `Manual TP-2` в обох таблицях (real 14 колонок,
paper 13); `submitManualTp1` — окремий сабміт, бо поле окреме. Налаштування —
тумблер `tm-pilot-autofill-tp` + «TP-1 закриває N%» + «мін. зазор %».
Тест: `test_trade_pilot.py` (30).

## 🎯 TP-1: ІНВЕНТАР ЦІЛЕЙ · ВІКНО ШЛЯХУ · СИЛА ОБʼЄКТА (не зламати!)

**Заміряно на проді (14 позицій).** TP-2 стояв у всіх, TP-1 — лише в 6.
Два різні дефекти:

**1. Інвентар був затонкий.** `collect_targets` знав ЛИШЕ 4 обʼєкти (2 пули
ліквідації + swing + POC), тож у 8 із 14 позицій між входом і ціллю не було
ЖОДНОГО рівня — частковій фіксації просто не було де стати. Додано ДВА
джерела, які НЕ коштують жодного зайвого запиту:
- **VAH/VAL** — `compute_poc` уже повертав їх разом із POC, ми просто не брали
  (`_pilot_context` тепер кладе `ctx['vah']/['val']`);
- **EQ — рівновага дилінг-діапазону** `(high+low)/2` з того самого
  `_swing_hl_for_tf`. Канонічний SMC-рівень: саме тут PD-зона перемикається
  між премією і дисконтом.

**2. Вибір «найближчий обʼєкт» був неправильний.** На FIL це дало TP-1 на
1.5% при цілі 13.4% — половина позиції знімалась через **11% шляху**
(DOT — 16%). Тепер TP-1 шукається у **ВІКНІ ЧАСТКИ ШЛЯХУ** до TP-2:
`tp1_min_path_pct`=**30%** … `tp1_max_path_pct`=**75%** (це % ШЛЯХУ, а не %
ціни!). Серед кандидатів виграє **НАЙСИЛЬНІШИЙ** обʼєкт за
`_TP1_WEIGHT` (liq_next 5 > liq_main 4 > poc/va 3 > eq 2 > swing 1), тайбрейк
— ближчий до середини вікна.

**3. Похідний рівень, коли обʼєкта у вікні немає** —
`tp1_fallback_path_pct`=**50%** шляху до цілі, `kind='path'`, підпис «50%
шляху до цілі». Це НЕ «рівень із повітря»: він похідний ВІД ВЛАСНОЇ ЦІЛІ
автопілота, і в лозі прямо сказано, що обʼєкта у вікні не знайшлось.
`0` = не ставити.
⚠️ Похідний рівень теж перевіряється на «строго перед TP-2» — без цього
`tp1_fallback_path_pct > 100` давав TP-1 ДАЛІ за TP-2 (упіймано тестом
`test_tp1_never_passes_tp2`).

UI: нові значки типів у `_PILOT_KIND` — `va: ◫`, `eq: ⚖`, `path: ➗`.
Тести: `test_trade_pilot.py` (45).

## 📨 TELEGRAM: TP-1 / TP-2 — КОРОТКО, з % біля ціни

`_tp_lines(pos)` — ЄДИНЕ джерело рядків рівнів для ОБОХ шляхів відкриття
(real і 🧪 paper), щоб формат не розʼїхався.

⚠️ **Немає даних — немає рядка.** На момент відкриття автопілот ще не рахував
рівні (він працює з монітора), і раніше там стояло «⏳ рахується». Користувач
попросив **не писати про TP нічого**, поки чисел немає: повідомлення про
відкриття має лишатись коротким. Тепер `_tp_lines` повертає ПОРОЖНІЙ рядок, і
блок TP просто не додається.

Щойно рівні реально стали — `_pilot_apply_tp` шле окреме коротке
`_notify_pilot_levels`: **заголовок + два рівні, і все**. Службового рядка
«вхід … · TP-1 (+1.50%)» НЕМАЄ (прибрано на прохання) — **відсоток стоїть
одразу після ціни кожного рівня**:
`🎯 TP-1 (50%): 0.69313 (+1.50%)` / `🎯 TP-2 (повний): 0.60960 (+13.37%)`.
Нема чого показувати → повідомлення не шлеться взагалі.
`is_shadow` передається явно → paper іде з міткою 🧪 у свій топік.
Тести: `test_autosl_chain.py` (розділ «TELEGRAM»).

## 🐞 АВТОПІЛОТ: СТАН НА КОЖНУ КНИГУ + ЛІЧИЛЬНИК = ФАКТ (кейс TRXUSDT)

Скрін paper-таблиці 03.09: `TRXUSDT LONG · вхід $0.32510 · SL 0.3205 · ціль
$0.34220`, а в колонці «🎯 Автопілот» — **114.38R** і **🛡432**. Обидва числа
неправдиві, і це ДВА РІЗНІ дефекти.

**1. R = 114.38 арифметично неможливий.** Вручну: `(0.34220−0.32510) /
(0.32510−0.32050)` = **3.72R**. Для 114.38R стоп мусив би стояти за 0.046% від
входу. Причина: **`_pilot_state` і `_pilot_at` були ключовані ЛИШЕ символом**, а
по монеті можуть одночасно стояти РЕАЛЬНА і ПАПЕРОВА позиції з різними входом,
стопом і ціллю. Наслідки (усі три реальні):
- `/api/tm/state` підмішував ОДИН знімок в ОБИДВІ таблиці → у paper-рядку
  світилось R реальної угоди (там стоп стояв майже в беззбитку);
- спільний троттл `_pilot_at` глушив пілот другої книги на цілий `PILOT_TTL` —
  одна з позицій фактично лишалась **без супроводу**, і в UI це не видно;
- закриття реальної позиції стирало стан паперової.

**Фікс:** `_pilot_key(symbol, is_shadow)` → `SYMBOL|R` / `SYMBOL|S` — ЄДИНЕ
джерело ключа для `_pilot_state`, `_pilot_at` і обох `pop` при закритті.
`get_pilot_state(symbol, is_shadow=False)`; `/api/tm/state` ітерує два списки
ОКРЕМО і передає книгу. Знімок несе `is_shadow`.
⚠️ Дефолт `is_shadow=False` лишено лише щоб старі виклики не падали — він дає
саме РЕАЛЬНУ книгу, а не «якусь».

**2. 🛡432 — лічильник рахував НАМІРИ.** Приріст стояв у блоці запису стану
(`trails += 1 if act == 'trail'`), який виконується **ДО** самої спроби трейлу.
А трейл нижче міг не застосуватись: операторський замок (`manual_sl_src ==
SRC_USER`) або відмова `update_manual_sl_tp`. На скріні SL бурштиновий = веде
оператор → стоп не зрушив ЖОДНОГО разу, а за 2.4 год (такт 20с) намоталось 432,
і тултип писав «Стоп підтягнуто 432 раз(и)». Іконка 🛡 «стоп підтягнуто» теж
світилась щотакту.

**Фікс:** знімок пишеться з `trails` = попереднє значення і `trail_block=''`;
приріст робить САМ трейл через `_pilot_mark(pkey, ...)` — **лише на
`r.get('ok')`**. Замок і відмова пишуть у `trail_block` причину.
UI: `_PILOT_BLOCKED` = **🔒 бурштиновий** перекриває значок дії, коли
`trail_block` не порожній (той самий принцип, що ⚖️ БЗ перекриває «бот» у полі
SL); причина — у тултипі, першим рядком.
⚠️ Знімок і далі пишеться ДО дії (щоб колонка не «замерзала», якщо дія впаде) —
тому результат дії доповнюється через `_pilot_mark`, а не вгадується наперед.

**3. Одне число — два написання.** `roundSlTp` робив `String(Number(...))` →
обрізав кінцеві нулі: поля показували `0.3205 / 0.33274 / 0.3422` (4/5/4 знаки,
рвана колонка), а той самий TP-2 у колонці автопілота — `$0.34220` (5 знаків
через `fmtPriceJS`). Точність у `_sltpDp` і `fmtPriceJS` **збігається**, тож
досить було лишити `n.toFixed(_sltpDp(n))` без обгортки `Number()`.
Плюс `lang="en"` на трьох полях рівнів: браузер локалізує `<input
type="number">` і малював кому замість крапки (функціонально безпечно — `.value`
повертає крапку, — але поле випадало з таблиці).

Тест: `test_pilot_books.py` (12). Перший тест дослівно відтворює скрін: дві
позиції з тією самою ціллю дають **paper 3.72R** і **real 94.67R**, і вони
більше не змішуються.

## 🖥 Колонка «🎯 Автопілот» — РІВНІ СТОВПЧИКИ + редаговані поля рівнів

- **Вирівнювання.** Сегменти йшли підряд, тож ціна різної довжини
  ($2,411.95 проти $0.07561) зсувала все наступне — %, R і значки не
  вишиковувались по вертикалі. Тепер КОЖЕН сегмент — `inline-block`
  ФІКСОВАНОЇ ширини зі своїм вирівнюванням (`seg(w, align, html)`):
  значок 14 · смужка 40 · ціль 86 (ліворуч) · залишок 44 (праворуч) ·
  R 50 (праворуч) · ⚠/🛡 30. Плюс `font-family:monospace` на всю комірку.
  Ширини підібрані під найдовші реальні значення — саме вони тримають
  стовпчик рівним.
- **Порожнє поле Manual SL/TP мусить ЧИТАТИСЬ як поле.** Воно завжди було
  `<input>`, але з ледь помітною рамкою `#333`: поряд із заповненими (яскраво
  обведеними) виглядало як звичайний прочерк, і складалось враження, що рівень
  не вписати руками. Тепер порожній стан — **пунктирна рамка `#4b5563`** +
  підказка «клікніть і впишіть рівень (Enter або клік поза полем зберігає)».
  Механіка не змінювалась: `submitManualTp1` / `submitManualSlTp` →
  `POST /api/tm/positions/manual-sltp`, захист редагування `_tmEditingIn`
  не дає 5-с полу стерти те, що друкуєш.

## 💧 ДРАБИНА ЛІКВІДНОСТІ BTC — біля банера настрою (у ВІДСОТКАХ)

Користувач читає в каналах зріз виду «на $82k — 4 150 стопів, на $78k — 2 011»
і попросив, щоб бот збирав те саме. **Виявилось, що 80% уже є:**
`detection/liquidation_map/` — повний рушій оцінки рівнів ліквідації
(провайдери OI Binance/Bybit/Hyperliquid → приріст OI між тиками → плечі
25×/40% · 50×/35% · 100×/25% → реальна формула ціни ліквідації → кошики $25 на
BTC → сітка 0.25% з часовим згасанням і мітигацією). Бракувало лише подання.

**`detection/liquidation_map/ladder.py`** (ЧИСТІ функції) згортає `levels` із
`get_state()` у КРОКИ ПО $N і переводить у **ЧАСТКИ** від усієї ліквідності:
`{above/below: {pct,usd}, pull: up|down|flat, pull_pct, rows[]}`, рядок —
`{price, price_hi, side, usd, pct, dist_pct, dir}`.

⚠️ **ЧОМУ ЧАСТКИ, А НЕ «КІЛЬКІСТЬ СТОПІВ» (рішення, не міняти без потреби).**
Реальних стоп-наказів трейдерів не публікує ЖОДНА централізована біржа. «4 150
стопів» — чужа оцінка: долари ÷ припущений середній розмір позиції, два
припущення поспіль, які неможливо перевірити. Частку ж ми справді знаємо зі
своїх даних. Єдине чесне джерело кількості — **Hyperliquid** (on-chain, ~16%
OI по BTC), але це обхід позицій користувачів; провайдер є, поки бере агрегат.
Тест-замок `test_module_does_not_promise_stop_counts` не дає завести поле
`stops`/`count`/`orders` у рядку драбини.

Інші рішення: `pull` = 'flat', поки перевага ≤ **10 п.п.** (52/48 — не
напрямок); відстань міряється від СЕРЕДИНИ сходинки; рядки < `MIN_ROW_PCT`
(1%) відкидаються як шум; авто-крок ≈1.25% ціни, округлений до людського
($1000 на BTC, $50 на ETH), явний `step` має пріоритет.

API: `GET /api/liquidity-ladder?symbol&step&lookback&top` — читає готовий стан
liq-map, біржу НЕ смикає. UI: блок `#sm-liq` під банером настрою (та сама
типографіка: monospace, 1px-бордер, стовпчики), оновлюється тим самим тактом.

**У рядку драбини праворуч — СУМА, не відсоток** (рішення користувача): частку
вже показує довжина смужки, а гроші дають відчуття масштабу («$95M проти $12M»
читається краще за «29% проти 4%»). Відсоток лишився в підказці рядка.

**📍 ПОРЯДОК ПОКАЗУ = ЦІНА, з ЛІНІЄЮ ПОТОЧНОЇ ЦІНИ посередині.** Драбина
читається як стакан: згори те, що над ціною, знизу — під нею, а між ними
пунктирна лінія `sm-liq-price` із самою ціною. ⚠️ Два РІЗНІ порядки, не
плутати: **відбір** рядків (`top_n`) — за ЧАСТКОЮ (значущістю), **показ** —
за ЦІНОЮ спаданням. «Магніт» для вердикту береться ДО пересортування, інакше
в нього потрапив би просто найвищий за ціною рядок (тест-замок
`test_selection_by_share_display_by_price`). Рядок із прапорцем `at_price`
(одна з меж = сама ціна) стоїть впритул до лінії; у підказці — «впритул до
поточної ціни».

**🐞 СХОДИНКА, ЩО НАКРИВАЄ ЦІНУ, ДІЛИТЬСЯ ПО НІЙ (знайдено на проді).**
Кошик $78 000 = `[78000..79000]` при ціні **$78 563** діставав ОДИН напрямок
за своєю серединою (78 500 < ціни → «↓ нижче»), хоча 44% його ширини лежить
ВИЩЕ ціни. У ньому сиділо **16% усієї ліквідності** — і банер сам собі
суперечив: підсумок «⬆50.5% / ⬇49.5%» (рахується по РІВНЯХ) проти рядків, які
сумарно давали **32% / 68%**. Фікс: якщо `lo <= mp < hi`, сходинка ріжеться
САМЕ по `mp` на дві. Плюс ключ кошика тепер **діапазон, а не (діапазон, сторона)**
— інакше виходило ДВА рядки з однаковим підписом «$78 000», які неможливо
розрізнити. Тест-замки: `test_step_containing_price_is_split_at_it`,
`test_rows_never_contradict_the_summary`, `test_no_duplicate_price_labels`.

**🎨 КОЛІР = НАПРЯМОК ТЯГИ, а не сторона позицій (не перевертати!).**
Спершу драбина фарбувалась за тим, ЧИЇ позиції ліквідують: шорти стоять над
ціною → червоний, лонги під ціною → зелений. Формально вірно, але банер читався
ПРОТИЛЕЖНО до всього іншого інтерфейсу (де зелений = LONG/вгору): «тягне вниз»
світилось зеленим. Тепер колір показує, КУДИ ТЯГНЕ ЦІНУ: вище ціни → **зелений**,
нижче → **червоний**. Це стосується смуги спліту, рядків драбини, підсумку
«⬆/⬇» і вердикту з магнітом — усе з одного правила.

**🧭 ВЕРДИКТ замість опису методики.** Замість постійного напису «оцінка з
відкритого інтересу…» (він переїхав у тултип заголовка) внизу блоку стоїть
`ladder.make_verdict(...)` — ВИСНОВОК ПО ЦИХ ДАНИХ, що змінюється РАЗОМ із
ними: напрямок від перекосу часток, сила словом (`_strength_word`: ≤10 п.п.
рівновага · ≤30 помірно · ≤60 виразно · далі сильно), плюс «найбільший магніт»
= найбільша сходинка з ціною, часткою і відстанню. ⚠️ При перекосі ≤10 п.п.
пишемо «Рівновага», а не витискаємо напрямок із 52/48. Немає даних → вердикт
усе одно є і чесно каже, що даних немає. Тест-замок
`test_verdict_flips_with_the_data`: дзеркальний набір мусить перевернути висновок.

**Вердикт віддається ШМАТКАМИ (`verdict.parts`)**, а не суцільним рядком:
`{icon, lead, lead_val, action, strength, skew, magnet{price,pct,dist,dir}}`.
Це дозволило UI (а) винести «найбільший магніт» у ДРУГИЙ рядок і (б) фарбувати
ЧИСЛА окремо від пояснювальних слів — раніше все зливалось в один зелений
абзац. ⚠️ Розмітку модуль НЕ робить: `parts` — чисті дані, кольори лишаються
справою фронта (тест це перевіряє). `text` зберігається як фолбек для логу/TG,
і тест `test_verdict_parts_mirror_the_text` не дає їм розійтися.

## 🧭 РЯДОК АНАЛІЗУ: ліворуч ліквідність, праворуч ОДИН «Потенціал»

Спроба вкласти банер рішення в ліву смугу рядка Trade Direction — **скасовано
користувачем** («дуже все заляпано»): банер повернуто на своє місце ОДИН-В-ОДИН
(усі id і порядок звірені з попередньою версією), компактний CSS прибрано.
⚠️ Не повторювати: банер рішення живе окремим блоком на повну ширину.

Натомість переставлено `.sm-analysis-row`:
- **ліворуч** — блок настрою Binance + 💧 драбина ліквідності BTC
  (`#sm-analysis-side`), перенесений із `sm-side-gates-right` (та колонка
  прибрана, у рядку Trade Direction лишились самі кнопки);
- **праворуч** — РІВНО ОДНА панель «Потенціал», та, що несе інформацію зараз.
Обидві половини `flex:1` → рівні за шириною.

**Хто «активний»** — `_movePickActive(moveLong, moveShort, verdict)`:
1) є вердикт LONG/SHORT і по ньому є дані → він;
2) WAIT/нейтраль → бік із БІЛЬШИМ ЗАПАСОМ: `_MOVE_RANK` FRESH 0 < MATURE 1 <
   EXHAUSTED 2, за однакового стану — менша `exhaustion`;
3) дані лише з одного боку → він.
Другу панель ХОВАЄМО (`display:none`), а не пригашуємо: раніше вона займала
половину рядка без користі. Кейс зі скріна (WAIT · LONG ДОЗРІЛИЙ 44.4 ·
SHORT СВІЖИЙ 28.7) → показується SHORT.

## 🚫 РЯДОК АНАЛІЗУ ВИМКНЕНО ДЛЯ ПОКАЗУ (`SM_ANALYSIS_ROW_ON = false`)

Вимога користувача: «щоб не навантажувати сторінку, вимкни повністю все, що
зображено на скріні… Розрахунок і алгоритм залиш, вимкни лише для сторінки
відображення». Ідеться про ВЕСЬ `.sm-analysis-row` — три банери разом:
🏦 настрій Binance · 💧 драбина ліквідності BTC · 📈/📉 Потенціал LONG/SHORT.

**Один вимикач** — `const SM_ANALYSIS_ROW_ON = false;` на початку скрипта
`templates/smart_money.html`. Керує ЧОТИРМА місцями:
- `loadSentiment()` — вихід ДО `loadSentimentFor` → **немає запиту**
  `/api/sentiment`;
- `loadLiquidityLadder()` — вихід ДО `fetch` → **немає запиту**
  `/api/liquidity-ladder`;
- `renderMovePanel()` — вихід ДО `renderMoveSide` → жодного DOM-малювання;
- обгортка `#sm-analysis-row` прихована інлайном, а при `true` init ставить їй
  `display:flex` назад.
**Повернути = поставити `true`.** Розмітку і всі id СВІДОМО лишено на місці.

⚠️ **Що НЕ вимкнено і чому.** `move_long`/`move_short` рахує бекенд у
`compute_bias`, і їх читає ТОРГОВА логіка — `fuel_filter._exhaustion`
(«Виснаженість» → ворота відкриття) і `confluence_direction`. Вирізати цей
розрахунок разом із банером означало б тихо змінити ТОРГІВЛЮ. Тому:
- настрій і драбина економлять **і запит, і серверний розрахунок**;
- «Потенціал» економить **лише малювання** — його числа приїжджають усередині
  `/api/smc/bias`, який сторінці потрібен для вердикту.

⚠️ Ендпоінти `/api/sentiment` і `/api/liquidity-ladder` НЕ видалено — вимкнено
саме виклик зі сторінки.
⚠️ Рядок `#sm-bias-updated` («BTCUSDT · оновлено … · авто кожні 60с») НЕ
чіпали: він стоїть ПОРУЧ, але належить банеру вердикту, який лишається.
Тест: `test_analysis_row_off.py` (8) — стереже ОБИДВІ половини вимоги.

## 🏦 Банер настрою — BINANCE із прозорим фолбеком

`/api/sentiment` дефолт **binance** (був bybit) + `?fallback=bybit`. Binance
періодично блокує IP дата-центрів (418/451), і банер тоді просто гас. Тепер за
потреби мовчки береться друга біржа, але **підпис малюється з поля `exchange`
у відповіді** (`#sm-sentiment-src`), а не з розмітки — тож банер ніколи не
бреше про походження цифр; фолбек підсвічується бурштиновим.

## 🔄 ПРОТИЛЕЖНИЙ СИГНАЛ — У ЧЕРГУ, а не в нікуди (кейс ARBUSDT)

**Що показав лог (31.08).** О 01:51 бот відкрив ARBUSDT SHORT (стоп із 1H OB —
**8.23%** від входу, ціль 1.60% → **0.19R**). О **15:02** прийшов LONG, який
пройшов **УСІ** фільтри (OB(1h)✓ · Прогноз[AND] 1H LONG 75% + 4H LONG 50%✓ ·
Decision LONG 82%✓). Але «Reverse on opposite signal» вимкнено, і TM написав
«тримаємо SHORT» — **сигнал зник назавжди**, його навіть не поклали в чергу.
Бот досидів у шорті до **−8.58%**, а рух пішов саме в бік того LONG.
За добу таке сталось **8 разів на 8 монетах**.

**Правило (`queue_opposite_signal`, дефолт УВІМК).** Протилежний сигнал при
відкритій угоді СТАЄ В ЧЕРГУ-4 з міткою **`opp_wait`** і чекає закриття
позиції. Автореверс лишається окремим тумблером — позицію ми не перевертаємо.

⚠️ **Три місця, без яких мітка не працює** (кожне закрито тестом):
1. `intercept(..., opp_wait=True)` знімає блок `_already_open` — інакше запис
   не поставився б саме тоді, коли він і потрібен;
2. **`note_trade_closed` НЕ прибирає `opp_wait`-записи.** Чистка викидає все,
   що старше за закриття, — а цей запис за визначенням старіший і його змело б
   рівно в момент, коли він став актуальним;
3. `_is_new_situation(..., opp_wait=True)` пропускає перевірку «сигнал
   старіший за закриття». ⚠️ Гейт «сигнал УЖЕ ВІДПРАЦЮВАВ» (`burned`)
   лишається в силі — виняток точковий.

📨 **Telegram:** `_notify_opposite_queued` — «🔄 ПРОТИЛЕЖНИЙ СИГНАЛ · #SYM ·
у позиції SHORT @ ціна (PnL) · новий сигнал LONG у Чергу-4, чекає закриття».
Раніше ця подія проходила непоміченою; тепер рішення (закрити руками зараз чи
дати боту відкрити після закриття) ухвалюється вчасно.

## 📐 ГЕЙТ ЗА R ПЕРЕД ВІДКРИТТЯМ (`queue4_min_rr`, дефолт 1.0)

Останній крок Черги-4 перед `_open`: рахуємо **очікуваний R** — те саме число,
що потім стоїть у колонці «🎯 Автопілот». Угода з R < порогу не відкривається.

**`_q4_expected_r(sym, side, s)` → `(r, detail)`** бере обидві складові з ТИХ
САМИХ джерел, що дадуть реальні рівні:
- **стоп** — `_q4_pick_sl` (винесено з `_q4_set_vob_sl` НАВМИСНО): той самий
  ланцюг джерел, буфер і стеля `autosl_max_pct`;
- **ціль** — `trade_pilot.pick_objective` над `TradeManager._pilot_context`.

⚠️ **Це НЕ повернення скасованого `tp_min_r`.** Той різав ВИСТАВЛЕННЯ TP уже
відкритій угоді — користувач це скасував, і воно лишається скасованим. Цей
рахує R **до входу** й вирішує, чи взагалі відкривати.
⚠️ Запис при відмові **НЕ виселяється**: R рухомий (ціна підійде до стопа —
ризик впаде; зʼявиться дальша ціль — виграш зросте), тож монета пробує на
наступному тіку. Анти-флуд `_q4_rr_logged` — як у 🔁 повторної перевірки.
⚠️ Немає стопа або цілі → `(None, причина)` і гейт **не блокує**: вигаданої
відмови не даємо.

На кейсі ARB: `0.19R · ціль Weak Low +1.60% / стоп 1H OB −8.22%` — відсіявся б.

**🐞 ЦІЛЬ ГЕЙТА ФІКСУЄТЬСЯ В УГОДІ (кейс VETUSDT 01.09 — не зламати!).**
Гейт пропустив шорт із вердиктом «**1.0R** · ціль головний пул ліквідності
+2.75% / стоп 1H OB −2.74%», а через **90 секунд** автопілот обрав ІНШУ ціль —
Strong Low +1.25% — і та сама угода стала **0.43R**. Тобто число, ЗА ЯКИМ
пропустили, не було числом, яке потім реально стоїть в угоді, і вимога «не
відкривати нижче 1.0R» обходилась сама собою.

Причина: ціль спирається на `runway`, а `_pilot_context` віддає його ЛИШЕ коли
напрямок МММ збігається з боком угоди. МММ фліпнув — пул зник із контексту,
найдальшою стала інша ціль. **Фікс:** `_q4_expected_r` запамʼятовує обрану ціль
у `_q4_rr_objective[sym]`; перед відкриттям вона йде в
`TradeManager.set_pending_objective`, лягає на позицію як `pilot_objective` і
стає ЗАФІКСОВАНОЮ ціллю автопілота (механізм `objective_lock` уже був).
Тепер R рішення = R у колонці «🎯 Автопілот».
⚠️ Ціль кладеться в ОБИДВА шляхи відкриття (real + paper) — тест це перевіряє.

⚠️ **Спостереження, яке НЕ виправлено (рішення користувача).** R механічно
РОСТЕ, коли ціна підходить до стопа: по VET стоп ішов 3.10% → 3.22% → 3.14% →
**2.74%**, а ціль 2.32% → 2.75%, тож поріг 1.0R «дозрів» не від кращого
сетапу, а від того, що ціна пішла ПРОТИ ідеї шорта на 0.4%. Тобто очікування в
черзі саме по собі рано чи пізно виробляє пропуск. Варіанти, якщо це заважає:
міряти R від ціни СИГНАЛУ (`added_price`), а не від поточної; або додати
окрему вимогу мінімальної відстані до цілі.
Зі стелею SL 2.5% той самий сетап дав би 0.64R (усе одно нижче 1.0).
UI: `ff-queue4-min-rr` у гармошці «⚙️ Налаштування (Черга-4)».
Тести: `test_opposite_and_rr.py` (11).

## 💧 СКАН ЛІКВІДНОСТІ ПО БІРЖІ (сторінка 📡 Tickr) — `detection/liq_scan.py`

Список монет, відсортований за ПЕРЕКОСОМ ліквідності: куди тягне маса рівнів
ліквідації, де найбільший магніт і де найближчий. Разовий запуск за кнопкою,
з вибором біржі.

⚠️ **ЦЕ НЕ ЖИВА liq-map — не плутати (принципово).** `liquidation_map` будує
рівні з ПРИРОСТУ OI між своїми тіками: точніше, але лише для монет, які демон
веде (BTC/ETH + замовлені), і потребує годин накопичення. Для разового скану
сотні монет таких даних НЕМАЄ і бути не може.
Тут — **МОМЕНТАЛЬНИЙ ЗРІЗ**: поточний сукупний OI (один bulk-запит на всю
біржу) розкладається по історії цін за обсягом («позиції відкривались там, де
торгувався обсяг»), далі — ТА САМА формула `liquidation_price` і ТА САМА
драбина `build_ladder`/`make_verdict`. Тест-замок
`test_summary_uses_the_same_ladder_and_verdict` не дає вердиктам розійтися.

**Що чесно:** напрямок перекосу і розташування магнітів надійні (їх задає
геометрія рівнів); абсолютні суми грубіші за живу карту; лонг/шорт беремо
50/50, бо співвідношення по КОЖНІЙ монеті пачкою не віддає жодна біржа.

**Ключові рішення:**
- **Мітигація обовʼязкова:** якщо після свого бару ціна вже проходила крізь
  рівень — позиції там немає, рівень викидаємо. Без цього список показував би
  давно знесені кластери.
- **Два РІЗНІ магніти в рядку:** `magnet_*` — найбільший за масою (куди тягне),
  `near_*` — найближчий за відстанню (що спрацює першим). Плутати не можна.
- **Біржі без bulk-OI відмовляють ЧЕСНО.** `NO_BULK_OI` = Binance, BingX: вони
  не віддають відкритий інтерес пачкою (лише по одній монеті — сотні запитів).
  Кажемо це прямо, а не показуємо нулі чи порожній список.
- Відсів перед скануванням: обіг ≥$20M і OI ≥$5M, інакше «перекіс 90%» буде
  побудований на трьох доларах. Кількість монет — стеля `MAX_SYMBOLS`=120.

**Вартість:** 1 bulk + 1 запит свічок на монету (1h × 168). При 40 монетах —
~41 HTTP і 15-30 с, у 8 потоків. На постійне навантаження не впливає.
API: `POST /api/tickr/liquidity-scan` {exchange, top_n, min_vol_usd,
min_oi_usd, bars, sort_by}. Сортування: `pull` / `magnet` / `near`.
Тести: `test_liq_scan.py` (12).

⚠️ **Урок із тесту:** один бар дає рівні в ОБИДВА боки (лонги ліквідуються
нижче входу, шорти — вище), тож «важчий обсяг знизу → маса знизу» НЕ працює.
Асиметрію створює геометрія: з входів далеко ПІД ціною у вікно потрапляють
лише шорт-ліквідації, з входів далеко НАД ціною — лише лонгові.

## 💧 BULK-OI: обмеження було в МАСШТАБІ, а не в біржі + режим ОДНІЄЇ монети

**Питання користувача:** «Чи можна вийти з положення "Binance не віддає
відкритий інтерес пачкою"?» — і власна відповідь: «можна по одній монеті
робити запити… і можливість вводити назву монети (за замовчуванням BTC)».
Слушно: **«пачкою» заважає лише тоді, коли монет СОТНІ.** Поштучний
`/fapi/v1/openInterest` Binance віддає без проблем — я помилково зробив із
відсутності bulk-ендпоінта повну відмову.

- **`_OI_ONE[exchange](session, symbol) → (ціна, OI у $)`** для всіх чотирьох
  бірж (Binance `openInterest`+`ticker/price`, BingX `quote/openInterest`+
  `quote/ticker`, Bybit/MEXC — свій тікер по символу). `_KLINES` теж доповнено
  Binance і BingX, тож підтримані всі чотири.
- **Скан СПИСКУ** більше не відмовляє: `BULK_OI={'bybit','mexc'}`, решта
  добирає OI поштучно. Порядок кроків інший і це принципово — без bulk-OI
  кандидати відбираються за **ОБІГОМ** (він у тікерах є завжди), OI питається
  лише для них, і аж ПІСЛЯ цього застосовується поріг `min_oi_usd`. Стеля
  `PER_SYMBOL_OI_CAP=60` (запитів удвічі більше: OI + свічки). Відповідь несе
  `bulk_oi` і `warnings` — UI пише, що скан довший, а не мовчить.
- **`scan_one(exchange, symbol, bars, rows)`** — аналіз ОДНІЄЇ монети: 2-3
  запити, повна драбина + вердикт, працює на ВСІХ біржах. `norm_symbol` зводить
  `btc`/`BTC-USDT`/`btc_usdt` до `BTCUSDT`, `_ex_symbol` — у формат біржі
  (`BTC_USDT` MEXC, `BTC-USDT` BingX). Порожнє поле → BTC.
  API: `POST /api/tickr/liquidity-one` {exchange, symbol, bars, rows}.
- **UI (`templates/tickr.html`):** під скан-таблицею — поле «МОНЕТА» (дефолт
  BTC, Enter запускає) + вибір глибини історії; результат малюється **тим
  самим виглядом**, що блок 💧 на Smart Money: смуга спліту, драбина за ціною
  з **лінією поточної ціни** посередині, вердикт шматками (числа окремо від
  слів, магніт другим рядком). **Біржа за замовчуванням — Binance** (рішення
  користувача), і для списку, і для однієї монети.

**🐞 Два дефекти, які виліз­ли одразу, щойно драбина пішла не по BTC:**
1. `step_for` мав СПИСОК готових кроків, що починався з **$1** → на монеті за
   $0.47 уся драбина сходилась в одну-дві сходинки. Тепер крок рахується як
   ~1.25% ціни, округлені до **1/2/5 × 10^k** — масштабується вниз так само
   вільно, як угору (BTC $1000 і ETH $50 лишились ті самі).
2. `_fmt_price_ua` робив `int(round(v))` → магніт дешевої монети підписувався
   **«$0»**. Нижче $10 показуємо знаки після коми (3/5/7 за масштабом).
Тест-замок: `test_cheap_coin_magnet_is_not_rounded_to_zero`.

⚠️ **Пастка тестування:** `scan_liquidity` робить `from detection import
tickr_core` ДО всіх перевірок, тож перший же виклик прив'язує РЕАЛЬНИЙ модуль
атрибутом пакета — підміни лише `sys.modules['detection.tickr_core']` замало,
треба ще `sys.modules['detection'].tickr_core = fake`. Інакше тест мовчки йде
в мережу.
Тести: `test_liq_scan.py` (17).

## 🐞 URL ФРОНТА ≠ МАРШРУТ FLASK — мовчазна втрата рівня (не повторити!)

**Симптом:** у Manual TP-2 значення зберігалось, а введене руками в
**Manual TP-1 зникало через кілька секунд**.

**Корінь — моя друкарська помилка.** `submitManualTp1` слав POST на
`/api/tm/positions/manual-sltp`, а маршрут зареєстровано як
**`manual-sl-tp`** (той, що використовує робочий `submitManualSlTp`).
404 → `x.json()` кидав виняток → його ковтав `catch` із `console.error` →
жодного повідомлення користувачу, а на наступному 5-с поллі поле
перемальовувалось зі старого (порожнього) стану. Бекенд був НІ ПРИ ЧОМУ:
перевірено прямим викликом `update_manual_sl_tp(manual_tp1=…)` — рівень
пишеться, персистить і доїжджає в `get_state` (там `{**pos}`, без білого списку).

**Два висновки, зашиті в код:**
1. `submitManualTp1` перевіряє **HTTP-статус ОКРЕМО** (`if (!res.ok)`) і кричить
   у alert. Без цього будь-який 404/500 виглядає як «збережено».
2. **Тест-замок `test_every_frontend_position_route_exists`**: усі
   `/api/tm/positions/*`, які кличе `smart_money.html`, звіряються з
   `@app.route(...)` у `flask_app.py`. Вигадати URL більше не можна.

## ⚖️ TP-1 → БЕЗЗБИТОК (тумблер `tp1_move_to_be`, дефолт ВИМКНЕНО)

Коли спрацював TP-1 і тумблер УВІМКНЕНО, Manual SL переїжджає на вхід + запас
на комісії. **Дефолт OFF** — рішення про ризик за користувачем: беззбитковий
стоп після часткової фіксації нерідко вибиває позицію на звичайному відкаті до
входу. Вимкнено → стоп лишається де стояв, у лог нічого не пишеться.

- `trade_manager.breakeven_with_fees(side, entry, buf_pct)` — ЧИСТА функція.
  Стоп РІВНО на вході беззбитком НЕ є: раунд-трип комісії (Bybit перпи ≈0.06%
  відкриття + 0.06% закриття) зробили б із нього мінус. Буфер —
  `be_commission_buffer_pct` (деф. **0.12%**), той самий ключ, що вже живить
  BE-Move (другий однаковий за змістом параметр не заводимо).
- `_tp1_move_to_breakeven(...)` кличеться з `_check_manual_tp1` ОДРАЗУ після
  успішного часткового закриття (обидві книги: real + paper).
- ⚠️ **НІКОЛИ не послаблює стоп** (`trade_pilot.better_stop`): чинний рівень,
  кращий за беззбиток, лишається. Стосується і рівня, введеного ОПЕРАТОРОМ.
- ⚠️ Результат `update_manual_sl_tp` ЧИТАЄТЬСЯ: ціна могла повернутись до
  входу → TM відхилить → у лог «НЕ прийнято», зелена позначка НЕ ставиться.
- **🎨 Третій колір поля Manual SL:** `sl_breakeven` → `_SLTP_SRC_STYLE.be` =
  **зелений `#4ade80`**, перекриває блакитний «бот» (стан УГОДИ важливіший за
  походження рівня). Ручна зміна/зняття SL позначку скидає; автотрейл зберігає.
  Легенда в `<th>` обох таблиць: 🤖 авто · ✏️ вручну · ⚖️ БЗ.
UI: тумблер `tm-tp1-move-be` під налаштуваннями TP-1/TP-2.
Тести: `test_autosl_chain.py` (розділ «БЕЗЗБИТОК ПІСЛЯ TP-1»).

## 📐 R — ДОВІДКА, а НЕ гейт (скасовано користувачем — не повертати!)

Була спроба відсікати цілі, ближчі за власний стоп (`tp_min_r`, дефолт 1.5):
на проді 5 із 9 позицій мали R < 1 (ARB 0.39R … ORDI 0.82R), і TP-2 їм не
виставлявся. **Користувач це СКАСУВАВ дослівно:** «Не потрібно відсікати ніякі
угоди, все має бути як і до введення Автопілот… розрахуй Manual TP-1/TP-2 по
відсотках, які ти вираховуєш по алгоритму Автопілот».

**Правило:** автопілот РАХУЄ рівні з обʼєктів графіка і ВИСТАВЛЯЄ їх. Судити
угоду за R — не його справа. Налаштування `pilot_tp_min_r` і ключ `tp_min_r`
ВИДАЛЕНІ з коду й UI (а не просто вимкнені дефолтом — інакше збережений у БД
`1.5` продовжував би різати; див. урок про блоб нижче).
- **TP-2 виставляється ЗАВЖДИ**, коли ціль є попереду ВХОДУ. `tp_min_gap_pct`
  тепер керує ЛИШЕ ПОДІЛОМ на два рівні, а не правом TP-2 існувати.
- **Стоп на рівні НЕ впливає.** Він передається в `plan_targets` виключно щоб
  порахувати `r` для ПОКАЗУ (колонка «🎯 Автопілот» + рядок логу).
- `trade_pilot.risk_reward(entry, stop, objective)` лишився: R рахується від
  ВХОДУ, фарбується (червоний <1R · бурштиновий <1.5R · зелений ≥1.5R) — це
  інформація для людини, жодної логіки за нею немає.
Тест-замок: `test_low_r_target_is_still_set` (+ `'tp_min_r' not in DEFAULTS`).

## ⚠️ ЗМІНА ДЕФОЛТУ НЕ ДІЄ НА ВЖЕ ЗБЕРЕЖЕНИЙ БЛОБ (не наступати вдруге!)

Після вмикання `pilot_autofill_tp` дефолтом поля Manual TP-1/TP-2 лишились
ПОРОЖНІ навіть там, де R був 3.53 / 2.23 / 1.76 (тобто поріг проходив).
**Корінь:** `TradeManager._load_settings` робить `merged.update(stored)`, а
сторінка налаштувань шле УСІ ключі одним блобом — тож `pilot_autofill_tp:false`
осів у БД ще з часів старого дефолту й перекривав новий. Правка дефолту в коді
на такій установці не робить НІЧОГО.

**Фікс — одноразова міграція** (той самий прийом, що `interval_migrated_v2`):
`pilot_autofill_migrated_v1` у `_load_settings` вмикає тумблер РАЗ і одразу
персистить; далі вибір користувача поважається. `_fresh_settings()` (чиста
установка / немає БД) ставить позначку ОДРАЗУ — інакше міграція спрацювала б на
ДРУГОМУ старті й перекрила б вибір, який користувач уже встиг зробити.

**Друга причина того ж симптому — стара позначка.** Попередня версія ставила
`pilot_tp_set` БЕЗУМОВНО, навіть коли не виставила жодного рівня; такий блоб
«замуровував» угоду без TP назавжди. Тепер гейт питає `_pilot_tp_done(pos)`:
робота вважається зробленою, ЛИШЕ коли рівень реально стоїть (`manual_tp` /
`manual_tp1` / `tp1_done`). ⚠️ Виняток — `pilot_tp_cleared`: рівень, знятий
ОПЕРАТОРОМ (`origin='user'` + clear), автопілот НЕ відновлює, інакше
самолікування сперечалося б із людиною.

**Правило на майбутнє:** міняючи дефолт налаштування, яке вже могло бути
збережене, ЗАВЖДИ додавати міграцію — інакше зміна побачить лише чисту БД.
Тести: `test_trade_pilot.py` (40).

## 🛡 ГЛОБАЛЬНІ налаштування — ОКРЕМА гармошка, не всередині черги

**Скарга користувача:** «Налаштування (Черга-2) — до чого тут Черга-2? Вона
вимкнена. Працює Черга-4.» Слушно: у гармошці Черги-2 лежав цілий рядок
параметрів, які до неї стосунку НЕ мають — вони діють на КОЖНЕ автоматичне
відкриття і на КОЖНУ відкриту угоду:
`q2_auto_ob_sl`(+буфер,+TF) · `autosl_fallback_on`(+%) · `autosl_max_pct` ·
`require_new_situation` · `open_max_signal_age_min` · `manual_close_lock_min`.

Винесено у ВЛАСНУ гармошку верхнього рівня **«🛡 Авто-SL та ворота відкриття»**
(`togglePanel('autosl')`, `#autosl-body`/`#autosl-caret`, `_PANEL_IDS[0]`), яка
стоїть НАД Чергою-1 і прямо підписана «діють на ВСІ черги (1/2/3/4) і на ручні
відкриття». Розгорнута за замовчуванням — саме через неї їх і не могли знайти.

⚠️ **Ключі НЕ перейменовані** (`q2_auto_ob_sl*` лишились як були): перейменування
зламало б уже збережені налаштування в БД. **Історична назва ключа ≠ область
дії** — не робити з префікса `q2_` висновків про те, на що параметр впливає.

## 🎚 СТЕЛЯ SL (`autosl_max_pct`) — ОДНА на ВСІ авто-SL (не зламати!)

Налаштування «🎚 Стеля SL ≤ N %» існувало, але діяло ЛИШЕ в
`trade_manager._auto_ob_manual_sl`. Угоди Черги-4 ставлять стоп СВОЇМ шляхом
(`fuel_filter._q4_set_vob_sl`) — стеля їх не бачила, тож на проді стопи стояли
там, де стоїть блок: **2.4-5.9% від входу при цілях 2-13%**, тобто ціль часто
була ближчою за власний стоп (R < 1) і TP-2 не виставлявся взагалі.

**Фікс:** ЧИСТА функція `trade_manager.clamp_sl_distance(side, entry, sl,
max_pct)` → `(рівень, була_відстань|None)` — ЄДИНЕ джерело правила. Її кличуть
ОБИДВА шляхи: `_auto_ob_manual_sl` і `_q4_set_vob_sl` (через
`_tm_entry_price(sym)` — ціна входу реальної/тіньової позиції).
⚠️ Стеля лише ПІДТЯГУЄ задалекий рівень; ближчий стоп ніколи не відсувається.
⚠️ Вибір самого блоку НЕ змінюється — тільки максимальна відстань.
⚠️ Діє в момент ВИСТАВЛЕННЯ стопа (тобто на НОВІ угоди) — «SET ONCE» лишається,
заднім числом уже відкриті позиції не чіпаємо.
У 🧾 Лозі видно `· підтягнуто до стелі N% (було X%)`.
Дефолт **0 = вимкнено** (політика ризику — рішення користувача).
Тести: `test_q4_controls.py` (розділ «стеля SL»).

## ⚠️ ЗНИКЛИЙ ГЕЙТ Черги-3: `queue3_require_ob_match` (не реалізовано)

Аудит тестів показав: `_ob_match_ok` і налаштування `queue3_require_ob_match`
**відсутні в коді ПОВНІСТЮ** — жодного посилання ніде, крім
`test_readiness_strategy.py::test_ob_match_gate` (єдиний падючий тест файлу).
Тобто задокументований запобіжник «Q3 не відкриває ПРОТИ 1H-OB» зараз **не
працює**, і в `_engine_tick_readiness` немає ані OB-перевірки, ані
`_signal_allowed`-recheck (на відміну від Черги-4, де є `queue4_recheck_filters`).
Вплив обмежений: `queue3_enabled` дефолт **OFF**. Тест лишено падати СВІДОМО —
як запис про відсутню функцію. Відновлювати — за рішенням користувача.

⚠️ Стаби `_open` у тестах мусять приймати ВСІ kwargs справжнього методу
(`by_hand`/`signal_at`/`origin_trace`/`**kw`) — інакше двигун ловить TypeError,
мовчки нічого не відкриває, і тест падає «на рівному місці» (саме так відстав
`test_hot_opens_and_logs`).

⚠️ `test_bybit_reconcile.py` падає на `test_reconcile_skipped_when_tm_disabled`
— це ДАВНІЙ збій, відтворюється і без цих змін (перевірено `git stash`).
