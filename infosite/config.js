// ─────────────────────────────────────────────────────────────────────────
//  Налаштування інфо-сайту. Єдиний файл, який треба змінити під свій хостинг.
// ─────────────────────────────────────────────────────────────────────────

window.INFOSITE_CONFIG = {
  // Базовий URL бота (де відповідає його JSON API).
  //   ""                        → той самий домен, з якого відкрито сайт
  //                               (якщо сайт віддає сам бот) — нічого не треба.
  //   "http://127.0.0.1:10000"  → бот локально/на тій самій машині (прод-порт)
  //   "http://127.0.0.1:5000"   → бот у dev-режимі
  //   "https://<твій-домен>"    → бот на окремому хостингу
  //
  // CORS для GET /api/* на боці бота вже увімкнено, тож крос-доменні запити
  // з цього сайту дозволені. Лиши "" якщо сайт і бот на одному домені.
  BOT_API_BASE: "https://bot.vsv-trade.com.ua",

  // Як часто оновлювати дані (мілісекунди).
  REFRESH_MS: 5000,

  // Символ для «вікна потенціалу». "" → автоматично береться активний символ
  // вердикту бота (sm_bias_symbol, дефолт BTCUSDT). Або вкажи явно, напр. "ETHUSDT".
  POTENTIAL_SYMBOL: "",

  // Посилання на Telegram-бота (для прив'язки/реєстрації).
  BOT_TELEGRAM_LINK: "https://t.me/svv_hris_bot",

  // 🔑 ОКРЕМИЙ ХОСТ (Варіант Б): щоб інфо-сайт на іншому домені (напр.
  // nod.inf.ua) читав стан бота крос-доменно БЕЗ cookie. Значення має
  // збігатися з env INFO_API_KEY на боті. Лиши "" якщо сайт віддає сам бот
  // (той самий домен) — тоді ключ не потрібен.
  //   БЕЗПЕКА: ключ дає лише READ-ONLY GET /api/*, керувати ботом ним не можна.
  //   ⚠️ Лиши "" якщо хочеш ОБОВ'ЯЗКОВУ авторизацію (дані лише після входу через
  //   Telegram) — саме так налаштовано зараз. Вхід видає персональний токен.
  INFO_API_KEY: "",
};
