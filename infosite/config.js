// ─────────────────────────────────────────────────────────────────────────
//  Налаштування інфо-сайту. Єдиний файл, який треба змінити під свій хостинг.
// ─────────────────────────────────────────────────────────────────────────

window.INFOSITE_CONFIG = {
  // Базовий URL бота (де відповідає його JSON API). Приклади:
  //   "http://127.0.0.1:10000"            — бот на тій самій машині
  //   "https://my-bot.onrender.com"       — бот на окремому хостингу
  // Якщо сайт віддається тим самим сервером, що й бот — лиши "".
  BOT_API_BASE: "",

  // Як часто оновлювати дані (мілісекунди).
  REFRESH_MS: 5000,
};
